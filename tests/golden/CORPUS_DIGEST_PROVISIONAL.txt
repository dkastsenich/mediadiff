# tests/golden/CORPUS_DIGEST_PROVISIONAL.txt -- names only, no hashes
# (D-GAP-01 gap closure, 04-13-PLAN.md Task 2).
#
# STATUS (as of debug session true-peak-cross-platform CYCLE 2,
# .planning/debug/): BOTH timeline_drift_base.mp4 AND
# timeline_drift_linear.mp4 are listed below -- NEITHER is new; both are
# here because their RECIPE CHANGED.
#
# Cycle 1 found timeline_drift_linear.mp4 was the only fixture in
# scripts/gen_corpus.sh generated through a resampler (`aresample`), whose
# per-architecture SIMD made the fixture's own bytes differ on every CI leg;
# the AAC encoder downstream amplified that into a multi-dB
# `audio.loudness.true_peak` swing that broke integration.timeline_av_sync
# on arm64-osx and x64-windows-static-md. That recipe now reaches the
# identical 0.1% clock error by GENERATING at 47952Hz and relabelling to
# 48000Hz (`asetrate` only, no DSP). CI run 35723466889 proved that fix
# NECESSARY but NOT SUFFICIENT: x64-windows-static-md went fully green, but
# arm64-osx still diverged (+1.299 dB, and the sign FLIPPED relative to
# x64-linux). The remaining amplifier was the native AAC encoder itself,
# independently arch-divergent.
#
# Cycle 2 therefore moved BOTH fixtures' audio to `-c:a pcm_s16le`, which is
# why base's line changes too. With PCM the decoded samples ARE the stored
# bytes (decode class 1 by src/probe/audio_decode.h's own definition), so
# the true peak is EXACTLY invariant rather than merely close. Both new
# hashes in tests/golden/CORPUS_DIGEST.txt were PREDICTED on the workstation
# by regenerating under `TZ=UTC taskset -c 0-3` -- the same method that, in
# the resolved test-898-ci-nonreproducible session, predicted the designated
# leg's bytes exactly. Validated here before the change by reproducing BOTH
# drift fixtures' then-current pinned hashes byte-for-byte, including
# timeline_drift_base.mp4's fc56ebd7..., which was itself TRANSCRIBED from a
# real designated leg (run 35269755235) rather than predicted -- the
# strongest confirmation of this protocol so far. Both entries still need
# the ordinary designated-leg transcription before they may be cleared.
#
# STATUS (as of 06-13-PLAN.md Task 1, CI run 35708992998): NOT EMPTY.
# Phase 6 (06-01-PLAN.md through 06-10-PLAN.md) added 41 fixture names,
# listed below, each workstation-computed and awaiting the SAME
# designated-leg transcription this file's own historical entries below
# already describe -- see 06-13-PLAN.md Task 3, which transcribes them
# into tests/golden/CORPUS_DIGEST.txt and clears this ledger back to
# EMPTY-on-purpose, exactly as 04-21-PLAN.md and 05-13-PLAN.md did for
# Phase 4 and Phase 5's own additions. The paragraph immediately below
# ("STATUS: EMPTY ON PURPOSE...") describes the file's PAST state, right
# after the Phase-5/debug-session transcription and before Phase 6 added
# anything -- preserved as history, not a claim about the file's current
# contents.
#
# STATUS: EMPTY ON PURPOSE. The two entries added by debug session
# test-898-ci-nonreproducible (.planning/debug/resolved/) -- the unflagged
# and flagged TS jump-trigger fixtures, whose bytes changed when
# scripts/gen_corpus.sh moved segment B's -output_ts_offset from 3.0 to
# 5.0 -- were replaced in
# tests/golden/CORPUS_DIGEST.txt by the designated x64-linux CI leg's own
# computed hashes, transcribed from run 35347044190 (the THIRD
# TRANSCRIBED-FROM-DESIGNATED-LEG marker below), and cleared from this
# ledger as a result. Both transcribed values equal the hashes predicted
# locally beforehand by regenerating under `taskset -c 0-3` / `-cpucount 4`
# + TZ=UTC -- the designated leg's bytes are reproducible from a 4-CPU
# environment, which is the root cause recorded in the debug session.
#
# Before that change: every fixture Phase 5 added (05-01-PLAN.md through
# 05-11-PLAN.md) was replaced in tests/golden/CORPUS_DIGEST.txt by the
# designated x64-linux CI leg's own computed hash (05-13-PLAN.md Task 2,
# transcribing run 35269755235's captured listing) and cleared from this
# ledger as a result -- see the second TRANSCRIBED-FROM-DESIGNATED-LEG
# marker below. Phase 4's own additions were transcribed and cleared the
# same way in 04-21-PLAN.md Task 1, recorded by the first marker below.
#
# Historical context for why this file exists at all: each fixture added
# during Phase 4 or Phase 5 was, at commit time, hashed only on the
# workstation that added it -- a fixture cannot be transcribed from a CI
# run that has not happened yet. Those hashes were EXPECTED TO DIFFER on
# the designated leg, the same host-CPU-dependent ffmpeg-encode
# nondeterminism documented at length in this file's own "Two kinds of
# golden live here" section (tests/golden/README.md), and this ledger
# recorded which committed hashes carried that caveat.
#
# 04-13-PLAN.md's own Human Decision #4 (04-VERIFICATION.md) said every
# name listed here would be replaced with the designated leg's real
# transcribed hash once a CI run against this branch produced one. 04-20
# captured that run's listing verbatim; 04-21 transcribed it into
# CORPUS_DIGEST.txt and cleared every Phase-4 entry as a result.
#
# 05-01-PLAN.md through 05-11-PLAN.md added the tracer, D-05 NTSC-in-MKV,
# DOC-03 duration-trigger, dts_monotonic/pts_unique, gaps/wrap_events,
# discontinuities/discontinuities.flagged, jitter/vfr_profile, av_offset,
# av_drift/av_drift.pattern, and timecode/timecode.value fixtures --
# workstation-computed hashes awaiting the same designated-leg
# transcription. 05-13-PLAN.md Task 2 captured run 35269755235's listing
# for all 26 of those names and transcribed it into CORPUS_DIGEST.txt,
# clearing every entry below as a result.
#
# Format: one fixture NAME per line (matching field 2 of a
# CORPUS_DIGEST.txt listing line), LC_ALL=C sorted, unique. No hashes are
# recorded here on purpose -- a hash column in this file would create a
# second, competing source of truth for exactly the numbers
# CORPUS_DIGEST.txt already owns.
#
# TRANSCRIBED-FROM-DESIGNATED-LEG: run=34776142545 job=103774491730 commit=196b52a68b05b0880c7c8b6335b345ea378f8219 date=2026-09-13
#
# TRANSCRIBED-FROM-DESIGNATED-LEG: run=35269755235 job=105365795054 commit=03dd909fac2ffde9f146be47eb7be14ab650f029 date=2026-09-17
#
# TRANSCRIBED-FROM-DESIGNATED-LEG: run=35347044190 job=105605976648 commit=8c343c790b1601ceea80e3b2fde44bfcaf8e8bad date=2026-09-18
#
# The lines above are machine-readable markers, not decoration:
# scripts/lint_corpus_digest_provenance.sh clause 3 accepts a zero-entry
# ledger ONLY when at least one such marker (with a run=<digits> and a
# commit=<40 hex> field) is present and well-formed. An empty ledger
# without any such marker still fails clause 3 exactly as it always has --
# that is the check 04-13 wrote to catch an accidentally emptied ledger,
# and it still fires for that case. With real entries present, that
# zero-entry branch does not apply -- clause 3 instead checks the entries
# themselves are LC_ALL=C sorted, unique, and each names a real line in
# CORPUS_DIGEST.txt.
#
# 06-01-PLAN.md added the nine content.audio.sample_hash tracer fixtures
# (audio_hash_*, audio_pcm_*) below -- workstation-computed hashes awaiting
# the same designated-leg transcription a later 06-plan performs.
#
# 06-02-PLAN.md Task 1 added the five hand-written HE-AAC/class-1 proof
# fixtures (audio_sbr_*, audio_aac_handwritten*) below -- also
# workstation-computed, also awaiting designated-leg transcription.
#
# 06-02-PLAN.md Task 2 added the eleven lossless loudness/true-peak/silence
# FLAC fixtures (audio_dropout*, audio_loud_*, audio_peak_*,
# audio_silence_*) below -- also workstation-computed, also awaiting
# designated-leg transcription.
#
# 06-02-PLAN.md Task 3 added the thirteen stream-parameter, layout and
# priming fixtures below (audio_51*, audio_flt_base.ogg, audio_mono_s16.wav,
# audio_mp2_base.mpg, audio_prime_*, audio_stereo_*), including the
# multi-edit and fragmented MP4 container-mechanism edge cases -- also
# workstation-computed, also awaiting designated-leg transcription.
#
# 06-10-PLAN.md added the meta.decode_errors recoverable-error/undecodable
# fixture triple (audio_corrupt_clean.mp4, audio_corrupt_frames.mp4,
# audio_undecodable.mp4) below -- also workstation-computed (AAC encoder
# output is not byte-stable across SIMD levels, 06-CONTEXT.md's own
# measured finding), also awaiting designated-leg transcription.
audio_51.flac
audio_51_side.flac
audio_aac_handwritten.mp4
audio_aac_handwritten_copy.mp4
audio_corrupt_clean.mp4
audio_corrupt_frames.mp4
audio_dropout.flac
audio_dropout_clean.flac
audio_flt_base.ogg
audio_hash_alt.mp4
audio_hash_base.mkv
audio_hash_base.mp4
audio_hash_base.ts
audio_hash_base_copy.mp4
audio_loud_floor.flac
audio_loud_plus3.flac
audio_loud_ref.flac
audio_loud_ref_copy.flac
audio_mono_s16.wav
audio_mp2_base.mpg
audio_pcm_base.mov
audio_pcm_base.wav
audio_pcm_flac_large.mkv
audio_pcm_flac_small.mkv
audio_peak_over.flac
audio_peak_under.flac
audio_prime_base.mp4
audio_prime_copy.ts
audio_prime_fragmented.mp4
audio_prime_multiedit.mp4
audio_prime_roundtrip.mkv
audio_prime_roundtrip2.mp4
audio_sbr_explicit.mp4
audio_sbr_explicit_copy.mp4
audio_sbr_implicit.mp4
audio_silence_lead.flac
audio_silence_none.flac
audio_silence_trail.flac
audio_stereo_s16.wav
audio_stereo_s24.wav
audio_undecodable.mp4
timeline_drift_base.mp4
timeline_drift_linear.mp4
