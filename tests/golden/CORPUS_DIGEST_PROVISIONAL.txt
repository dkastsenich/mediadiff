# tests/golden/CORPUS_DIGEST_PROVISIONAL.txt -- names only, no hashes
# (D-GAP-01 gap closure, 04-13-PLAN.md Task 2).
#
# STATUS: EMPTY. Every fixture name that used to be listed below has been
# replaced in tests/golden/CORPUS_DIGEST.txt by the designated x64-linux CI
# leg's own computed hash (04-21-PLAN.md Task 1, transcribing 04-20's
# captured listing). No hash in CORPUS_DIGEST.txt is workstation-derived
# any longer -- this ledger has nothing left to track and is empty on
# purpose, not by accident.
#
# Historical context for why this file exists at all: each fixture added
# during Phase 4 was, at commit time, hashed only on the workstation that
# added it -- a fixture cannot be transcribed from a CI run that has not
# happened yet. Those hashes were EXPECTED TO DIFFER on the designated leg,
# the same host-CPU-dependent ffmpeg-encode nondeterminism documented at
# length in this file's own "Two kinds of golden live here" section
# (tests/golden/README.md), and this ledger recorded which committed
# hashes carried that caveat.
#
# 04-13-PLAN.md's own Human Decision #4 (04-VERIFICATION.md) said every
# name listed here would be replaced with the designated leg's real
# transcribed hash once a CI run against this branch produced one. 04-20
# captured that run's listing verbatim; 04-21 transcribed it into
# CORPUS_DIGEST.txt and cleared every entry below as a result.
#
# Format: one fixture NAME per line (matching field 2 of a
# CORPUS_DIGEST.txt listing line), LC_ALL=C sorted, unique. No hashes are
# recorded here on purpose -- a hash column in this file would create a
# second, competing source of truth for exactly the numbers
# CORPUS_DIGEST.txt already owns.
#
# TRANSCRIBED-FROM-DESIGNATED-LEG: run=34776142545 job=103774491730 commit=196b52a68b05b0880c7c8b6335b345ea378f8219 date=2026-09-13
#
# The line above is a machine-readable marker, not decoration:
# scripts/lint_corpus_digest_provenance.sh clause 3 accepts a zero-entry
# ledger ONLY when this exact marker (with a run=<digits> and a
# commit=<40 hex> field) is present and well-formed. An empty ledger
# without this marker still fails clause 3 exactly as it always has --
# that is the check 04-13 wrote to catch an accidentally emptied ledger,
# and it still fires for that case.
