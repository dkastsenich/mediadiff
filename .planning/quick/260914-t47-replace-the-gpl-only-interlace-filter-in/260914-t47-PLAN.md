---
phase: quick-260914-t47
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/gen_corpus.sh
autonomous: true
requirements: [BUILD-08]
user_setup: []

estimate:
  tokens: 24000
  raw_tokens: 48000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "The three interlace fixture recipes in scripts/gen_corpus.sh invoke only filters that an LGPL ffmpeg build provides, so step 9 of the x64-windows-static-md CI leg can parse every filterchain it is handed — no filter used there carries a _deps=\"gpl\" gate in ffmpeg's configure."
    - "video_ilace_tff.mp4, video_ilace_tff_copy.mp4, video_ilace_bff.mp4, video_ilace_mixed.mp4 and the three .video_ilace_*.m2v sidecars come out byte-for-byte unchanged — proven twice before the edit lands (old-vs-new sha256 pairs under the workstation generator AND under the pinned Linux generator) and once after (whole-corpus regeneration compared against the seven pre-run hashes)."
    - "tests/golden/CORPUS_DIGEST.txt and tests/golden/CORPUS_DIGEST_PROVISIONAL.txt are byte-untouched; a local scripts/assert_corpus_digest.sh exit 1 is recorded as EXPECTED and is never 'fixed' by rewriting a digest."
    - "A maintainer reading the interlace recipe block learns that BOTH the t-prefixed temporal filter and the interlace filter are GPL-gated in ffmpeg's configure, what the replacement field bookkeeping does, and why the frame rate is set with an output -r option rather than a filter."
  artifacts:
    - "scripts/gen_corpus.sh — three -vf filterchains replaced with the LGPL separatefields/select/weave chain plus an output -r 25/2, and the quick-260914-ryu comment paragraph rewritten to record the corrected license finding and the new chain's semantics."
  key_links:
    - "select='eq(mod(n\\,4)\\,0)+eq(mod(n\\,4)\\,3)' + weave=first_field=<top|bottom> <-> the exact field pairing the old filter produced <-> byte identity of the four interlace fixtures and three .m2v sidecars."
    - "-r 25/2 as an OUTPUT option <-> the 12.5 fps time base the old filter advertised <-> identical MPEG-2 sequence header and mp4 timing tables (a filter-side fps= drops a frame; omitting it duplicates frames)."
    - "scripts/check_corpus.sh's mechanical $OUT_DIR/<name> extraction <-> the recipe output paths AND any $OUT_DIR/<name> token the rewritten comment might introduce <-> the expected fixture count staying exactly 138."
---

<objective>
Replace the GPL-gated `interlace` filter in the three interlace recipes of `scripts/gen_corpus.sh` with an LGPL-only `separatefields`/`select`/`weave` chain plus an output `-r 25/2`, producing byte-identical fixtures.

Purpose: quick task 260914-ryu swapped the t-prefixed temporal filter for `interlace` on the belief that only the former was GPL-gated. That was wrong. FFmpeg n9.0.1's configure gates BOTH (`tinterlace_filter_deps="gpl"` AND `interlace_filter_deps="gpl"`), so the pinned Windows generator (BtbN `win64-lgpl`, built without `--enable-gpl`) still has neither. Draft PR #5, CI run 34882668138, job `build (x64-windows-static-md)` (id 104105514605), step 9 "Generate media fixture corpus (BUILD-08 / D-08)" failed with `[AVFilterGraph @ ...] No such filter: 'interlace'`, `Error opening output file tests/fixtures/video_ilace_tff.mp4.`, `Error opening output files: Filter not found`, `Process completed with exit code 8`. Everything else the script uses was audited as ungated in that configure (filters testsrc2, sine, color, setparams, setsar, select, separatefields, weave; encoders mpeg4, mpeg2video, mjpeg, huffyuv, aac, mp2, pcm_s16le, libopus; muxers/demuxers mp4, mpegts, matroska, mpegvideo), so this is the last GPL dependency in the corpus generator.

Output: `scripts/gen_corpus.sh` with three rewritten `-vf` filterchains (each followed by `-r 25/2`) and a rewritten comment paragraph. No fixture, no golden, no pin file, no other script changes.

Scope note (MUTABLE-SCOPE AUTHORITY): edit authority is granted for `scripts/gen_corpus.sh` ONLY, confirmed against the live working tree at planning time (`git status --porcelain` shows only two untracked `.planning/` files; `git grep -nE 'tinterlace|interlace='` over tracked files hits exactly `scripts/gen_corpus.sh` lines 1283/1288/1297/1305/1326 plus the frozen `claude_docs/03-video-analysis.md:70`). Explicitly forbidden to touch: `tests/golden/CORPUS_DIGEST.txt`, `tests/golden/CORPUS_DIGEST_PROVISIONAL.txt`, `scripts/lint_corpus_digest_provenance.sh`, `scripts/ffmpeg_pin.json`, `scripts/gen_corpus.ps1` (version gate only — it holds no interlace recipe), `claude_docs/` (frozen design-input set, one commit `67e8d90`, per 260914-ryu's recorded decision), any other recipe, and any fixture. `tests/fixtures/GENERATOR_MANIFEST.json` is tracked and WILL be rewritten by Task 2's corpus regeneration (it carries a `generated_at` timestamp) — restore it to HEAD before committing.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md

@scripts/gen_corpus.sh
@scripts/check_corpus.sh
</context>

<!-- planner-discipline-allow: tinterlace -->
<!-- planner-discipline-allow: interlace= -->
<!-- planner-discipline-allow: fps=25/2 -->
<!-- planner-discipline-allow: lowpass -->

<tasks>

<task type="tracer">
  <name>Task 1: Prove byte identity on both generators, then swap the three filterchains and rewrite the comment</name>

  <files>scripts/gen_corpus.sh</files>

  <read_first>
    - `scripts/gen_corpus.sh` lines 1269-1340 — the `Interlace (VIDEO-06)` comment block, the quick-260914-ryu paragraph that this task rewrites (lines 1282-1295), the three recipes (currently lines 1296-1300, 1304-1308, 1325-1329), the `cp` producing `video_ilace_tff_copy.mp4`, the progressive `ILACE_SEG_B` recipe, the `cat` concatenation and the `-f mpegvideo ... -c copy` remux.
    - `scripts/gen_corpus.sh` lines 1-80 — the recipe conventions (`-flags +bitexact -fflags +bitexact`, writes only under `$OUT_DIR`), `mediadiff_resolve_ffmpeg`'s pinned-first contract that sets `$FFMPEG_BIN`, and the `GENERATOR_MANIFEST.json` writer (it stamps `generated_at`).
    - `/tmp/claude-1000/-home-dzka-projects-mediadiff/b6e09ab9-d1aa-484b-8975-195c389c2d25/scratchpad/ilace2/run.sh` — the byte-identity harness. Its `_r` variant is the chosen recipe; `_fps` and `_plain` are the two rejected variants and exist so you can re-confirm the rejection rather than take it on faith.
  </read_first>

  <precondition>Both generators used by this task's proof are present and can render the OLD recipe: an `ffmpeg` on PATH that is the workstation build N-126086 (it has both the GPL-gated filters and every filter in the new chain), and the pinned Linux generator at `.ffmpeg-pinned/linux-x86_64/ffmpeg` (martin-riedl 9.0.1, an `--enable-gpl` build — this is the binary `gen_corpus.sh` itself resolves, so it is the one whose bytes the committed digests describe). If either is missing, halt: the whole plan rests on rendering old and new with one binary that can run both.</precondition>

  <action>
    STEP A — proof before edit. Do not modify `scripts/gen_corpus.sh` until every comparison below reports identical. Work in a scratch directory outside the repository (your session scratchpad); never write into `tests/fixtures/` in this task, and do not run `scripts/gen_corpus.sh` yet (Task 2 owns the full regeneration).

    Render three OLD/NEW pairs, twice over — once with the workstation `ffmpeg` and once with `.ffmpeg-pinned/linux-x86_64/ffmpeg` — and print the sha256 of every file so each pair's equality is on the record, not merely asserted. All six renders share `-c:v mpeg2video -flags +ilme+ildct -flags +bitexact -fflags +bitexact -y` and the source `-f lavfi -i "testsrc2=size=320x240:rate=25:duration=<D>"`:
    (1) TFF, D=2, `.mp4` output — the `video_ilace_tff.mp4` recipe (and, through the `cp` right after it, `video_ilace_tff_copy.mp4`). OLD `-vf` is `interlace=scan=tff:lowpass=off,setparams=field_mode=tff` with no `-r`; NEW is the TFF chain in Step B.
    (2) BFF, D=2, `.mp4` output — the `video_ilace_bff.mp4` recipe. OLD `-vf` is `interlace=scan=bff:lowpass=off,setparams=field_mode=bff` with no `-r`; NEW is the BFF chain in Step B.
    (3) TFF, D=1 (note: one second, not two), raw `.m2v` output — the `.video_ilace_seg_a.m2v` segment that `video_ilace_mixed.mp4` is concatenated from. Same OLD/NEW TFF chains as pair (1). `ILACE_SEG_B` is progressive and carries no interlacing filter, so it needs no pair.

    Observed at planning time on this machine, with BOTH binaries agreeing: TFF 2 s mp4 = `bc3740d9b7911dad97e1744d707ac4d41476a3589f4806762ee6a1d1283a898e`, BFF 2 s mp4 = `e538b59b18209e21dcc3f9f473a87a461e23b9dc7045c2744bc088babcac4d35`, TFF 1 s m2v = `23d782f1a6e30b034e11c99a71d462760e6335f51ca8bb43e714460e3343052f`; the three on-disk fixtures `tests/fixtures/video_ilace_tff.mp4`, `tests/fixtures/video_ilace_bff.mp4` and `tests/fixtures/.video_ilace_seg_a.m2v` currently carry exactly those same three hashes, which is what makes Task 2's whole-corpus before/after comparison meaningful. These are LOCAL bytes; the committed `tests/golden/CORPUS_DIGEST.txt` lines (`c04e6c51…` for `video_ilace_tff.mp4`, `cfebdd8e…` for `video_ilace_bff.mp4`, `7e655416…` for `.video_ilace_seg_a.m2v`) are the designated x64-linux CI runner's bytes and differ for the CPU-SIMD-dispatch reason recorded in STATE.md (03-16). Do not reconcile the two sets.

    If ANY pair differs, STOP, commit nothing, and report which pair and under which binary — a mismatch means seven committed digest lines are about to move, which is the single outcome this task exists to prevent.

    STEP B — the edit, three sites, nothing else. Replace only the `-vf` argument line at each of the three recipes, adding `-r 25/2` to that same physical line immediately after the closing double quote of the `-vf` argument and before the trailing line-continuation backslash; the following line stays `-c:v mpeg2video -flags +ilme+ildct \` exactly as it is.

    TFF sites (`video_ilace_tff.mp4` and the `$ILACE_SEG_A` `.m2v`), verbatim:
    `-vf "setparams=field_mode=tff,separatefields,select='eq(mod(n\,4)\,0)+eq(mod(n\,4)\,3)',weave=first_field=top,setparams=field_mode=tff" -r 25/2 \`

    BFF site (`video_ilace_bff.mp4`), verbatim:
    `-vf "setparams=field_mode=bff,separatefields,select='eq(mod(n\,4)\,0)+eq(mod(n\,4)\,3)',weave=first_field=bottom,setparams=field_mode=bff" -r 25/2 \`

    The backslash-escaped commas inside the `select` expression are mandatory and survive bash's double quotes untouched — this exact inline spelling, in this exact continuation layout, was rendered at planning time with the pinned generator and reproduced `bc3740d9…` byte for byte. Do not "tidy" the escapes, do not move the expression into a shell variable, do not reflow the continuation.

    `-r 25/2` is an OUTPUT option and both alternatives are forbidden with cause: an `fps=25/2` FILTER in the chain drops the last frame (24 instead of 25) and changes the bytes; omitting the rate entirely also changes the bytes, because `weave`'s output link still advertises 25 fps with a frame on every other tick, so the CLI duplicates frames to fill it. Both rejected variants are rendered by the harness (`_fps`, `_plain`) if you want to see the divergence rather than trust this sentence.

    Change NOTHING else at the three sites: the same `-f lavfi -i "testsrc2=…"` sources and durations, the same `-c:v mpeg2video -flags +ilme+ildct`, the same `-flags +bitexact -fflags +bitexact -y`, the same output paths and ordering. Leave untouched: the `cp` for `video_ilace_tff_copy.mp4`, the `ILACE_SEG_B` progressive recipe, the `cat "$ILACE_SEG_A" "$ILACE_SEG_B" > "$ILACE_MIXED_RAW"` concatenation and the `-f mpegvideo … -c copy` remux to `video_ilace_mixed.mp4`. Output filenames must not move: `scripts/check_corpus.sh` derives its 138-name expected list mechanically from the literal `$OUT_DIR/<name>` tokens in this file's own source.

    STEP C — rewrite the comment paragraph. Keep the first paragraph (lines 1269-1281, the `mpeg4video_parser.c` / `mpegvideo_parser.c` / `mpeg2video` rationale and the TFF -> `AV_FIELD_TT` / BFF -> `AV_FIELD_BB` read-back expectations) intact and still true. Replace the paragraph that begins `# Quick task 260914-ryu:` with a quick-260914-t47 paragraph, in the same comment style, stating all of:
    - that 260914-ryu's finding was incomplete: FFmpeg n9.0.1's configure gates BOTH filters on GPL (`tinterlace_filter_deps="gpl"` and `interlace_filter_deps="gpl"`), so the `interlace` filter is ALSO absent from the LGPL Windows pinned build, which is how run 34882668138's `build (x64-windows-static-md)` step 9 died with `No such filter: 'interlace'` (use the words "ALSO" and "GPL" in that sentence — an acceptance gate reads for it, so the corrected finding cannot silently disappear in a later edit);
    - what the LGPL chain does and why it is equivalent: the old filter was the temporal interleave-top/bottom mode without lowpass, i.e. the upper field of odd frames woven with the lower field of even frames at half the frame rate. `setparams=field_mode=tff` makes `separatefields` emit each frame's top field first (F1T,F1B,F2T,F2B,…); the `select` keeps field frames with n mod 4 in {0,3} (F1T,F2B,F3T,F4B,…); `weave=first_field=top` writes the first frame of each pair into the even lines and the second into the odd lines. For BFF, `setparams=field_mode=bff` makes `separatefields` emit the bottom field first, the same `select` keeps (F1B,F2T,…), and `weave=first_field=bottom` writes them into the odd and even lines respectively;
    - why the rate is set with the output option `-r 25/2`: it hands the encoder the same 12.5 fps time base the old filter advertised, so the MPEG-2 sequence header and the mp4 timing tables come out identical. An `fps=25/2` filter must NOT be used instead — it drops the last frame (24 instead of 25) and moves the bytes — and the option must not be omitted, because `weave`'s output link advertises 25 fps and the CLI would duplicate frames to fill it;
    - that the committed digest lines for `video_ilace_tff.mp4`, `video_ilace_tff_copy.mp4`, `video_ilace_bff.mp4`, `video_ilace_mixed.mp4` and the three `.video_ilace_*.m2v` sidecars are unchanged BY DESIGN, proven old-vs-new on a generator that has both filters before the swap landed.

    Two constraints on the comment text itself. First, do NOT write any literal `$OUT_DIR/<name>` token into it — `check_corpus.sh` extracts those from comments too, and a name that is not a real fixture would change the expected count away from 138. Second, naming the GPL-gated filters and the rejected `fps=25/2` spelling inside this comment is intentional and permitted: every negative gate below strips comment lines first, precisely so an explanatory comment cannot invalidate a gate about executed code (the 03-18 lesson recorded in STATE.md).
  </action>

  <verify>
    <automated>
# (1) Byte identity, OLD vs NEW, three recipes, BOTH generators. sha256 printed per file.
set -eu
SC="${SCRATCH:-$(mktemp -d -t mdiff-t47-XXXXXX)}"
mkdir -p "$SC"
echo "scratch: $SC"
SEL="select='eq(mod(n\,4)\,0)+eq(mod(n\,4)\,3)'"
PIN=.ffmpeg-pinned/linux-x86_64/ffmpeg
[ -x "$PIN" ] || { echo "PRECONDITION FAILED: pinned linux generator missing at $PIN"; exit 1; }
command -v ffmpeg >/dev/null || { echo "PRECONDITION FAILED: no workstation ffmpeg on PATH"; exit 1; }
for tag in ws pin; do
  case "$tag" in ws) FF=ffmpeg ;; pin) FF="$PIN" ;; esac
  for scan in tff bff; do
    ff=top; [ "$scan" = bff ] && ff=bottom
    "$FF" -hide_banner -loglevel error -f lavfi -i "testsrc2=size=320x240:rate=25:duration=2" \
      -vf "interlace=scan=$scan:lowpass=off,setparams=field_mode=$scan" \
      -c:v mpeg2video -flags +ilme+ildct -flags +bitexact -fflags +bitexact -y "$SC/${tag}_${scan}_old.mp4"
    "$FF" -hide_banner -loglevel error -f lavfi -i "testsrc2=size=320x240:rate=25:duration=2" \
      -vf "setparams=field_mode=$scan,separatefields,$SEL,weave=first_field=$ff,setparams=field_mode=$scan" -r 25/2 \
      -c:v mpeg2video -flags +ilme+ildct -flags +bitexact -fflags +bitexact -y "$SC/${tag}_${scan}_new.mp4"
  done
  "$FF" -hide_banner -loglevel error -f lavfi -i "testsrc2=size=320x240:rate=25:duration=1" \
    -vf "interlace=scan=tff:lowpass=off,setparams=field_mode=tff" \
    -c:v mpeg2video -flags +ilme+ildct -flags +bitexact -fflags +bitexact -y "$SC/${tag}_sega_old.m2v"
  "$FF" -hide_banner -loglevel error -f lavfi -i "testsrc2=size=320x240:rate=25:duration=1" \
    -vf "setparams=field_mode=tff,separatefields,$SEL,weave=first_field=top,setparams=field_mode=tff" -r 25/2 \
    -c:v mpeg2video -flags +ilme+ildct -flags +bitexact -fflags +bitexact -y "$SC/${tag}_sega_new.m2v"
done
sha256sum "$SC"/ws_* "$SC"/pin_*
FAIL=0
for tag in ws pin; do
  cmp "$SC/${tag}_tff_old.mp4"  "$SC/${tag}_tff_new.mp4"  || FAIL=1
  cmp "$SC/${tag}_bff_old.mp4"  "$SC/${tag}_bff_new.mp4"  || FAIL=1
  cmp "$SC/${tag}_sega_old.m2v" "$SC/${tag}_sega_new.m2v" || FAIL=1
done
[ "$FAIL" -eq 0 ] || { echo "FAIL: at least one OLD/NEW pair differs -- seven committed digest lines would move"; exit 1; }
# Cross-reference against the hashes observed at planning time (informational, not fatal).
EXP_TFF=bc3740d9b7911dad97e1744d707ac4d41476a3589f4806762ee6a1d1283a898e
GOT_TFF=$(sha256sum "$SC/pin_tff_new.mp4" | cut -d' ' -f1)
[ "$GOT_TFF" = "$EXP_TFF" ] || echo "WARN: pinned TFF hash $GOT_TFF != planning-time $EXP_TFF (environment drift -- investigate before trusting the corpus comparison in Task 2)"
echo "BYTE-IDENTITY PROOF: 3 recipes x 2 generators, all identical"
    </automated>
    <fails_when>Any OLD/NEW `cmp` differs under either generator; either generator is missing; or an ffmpeg invocation exits non-zero (e.g. a filter absent from the build being used for the proof).</fails_when>

    <automated>
# (2) Executed lines carry only LGPL filters, in exactly the sanctioned shape. Comment lines are stripped first.
set -eu
CODE=$(grep -vE '^[[:space:]]*#' scripts/gen_corpus.sh)
chk() { n=$(printf '%s\n' "$CODE" | grep -cE -- "$1" || true); [ "$n" -eq "$2" ] || { echo "FAIL: pattern [$1] matched $n executed line(s), expected $2"; exit 1; }; echo "ok [$1] = $n"; }
chk 'tinterlace' 0
chk 'interlace=' 0
chk 'lowpass' 0
chk 'fps=25/2' 0
chk '^[[:space:]]*-vf "setparams=field_mode=(tff|bff),separatefields,select=.*,weave=first_field=(top|bottom),setparams=field_mode=(tff|bff)" -r 25/2' 3
chk 'weave=first_field=top' 2
chk 'weave=first_field=bottom' 1
chk '\-r 25/2' 3
echo "FILTERCHAIN GATE: clean (0 GPL-gated filters / 0 lowpass / 0 fps=25/2 on executed lines; 3 LGPL chains, each with -r 25/2; 2 top + 1 bottom)"
# check 5 from the task description, stated its own way: every remaining mention is a comment.
NONCOMMENT=$(grep -nE 'tinterlace|interlace=' scripts/gen_corpus.sh | grep -vE '^[0-9]+:[[:space:]]*#' | wc -l | tr -d ' ')
[ "$NONCOMMENT" -eq 0 ] || { echo "FAIL: $NONCOMMENT non-comment line(s) still name a GPL-gated filter"; exit 1; }
echo "--- surviving mentions (all comments, for the record) ---"
grep -nE 'tinterlace|interlace=' scripts/gen_corpus.sh || true
    </automated>
    <fails_when>A GPL-gated filter name, `lowpass` or `fps=25/2` survives on any non-comment line; the new chain is present at other than exactly 3 sites, or with other than a 2-top/1-bottom split; `-r 25/2` appears on other than exactly 3 executed lines (a missed site, or a fourth recipe accidentally rewritten).</fails_when>

    <automated>
# (3) The rewritten comment records the corrected finding, and adds no phantom fixture token.
set -eu
grep -qE '^[[:space:]]*#.*interlace_filter_deps' scripts/gen_corpus.sh || { echo "FAIL: comment does not cite the configure gate by name"; exit 1; }
grep -qiE '^[[:space:]]*#.*ALSO.*GPL' scripts/gen_corpus.sh || { echo "FAIL: comment does not state that the interlace filter is ALSO GPL-gated"; exit 1; }
grep -qE '^[[:space:]]*#.*separatefields' scripts/gen_corpus.sh || { echo "FAIL: comment does not name the replacement chain"; exit 1; }
grep -qE '^[[:space:]]*#.*weave' scripts/gen_corpus.sh || { echo "FAIL: comment does not explain the weave step"; exit 1; }
grep -qE '^[[:space:]]*#.*-r 25/2' scripts/gen_corpus.sh || { echo "FAIL: comment does not explain why -r 25/2 is required"; exit 1; }
grep -qE '^[[:space:]]*#.*BY DESIGN' scripts/gen_corpus.sh || { echo "FAIL: comment does not record that the committed digest lines are unchanged BY DESIGN"; exit 1; }
grep -q '260914-ryu' scripts/gen_corpus.sh && { echo "FAIL: the superseded 260914-ryu paragraph was not replaced"; exit 1; } || true
bash scripts/check_corpus.sh | tail -1 | grep -q 'Verified 138 fixture(s)' || { echo "FAIL: expected-fixture extraction no longer yields 138 -- the comment probably introduced a phantom \$OUT_DIR/<name> token, or an output path moved"; exit 1; }
[ "$(git diff --name-only)" = "scripts/gen_corpus.sh" ] || { echo "FAIL: unexpected files modified:"; git diff --name-only; exit 1; }
[ -z "$(git status --porcelain -- tests/)" ] || { echo "FAIL: something under tests/ was modified"; git status --porcelain -- tests/; exit 1; }
echo "COMMENT + SCOPE GATE: clean"
    </automated>
    <fails_when>The rewritten comment omits the configure-gate citation, the ALSO-GPL correction, the chain description, the `-r 25/2` rationale or the BY DESIGN statement; the superseded 260914-ryu paragraph survives; `check_corpus.sh` no longer derives 138 fixtures; or any file other than `scripts/gen_corpus.sh` is dirty.</fails_when>
  </verify>

  <acceptance_criteria>
    - Six OLD/NEW `cmp` comparisons (3 recipes x 2 generators) report identical bytes, with all 12 sha256 values printed.
    - On comment-stripped lines of `scripts/gen_corpus.sh`: 0 hits for `tinterlace`, 0 for `interlace=`, 0 for `lowpass`, 0 for `fps=25/2`.
    - Exactly 3 executed lines match the new chain shape and carry `-r 25/2` on the same line; `weave=first_field=top` appears twice, `weave=first_field=bottom` once.
    - `grep -nE 'tinterlace|interlace=' scripts/gen_corpus.sh` returns only comment lines (check 5 of the task description).
    - The comment cites `interlace_filter_deps`, says the filter is ALSO GPL-gated, names `separatefields`/`weave`, explains `-r 25/2`, says BY DESIGN, and no longer carries the 260914-ryu paragraph.
    - `bash scripts/check_corpus.sh` still derives and verifies exactly 138 fixtures.
    - `git diff --name-only` lists `scripts/gen_corpus.sh` and nothing else; `git status --porcelain -- tests/` is empty.
  </acceptance_criteria>

  <done>The three recipes use only LGPL-available filters with `-r 25/2` in the mandated position, byte identity is proven on both generators before the edit landed, and the comment records the corrected license finding and the new chain's semantics.</done>

  <reversibility rating="reversible">Three filterchain lines and one comment paragraph in a generator script, proven byte-neutral before landing; a one-commit revert with no fixture or golden consequence.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Whole-corpus regeneration, regression sweep, and commit</name>

  <files>scripts/gen_corpus.sh</files>

  <read_first>
    - `scripts/assert_corpus_digest.sh` lines 1-40 — what it compares and against which file, so its local exit 1 can be reported accurately rather than guessed at.
    - `scripts/lint_corpus_digest_provenance.sh` lines 1-25 — the four clauses it runs over `tests/golden/CORPUS_DIGEST.txt`.
  </read_first>

  <action>
    Run the sweep in `<verify>` from the repository root and report each numbered result in the SUMMARY, including the ones that are expected to be non-zero.

    Sequence matters. Record the sha256 of the seven interlace artifacts BEFORE regenerating (`tests/fixtures/video_ilace_tff.mp4`, `video_ilace_tff_copy.mp4`, `video_ilace_bff.mp4`, `video_ilace_mixed.mp4`, `.video_ilace_seg_a.m2v`, `.video_ilace_seg_b.m2v`, `.video_ilace_mixed_raw.m2v`); the on-disk set was confirmed at planning time to be a faithful pre-edit product of the same pinned generator, so a post-regeneration difference on any of the seven is a hard failure, not drift to be waved through. Then run `bash scripts/gen_corpus.sh`, then compare all seven.

    `bash scripts/assert_corpus_digest.sh` exiting 1 locally is EXPECTED and must not be "fixed": that digest holds the designated x64-linux CI runner's bytes, and the same pinned binary emits different bytes on this workstation for the CPU-SIMD-dispatch reason recorded in STATE.md (03-16). Quote its output in the SUMMARY and state explicitly that no digest file was edited. The load-bearing local evidence is the seven-artifact before/after comparison plus Task 1's proof, not this script.

    `tests/fixtures/GENERATOR_MANIFEST.json` is tracked and its `generated_at` timestamp is rewritten by every generator run. After the sweep, restore it with `git checkout -- tests/fixtures/GENERATOR_MANIFEST.json` so the commit carries only `scripts/gen_corpus.sh`. Do not commit a re-stamped manifest and do not add it to `.gitignore`.

    Then commit on the current branch `gsd/phase-04-video-analysis` with a `fix(quick-260914-t47): ` subject naming the corrected finding (both filters GPL-gated in configure) and the byte-neutral LGPL chain. Never use `--no-verify`. Never push.
  </action>

  <verify>
    <automated>
# (A) Pre-run hashes of the seven interlace artifacts, (B) full regeneration, (C) unchanged-bytes proof.
set -eu
SC="${SCRATCH:-$(mktemp -d -t mdiff-t47b-XXXXXX)}"
mkdir -p "$SC"
ART="tests/fixtures/video_ilace_tff.mp4 tests/fixtures/video_ilace_tff_copy.mp4 tests/fixtures/video_ilace_bff.mp4 tests/fixtures/video_ilace_mixed.mp4 tests/fixtures/.video_ilace_seg_a.m2v tests/fixtures/.video_ilace_seg_b.m2v tests/fixtures/.video_ilace_mixed_raw.m2v"
sha256sum $ART > "$SC/before.sha256"
cat "$SC/before.sha256"
bash -n scripts/gen_corpus.sh && echo "bash -n: OK"
bash scripts/gen_corpus.sh
sha256sum $ART > "$SC/after.sha256"
cat "$SC/after.sha256"
diff -u "$SC/before.sha256" "$SC/after.sha256" || { echo "FAIL: a regenerated interlace artifact changed bytes -- the filterchain swap is NOT byte-neutral"; exit 1; }
echo "SEVEN-ARTIFACT GATE: all interlace fixtures and sidecars byte-identical across the swap"
    </automated>
    <fails_when>`scripts/gen_corpus.sh` fails to parse or exits non-zero (e.g. a filter missing from the resolved generator, or the pin gate rejecting it), or any of the seven interlace artifacts changes hash across the regeneration.</fails_when>

    <automated>
# (D) Corpus + lint sweep. assert_corpus_digest.sh is run for the record and is EXPECTED to exit 1 locally.
set -eu
bash scripts/check_corpus.sh | tail -2
bash scripts/lint_corpus_digest_provenance.sh | tail -1
bash scripts/lint_bash4_builtins.sh | tail -1
bash scripts/test_gen_corpus_pin_gate.sh | tail -1
echo "--- assert_corpus_digest.sh (local exit 1 EXPECTED: digest holds CI-runner bytes) ---"
set +e
bash scripts/assert_corpus_digest.sh > "${SCRATCH:-/tmp}/t47_assert_digest.log" 2>&1
DIGEST_RC=$?
set -e
tail -15 "${SCRATCH:-/tmp}/t47_assert_digest.log"
echo "assert_corpus_digest.sh exit code: $DIGEST_RC (1 is expected on this workstation; 0 would also be fine; any other code is a real failure)"
[ "$DIGEST_RC" -eq 0 ] || [ "$DIGEST_RC" -eq 1 ] || { echo "FAIL: assert_corpus_digest.sh exited $DIGEST_RC"; exit 1; }
git checkout -- tests/fixtures/GENERATOR_MANIFEST.json
[ -z "$(git status --porcelain -- tests/)" ] || { echo "FAIL: something under tests/ is dirty after restoring the manifest"; git status --porcelain -- tests/; exit 1; }
[ "$(git diff --name-only)" = "scripts/gen_corpus.sh" ] || { echo "FAIL: unexpected files modified:"; git diff --name-only; exit 1; }
[ -z "$(git status --porcelain -- tests/golden/ scripts/ffmpeg_pin.json scripts/lint_corpus_digest_provenance.sh scripts/gen_corpus.ps1 claude_docs/)" ] || { echo "FAIL: a forbidden file was modified"; exit 1; }
echo "REGRESSION SWEEP: clean"
    </automated>
    <fails_when>`check_corpus.sh` reports other than 138 present non-empty fixtures; the digest-provenance lint, the bash-3.2 portability lint or the pin-gate test exits non-zero; `assert_corpus_digest.sh` exits with something other than 0 or 1; or any golden, pin, lint, PowerShell generator, `claude_docs/` file, or `tests/` path is dirty after the manifest restore.</fails_when>

    <automated>
# (E) The C++ suite still passes -- the VIDEO-06 field-order tests read the regenerated fixtures.
set -eu
cmake --build --preset x64-linux 2>&1 | tail -5
ctest --preset x64-linux --output-on-failure 2>&1 | tail -10
    </automated>
    <fails_when>The build fails, or `ctest` reports other than 771 tests passed with 0 failures — in particular any VIDEO-06 field-order test, which reads the regenerated interlace fixtures and would notice a field-order or frame-count change the hash gate somehow missed.</fails_when>
  </verify>

  <acceptance_criteria>
    - All seven interlace artifacts carry identical sha256 before and after a full `bash scripts/gen_corpus.sh` run (both hash lists quoted in the SUMMARY).
    - `bash scripts/check_corpus.sh` verifies 138 fixtures present and non-empty.
    - `bash scripts/lint_corpus_digest_provenance.sh`, `bash scripts/lint_bash4_builtins.sh` and `bash scripts/test_gen_corpus_pin_gate.sh` each exit 0.
    - `bash scripts/assert_corpus_digest.sh` exits 1 locally, its output is quoted, and the SUMMARY states plainly that this is expected and that no digest file was edited.
    - `cmake --build --preset x64-linux` succeeds and `ctest --preset x64-linux --output-on-failure` reports 771 passed, 0 failed.
    - `tests/fixtures/GENERATOR_MANIFEST.json` restored to HEAD; `git diff --name-only` lists `scripts/gen_corpus.sh` only.
    - One commit on `gsd/phase-04-video-analysis` with a `fix(quick-260914-t47): ` subject, created without `--no-verify` and not pushed.
  </acceptance_criteria>

  <done>The regenerated corpus is byte-identical on every interlace artifact, every lint and the full test suite are green, the expected digest mismatch is documented rather than papered over, and the change is committed locally on the phase branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator script -> external ffmpeg binary | `scripts/gen_corpus.sh` hands filterchain strings to a pinned third-party binary whose available filter set varies with its license configuration (GPL on the Linux/macOS pins, LGPL on the Windows pin). A filter that exists on the authoring machine may not exist on the executing one — this task exists because that already happened twice. |
| generator script -> committed goldens | Recipe text silently determines the bytes that `tests/golden/CORPUS_DIGEST.txt` pins. Nothing in the edit path forces the two to agree, and the workstation cannot reproduce the designated leg's bytes to check. |
| comment text -> `check_corpus.sh`'s expected-fixture list | The completeness gate extracts `$OUT_DIR/<name>` tokens from the generator's source text, comments included, so prose can change what the gate expects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-t47-01 | Tampering | the three `-vf` filterchains in `scripts/gen_corpus.sh` | high | mitigate | A filterchain edit that moves bytes would invalidate seven committed digest lines while local checks still looked plausible (the corpus-digest-rewrite trap already recorded against this repo). Mitigated by making a two-generator OLD/NEW `cmp` proof a blocking precondition of the edit, by banning the `fps=25/2` and no-rate variants on executed lines, and by re-proving byte identity across a full corpus regeneration in Task 2. |
| T-t47-02 | Denial of Service | x64-windows-static-md CI leg, corpus step 9 | high | mitigate | The current recipes hard-fail the Windows corpus step with exit 8, blocking the leg. Mitigated by moving to filters audited as ungated in n9.0.1's configure (setparams, separatefields, select, weave). Residual risk — one of those four also turning out to be unavailable in the BtbN LGPL build — is bounded and can only be closed by a real CI run; it is not locally observable, and no further local substitution exists to fall back to. |
| T-t47-03 | Tampering | `tests/golden/CORPUS_DIGEST*.txt` | high | mitigate | The tempting "fix" for a local `assert_corpus_digest.sh` failure is to regenerate the digest from workstation bytes, which would silently replace the designated-leg record. Mitigated by declaring exit 1 EXPECTED up front, by gating on `git status --porcelain -- tests/golden/` being empty, and by keeping `lint_corpus_digest_provenance.sh` in the sweep. |
| T-t47-04 | Tampering | recipe output paths and comment prose vs `check_corpus.sh`'s mechanical extraction | medium | mitigate | A moved output name, or a phantom `$OUT_DIR/<name>` token introduced by the rewritten comment, would change the expected list and could make the completeness gate scan the wrong set. Mitigated by the explicit no-`$OUT_DIR`-token instruction and by asserting the derived count stays exactly 138 in both tasks. |
| T-t47-05 | Tampering | `tests/fixtures/GENERATOR_MANIFEST.json` | low | mitigate | The tracked manifest is re-stamped by every generator run; committing it would add unrelated provenance churn to a byte-neutral fix. Mitigated by an explicit restore step plus an allow-one-file `git diff --name-only` gate. |
| T-t47-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install occurs in this plan. No dependency is added; the only external binaries invoked are the already-pinned SHA-256-verified ffmpeg and the workstation ffmpeg, used solely to render throwaway scratch files. |
</threat_model>

<verification>
1. Byte identity is proven, not assumed: three OLD/NEW pairs rendered twice (workstation and pinned generators), sha256 printed for all twelve files, `cmp` asserted on all six pairs — then re-proven end-to-end by a full corpus regeneration whose seven interlace artifacts must hash identically before and after.
2. On comment-stripped lines of `scripts/gen_corpus.sh`: zero GPL-gated filter names, zero `lowpass`, zero `fps=25/2`; exactly three new chains, each carrying `-r 25/2` on the `-vf` line; `grep -nE 'tinterlace|interlace=' scripts/gen_corpus.sh` returns comment lines only.
3. The rewritten comment states the corrected finding (BOTH filters GPL-gated), names the LGPL chain, explains the field bookkeeping and the `-r 25/2` requirement, and records the unchanged digests as BY DESIGN.
4. `bash scripts/gen_corpus.sh` completes; `check_corpus.sh` verifies 138 fixtures; `lint_corpus_digest_provenance.sh`, `lint_bash4_builtins.sh` and `test_gen_corpus_pin_gate.sh` pass; `assert_corpus_digest.sh` exits 1 as expected and is reported, not fixed.
5. `cmake --build --preset x64-linux` then `ctest --preset x64-linux --output-on-failure` still reports 771/771.
6. Scope holds: `git diff --name-only` is exactly `scripts/gen_corpus.sh`; goldens, pin file, provenance lint, PowerShell generator, `claude_docs/` and every fixture are untouched, with `GENERATOR_MANIFEST.json` restored to HEAD.
</verification>

<success_criteria>
`scripts/gen_corpus.sh` generates the four interlace fixtures and three `.m2v` sidecars byte-for-byte as before while using only filters an LGPL ffmpeg build provides — so step 9 of `build (x64-windows-static-md)` can get past `video_ilace_tff.mp4` with no golden needing to move.
</success_criteria>

<output>
Create `.planning/quick/260914-t47-replace-the-gpl-only-interlace-filter-in/260914-t47-SUMMARY.md` when done.
</output>
