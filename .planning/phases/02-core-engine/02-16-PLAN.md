---
phase: 02-core-engine
plan: 16
type: execute
wave: 2
depends_on: ["02-14", "02-15"]
files_modified:
  - .github/workflows/ci.yml
autonomous: false
gap_closure: true
gap_ids: [G-02-3, G-02-4]
requirements: [BUILD-01, BUILD-05]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Both new portability lints run on every push as steps of the existing `lint` job, so the FAIL()-then-statement shape and the case-colliding-fixture shape are caught by a required status check rather than by a platform leg that only compiles half the test tree."
    - "The `lint` job's `name:` field is unchanged. `lint (ENG-16 boundary)` is a required status check context on the active `main merge gate` ruleset (id 20862843); renaming the job would orphan that required check and silently remove a merge gate while appearing to be a tidy-up."
    - "Both lint scripts were executed locally and exited 0 before the branch was pushed, so wiring them cannot be what turns a previously-green required check red."
    - "A CI run exists whose headSha equals the local HEAD containing both gap fixes, and its per-job conclusions were read from the run itself rather than inferred from a prior run or from the local build."
    - statement: "Job `build (x64-windows-static-md)` reaches Build step [97/97] with zero occurrences of C4702, C2220 and C4996 in its log, then executes its Test step and reports 297 tests with 0 failed."
      verification: backstop
    - statement: "Job `build (arm64-osx)` reports `100% tests passed, 0 tests failed out of 297`, with test #40 `unit.dir_pairing: the returned order is byte-wise sorted` at `Passed` and no `tests/unit/test_dir_pairing.cpp:108: FAILED` in its log."
      verification: backstop
    - statement: "Jobs `lint (ENG-16 boundary)` and `build (x64-linux)` are `success` on that same run, so neither wave-1 plan regressed a check that was already green."
      verification: backstop
    - statement: "Jobs `build (x64-osx)` and `build (arm64-linux)` are still `failure` with their previously recorded signatures (`found architecture 'x86_64', required architecture 'arm64'` and a `Register vcpkg NuGet feed` step exiting 1 with no output), confirming their redness is the recorded pre-existing non-blocking condition and not a new effect of this phase."
      verification: backstop
  prohibitions:
    - "Do not change the `lint` job's `name:` field, its `runs-on`, or its `permissions`. The name is the required-status-check context string; changing it removes a merge gate without any visible failure."
    - "Do not add a `continue-on-error`, an `if: always()`, or any other escape to either new lint step. A gate wired so it cannot fail the job is decoration."
    - "Do not modify any build leg, the matrix, the ctest count guard, or the vcpkg cache configuration in this plan. The whole value of the next run is a clean read of two test-only fixes against an otherwise byte-identical pipeline."
    - "Do not plan, attempt, or opportunistically fix `CI-x64-osx` or `CI-arm64-linux`. They reproduce on `main`, are not required checks, and are recorded as out of scope; touching them would confound the read this plan exists to perform."
    - "Do not report the phase as CI-clean on the basis of a partial read. Every one of the four required contexts must be read from the new run by name, and a leg that is red for a NEW reason must be written down as new information rather than absorbed into an existing gap."
  artifacts:
    - ".github/workflows/ci.yml — two new steps in the existing `lint` job, invoking the two new portability lints"
    - ".planning/phases/02-core-engine/02-16-SUMMARY.md — the per-job CI read, with run id, headSha, and the exact observable for each of the four required contexts"
  key_links:
    - "local branch gsd/phase-2-core-engine -> git push -> PR #2 head -> a new GitHub Actions run -> the only oracle that can observe MSVC C4702 or an APFS case collision"
    - ".github/workflows/ci.yml `lint` job `name:` -> ruleset 20862843's required_status_checks context `lint (ENG-16 boundary)` -> whether the merge gate still has four required contexts or three"
    - "scripts/lint_dead_code_after_fail.sh + scripts/lint_fixture_case_collisions.sh -> the `lint` job -> every future push, on every platform, in seconds"
---

<objective>
Deliver and observe. Wire the two portability lints created by `02-14-PLAN.md` and `02-15-PLAN.md` into the existing `lint` job, push the branch, and read the resulting CI run against named, per-job observables — because GitHub Actions is the only oracle in existence for either gap, and this phase has twice now been told that a locally-clean fix is not a green leg.

Purpose: `build (x64-windows-static-md)`, `build (arm64-osx)`, `build (x64-linux)` and `lint (ENG-16 boundary)` are the four required status check contexts on the active `main merge gate` ruleset (id 20862843). Two of them have been red for the whole of Phase 2. The wave-1 plans fixed the diagnosed defects; nothing in this repository can tell you whether that was sufficient. This plan closes that loop explicitly, and records what it finds whether the answer is good or not.

Output: two CI steps, one push, one per-job read, and an honest verdict.

**Honesty about what this plan can conclude.** The two legs are in very different positions and the verdict must say so rather than averaging them.

- **`arm64-osx`** ran end to end in run 31943688186 — 296 of 297 passed, one failure, no unexecuted remainder. A green leg here is a genuinely likely outcome.
- **`x64-windows-static-md`** has never compiled build steps [66/97] through [97/97] in any run in the project's history: four unit TUs (`test_dir_pairing.cpp`, `test_golden.cpp`, `test_process_spawn.cpp`, `test_worker_pool.cpp`), all fourteen integration TUs, the `tests/support` sources compiled into the integration target, and the link steps. The Windows test suite has never executed at all. A NEW first failure on this leg is a budgeted outcome, not a failure of the wave-1 work — the first 32 steps of this leg took two rounds to clear.

Nothing in this plan should be written, or read, as a promise that one push produces two green legs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# The gap definitions this run is the oracle for.
@.planning/phases/02-core-engine/02-UAT.md
@.planning/phases/02-core-engine/02-VERIFICATION.md

# The two wave-1 plans whose scripts this plan wires and whose fixes this
# plan observes.
@.planning/phases/02-core-engine/02-14-PLAN.md
@.planning/phases/02-core-engine/02-15-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire both portability lints into the existing lint job</name>
  <precondition>Both wave-1 plans have landed on this branch: `scripts/lint_dead_code_after_fail.sh` and `scripts/lint_fixture_case_collisions.sh` both exist and both exit 0 when run from the repository root. If either is missing or non-zero, stop — wiring a failing script into a required check would turn a green gate red for a reason unrelated to either gap.</precondition>
  <reversibility rating="reversible">Two `run:` steps appended to one existing job. Removing them is a two-hunk revert with no state, no cache and no dependency implication.</reversibility>
  <read_first>
- `.github/workflows/ci.yml`, the `lint` job block (currently around lines 370-384) — its `name:`, `runs-on: ubuntu-24.04`, `permissions: contents: read`, the submodule-free checkout and its comment explaining why no submodules are needed, and the two existing `run: bash scripts/lint_*.sh` steps whose shape the new ones copy exactly.
- `scripts/lint_dead_code_after_fail.sh` and `scripts/lint_fixture_case_collisions.sh` — both delivered by wave 1. Read them to confirm each is invoked as `bash scripts/<name>.sh` from the repository root, needs no execute bit, no submodules, and no build tree.
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
Append exactly two steps to the existing `lint` job, after its two current lint steps, matching their shape byte for byte: a `- name:` line and a `run: bash scripts/<script>.sh` line, nothing else. Name them `Run dead-code-after-FAIL portability lint` and `Run fixture case-collision portability lint`.

**Do not touch the job's `name:` field.** It currently reads `lint (ENG-16 boundary)`, and that exact string is a required status check context on the repository's active `main merge gate` ruleset (id 20862843) alongside `build (arm64-osx)`, `build (x64-windows-static-md)` and `build (x64-linux)`. GitHub matches required checks by context string, so widening the job's responsibilities is tempting grounds for renaming it to something like "lint (portability)" — and that rename would silently orphan the required check: the ruleset would wait forever for a context nothing produces, or the branch protection would simply stop enforcing a gate, with no error anywhere. If the name genuinely wants updating later, the ruleset must be edited in the same change; that is deliberately not this plan's work. Record this constraint as a comment above the job's `name:` line so the next editor sees it before making the same reasonable-looking change.

Change nothing else in the file — not `runs-on`, not `permissions`, not the checkout step, not any build leg, not the matrix, not the ctest count guard `02-13-PLAN.md` repaired, not the vcpkg cache configuration. The entire value of the next run is a read of two test-only source fixes against an otherwise byte-identical pipeline; any additional pipeline change makes a red or green leg ambiguous.

Do not add `continue-on-error` or an `if:` condition to either new step. A lint wired so it cannot fail the job is decoration, and this repository already has one documented instance of a gate that silently stopped biting.

Both scripts run on `ubuntu-24.04` only, need no submodules, no vcpkg, and no build tree — they read first-party sources exclusively, which is exactly why the `lint` job is the correct home rather than a per-platform build leg. Note that in passing where the job's existing "No submodules needed" comment already makes the same point.
  </action>
  <acceptance_criteria>
- **Locally provable.** `grep -c 'name: lint (ENG-16 boundary)' .github/workflows/ci.yml` returns exactly `1` — the required-check context string is intact.
- **Locally provable.** `grep -c 'bash scripts/lint_dead_code_after_fail.sh' .github/workflows/ci.yml` returns exactly `1` and `grep -c 'bash scripts/lint_fixture_case_collisions.sh' .github/workflows/ci.yml` returns exactly `1`.
- **Locally provable.** `grep -c 'continue-on-error' .github/workflows/ci.yml` returns exactly `0`.
- **Locally provable.** `git diff --unified=0 .github/workflows/ci.yml` shows added lines only, all of them inside the `lint` job, and no removed or modified line anywhere — in particular no change to the ctest count guard, the matrix, or any build leg.
- **Locally provable.** The file still parses as YAML: `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0.
- **Locally provable.** Both scripts exit 0 from the repository root, so the two new steps cannot be what turns the `lint` job red.
- **CI-only.** On the new run, job `lint (ENG-16 boundary)` is `success` and its log contains the clean line from each of the four lint steps. Read in Task 2.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'name: lint (ENG-16 boundary)' .github/workflows/ci.yml)" -eq 1 && test "$(grep -c 'bash scripts/lint_dead_code_after_fail.sh' .github/workflows/ci.yml)" -eq 1 && test "$(grep -c 'bash scripts/lint_fixture_case_collisions.sh' .github/workflows/ci.yml)" -eq 1 && test "$(grep -c 'continue-on-error' .github/workflows/ci.yml)" -eq 0 && python3 -c "import yaml;yaml.safe_load(open('.github/workflows/ci.yml'))" && test "$(git diff --unified=0 .github/workflows/ci.yml | grep -c '^-[^-]')" -eq 0 && bash scripts/lint_dead_code_after_fail.sh && bash scripts/lint_fixture_case_collisions.sh && bash scripts/lint_eng16.sh && bash scripts/lint_check_id_strings.sh</automated>
  </verify>
  <!-- planner-discipline-allow: continue-on-error -->
  <done>The `lint` job runs four lint scripts instead of two; its `name:` field, `runs-on`, `permissions` and checkout are unchanged; a comment above the name records that the string is a required-check context and must not be edited without editing ruleset 20862843; no step carries an escape clause; the diff against `ci.yml` is additions only and contains nothing outside the `lint` job; the file still parses as YAML; and all four lint scripts exit 0 locally.</done>
</task>

<task type="auto">
  <name>Task 2: Push, then read the new run per required context</name>
  <precondition>`gh auth status` succeeds and the authenticated account can push to `origin` on branch `gsd/phase-2-core-engine`; PR #2 is open against that branch, so the push updates a PR head and triggers the `CI` workflow. `git status --porcelain` is empty — every wave-1 and Task-1 change is committed, because the run is only meaningful if its headSha equals the local HEAD.</precondition>
  <reversibility rating="reversible">A push to a feature branch with an open PR. Recoverable by a further commit; nothing is merged, released, or published to a registry by this step.</reversibility>
  <read_first>
- `.planning/phases/02-core-engine/02-UAT.md` §Gaps `G-02-3` and `G-02-4` `evidence:` fields, and §Pre-Existing CI Failures — the prior run's exact observables (`[60/97]`, `[65/97]`, C4702 at `test_report_model.cpp(56)` and `(57)`, `4 == 5` at `test_dir_pairing.cpp:108`, test #40, 296/297) are the baseline every criterion below is measured against, and the two non-blocking legs' recorded signatures are what stops their redness being misread.
- `.planning/phases/02-core-engine/02-14-PLAN.md` and `02-15-PLAN.md` `<verification>` sections — the precise list of Windows build steps that have never been compiled, so a NEW first failure on that leg is recognised as budgeted rather than reported as a regression.
  </read_first>
  <files>.planning/phases/02-core-engine/02-16-SUMMARY.md</files>
  <action>
Push the branch, wait for the run, then read it. Every claim below must come from the new run's own logs — not from a prior run, not from the local build, not from either wave-1 SUMMARY.

**1. Push.** Push `gsd/phase-2-core-engine` to `origin`. Capture the local HEAD sha first; it is the value every subsequent step matches against.

**2. Identify the run.** List the workflow runs for this branch and select the one whose `headSha` equals the captured local HEAD. Do not fall back to "the most recent run" — a run against a different sha proves nothing, and this phase has already been misled once by reading a run that predated the fixes. If no run appears within a couple of minutes, check whether the workflow was triggered at all before assuming it is queued.

**3. Wait for completion,** then enumerate every job with its conclusion.

**4. Read each of the four required contexts by name.** For each, record the job id and the specific observable, not a conclusion word:

- **`lint (ENG-16 boundary)`** — expect `success`. Confirm the log shows a clean line from all four lint steps, including the two added in Task 1. A green job whose new steps did not actually run would be a false pass.
- **`build (x64-linux)`** — expect `success` with `100% tests passed, 0 tests failed out of 297`. This leg was already green; its job here is to prove neither wave-1 plan regressed it.
- **`build (x64-windows-static-md)`** — the primary read for G-02-3. Confirm, in order: the Build step logs `Building CXX object tests\unit\CMakeFiles\mediadiff_unit_tests.dir\test_report_model.cpp.obj` with no diagnostic following it; the job log contains zero occurrences of `C4702`, zero of `C2220` and zero of `C4996`; the Build step reaches `[97/97]` rather than stopping with `Process completed with exit code 2`; and the Test step then runs and reports `Total Tests: 297` with 0 failed. If the Build step stops at a step between [66/97] and [97/97], record the step number, the object file and the first diagnostic verbatim — that is a NEW defect in a TU MSVC has never compiled, it is the budgeted outcome both wave-1 plans warned about, and it must be written down as new information rather than reported as G-02-3 having failed.
- **`build (arm64-osx)`** — the primary read for G-02-4. Confirm test #40 `unit.dir_pairing: the returned order is byte-wise sorted` reports `Passed`; the log contains no `tests/unit/test_dir_pairing.cpp:108: FAILED` and no `4 == 5` expansion; and the Test step ends with `100% tests passed, 0 tests failed out of 297`. If a different test fails, record its number, name, file and line verbatim.

**5. Read the two non-required legs only to confirm they are unchanged.** `build (x64-osx)` should still fail with the architecture mismatch on the vcpkg static libraries, and `build (arm64-linux)` should still fail at the `Register vcpkg NuGet feed` step exiting 1 with no output. Confirm each matches its recorded signature and say so. Do not fix either, do not open a gap for either, and do not let either count against the four required contexts. If either fails for a *different* reason than recorded, that is new information worth one line in the SUMMARY — still not this plan's work.

**6. Write the SUMMARY** with the run id, the headSha, and a per-job table carrying the exact observable for each of the six jobs. State plainly which of G-02-3 and G-02-4 the run confirms closed, which it does not, and whether a further round is needed. If the Windows leg is red at a new step, say so in the first paragraph rather than burying it — this phase's whole history is a sequence of fixes that worked and then revealed the next thing, and that pattern is only useful if it is reported plainly each time.

Useful command shapes, all verified to work against this repository: `gh run list --branch gsd/phase-2-core-engine --limit 5 --json databaseId,headSha,status,conclusion`; `gh run view <run-id> --json jobs -q '.jobs[] | "\(.databaseId) \(.name) \(.conclusion)"'`; `gh run view <run-id> --job <job-id> --log`. Grep the job log for the literal diagnostics rather than eyeballing it — a `grep -c` of zero for each MSVC error code is the criterion, and the full log is long.
  </action>
  <acceptance_criteria>
- **CI-only, and the whole point of this task.** A run exists whose `headSha` string-equals the local `git rev-parse HEAD` taken immediately after the push, and every reading below is taken from that run's job logs.
- **CI-only.** `lint (ENG-16 boundary)` conclusion is `success`, and its log contains a clean line from `lint_eng16.sh`, `lint_check_id_strings.sh`, `lint_dead_code_after_fail.sh` and `lint_fixture_case_collisions.sh` — four, not two.
- **CI-only.** `build (x64-linux)` conclusion is `success` and its Test step log contains `100% tests passed, 0 tests failed out of 297`.
- **CI-only.** `build (x64-windows-static-md)`: `grep -c 'C4702'`, `grep -c 'C2220'` and `grep -c 'C4996'` over the job log each return `0`; the log contains `[97/97]`; and the Test step reports `Total Tests: 297` with 0 failed. Any Build-step stop is recorded with its `[N/97]` step number, its object path and the verbatim first diagnostic.
- **CI-only.** `build (arm64-osx)`: the log contains `100% tests passed, 0 tests failed out of 297`; `grep -c 'test_dir_pairing.cpp:108: FAILED'` returns `0`; and test #40 `unit.dir_pairing: the returned order is byte-wise sorted` is listed as `Passed`.
- **CI-only.** `build (x64-osx)` and `build (arm64-linux)` conclusions and first-failure signatures are recorded and compared against the ones in `02-UAT.md` §Pre-Existing CI Failures, with an explicit statement that they are out of scope and are not counted against the four required contexts.
- **Locally provable.** `git status --porcelain` is empty before the push, and `git rev-parse HEAD` equals `git rev-parse origin/gsd/phase-2-core-engine` after it — proving the run is testing exactly the local tree and not a subset of it.
- **Locally provable.** The SUMMARY contains a six-row per-job table with a run id and a headSha, and makes no CI claim that is not sourced from that run.
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git status --porcelain)" && git push origin gsd/phase-2-core-engine && HEAD_SHA=$(git rev-parse HEAD) && test "$HEAD_SHA" = "$(git rev-parse origin/gsd/phase-2-core-engine)" && sleep 20 && RUN_ID=$(gh run list --branch gsd/phase-2-core-engine --limit 10 --json databaseId,headSha -q "[.[] | select(.headSha==\"$HEAD_SHA\")][0].databaseId") && test -n "$RUN_ID" && echo "run=$RUN_ID headSha=$HEAD_SHA" && gh run watch "$RUN_ID" --exit-status; gh run view "$RUN_ID" --json jobs -q '.jobs[] | "\(.databaseId) \(.name) \(.conclusion)"'</automated>
  </verify>
  <done>The branch is pushed, a CI run exists whose headSha equals the pushed HEAD, and every one of the six jobs has been read from that run's own log with its specific observable recorded — the four required contexts against the criteria above, and the two non-required legs against their recorded pre-existing signatures. The SUMMARY states which gaps the run confirms closed and which it does not, in its first paragraph, with no averaged verdict.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the verdict and decide certify-or-iterate</name>
  <read_first>
- `.planning/phases/02-core-engine/02-16-SUMMARY.md` as written by Task 2 — the per-job table is what this checkpoint presents.
- `.planning/phases/02-core-engine/02-UAT.md` — tests 1 and 2, and both gaps' `scope_caveat` fields, which set the expectation that a further round on the Windows leg is budgeted rather than a failure.
  </read_first>
  <what-built>
Two portability lints wired into the required `lint` job; the branch pushed; a new CI run read job by job. Task 2's SUMMARY carries the run id, the headSha, and the exact observable for all six jobs.
  </what-built>
  <how-to-verify>
Open the run linked in `02-16-SUMMARY.md` and confirm three things for yourself:

1. **`build (x64-windows-static-md)`** — does the Build step reach `[97/97]` with no `C4702` / `C2220` / `C4996`, and does the Test step then report 297 tests with 0 failed? If it stopped at a step between [66/97] and [97/97], that is a translation unit MSVC has never compiled in the project's history and the failure is new information, not G-02-3 recurring. The decision is yours: open a round-4 gap for it, or accept the leg as still-red and carry it.
2. **`build (arm64-osx)`** — does it report `100% tests passed, 0 tests failed out of 297`, with test #40 `unit.dir_pairing: the returned order is byte-wise sorted` at `Passed`?
3. **The two red non-required legs** — `build (x64-osx)` (architecture mismatch) and `build (arm64-linux)` (NuGet registration step exits 1) should still be red with their recorded signatures. Confirm the SUMMARY did not quietly count them as this phase's problem, and confirm you still want them deferred.

If the Windows leg is green for the first time, UAT test 2 (colour renders as real styling in a Windows console) becomes reachable for the first time in this phase's history — the build artifact it has been waiting on since round 1 now exists. Say so if you want that scheduled.
  </how-to-verify>
  <acceptance_criteria>
- The human has seen the run id and headSha and confirmed the run tests the pushed fixes rather than an earlier commit.
- The human has an explicit verdict per required context — not an averaged "CI is green".
- If either required leg is still red, the human has stated whether it becomes a round-4 gap or is accepted as carried, and that statement is recorded.
- The out-of-scope status of `CI-x64-osx` and `CI-arm64-linux` has been re-confirmed rather than assumed.
  </acceptance_criteria>
  <resume-signal>Type "approved" to certify the legs that went green, or describe what is still red and whether it becomes a round-4 gap.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → merge gate | The `lint` job's `name:` is the required-status-check context string on ruleset 20862843. This plan edits that job, which is the one place a well-meaning edit can remove a merge gate with no visible failure. |
| local worktree → origin / PR #2 | The push is the delivery step; after it, the code is observable by CI and by anyone watching the PR. |
| GitHub Actions job log → this phase's verdict | The run log is the sole evidence for every CI claim Phase 2 makes. Reading it selectively is how a phase certifies itself on a partial truth. |

No new runtime trust boundary is created and nothing in the distributed binary changes. This plan adds two `run: bash` steps that execute first-party scripts already committed to the repository.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-16-01 | Tampering | the `lint` job's required-check context | high | mitigate | Adding two lints to a job called `lint (ENG-16 boundary)` makes renaming it look like tidy-up, and a rename orphans the ruleset's required context — removing a merge gate with no error anywhere. The plan forbids the rename in `must_haves.prohibitions`, the gate asserts the exact name string survives, and Task 1 writes the constraint into the workflow as a comment so the next editor meets it before making the same reasonable-looking change. |
| T-02-16-02 | Elevation of privilege | the `lint` job's permissions and inputs | low | accept | The job keeps `permissions: contents: read`, `runs-on: ubuntu-24.04` and its submodule-free checkout unchanged. Both new steps execute repository-committed scripts that read first-party sources; neither takes network input, consumes a secret, or touches the vcpkg cache credentials used by the build legs. |
| T-02-16-03 | Repudiation | the CI read itself | high | mitigate | The failure mode this phase has already hit twice is a verdict sourced from the wrong evidence — round 2 was assessed against a run that predated the fixes entirely. The plan requires selecting the run by headSha equality with the pushed HEAD, reading all six jobs by name from that run's own log, and grepping for literal diagnostics rather than trusting a conclusion word; and it forbids reporting CI-clean on a partial read. |
| T-02-16-04 | Tampering | pipeline comparability | medium | mitigate | Any additional pipeline change in the same push makes a red or green leg ambiguous — you could no longer attribute the result to the two test fixes. The plan forbids touching the matrix, any build leg, the ctest count guard and the cache configuration, and the gate asserts the `ci.yml` diff is additions-only and confined to the `lint` job. |
| T-02-16-05 | Denial of service | CI minutes on the 6-leg matrix | low | accept | One additional run on an already-open PR, with the two new steps costing milliseconds. The vcpkg binary cache amortises the FFmpeg build that dominates the legs' runtime. |

No package-manager install task exists in this plan, so no `T-02-SC` supply-chain row applies: no action version is bumped, no runner image is changed, and no dependency is added or removed.
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Every symbol and path this plan creates or changes, so downstream source-grounding passes can exclude newly-created names from drift verification:

| Kind | Name | Status |
|------|------|--------|
| CI step | `Run dead-code-after-FAIL portability lint` in the `lint` job of `.github/workflows/ci.yml` | created |
| CI step | `Run fixture case-collision portability lint` in the same job | created |
| CI job name | `lint (ENG-16 boundary)` — a required-status-check context on ruleset `main merge gate` (id 20862843) | **unchanged, deliberately** |
| workflow comment | a note above the `lint` job's `name:` recording that the string is a required-check context | created |
| document | `.planning/phases/02-core-engine/02-16-SUMMARY.md` — the per-job CI read | created |
| new script | *(none — both lint scripts are delivered by 02-14 and 02-15; this plan only invokes them)* | n/a |
| new CMake target or flag | *(none — no build file is modified)* | n/a |
| source change | *(none — no file under `src/` or `tests/` is modified by this plan)* | n/a |
</artifacts_this_phase_produces>

<verification>
**What the local gates prove.** The `ci.yml` diff is additions-only and confined to the `lint` job; the required-check context string is byte-identical; no step carries `continue-on-error`; the file still parses as YAML; and all four lint scripts exit 0 from the repository root, so wiring them cannot itself turn a previously-green required check red. The push is verified by sha equality between local HEAD and the remote branch tip.

**What only the run proves.** Everything else. MSVC, `/W4 /WX`, C4702 and APFS case-insensitivity have no local analogue on this host — `02-14-PLAN.md` records that the obvious Clang proxy was tried against the real translation unit and produced zero diagnostics, and `02-15-PLAN.md` records that no case-insensitive filesystem is available here. This plan exists precisely because the two wave-1 plans can prove their fixes are *correct* and cannot prove their fixes are *sufficient*.

**The two legs are not symmetric and the verdict must not average them.** `arm64-osx` executed all 297 tests in run 31943688186 with exactly one failure, so there is no unexecuted remainder for a second defect to hide in and a green leg is a genuinely likely outcome. `x64-windows-static-md` has never compiled steps [66/97] through [97/97] in any run — four unit TUs, all fourteen integration TUs, the shared support sources, and every link step. A NEW first failure there is the budgeted outcome both wave-1 plans warned about and the outcome this phase has now seen twice in a row: a fix that worked and immediately revealed what it had been hiding. Record it as new information; do not report it as G-02-3 recurring, and do not report the phase CI-clean on the strength of the macOS leg alone.

**Explicitly out of scope.** `build (x64-osx)` (the preset cross-builds from the arm64 host without `-arch x86_64`) and `build (arm64-linux)` (the `Register vcpkg NuGet feed` step exits 1 in ~15ms with no output) reproduce on `main`, are not among the four required contexts, and are not fixed here. This plan reads them only to confirm their failure signatures are unchanged, so their redness is not mistaken for either gap. The three items in `02-UAT.md` §Deferred Follow-Ups — the `getenv_utf8` thread-safety doc comment, the dead `<cstdlib>` include in `src/cli/options.cpp`, and the severity-colour scope ambiguity — are likewise not folded in; the value of this particular push is a clean read of two test-only fixes against an otherwise byte-identical pipeline, and unrelated source edits would cost that for nothing.

**If UAT test 2 becomes reachable.** A green Windows leg produces `mediadiff.exe` — the artifact the console-VT colour check has been blocked on since round 1. That check is inherently human and inherently Windows-hosted; it is not this plan's work, but Task 3 surfaces it so it can be scheduled rather than forgotten.
</verification>

<success_criteria>
- Two steps added to the existing `lint` job invoking the two new portability lints; the job's `name:`, `runs-on`, `permissions` and checkout unchanged; a comment recording that the name is a required-check context.
- No `continue-on-error` or conditional escape on any lint step; the `ci.yml` diff is additions-only and contains nothing outside the `lint` job.
- The branch is pushed and a CI run exists whose headSha equals the pushed HEAD.
- All six jobs read from that run's own log, with the exact observable recorded per job: four required contexts against named criteria, two non-required legs against their recorded pre-existing signatures.
- `build (x64-windows-static-md)` assessed on zero `C4702` / `C2220` / `C4996`, reaching `[97/97]`, and a Test step reporting 297 tests — or, if red, on the new step number, object path and verbatim first diagnostic.
- `build (arm64-osx)` assessed on `100% tests passed, 0 tests failed out of 297` and test #40 at `Passed`.
- The SUMMARY gives a per-context verdict, never an averaged one, and leads with the Windows result if it is red.
- `CI-x64-osx` and `CI-arm64-linux` explicitly confirmed out of scope rather than silently absorbed or silently fixed.
- The human checkpoint recorded an explicit certify-or-iterate decision per red leg.
</success_criteria>

<output>
Create `.planning/phases/02-core-engine/02-16-SUMMARY.md` when done.
</output>
