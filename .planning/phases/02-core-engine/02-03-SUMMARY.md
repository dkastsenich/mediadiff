---
phase: 02-core-engine
plan: 03
subsystem: testing
tags: [catch2, cmake, codegen, fail-first, golden-testing, determinism]

# Dependency graph
requires:
  - phase: 02-core-engine (plan 01)
    provides: "compare_fingerprints/read_snapshot/render_json pipeline, the frozen schema_version/seed-ID/time-value/ID-grammar contracts"
  - phase: 02-core-engine (plan 02)
    provides: "tools/gen_registry.py's full CheckDef surface (profile overrides, structured aliases, docs manifest), tests/process_spawn.h extracted from tests/integration/cli_harness.h"
provides:
  - "tools/gen_registry.py --symbol-prefix: a second, independent, non-colliding registry (TestCheckId/test_registry()/test_check_id.h) generated by the same generator, out-dir-basename-derived include paths so a prefixed and unprefixed registry never collide"
  - "tests/support/test_checks.def: ten t.* checks covering all seven comparison semantics plus volatile/requires_pass/alias flags, linked only into test targets (D-11, verified via nm/strings against the shipped binary)"
  - "tests/support/stub_analyzer.{h,cpp}: make_stub_fingerprint() -- a synthetic measurement source for building arbitrary Fingerprints with no media and no snapshot file"
  - "tests/support/fixture_paths.h: fixture_dir()/golden_dir()/snapshot_dir() resolved from CMake compile definitions, not the process working directory"
  - "tests/unit/test_fail_first_coverage.cpp: the (Semantic, Status) coverage gate (D-14/D-15) -- fails naming the exact uncovered cell, with a self-verifying allow list for the six not-yet-implemented semantics"
  - "tests/unit/test_canary.cpp + tests/fixtures/snapshots/canary_{a,b}.snap.json: the permanent D-16 divergent-fixture proof"
  - "tests/support/golden.{h,cpp}: check_golden()/golden_check_result(), UPDATE_GOLDENS-aware, read-only by default (D-12)"
  - "tests/integration/test_determinism.cpp: TRUST-05 byte-identity re-verified against the full test registry, plus registry-declaration-order-not-insertion-order proof"
  - "tests/process_spawn.h: POSIX read()/select()/waitpid() EINTR and pipe-leak fixes (T-2-12), proven by a >64 KiB dual-stream capture test"
  - ".planning/PROJECT.md Conventions section: the fixture-pair/semantic-crossed-with-status/canary discipline recorded as a project rule (D-17), not only this phase's gate"
affects: ["02-04 (implements tol/set/presence/hash/dist/span; each removal from test_fail_first_coverage.cpp's allow list must add real fixture coverage)", "02-05 (policy precedence merge — resolve_severity currently ignores Policy/profile overrides entirely; test_registry()'s t.exact_string profile_severity override is inert until then)", "02-08..02-10 (report formats consume tests/support/golden.h directly for their golden-byte tests)"]

# Actuals (#2632)
actuals:
  tokens: 19631
  tasks: 3
  commits: 3

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Generator name-prefixing: tools/gen_registry.py derives every prefix-dependent identifier/filename from a single compute_names() call, and derives each generated .cpp's #include path from --out-dir's basename rather than hard-coding 'core/' -- the same generator now produces two non-colliding registries with zero special-casing at call sites"
    - "Coverage-gate-independent-of-implementation-status: a checked-in allow list (not a compile-time #ifdef) tracks which semantics still dispatch through compare_unimplemented; the allow list's own integrity is asserted by calling comparator_for() directly and checking the literal 'not yet implemented' error message, so an allow-list entry that silently stops being true is itself a build failure"
    - "Golden assertion logic split from its Catch2 wrapper (golden_check_result() returns std::optional<std::string>, check_golden() translates to REQUIRE/FAIL) specifically so a test can assert on the failure branch itself without a subprocess"
    - "D-09's 'error' status cell is proven at the read_snapshot() layer (a value_kind mismatch is an Error before a Finding exists), not by asserting Finding.status == error -- no current comparator ever constructs that Finding"

key-files:
  created:
    - tests/support/test_checks.def
    - tests/support/docs/t.*.md (10 files)
    - tests/support/stub_analyzer.h
    - tests/support/stub_analyzer.cpp
    - tests/support/fixture_paths.h
    - tests/support/golden.h
    - tests/support/golden.cpp
    - tests/unit/test_support_registry.cpp
    - tests/unit/test_fail_first_coverage.cpp
    - tests/unit/test_canary.cpp
    - tests/unit/test_process_spawn.cpp
    - tests/unit/test_golden.cpp
    - tests/integration/test_determinism.cpp
    - tests/golden/README.md
    - tests/fixtures/snapshots/canary_a.snap.json
    - tests/fixtures/snapshots/canary_b.snap.json
    - tests/fixtures/snapshots/t.exact_string__pass.{a,b}.snap.json
    - tests/fixtures/snapshots/t.exact_string__error.{a,b}.snap.json
    - tests/fixtures/snapshots/t.aliased_new__fail.{a,b}.snap.json
  modified:
    - tools/gen_registry.py
    - CMakeLists.txt
    - tests/unit/CMakeLists.txt
    - tests/integration/CMakeLists.txt
    - tests/process_spawn.h
    - .planning/PROJECT.md

key-decisions:
  - "The EINTR-as-EOF/select-error-leak/unchecked-waitpid fix landed in tests/process_spawn.h, not tests/integration/cli_harness.h -- 02-02-PLAN.md Task 3 already extracted the POSIX spawn loop there; cli_harness.h is a thin MEDIADIFF_BINARY-pinned wrapper with no loop of its own. Documented as a deviation below."
  - "The generator's include path for each generated .cpp is derived from --out-dir's basename (os.path.basename(normpath(out_dir))) rather than a hard-coded 'core/' literal -- this is what let --symbol-prefix's test registry (out-dir generated/test) reuse the exact same rendering code as production (out-dir generated/core) with zero special-casing"
  - "The (exact, error) coverage cell is satisfied by read_snapshot() failing on a value_kind mismatch (t.exact_string__error.a.snap.json carries a bare number where value_kind=string is registered), not by a Finding.status==error -- no comparator in this codebase constructs that Finding today; D-09's error is a pipeline-level Error, not (yet) a Finding-level status"
  - "golden.h exposes golden_check_result() (Catch2-independent, returns std::optional<std::string>) alongside check_golden() (the Catch2-wrapped convenience function) specifically so tests/unit/test_golden.cpp can assert on check_golden's own failure branch without a subprocess"

patterns-established:
  - "Fixture naming convention: <check_id>__<status>.{a,b}.snap.json, scanned by directory iteration (not hard-coded per-cell filenames) so a later check added under an already-implemented semantic is automatically eligible to satisfy that cell"
  - "D-17 fixture-pair/semantic-crossed-with-status/canary discipline recorded in .planning/PROJECT.md's Conventions section -- binding on Phases 3-7, not only this phase's own gate"

requirements-completed: [ENG-14, TRUST-05]

coverage:
  - id: D1
    description: "Test-only check registry (tools/gen_registry.py --symbol-prefix test_) covering all seven comparison semantics, linked only into test targets and absent from the shipped binary"
    requirement: "D-11"
    verification:
      - kind: unit
        ref: "tests/unit/test_support_registry.cpp (4 test cases: semantic coverage, size, find, flags/alias round-trip)"
        status: pass
      - kind: other
        ref: "nm -C build/x64-linux/mediadiff | grep -c test_registry -> 0; strings build/x64-linux/mediadiff | grep -c t.exact_string -> 0"
        status: pass
    human_judgment: false
  - id: D2
    description: "Semantic-by-status fail-first coverage gate: fails naming the exact uncovered (semantic, status) cell; self-verifying allow list for not-yet-implemented semantics"
    requirement: "D-14, D-15"
    verification:
      - kind: unit
        ref: "tests/unit/test_fail_first_coverage.cpp (2 test cases: cell coverage, allow-list integrity)"
        status: pass
      - kind: other
        ref: "manual negative control: removing t.aliased_new__fail.a.snap.json makes unit.fail_first fail naming '(exact, fail)'; restored and green again"
        status: pass
    human_judgment: false
  - id: D3
    description: "Permanent canary fixture pair always reports exactly one fail finding; a clean canary is a broken harness, not a fixed fixture"
    requirement: "D-16"
    verification:
      - kind: unit
        ref: "tests/unit/test_canary.cpp"
        status: pass
      - kind: other
        ref: "manual negative control: editing canary_b.snap.json to match canary_a.snap.json makes unit.canary fail; reverted and green again"
        status: pass
    human_judgment: false
  - id: D4
    description: "cli_harness.h/process_spawn.h no longer treats EINTR as end-of-stream, closes both pipe descriptors on a select() error, and records a genuine waitpid() failure as exit_code -1"
    requirement: "T-2-12"
    verification:
      - kind: unit
        ref: "tests/unit/test_process_spawn.cpp - process_spawn: captures more than one pipe buffer's worth of stdout and stderr exactly (82,500 bytes/stream)"
        status: pass
    human_judgment: false
  - id: D5
    description: "Golden-file harness: UPDATE_GOLDENS-gated refresh, read-only and failing on a missing/mismatched golden otherwise"
    requirement: "D-12"
    verification:
      - kind: unit
        ref: "tests/unit/test_golden.cpp (2 test cases: missing-file-fails, UPDATE_GOLDENS-creates-and-matches)"
        status: pass
    human_judgment: false
  - id: D6
    description: "Determinism: two runs over the full test registry produce byte-identical --json; registry declaration order (not measurement insertion order) governs report output"
    requirement: "D-13, TRUST-05"
    verification:
      - kind: integration
        ref: "tests/integration/test_determinism.cpp (2 test cases)"
        status: pass
    human_judgment: false

duration: 22min
completed: 2026-08-15
status: complete
---

# Phase 2 Plan 3: Fail-First Test Infrastructure Summary

**A second, independent check registry (`--symbol-prefix`) covering all seven comparison semantics, a coverage gate that fails naming the exact uncovered (semantic, status) cell, a permanent canary proven to actually go red, a golden-file harness, a determinism harness re-verified against the full test registry, and a `cli_harness.h`/`process_spawn.h` fix that closes a real captured-output truncation defect — built before any real analyzer exists, per D-14 through D-17.**

## Performance

- **Duration:** ~22 min (task-commit span)
- **Started:** 2026-08-15T17:15:13+02:00
- **Completed:** 2026-08-15T17:37:14+02:00
- **Tasks:** 3
- **Files modified:** 38 (32 created, 6 modified)

## Accomplishments

- `tools/gen_registry.py` gained `--symbol-prefix`, letting the *same* generator emit a second, independent registry (`TestCheckId`/`test_registry()`/`test_check_id.h`) that never collides with production's `CheckId`/`builtin_registry()`/`check_id.h` — the include path each generated `.cpp` uses to reach its own header is now derived from `--out-dir`'s basename rather than a hard-coded `"core/"` literal, which is what let this generalize with zero special-casing
- `tests/support/test_checks.def` registers ten `t.*` checks spanning all seven comparison semantics plus `volatile`/`requires_pass`/alias flags — linked only into the two test executables, verified absent from the shipped `mediadiff` binary via `nm`/`strings` (D-11)
- `tests/unit/test_fail_first_coverage.cpp`: the `(Semantic, Status)` coverage gate doc 01 section 3 implies (`exact` emits `pass`/`fail`/`error`; `hash` also emits `skipped`) — fails naming the exact uncovered cell rather than a bare non-zero exit, with a self-verifying allow list for the six semantics 02-04 hasn't implemented yet
- `tests/unit/test_canary.cpp` + `tests/fixtures/snapshots/canary_{a,b}.snap.json`: the permanent D-16 fixture, proven to actually go red when tampered with (both negative controls run and reverted during this execution)
- `tests/support/golden.{h,cpp}` (D-12) and `tests/integration/test_determinism.cpp` (D-13/TRUST-05, re-verified against the full ten-check test registry rather than only the three shipped `meta.*` checks) — the harnesses every later report-format test builds on
- `tests/process_spawn.h`'s POSIX read loop no longer treats `EINTR` as end-of-stream (the truncation bug this task exists to close), closes both open pipe descriptors on a genuine `select()` error, and retries `waitpid()` on `EINTR` rather than reading uninitialized status as a successful exit — proven by a new test capturing more than one pipe buffer (82,500 bytes) on both stdout and stderr

## Task Commits

Each task was committed atomically:

1. **Task 1: Test-only registry and stub measurement source** - `ce1c77d` (feat)
2. **Task 2: Semantic-by-status coverage gate and the permanent canary** - `ccc9bd4` (feat)
3. **Task 3: Golden harness, determinism harness, and the cli_harness capture defects** - `f2385eb` (feat)

**Plan metadata:** _pending — see final commit below_

## Files Created/Modified

- `tools/gen_registry.py` - `--symbol-prefix` argument; `compute_names()`; out-dir-basename-derived include paths
- `CMakeLists.txt` - second `add_custom_command`/`generate_test_registry` target generating `tests/support/test_checks.def` into `generated/test/`
- `tests/support/test_checks.def`, `tests/support/docs/t.*.md` (10) - the ten-check test-only registry and its docs
- `tests/support/stub_analyzer.{h,cpp}` - `make_stub_fingerprint()`/`StubMeasurement`
- `tests/support/fixture_paths.h` - `fixture_dir()`/`golden_dir()`/`snapshot_dir()`
- `tests/support/golden.{h,cpp}` - `check_golden()`/`golden_check_result()`
- `tests/unit/test_support_registry.cpp` - proves `test_checks.def` covers all seven `Semantic` values
- `tests/unit/test_fail_first_coverage.cpp`, `tests/unit/test_canary.cpp` - the D-14/D-15/D-16 gates
- `tests/unit/test_process_spawn.cpp`, `tests/unit/test_golden.cpp` - new unit suites (`[process_spawn]`, `[golden]`)
- `tests/integration/test_determinism.cpp` - the D-13/TRUST-05 harness against the full test registry
- `tests/golden/README.md` - `UPDATE_GOLDENS` local-refresh / CI-read-only convention
- `tests/fixtures/snapshots/canary_{a,b}.snap.json`, `t.exact_string__{pass,error}.{a,b}.snap.json`, `t.aliased_new__fail.{a,b}.snap.json` - hand-authored coverage/canary fixtures
- `tests/process_spawn.h` - EINTR/select-leak/waitpid fixes
- `tests/unit/CMakeLists.txt`, `tests/integration/CMakeLists.txt` - new sources, `generate_test_registry` dependency, `MEDIADIFF_FIXTURE_DIR`/`MEDIADIFF_GOLDEN_DIR` compile definitions, `libmediadiff` linked into the integration target
- `.planning/PROJECT.md` - new Conventions section recording the D-17 fail-first discipline as a project rule

## Decisions Made

See frontmatter `key-decisions` for full rationale on: where the `cli_harness.h`/`process_spawn.h` fix actually landed, how the generator's include-path derivation generalizes without special-casing, how the `(exact, error)` coverage cell is satisfied at the `read_snapshot()` layer rather than via a `Finding.status`, and why `golden.h` exposes a Catch2-independent `golden_check_result()` alongside `check_golden()`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] The EINTR-as-EOF/pipe-leak/waitpid fix targeted `tests/process_spawn.h`, not `tests/integration/cli_harness.h`**
- **Found during:** Task 3 (reading `cli_harness.h` per the plan's own `read_first` pointer)
- **Issue:** The plan's `read_first`/`files` lists point at `tests/integration/cli_harness.h`'s POSIX `select()`/`read()` loop "at lines 200-252" — but 02-02-PLAN.md Task 3 (committed before this plan started) already extracted that loop into `tests/process_spawn.h`, leaving `cli_harness.h` as a thin `MEDIADIFF_BINARY`-pinned wrapper with no loop of its own. The plan's file references predate that extraction.
- **Fix:** Applied the EINTR retry, select()-error pipe cleanup, and waitpid()-retry/failure-recording fixes to the actual location, `tests/process_spawn.h` (which both `cli_harness.h` and `tests/unit/test_registry_generator.cpp` share).
- **Files modified:** `tests/process_spawn.h` (not `tests/integration/cli_harness.h`, which required no change since it has no loop of its own).
- **Commit:** `f2385eb`

**2. [Rule 2 - Missing functionality] Added `tests/unit/test_support_registry.cpp` and `tests/unit/test_golden.cpp`, not explicitly named in the plan's file lists**
- **Found during:** Task 1 and Task 3
- **Issue:** Task 1's acceptance criteria requires "a test asserting the set of semantics present in `test_registry()` equals the full enum," and Task 3's acceptance criteria requires "both branches are asserted by a test" for `check_golden`'s `UPDATE_GOLDENS` behavior — neither names a specific new file to hold these assertions.
- **Fix:** Added two small, narrowly-scoped test files matching the existing `test_<component>.cpp` naming convention, wired into `tests/unit/CMakeLists.txt`.
- **Files modified:** `tests/unit/test_support_registry.cpp`, `tests/unit/test_golden.cpp`, `tests/unit/CMakeLists.txt`.
- **Commit:** `ce1c77d` (test_support_registry.cpp), `f2385eb` (test_golden.cpp)

**3. [Rule 2 - Missing functionality] Added `tests/unit/test_process_spawn.cpp` for the >64 KiB dual-stream capture test**
- **Found during:** Task 3
- **Issue:** Task 3's acceptance criteria requires a test that "spawns a child emitting more than 64 KiB on each of stdout and stderr and asserts both captured lengths equal the produced lengths exactly," but no filename is named in the plan's file lists (only `tests/unit/CMakeLists.txt` is listed as modified).
- **Fix:** Added `tests/unit/test_process_spawn.cpp`, spawning the Python interpreter (already available via `MEDIADIFF_PYTHON`) to emit 82,500 bytes on each stream.
- **Files modified:** `tests/unit/test_process_spawn.cpp`, `tests/unit/CMakeLists.txt`.
- **Commit:** `f2385eb`

---

**Total deviations:** 3 auto-fixed (1 Rule 1, 2 Rule 2)
**Impact on plan:** All three are necessary to satisfy the plan's own acceptance criteria and `must_haves`; none change scope, architecture, or any frozen contract from prior waves.

## Issues Encountered

- **Test-case names for the coverage gate initially used a hyphen (`fail-first coverage: ...`)**, which the plan's own `-R "unit.(fail_first|canary)"` filter (an underscore) does not match. Caught immediately by running that exact filter before committing; renamed to `fail_first coverage: ...` and re-verified both `-N` counts are non-zero.
- **No comparator in this codebase ever constructs a `Finding` with `Status::error`** — `compare_exact.cpp` only emits `pass`/`fail`/`info`/`warn`. The `(exact, error)` coverage cell is therefore satisfied at the `read_snapshot()` layer (a `value_kind` mismatch is an `Error` before a `Finding` exists, per D-09), not by inspecting a `Finding.status` — documented in the test file's own comment and in `key-decisions` above.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 02-04 implements `tol`/`set`/`presence`/`hash`/`dist`/`span`; each removal from `test_fail_first_coverage.cpp`'s `not_yet_implemented_semantics()` allow list must be paired with real `tests/fixtures/snapshots/<check_id>__<status>.{a,b}.snap.json` fixture coverage for every status that semantic can emit, or the coverage gate itself fails.
- Plan 02-05's policy precedence merge replaces `core/policy.h`'s `resolve_severity` body; `test_checks.def`'s `t.exact_string` `[check.profile_severity]` override is generated and structurally correct today but inert until that plan wires `Policy`'s profile through to `severity_for()`.
- Plans 02-08 through 02-10 (report formats) consume `tests/support/golden.h` directly for their own golden-byte tests — no signature changes anticipated.
- No blockers.

---
*Phase: 02-core-engine*
*Completed: 2026-08-15*

## Self-Check: PASSED
