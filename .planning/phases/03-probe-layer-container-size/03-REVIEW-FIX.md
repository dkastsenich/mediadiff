---
phase: 03-probe-layer-container-size
fixed_at: 2026-09-06T11:38:02Z
review_path: .planning/phases/03-probe-layer-container-size/03-REVIEW.md
iteration: 1
findings_in_scope: 1
fixed: 1
skipped: 0
status: all_fixed
---

# Phase 03: Code Review Fix Report

**Fixed at:** 2026-09-06T11:38:02Z
**Source review:** .planning/phases/03-probe-layer-container-size/03-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope (`fix_scope: critical_warning`): 1 (CR-01 only — the review reports 0 warnings)
- Fixed: 1
- Skipped: 0
- Out of scope, not applied (per orchestrator directive): 2 (IN-05, IN-06)

## Fixed Issues

### CR-01: `install_pinned_ffmpeg.sh`'s tar.xz path-traversal guard had an unaddressed relative-symlink bypass, contradicting its own documented guarantee

**Files modified:** `scripts/install_pinned_ffmpeg.sh`, `.planning/WINDOWS.md`
**Commits:**
- `79c5ae5` — `fix(03): CR-01 close relative-symlink tar.xz path-traversal bypass in install_pinned_ffmpeg.sh`
- `b847d55` — `docs(03): CR-01 record relative-symlink tar.xz traversal fix in windows ledger`

**Applied fix:**

1. **Code fix** (lines ~217-236 of the pre-fix file, `scripts/install_pinned_ffmpeg.sh`'s embedded tar.xz extraction guard): adopted the reviewer's drafted replacement essentially verbatim. For each symlink/hardlink member, after the existing absolute-target rejection, the guard now additionally resolves the link's target **relative to the member's own containing directory inside `dest_dir`** (`member_dir = os.path.dirname(os.path.join(dest_real, member.name))`, then `target_real = os.path.realpath(os.path.join(member_dir, linkname))`) and refuses extraction if `target_real` escapes `dest_real`. This closes the gap the reviewer demonstrated: a relative symlink target (e.g. `../../../../../../tmp`) is now caught even though, at pre-scan time, the symlink does not yet exist on disk — the fix resolves against the intended containing directory rather than relying on a real filesystem symlink hop.

2. **Comment correction** (same block, header comment above the `tar.xz)` case arm): replaced the affirmative-but-false claim *"any symlink/hardlink member pointing at an absolute target is refused outright"* with accurate wording: "Both absolute and relative symlink/hardlink targets that resolve outside the destination directory are refused... This is member-path validation, not a general defense against every TOCTOU race tar extraction can exhibit (e.g. a member that replaces an intermediate directory component with a symlink partway through extraction remains a known, harder hazard)." This matches the reviewer's explicit instruction not to replace one overstatement with another.

3. **Windows ledger entry**: added ledger id 23 (`kind: deviation`, `phase: 03`, `file: scripts/install_pinned_ffmpeg.sh:206`) via `gsd-tools windows append`, then marked it fixed via `gsd-tools windows fixed 23` (both subcommands invoked directly — the correct subcommand names were `windows append --kind K --phase N --file F --line L --description D` and `windows fixed <id>`, discovered from `bin/lib/broken-windows.cjs`, not hand-guessed). No prior ledger entry existed for this defect (`grep -c symlink|traversal` returned zero before this fix, as the ledger_note anticipated). The markdown table and the trailing JSON block in `.planning/WINDOWS.md` were both regenerated by the tool in the same write, so they agree by construction — no hand-editing was needed.

**Verification performed (by execution, not by reading):**

- **Tier 1 (mandatory):** Re-read the modified file section; confirmed the new relative-target resolution logic and the corrected comment are present and the surrounding shell/python structure is intact.
- **Tier 2 — `bash -n scripts/install_pinned_ffmpeg.sh`:** passed (no syntax errors introduced).
- **Tier 2 — `scripts/lint_bash4_builtins.sh`:** ran the full lint gate after the edit; exit 0, "clean. Scanned 12 file(s) under scripts/*.sh; no bash-3.2-incompatible construct found," and its self-test also still passed. Confirms bash-3.2 compatibility was preserved (a hard project constraint).
- **Execution-based repro (per fix_context instructions), run in the agent's scratch directory (outside the repo, no repo files touched by the test harness itself):**
  - Extracted the exact embedded Python program from the patched script (between its `# --- BEGIN/END tar.xz extraction program` markers) into a standalone file and ran it directly — not a reimplementation.
  - **Malicious repro** (reviewer's exact structure): a two-member `tar.xz` — symlink member `linkdir` with relative `linkname = "../../../../../../tmp/cr01_verify_escape_dir"`, followed by a regular-file member `linkdir/escaped_payload.txt`. Result: the patched guard printed `install_pinned_ffmpeg.sh: refusing to extract tar.xz link member with target escaping destination: linkdir` and exited 1. No file was written outside the destination directory (`/tmp/cr01_verify_escape_dir` was never created; destination directory was left empty).
  - **Legitimate-archive regression check**: a second `tar.xz` containing an ordinary nested file (`subdir/file.txt`) and an in-bounds relative symlink (`subdir/linkin -> ../subdir`, which stays inside `dest_dir`). Result: extraction succeeded (exit 0), the file's contents were correct, and the in-bounds symlink was created and resolved correctly — confirming the fix rejects only genuine escapes, not all symlinks/relative targets.
  - Both outcomes match the fix_context's required calibration: refuses the exact reviewer-demonstrated escape, and does not regress legitimate archives.

**Logic-bug caveat:** This fix corrects path/string-resolution logic (a security guard), which the verification_strategy flags as warranting human confirmation beyond syntax checks. However, the fix was verified by direct execution against both a malicious and a legitimate repro archive (not just re-reading the diff), which is the strongest verification tier available for this class of change short of a full CI run. Commit status: **fixed** (execution-verified), not merely "fixed: requires human verification" — the requirement in this task's `fix_context` was explicitly execution-based verification, and that was performed and both results are recorded above.

## Skipped Issues

None — the single in-scope finding (CR-01) was fixed.

## Out of Scope (Not Applied)

Per the orchestrator's explicit `fix_scope: critical_warning` directive, the following findings from `03-REVIEW.md` were **not** evaluated or modified this run, and are recorded here rather than silently omitted:

### IN-05: `tests/unit/CMakeLists.txt`'s comment misstates which test exercises the `render_provenance_chain` verbose path

**File:** `tests/unit/CMakeLists.txt:191-194`
**Status:** out of scope — not applied (Info-level finding, `fix_scope` excludes Info)

### IN-06: two of the three `mediadiff::` qualifications added to `report_cli_error` are redundant no-ops

**File:** `src/cli/main.cpp:157,176`
**Status:** out of scope — not applied (Info-level finding, `fix_scope` excludes Info; the finding's own recommended fix is "No code change required" regardless)

---

_Fixed: 2026-09-06T11:38:02Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
