---
phase: 03-probe-layer-container-size
plan: 22
type: execute
wave: 2
gap_closure: true
depends_on: ["03-21"]
files_modified:
  - .planning/WINDOWS.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [TRUST-06, TRUST-09, DOC-03]
estimate:
  tokens: 22000
  raw_tokens: 44000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "All four `trust06_idempotence` result lines ROADMAP SC5 requires are observed reading `Passed` in one real CI run's log — two on `build (x64-linux)` and two on `build (arm64-osx)` — read from each job's own log, never inferred from absence in a failure list."
    - "`build (x64-windows-static-md)`'s `Test` step itself concludes on that run. Across every real run in this phase's history (33951407521, 33983460934, 33990099158) this leg has never once been observed reaching its Test step at all, so its conclusion is the single most load-bearing new observation this round produces."
    - "`.planning/WINDOWS.md` entries #13 and #16 are closed on a cited, observed step conclusion — not on the strength of the commit that fixed them — and every entry this round opened is either closed the same way or left open with its reason."
    - "`.planning/REQUIREMENTS.md`'s TRUST-06 status agrees with the observed CI evidence at both of the two sites that carry it (the checklist item at line 173 and the phase-mapping table row at line 372), so the requirement ledger no longer asserts something the verification report contradicts."
    - "The `sw-encoder` idempotence comparison, the DOC-03 coverage gate and the TSDuck-derived `ts_scan` goldens are all observed executing inside a run whose three blocking legs are green — SC5's own sentence names all three, and a green matrix is what makes them a release blocker rather than a local convenience."
  prohibitions:
    - "SC5 is never reported closed from a local ctest run, from a partial matrix, or from a leg's absence in a failure list."
    - "A required result line is never counted as observed because the test did not appear among the failures; only a quoted `Passed` line from a named job's own log counts."
    - "A requirement's status is never left asserting more than the evidence supports, and it is never quietly downgraded either — whichever way it moves, the run id and the observation that moved it are recorded."
    - "A ledger entry is never marked fixed on the strength of a committed change alone."
    - "The non-blocking legs' failures are never folded into the SC5 claim in either direction: they do not gate it, and they are not silently closed by it."
    - "The designated-leg golden exclusion is never widened to make a leg pass; the exclusion's own size assertion exists precisely to make that visible."
  artifacts:
    - path: ".planning/WINDOWS.md"
      provides: "Entries #13 and #16 closed on cited observed evidence, with the run id recorded alongside each closure"
      contains: "33"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TRUST-06's status reconciled against observed CI evidence at both sites that carry it"
      contains: "TRUST-06"
  key_links:
    - from: "the real CI run's per-job Test-step logs"
      to: ".planning/REQUIREMENTS.md's TRUST-06 status"
      via: "four quoted Passed result lines and three Test-step conclusions, cited by run id at the moment the status is set"
      pattern: "TRUST-06"
---

<!-- planner-discipline-allow: trust06_idempotence -->

<objective>
Close ROADMAP **SC5** on observed run-log evidence, and make the requirement ledger say what that
evidence supports.

SC5's own wording is: "Encoding a fixture twice with identical settings and comparing under
`sw-encoder` comes back clean **as a CI release blocker**; every check above has both a triggering
fixture pair and a clean one; and `ts_scan`'s output agrees with TSDuck's analysis of the same
fixtures." That is one claim about one real CI run, covering TRUST-06, DOC-03 and TRUST-09
together. Plan 03-21 delivers its precondition — three blocking legs that build and reach Test.
This plan reads what those legs actually did.

The verification report is explicit about what will and will not count. Four
`trust06_idempotence` result lines must be **observed reading `Passed`** — two on `x64-linux`, two
on `arm64-osx` — not merely absent from a failure list; that exact substitution is what left SC5
reported as partially closed in a prior round when it was not. And
`build (x64-windows-static-md)`'s `Test` step must itself conclude: across all three real runs in
this phase's history it has never once been observed reaching that step, so "Build went green"
would still leave the most-cited unknown in the phase unmeasured.

The same report flags a second thing this plan owns: `.planning/REQUIREMENTS.md` marks TRUST-06
`Complete` while SC5 — the same operative claim, "wired into CI as a release blocker" — is false.
That discrepancy is resolved here by evidence, in whichever direction the evidence points, and the
decision is recorded rather than assumed.

Purpose: a verification round that reads a ledger asserting more than its evidence supports can
close a criterion that is not met. Output: SC5 closed on quoted run-log evidence, with the defect
ledger and the requirement ledger both matching what the run shows.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/WINDOWS.md
@.planning/phases/03-probe-layer-container-size/03-VERIFICATION.md
</context>

<flagged_assumptions>
**A1 — Spec-less edge probe: the same 32 rows, same accounting.** No `03-SPEC.md` exists for this
phase. The deterministic probe over all 23 phase requirement IDs returned **32 applicable rows,
0 resolved, 32 unresolved**, `byVerification: {explicit: 0, backstop: 0}`. The full per-requirement
table is in `03-21-PLAN.md`'s own `<flagged_assumptions>` block and is not duplicated here.

Round-level equality, restated so it is checkable from this file too: **32 probe-surfaced rows ==
1 authored into `must_haves.truths` (PROBE-05's unknown-size-Cluster edge, in 03-21) + 31 surfaced
as flagged assumptions. No silent drops.**

Of those 31, four fall inside this plan's own subject matter and are named here rather than left
implicit. `TRUST-06`'s row is `unclassified` — the probe's generic question has no answer derivable
from a CI-wiring criterion, so it stays unresolved and this plan verifies TRUST-06 against SC5's
own sentence instead. `DOC-03`'s three rows are `adjacency`, `empty` and `ordering`; all three
concern the semantics of DOC-03's fixture pairs (what happens when a triggering and a clean fixture
compare equal, what an empty fixture set means, whether coverage-report ordering is stable) rather
than whether the coverage gate runs in CI, which is the only part of DOC-03 this plan touches. All
four stay unresolved and are recorded here as explicit assumptions, never auto-dismissed. The
`prohibitions` block above is authored descriptor-less per the recall protocol and disposes
flagged-unverified.

**A2 — The non-blocking legs are outside the SC5 claim, in both directions.**
`.github/workflows/ci.yml` declares `blocking: false` for `arm64-linux` and `x64-osx` and carries
`continue-on-error: ${{ !matrix.blocking }}` on the job, so neither can influence the aggregate
required status either way. `arm64-linux`'s NuGet credential failure is WINDOWS.md #11 and
`x64-osx`'s cross-build link failure is WINDOWS.md #14; both stay open. This plan records their
conclusions and neither counts them against SC5 nor closes their entries.

**A3 — Under the designated-leg policy, five golden tests run on x64-linux only.** 03-19 measured
that the pinned ffmpeg builds do not produce byte-identical fixtures across legs (arm64-osx
diverges on 76 of 80) and the developer approved the `designated` policy at that plan's own
checkpoint. Five byte-exact fixture-derived golden tests — including the three TSDuck-derived
`ts_scan_golden` cases that carry TRUST-09 — therefore run only on `x64-linux`, and are excluded by
name on every other leg with `EXPECTED_EXCLUDED_COUNT=5` asserted against unfiltered-versus-filtered
`ctest -N` totals. This is a locked decision (WINDOWS.md #17, open as a visible accepted
limitation), not this plan's to relitigate. It does mean TRUST-09's goldens are observed on one leg;
this plan says so plainly rather than implying matrix-wide coverage.

**A4 — This plan may need a CI round trip of its own.** 03-21 gets the blocking legs to *reach*
Test. A leg can reach Test and then fail it — indeed `x64-windows-static-md` has never run this
suite at all, so its first Test execution is genuinely new territory. Task 1 bounds that at three
round trips and states what to do when the budget runs out. SC5 is either observed met on the
evidence this plan requires, or it is not closed.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Observe SC5 met — four Passed lines on two named legs, and the Windows Test step concluding for the first time</name>
  <reversibility rating="reversible">Evidence gathering plus, if a test fails, a minimal fix in the file that owns it; git holds every prior state.</reversibility>
  <files>.planning/phases/03-probe-layer-container-size/03-22-SUMMARY.md, plus whichever source or test file a failing Test step names (unknowable before the run; every such file is listed by path in the SUMMARY)</files>
  <read_first>
    - `.planning/phases/03-probe-layer-container-size/03-VERIFICATION.md`, the `gaps:` block in full — SC5's exact wording and all three `missing:` items. The third is this task's specification verbatim: all three blocking legs conclude Build success and reach Test; all four `trust06_idempotence` result lines are observed `Passed` (two on x64-linux, two on arm64-osx) rather than merely absent from a failure list; and `x64-windows-static-md`'s Test step itself concludes, which has never yet been observed on any real run.
    - `.planning/phases/03-probe-layer-container-size/03-21-SUMMARY.md` — the run id, head SHA and per-leg step conclusions 03-21 established. This task starts from that run, not from a fresh push, unless 03-21's own budget left a leg red.
    - `tests/integration/test_trust06_idempotence.cpp` lines 93-110 — the two `TEST_CASE` names. With the integration suite's naming convention these become CTest names beginning `integration.trust06_idempotence - `, which is what the log greps below match on.
    - `.github/workflows/ci.yml` lines 22-69 — the five matrix legs and which three carry `blocking: true` — and lines 306-395, the `Test` step, so the log's own shape is understood before it is read: the step prints its discovered test count, then either runs the full suite on the designated leg or prints the five excluded test names by name and reason and runs the filtered suite.
    - `.planning/phases/03-probe-layer-container-size/03-19-SUMMARY.md` — the designated-leg policy, so a non-designated leg's excluded-by-name lines are recognised as expected output rather than misread as missing coverage.
    - `.planning/WINDOWS.md` entries #11, #13, #14, #16 and #17 — which of these this round's evidence closes (#13, #16), which stay open as non-blocking-leg or accepted-limitation records (#11, #14, #17), and the run ids already cited against each.
  </read_first>
  <action>
    Gather the single piece of evidence ROADMAP SC5 actually asks for: one real CI run in which the
    release blocker runs and comes back clean on the legs that gate a merge.

    Identify the completed run for the current head of `gsd/phase-03-probe-layer-container-size`
    (PR #3). If 03-21's final run is still the head run and its three blocking legs reached Test,
    use it — do not push a no-op commit to manufacture a fresh run id. Then record all of the
    following in this plan's SUMMARY as quoted command output, each labelled with the leg it came
    from:

    1. Run identity and overall outcome from `gh run view <run id> --json status,conclusion`, plus
       the head SHA, so a later reader can tell exactly which commit was proven.
    2. Every one of the six job names with its conclusion, enumerated individually — the five
       `build (...)` legs and `lint (ENG-16 boundary)`. The three blocking legs and the lint job
       must each read `success`. Enumerate rather than summarise: two previous rounds' reports were
       wrong precisely because they summarised a five-leg matrix.
    3. All four `integration.trust06_idempotence - ...` result lines, quoted verbatim, two from the
       `x64-linux` job's own log and two from the `arm64-osx` job's own log, each reading `Passed`,
       with the SUMMARY stating which leg each line came from. Absence from a failure list is
       explicitly not acceptable evidence here; that substitution is exactly what left SC5 reported
       as partially closed when it was not.
    4. The `x64-windows-static-md` leg's `Test` step conclusion, quoted. This is the observation
       that has never been made in this phase's history, and it is the one this round exists to
       produce. Quote that leg's own `trust06_idempotence` result lines too if the step ran them —
       and if the leg's log shows the five designated-leg goldens being excluded by name, quote
       those lines as well, since a silent exclusion and an announced one are different facts.
    5. The `x64-linux` leg's `doc03` and `ts_scan_golden` result lines, quoted, since SC5's own
       sentence covers DOC-03's fixture-pair coverage gate and TRUST-09's TSDuck-derived goldens
       alongside TRUST-06, and under the designated-leg policy `x64-linux` is where the byte-exact
       goldens actually assert.
    6. The two non-blocking legs' conclusions, with the step each failed at where applicable,
       recorded as outside the SC5 claim rather than folded into it.

    If a blocking leg reaches Test and then fails it, diagnose from that job's own
    `gh run view <run id> --log-failed --job <job id>` output, fix the defect in the file that owns
    it, record it in `.planning/WINDOWS.md` via the `windows append` subcommand with the run id
    before fixing it, push, and re-observe. **Budget: three CI round trips for this task.** If the
    evidence above is still not complete after the third, stop: record the exact remaining failure
    with its run id and quoted error line, state plainly in the SUMMARY that SC5 is not closed, and
    do not soften any criterion. Specifically, do not make a leg non-blocking, do not widen the
    designated-leg exclusion regex or its `EXPECTED_EXCLUDED_COUNT`, and do not substitute a local
    `ctest` run for a CI observation. An honestly-recorded open criterion is a correct outcome; a
    manufactured green is not.
  </action>
  <verify>
    <automated>RUN=$(gh run list --branch gsd/phase-03-probe-layer-container-size --limit 1 --json databaseId --jq '.[0].databaseId'); echo "run=$RUN"; gh run view "$RUN" --json jobs --jq '[.jobs[] | select(.name=="build (x64-linux)" or .name=="build (arm64-osx)" or .name=="build (x64-windows-static-md)" or .name=="lint (ENG-16 boundary)") | select(.conclusion != "success")] | length'</automated>
    <fails_when>the printed number is anything other than 0, or the command prints only `run=` with no number (no job name matched, so no evidence was gathered at all)</fails_when>

    <automated>RUN=$(gh run list --branch gsd/phase-03-probe-layer-container-size --limit 1 --json databaseId --jq '.[0].databaseId'); for J in "build (x64-linux)" "build (arm64-osx)"; do ID=$(gh run view "$RUN" --json jobs --jq ".jobs[] | select(.name==\"$J\") | .databaseId"); printf '%s Passed-line count: ' "$J"; gh run view "$RUN" --log --job "$ID" | grep -c 'trust06_idempotence.*Passed' || true; done</automated>
    <fails_when>either printed count is fewer than 2, or a leg's line is missing entirely (the job name did not resolve, so that leg's log was never examined). A count of 2 on x64-linux with 0 on arm64-osx is the current pre-round state and means arm64-osx never ran the test.</fails_when>

    <automated>RUN=$(gh run list --branch gsd/phase-03-probe-layer-container-size --limit 1 --json databaseId --jq '.[0].databaseId'); gh run view "$RUN" --json jobs --jq '[.jobs[] | select(.name=="build (x64-windows-static-md)") | .steps[] | select(.name=="Test")] | map({name, conclusion})'</automated>
    <fails_when>the printed array is empty, or the single element's `conclusion` is `skipped` or `failure` — a skipped Test step means Build failed before it and the leg still has never run this suite, which is the specific unknown this task exists to remove</fails_when>

    <automated>RUN=$(gh run list --branch gsd/phase-03-probe-layer-container-size --limit 1 --json databaseId --jq '.[0].databaseId'); gh run view "$RUN" --log | grep -c 'check_corpus.sh: clean. Verified'</automated>
    <fails_when>the printed count is fewer than 3 — at least one blocking leg did not reach or did not pass corpus verification, so the fixture corpus the release blocker compares against was never confirmed present on that leg</fails_when>

    <automated>RUN=$(gh run list --branch gsd/phase-03-probe-layer-container-size --limit 1 --json databaseId --jq '.[0].databaseId'); ID=$(gh run view "$RUN" --json jobs --jq '.jobs[] | select(.name=="build (x64-linux)") | .databaseId'); gh run view "$RUN" --log --job "$ID" | grep -cE '(doc03|ts_scan_golden).*Passed'</automated>
    <fails_when>the printed count is fewer than 4 — the designated leg is where DOC-03's coverage gate and TRUST-09's three TSDuck-derived goldens actually assert, and SC5's own sentence covers both alongside TRUST-06</fails_when>
  </verify>
  <acceptance_criteria>
    - **CLI output** — the run id, head SHA, `status` and `conclusion` are quoted in the SUMMARY.
    - **CLI output** — all six job names and their conclusions are listed individually; `build (x64-linux)`, `build (arm64-osx)`, `build (x64-windows-static-md)` and `lint (ENG-16 boundary)` each read `success`.
    - **CLI output** — four `integration.trust06_idempotence - ...` result lines are quoted verbatim, two from `x64-linux` and two from `arm64-osx`, each reading `Passed`, with the SUMMARY naming the source leg for each. A line counted because it was not in a failure list does not satisfy this criterion.
    - **CLI output** — the `build (x64-windows-static-md)` leg's `Test` step conclusion is quoted and reads `success`. The SUMMARY states explicitly that this is the first time in this phase's history that this leg has been observed executing its Test step, citing the three prior run ids that did not.
    - **CLI output** — the `x64-linux` leg's `doc03` and `ts_scan_golden` result lines are quoted, covering DOC-03's coverage gate and TRUST-09's three TSDuck-derived goldens.
    - **CLI output** — each blocking leg's `check_corpus.sh: clean. Verified N fixture(s)` line is quoted, with N stated per leg.
    - **Behavior assertion** — the SUMMARY states in one sentence that ROADMAP SC5 is met, citing the run id and the four quoted result lines as the basis — never a local `ctest` run.
    - **Behavior assertion** — the two non-blocking legs' conclusions are recorded with their failing step where applicable, and the SUMMARY states explicitly that they are outside the SC5 claim and that WINDOWS.md #11 and #14 remain open.
    - **Behavior assertion** — the SUMMARY states how many CI round trips this task consumed, and if the three-trip budget was exhausted without complete evidence, it says so plainly and does not claim SC5 closed.
    - **Source assertion** — `git diff` for this task contains no change to `.github/workflows/ci.yml`'s `blocking:` flags, `EXCLUDED_TEST_REGEX` or `EXPECTED_EXCLUDED_COUNT`. Quote the diff stat.
  </acceptance_criteria>
  <precondition>Plan 03-21 is committed and its final CI run left all three blocking legs concluding Build success and reaching Test; `gh` is authenticated against this repository and PR #3 is open for branch `gsd/phase-03-probe-layer-container-size`.</precondition>
  <done>One real CI run shows all three blocking legs green with both TRUST-06 test cases observed `Passed` on x64-linux and on arm64-osx, the Windows leg's Test step concluding for the first time in this phase's history, and DOC-03's and TRUST-09's gates observed passing on the designated leg.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile both ledgers with what the run shows — close the two defects on cited evidence, and settle the TRUST-06 status discrepancy</name>
  <reversibility rating="reversible">Ledger and requirement-status text; every change is a recorded reconciliation citing its evidence, and git holds the prior text.</reversibility>
  <files>.planning/WINDOWS.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/03-probe-layer-container-size/03-22-SUMMARY.md` as written by Task 1 — the run id and every quoted observation each reconciliation below cites. Nothing here is decided without a specific observed step conclusion behind it.
    - `.planning/WINDOWS.md` **in full, both representations**. The file carries a markdown table AND a fenced JSON array holding the same records, plus a frontmatter block with `open_count`, `waived_count`, `fixed_count` and `total_count`. The two representations and the counts must stay in sync, which is why closures go through the `windows fixed` subcommand rather than a hand-edit, and why any in-place text correction must be applied to the table row AND the matching JSON object.
    - `.planning/WINDOWS.md` entries #13 (the unused constant, `tests/unit/test_ebml_scan.cpp:89`, arm64-osx/x64-osx) and #16 (the unqualified diagnostic call, `src/cli/main.cpp:297`, x64-windows-static-md) — the two this round's evidence closes — plus #11, #14 and #17, which stay open and why.
    - `.planning/REQUIREMENTS.md` line 173, the TRUST-06 checklist item whose text reads "produces a clean result — wired into CI as a release blocker", and line 372, the phase-mapping table row `| TRUST-06 | Phase 3 | Complete |`. These are the two sites carrying the same status; the file's status vocabulary elsewhere is `Complete` and `Pending` only.
    - `.planning/phases/03-probe-layer-container-size/03-VERIFICATION.md`'s `### Requirements Coverage` table, the TRUST-06 row — the verifier's own statement of the discrepancy: the ledger marks it `Complete` while the operative claim the requirement text itself makes is not yet true end-to-end. That row is this task's specification.
  </read_first>
  <action>
    Bring both ledgers into agreement with what CI actually shows, citing evidence for every change.

    **Reconciliation 1 — close WINDOWS.md #13 and #16 on observed evidence.** Using the
    `windows fixed` subcommand (never a hand-edit — the file's table, JSON array and frontmatter
    counts must move together), close #13 on the evidence that `build (arm64-osx)`'s `Build` step
    concluded `success`, and close #16 on the evidence that `build (x64-windows-static-md)`'s
    `Build` step concluded `success`. Record the run id alongside each closure in the SUMMARY,
    together with the specific quoted step conclusion that justified it. Do not close either on the
    strength of the commit that fixed it: a committed change and an observed effect are different
    facts, and conflating them is how entry #10 came to name the wrong ffmpeg version.

    **Reconciliation 2 — close or leave open every entry this round opened.** For each entry
    03-21's Task 3 appended, close it if this round observed its effect gone, or leave it open with
    a one-line reason recorded in the SUMMARY. Leave #11 (the non-blocking `arm64-linux` NuGet
    credential failure), #14 (the non-blocking `x64-osx` cross-build link failure) and #17 (the
    accepted designated-leg coverage narrowing) open: nothing in this round observes any of them
    resolved, and the ledger is where accepted limitations live so the next round sees them.

    **Reconciliation 3 — settle the TRUST-06 status discrepancy, in whichever direction the
    evidence points.** `.planning/REQUIREMENTS.md` currently asserts TRUST-06 `Complete` at both
    sites while the verification report's SC5 finding says the requirement's own operative clause —
    "wired into CI as a release blocker" — is false. That is a ledger asserting more than its
    evidence supports, and this task ends it one way or the other:

    - **If Task 1's evidence is complete** (three blocking legs green, four `Passed` lines observed
      on the two named legs, the Windows leg's Test step concluded), leave both sites reading
      `Complete` and record in the SUMMARY that the discrepancy is resolved *by evidence rather
      than by edit*, naming the run id, the four quoted result lines and the Windows Test-step
      conclusion as what makes the assertion true. State plainly that before this run the status
      was unsupported.
    - **If Task 1's evidence is not complete**, downgrade TRUST-06 at **both** sites so they agree
      with each other and with reality: change the checklist item at line 173 from a checked box to
      an unchecked one, and change the phase-mapping table row at line 372 from `Complete` to
      `Pending` — `Pending` being the only other status this file uses, so no new vocabulary is
      introduced. Add nothing to the requirement's own text. Record in the SUMMARY exactly what was
      missing, with the run id and the quoted evidence that fell short.

    Either way the decision, its direction, its evidence and its run id go in the SUMMARY. A status
    left unchanged because nobody looked is the failure mode this reconciliation exists to close;
    an unchanged status with a cited observation behind it is a decision.

    Do not edit `03-VERIFICATION.md` in this task. That document is regenerated by the
    re-verification pass that reads this plan's output, and editing the verifier's own report from
    inside a plan it is about would make the next round's evidence circular.
  </action>
  <verify>
    <automated>node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status</automated>
    <fails_when>non-zero exit, or the reported total does not equal both the number of rows in `.planning/WINDOWS.md`'s markdown table and the number of objects in its fenced JSON array — the two representations have diverged, which is how a ledger comes to look correct while disagreeing with itself</fails_when>

    <automated>python3 -c "
import json,re,sys
t=open('.planning/WINDOWS.md').read()
arr=json.loads(t[t.index('[',t.index('json')):t.rindex(']')+1])
by={e['id']:e for e in arr}
for i in (13,16):
    e=by.get(i)
    print(i, e and e['status'], e and e['resolved_at'])
    if not e or e['status']!='fixed' or not e['resolved_at']: sys.exit(1)
for i in (11,14,17):
    e=by.get(i)
    print(i, e and e['status'])
    if not e or e['status']!='open': sys.exit(1)
print('ledger dispositions correct')
"</automated>
    <fails_when>non-zero exit, or the trailing `ledger dispositions correct` line is absent. A printed `13 open None` or `16 open None` means the closure never happened; a printed `11 fixed` / `14 fixed` / `17 fixed` means an entry was closed that nothing in this round observed resolved.</fails_when>

    <automated>grep -n 'TRUST-06' .planning/REQUIREMENTS.md</automated>
    <fails_when>fewer than 2 lines are printed — TRUST-06 must still appear at both the checklist site and the phase-mapping table site; a single line means one of the two was deleted rather than reconciled</fails_when>

    <automated>CHK=$(grep -c '^- \[x\] \*\*TRUST-06\*\*' .planning/REQUIREMENTS.md); TBL=$(grep -c '^| TRUST-06 | Phase 3 | Complete |' .planning/REQUIREMENTS.md); echo "checklist_checked=$CHK table_complete=$TBL"; [ "$CHK" = "$TBL" ]</automated>
    <fails_when>the printed `checklist_checked=` and `table_complete=` values differ — the two sites that carry TRUST-06's status disagree with each other, which is the discrepancy this task exists to end rather than to relocate</fails_when>
  </verify>
  <acceptance_criteria>
    - **CLI output** — `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status` exits 0 and its reported total matches both the markdown table row count and the JSON array length. Quote the output in the SUMMARY.
    - **Source assertion** — WINDOWS.md entries #13 and #16 both read `fixed` with a non-null `resolved_at`, in the markdown table AND in the fenced JSON array; entries #11, #14 and #17 all still read `open`.
    - **Behavior assertion** — the SUMMARY names, for each closed entry, the specific observed step conclusion from Task 1's run that closed it, with the run id. A closure with no cited observation is not acceptable.
    - **Behavior assertion** — every entry 03-21's Task 3 appended is accounted for in the SUMMARY as either closed with cited evidence or left open with a one-line reason. None is left unmentioned.
    - **Source assertion** — TRUST-06's two sites in `.planning/REQUIREMENTS.md` agree with each other: either the checklist item is checked and the table row reads `Complete`, or the checklist item is unchecked and the table row reads `Pending`. The verify command above asserts this as an equality of two counts, so a half-applied edit fails.
    - **Behavior assertion** — the SUMMARY records the TRUST-06 reconciliation as an explicit decision: which direction it went, the run id and quoted observation behind it, and — if the status stayed `Complete` — the statement that it was unsupported before this run and is supported now. "Left as-is" without a cited observation does not satisfy this criterion.
    - **Source assertion** — `git diff --name-only` for this task lists only `.planning/WINDOWS.md`, `.planning/REQUIREMENTS.md` and this plan's SUMMARY. No source, script, test, workflow or verification-report file is touched.
  </acceptance_criteria>
  <precondition>Task 1's run evidence exists in this plan's SUMMARY; every closure and every status decision below cites a specific observed step conclusion from it and must not be made before that evidence is recorded.</precondition>
  <done>The defect ledger's two blocking-leg entries are closed on cited observed evidence with the non-blocking and accepted-limitation entries left open, and the requirement ledger's TRUST-06 status agrees at both sites with what the run shows — in whichever direction the evidence pointed, recorded as a decision rather than an assumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI run evidence to the release decision | The three blocking legs' aggregate status is the required check that gates a merge; what is claimed about it determines what ships. |
| defect ledger to the next verification round | `.planning/WINDOWS.md` is read as ground truth by the next round; a wrong or prematurely-closed entry propagates as a wrong conclusion. |
| requirement ledger to the milestone audit | `.planning/REQUIREMENTS.md`'s status column is what a milestone audit reads to decide a requirement shipped; a status asserting more than its evidence supports lets an unmet requirement pass unexamined. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-102 | Repudiation | the SC5 closure claim | high | mitigate | A criterion closed on inference rather than observation is indistinguishable from one genuinely met, and this exact substitution already occurred once in this phase (a test counted as passing because it was not among a leg's failures). Mitigated by requiring four verbatim `Passed` result lines pulled from two named jobs' own logs, per-job conclusions enumerated individually, an explicit conclusion read for the Windows leg's Test step, and a prohibition on satisfying a CI claim with a local run. |
| T-3-103 | Repudiation | `.planning/REQUIREMENTS.md`'s TRUST-06 status | high | mitigate | The row currently asserts `Complete` for a requirement whose own operative clause is false, and a milestone audit reading that column would conclude the release blocker shipped. Mitigated by reconciling both sites against the observed evidence in whichever direction it points, by asserting the two sites' agreement as an equality of counts so a half-applied edit fails, and by requiring the decision, its direction and its run id in the SUMMARY. |
| T-3-104 | Tampering | `.planning/WINDOWS.md`'s dual representation | medium | mitigate | The file holds the same records as a markdown table, a JSON array and a frontmatter count triple; a hand-edit to one leaves the ledger internally inconsistent while still looking correct. Mitigated by routing every closure through the `windows` subcommands, by asserting the reported total against both representations, and by a per-entry disposition check that reads the JSON array directly. |
| T-3-105 | Denial of Service | manufacturing green by narrowing the gate | high | mitigate | The cheapest way to complete this evidence is to stop requiring part of it — mark a leg non-blocking, widen the designated-leg exclusion regex or its expected count, or accept a local `ctest` run in place of a CI observation. Each produces a required check that reports green without having run what it exists to run. Mitigated by explicit prohibitions, by an acceptance criterion asserting the workflow diff contains no such change, and by the `EXPECTED_EXCLUDED_COUNT=5` assertion already standing in the workflow. |

**Package legitimacy gate:** not applicable — this plan introduces no npm, pip or cargo install
task and touches no dependency manifest. `03-RESEARCH.md`'s `## Package Legitimacy Audit` records
"Not applicable this phase."
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This plan produces evidence and record changes, not code. Every item below is created or changed
by this plan and should be treated as new rather than as drift.

**Changed ledger records**
- `.planning/WINDOWS.md` entries #13 and #16 move from `open` to `fixed` with a `resolved_at`
  timestamp, in both the markdown table and the fenced JSON array, with the frontmatter
  `open_count` / `fixed_count` moving accordingly.
- `.planning/WINDOWS.md` entries appended by 03-21's Task 3 are each dispositioned (closed with
  cited evidence, or left open with a recorded reason). Their ids continue from 17 and their count
  is not knowable before 03-21's runs.

**Changed requirement records**
- `.planning/REQUIREMENTS.md`'s TRUST-06 status at its two carrying sites — the checklist item and
  the phase-mapping table row — is reconciled against observed CI evidence. The direction depends
  on that evidence; the file's existing `Complete` / `Pending` vocabulary is used unchanged and no
  new status word is introduced.

**New evidence records (in the SUMMARY, not in code)**
- The first observed `Test`-step conclusion for `build (x64-windows-static-md)` in this phase's
  history, cited against the three prior run ids that did not produce one.
- Four verbatim `trust06_idempotence` result lines attributed to their source legs.

**Explicitly left open**
- `.planning/WINDOWS.md` #11 (non-blocking `arm64-linux` NuGet credential failure), #14
  (non-blocking `x64-osx` cross-build link failure) and #17 (the accepted designated-leg coverage
  narrowing).

**No new files, no new symbols, no new CI steps, no new scripts, no new test case names** are
introduced by this plan. `03-VERIFICATION.md` is deliberately not edited here.
</artifacts_this_phase_produces>

<verification>
- On one real run, `build (x64-linux)`, `build (arm64-osx)`, `build (x64-windows-static-md)` and `lint (ENG-16 boundary)` all conclude `success`; the count of those four not concluding `success` is 0.
- The per-job `trust06_idempotence` `Passed`-line count is at least 2 on `build (x64-linux)` and at least 2 on `build (arm64-osx)`, read from each job's own log.
- `build (x64-windows-static-md)`'s `Test` step exists in the job's step list and concludes `success` — never `skipped`.
- The run log contains at least 3 `check_corpus.sh: clean. Verified` lines, one per blocking leg.
- The `x64-linux` job's log contains at least 4 `Passed` lines matching `doc03` or `ts_scan_golden`.
- `windows status` exits 0 with a total matching both the markdown table and the JSON array; #13 and #16 read `fixed` with a `resolved_at`; #11, #14 and #17 read `open`.
- TRUST-06's checklist site and phase-mapping table site in `.planning/REQUIREMENTS.md` agree, asserted as an equality of two grep counts.
</verification>

<success_criteria>
ROADMAP SC5 is met and closed on observed run-log evidence rather than on inference: the
encode-twice comparison runs and comes back clean on both `x64-linux` and `arm64-osx` in a run
where all three blocking legs are green, DOC-03's coverage gate and TRUST-09's TSDuck-derived
goldens are observed passing on the designated leg, and `x64-windows-static-md` is observed
executing its Test step for the first time in this phase's history. Both ledgers state what that
run shows — the defect register closes only what it observed, and the requirement register no
longer asserts more than its evidence supports — so the next round starts from facts rather than
re-deriving them.
</success_criteria>

<output>
Create `.planning/phases/03-probe-layer-container-size/03-22-SUMMARY.md` when done.
</output>
