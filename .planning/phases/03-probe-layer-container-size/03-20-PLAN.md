---
phase: 03-probe-layer-container-size
plan: 20
type: execute
wave: 4
gap_closure: true
depends_on: ["03-19"]
files_modified:
  - .planning/WINDOWS.md
  - .planning/phases/03-probe-layer-container-size/03-VERIFICATION.md
autonomous: true
requirements: [TRUST-06, DOC-03]
estimate:
  tokens: 17500
  raw_tokens: 35000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "On one real GitHub Actions run, all three blocking legs — x64-linux, arm64-osx and x64-windows-static-md — conclude success (ROADMAP SC5: 'comes back clean as a CI release blocker')."
    - "Both integration.trust06_idempotence test cases are observed executing and reported Passed in that run's log on BOTH x64-linux and arm64-osx — an observation of the result lines, never an inference from their absence in a failure list."
    - "The x64-windows-static-md leg's Build step concludes success, so every Phase-3 check has been proven to compile on the toolchain PROJECT.md names as a hard parity requirement (WINDOWS.md #9 closed)."
    - "The arm64-osx leg's corpus generation and verification steps conclude success, proving the bash-3.2 fix at runtime rather than at source level (D-GAP-02 item c)."
    - ".planning/WINDOWS.md and 03-VERIFICATION.md state what the CI run actually shows, so the next verification round does not re-derive corrections that were already established."
  prohibitions:
    - "SC5 is never reported closed from a local ctest run, from a partial matrix, or from a leg's absence from a failure list."
    - "A ledger entry is never marked fixed on the strength of a committed change alone; the entry closes on observed CI evidence."
    - "The stale records are never 'corrected' by deleting the erroneous claim without recording what was wrong — the ledger is the input the next round reads, and an unexplained edit teaches nothing."
    - "The non-blocking arm64-linux credential failure is never folded into the SC5 claim in either direction: it does not gate, and it is not silently closed."
  artifacts:
    - path: ".planning/WINDOWS.md"
      provides: "Corrected ffmpeg-version claim, a new entry for the macOS bash-3.2 CI failure, and closed entries for the two defects this round fixed"
      contains: "6.1.1-3ubuntu5"
    - path: ".planning/phases/03-probe-layer-container-size/03-VERIFICATION.md"
      provides: "A correction note recording that both macOS legs died at the corpus verification step, contradicting the report's original 'every leg it reached' claim"
  key_links:
    - from: "the real CI run's per-job conclusions"
      to: ".planning/WINDOWS.md"
      via: "entries closed only on observed evidence, with the run id recorded"
      pattern: "33951407521|run "
---

<!-- planner-discipline-allow: installs ffmpeg 9.0.1 via apt -->
<!-- planner-discipline-allow: generated and verified cleanly on every leg it reached -->

<objective>
Close ROADMAP **SC5** on evidence, and repair the records that made the previous round's
picture of CI wrong.

SC5's own wording is "Encoding a fixture twice with identical settings and comparing under
`sw-encoder` comes back clean **as a CI release blocker**." That is a claim about a real CI
run. The prior round reported it partially closed on an inference — `test_trust06_idempotence`
was "not among the 5 x64-linux failures" — which `03-VERIFICATION.md`'s own third `missing:`
item flags as never independently confirmed against a run log.

Three records are wrong on disk today, established by the orchestrator against the real run
`33951407521`: `03-VERIFICATION.md` claims the corpus verified cleanly on every leg it
reached, when both macOS legs died at exactly that step; `.planning/WINDOWS.md` #10 names the
wrong ffmpeg version for what CI installs; and the macOS bash-3.2 failure on the blocking
`arm64-osx` leg — the fourth defect that run exposed — appears in no ledger at all.

Purpose: a verification round that reads a wrong ledger re-derives the same corrections and
can close a criterion that is not met. Correcting the record is the cheapest thing in this
round and the highest-leverage.
Output: SC5 closed on observed run-log evidence, with the ledgers matching what happened.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/phases/03-probe-layer-container-size/03-VERIFICATION.md
</context>

<flagged_assumptions>
**A1 — The non-blocking legs are not part of the SC5 claim.** `arm64-linux` fails at
`Register vcpkg NuGet feed (read-write, trusted runs only)` for credential/infrastructure
reasons (`WINDOWS.md` #11, open, out of this round's scope), and `x64-osx` is a non-blocking
cross-build. `.github/workflows/ci.yml` declares both `blocking: false` and the job carries
`continue-on-error: ${{ !matrix.blocking }}`, so neither can influence the aggregate required
status in either direction. This plan records their conclusions and neither counts them
against SC5 nor closes their ledger entries.

**A2 — Spec-less edge probe.** No `-SPEC.md` exists for this phase; all 32 applicable rows
returned `unresolved` with `byVerification: {explicit: 0, backstop: 0}`. None is auto-resolved
and none is dropped. In scope here: `TRUST-06` (unclassified) and `DOC-03`'s `adjacency` /
`empty` / `ordering` rows, all three of which concern DOC-03's fixture-pair semantics rather
than its CI wiring and are therefore recorded as out of this round's scope. The other 28 rows
belong to SC1–SC4, verified and closed this phase. The `prohibitions` block above is authored
descriptor-less per the §B recall protocol and disposes flagged-unverified.

**A3 — This plan may need more than one CI round trip.** If a blocking leg is still red, the
correct response is to diagnose and fix within the owning plan's scope and push again, not to
soften this plan's criteria. SC5 is either observed green on three blocking legs or it is not
closed.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Observe SC5 met on a real run — three blocking legs green, TRUST-06 passing on two of them</name>
  <files>.planning/phases/03-probe-layer-container-size/03-20-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/03-probe-layer-container-size/03-VERIFICATION.md`, the `gaps:` block — SC5's exact wording and all three `missing:` items. The third one is this task's specification: confirm all three blocking legs green end-to-end "with `test_trust06_idempotence` explicitly observed to execute and pass in the run log on at least x64-linux and arm64-osx — this has still never been independently confirmed against a real run log, only inferred".
    - `tests/integration/test_trust06_idempotence.cpp` lines 93-110 — the two `TEST_CASE` names, which with `TEST_PREFIX "integration."` become CTest names beginning `integration.trust06_idempotence - `.
    - `.github/workflows/ci.yml` lines 22-69 — the six job names (`build (x64-linux)`, `build (arm64-osx)`, `build (x64-windows-static-md)`, `build (arm64-linux)`, `build (x64-osx)`, `lint (ENG-16 boundary)`) and which three are `blocking: true`.
    - `.github/workflows/ci.yml` — the step names whose conclusions this task must read individually: `Install the pinned ffmpeg build (D-GAP-01)`, `Generate media fixture corpus (BUILD-08 / D-08)`, `Verify the fixture corpus is complete`, `Report corpus digest (cross-platform byte-identity evidence)`, `Assert the corpus digest matches the committed pin (D-GAP-01)`, `Configure`, `Build`, `Test`.
    - `.planning/phases/03-probe-layer-container-size/03-19-SUMMARY.md` — which corpus-identity policy was implemented, since under the designated-leg branch some legs deliberately exclude named tests and their logs must show those lines.
    - `.planning/WINDOWS.md` entries #9, #10 and #11 — what each claims and which of them this round's evidence closes.
  </read_first>
  <action>
    Gather the single piece of evidence ROADMAP SC5 actually asks for: one real CI run in which
    the release blocker runs and comes back clean on the legs that gate a merge.

    Push the branch if there are uncommitted changes, identify the completed run, and record
    all of the following in this plan's SUMMARY as quoted command output, each labelled:

    1. Run identity and overall outcome from `gh run view <id> --json status,conclusion`, plus
       the head SHA, so a later reader can tell which commit was proven.
    2. Every one of the six job names with its conclusion, enumerated individually. The three
       blocking jobs must each read `success`. Enumerate rather than summarise: the previous
       round's report was wrong precisely because it summarised five failing legs as two.
    3. Both `integration.trust06_idempotence - ...` CTest result lines, quoted verbatim, from
       the `x64-linux` log AND from the `arm64-osx` log — four lines in total, each showing
       `Passed`. Absence from a failure list is explicitly not acceptable evidence here; that
       substitution is what left SC5 reported as partially closed when it was not.
    4. The `x64-windows-static-md` leg's `Build` step conclusion, which is the observation
       WINDOWS.md #9 closes on.
    5. The `arm64-osx` leg's `Generate media fixture corpus (BUILD-08 / D-08)` and
       `Verify the fixture corpus is complete` step conclusions, which are the observation that
       proves the bash-3.2 fix at runtime rather than at source level (D-GAP-02 item c). Quote
       the corpus-verification step's own "clean. Verified N fixture(s)" output line from that
       leg.
    6. The two non-blocking legs' conclusions and, for each that failed, the step it failed at.
       Record them as out of the SC5 claim rather than folding them in.
    7. Under the designated-leg corpus policy only: the per-excluded-test log lines from one
       non-designated leg, proving the exclusion is announced rather than silent.

    If any blocking leg is not `success`, diagnose it, fix it within the scope of whichever
    plan owns the affected file, push, and re-observe. Do not weaken this task's criteria and
    do not declare SC5 closed on a partial matrix.
  </action>
  <verify>
    <automated>gh run view "$(gh run list --branch gsd/phase-03-probe-layer-container-size --limit 1 --json databaseId --jq '.[0].databaseId')" --json jobs --jq '[.jobs[] | select(.name=="build (x64-linux)" or .name=="build (arm64-osx)" or .name=="build (x64-windows-static-md)" or .name=="lint (ENG-16 boundary)") | select(.conclusion != "success")] | length'</automated>
    <fails_when>the printed number is anything other than 0, or the command prints nothing (no job name matched, meaning the evidence was never gathered)</fails_when>
    <automated>gh run view "$(gh run list --branch gsd/phase-03-probe-layer-container-size --limit 1 --json databaseId --jq '.[0].databaseId')" --log | grep 'integration\.trust06_idempotence' | grep -c 'Passed'</automated>
    <fails_when>the printed count is fewer than 4 — SC5 needs both test cases observed Passed on both x64-linux and arm64-osx, and anything less means at least one leg's evidence is missing rather than merely unquoted</fails_when>
    <automated>gh run view "$(gh run list --branch gsd/phase-03-probe-layer-container-size --limit 1 --json databaseId --jq '.[0].databaseId')" --log | grep -c 'check_corpus.sh: clean. Verified'</automated>
    <fails_when>the printed count is fewer than 3 — at least one blocking leg did not reach or did not pass corpus verification, so the bash-3.2 runtime proof is incomplete</fails_when>
  </verify>
  <acceptance_criteria>
    - **CLI output** — the run id, head SHA, `status` and `conclusion` are quoted in the SUMMARY.
    - **CLI output** — all six job names and conclusions are listed individually; `build (x64-linux)`, `build (arm64-osx)`, `build (x64-windows-static-md)` and `lint (ENG-16 boundary)` each read `success`.
    - **CLI output** — four `integration.trust06_idempotence - ...` result lines are quoted verbatim (two from `x64-linux`, two from `arm64-osx`), each reading `Passed`. The SUMMARY states which leg each line came from.
    - **CLI output** — the `x64-windows-static-md` leg's `Build` step conclusion is quoted and reads `success`.
    - **CLI output** — the `arm64-osx` leg's corpus generation and verification step conclusions are quoted and read `success`, with that leg's own `clean. Verified N fixture(s)` line included.
    - **CLI output** — the `Install the pinned ffmpeg build (D-GAP-01)` step's version line is quoted for all three blocking legs and names the same pinned version on each.
    - **Behavior assertion** — the SUMMARY states, in one sentence, that ROADMAP SC5 is met, and cites the run id and the four quoted result lines as the basis — not a local `ctest` run.
    - **Behavior assertion** — the two non-blocking legs' conclusions are recorded with their failing step where applicable, and the SUMMARY states explicitly that they are outside the SC5 claim and that `WINDOWS.md` #11 remains open.
  </acceptance_criteria>
  <precondition>Plans 03-16 through 03-19 are committed on the branch, the branch has an open pull request so a push produces a `pull_request` run, and the `gh` CLI is authenticated against this repository.</precondition>
  <done>One real CI run shows all three blocking legs green with both TRUST-06 test cases observed Passed on x64-linux and arm64-osx, the Windows Build step green, and the arm64-osx corpus steps green.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the stale ledger and verification records against what CI actually shows</name>
  <reversibility rating="reversible">Ledger and report text; every change is a recorded correction with the evidence cited, and git holds the prior text.</reversibility>
  <files>.planning/WINDOWS.md, .planning/phases/03-probe-layer-container-size/03-VERIFICATION.md</files>
  <read_first>
    - `.planning/WINDOWS.md` in full — BOTH representations. The file carries a markdown table AND a fenced JSON array holding the same records, plus a frontmatter block with `open_count`, `waived_count`, `fixed_count` and `total_count`. The two representations and the counts must stay in sync, which is why the append and close operations below go through the `windows` subcommands (`status`, `append`, `waive`, `fixed`) rather than a hand-edit, and why any in-place text correction must be applied to the table row AND the JSON object.
    - `.planning/WINDOWS.md` entry #10 — its description states the CI-installed ffmpeg version as 9.0.1 obtained via the distribution package manager. The run log's own `ffmpeg -version` output on the Linux leg reads `ffmpeg version 6.1.1-3ubuntu5`, and the committed goldens were captured against `N-126086-ge5ecfe8970-20260812` per `tests/fixtures/GENERATOR_MANIFEST.json`. That is a two-major-version gap, not the patch drift the entry describes.
    - `.planning/WINDOWS.md` entries #9 and #11 — what each claims, and that #11 (the non-blocking `arm64-linux` NuGet credential failure) stays open.
    - `.planning/phases/03-probe-layer-container-size/03-VERIFICATION.md`, the `gaps:` block's `reason:` field — the sentence claiming the corpus generated and verified cleanly on every leg it reached, and the `re_verification.gaps_remaining` entry making the equivalent claim. Both are contradicted by the run's own step conclusions: `arm64-osx` and `x64-osx` each died at `Verify the fixture corpus is complete` with a command-not-found error at `scripts/check_corpus.sh` line 62, exit 127.
    - `.planning/phases/03-probe-layer-container-size/03-20-SUMMARY.md` as written by Task 1 — the run id and quoted evidence every correction below cites.
    - `.planning/phases/03-probe-layer-container-size/03-19-SUMMARY.md` — whether the designated-leg corpus policy was taken, which determines whether a new ledger entry is owed for narrowed coverage.
  </read_first>
  <action>
    Bring the two records into agreement with observed CI, citing evidence for every change.

    **Correction 1 — the ffmpeg version in entry #10.** Replace the claim about which version
    CI installs with the version the run log actually printed on the Linux leg,
    `6.1.1-3ubuntu5`, and state the real magnitude: the committed goldens were captured against
    the master snapshot recorded in `tests/fixtures/GENERATOR_MANIFEST.json`, a two-major-
    version gap rather than a patch drift, and `scripts/gen_corpus.sh`'s floor of 6.1 admits
    that build so the version gate passes and different bytes come out. Apply the correction to
    the markdown table row AND to the matching object in the fenced JSON array; leaving them
    divergent produces a ledger whose two halves disagree.

    **Correction 2 — a missing entry.** Append a ledger entry for the fourth defect run
    `33951407521` exposed and no ledger records: the blocking `arm64-osx` leg failed at
    `Verify the fixture corpus is complete` because `scripts/check_corpus.sh` used a bash-4-only
    array-reading builtin that macOS's bash 3.2 does not provide, aborting with exit 127 before
    Configure, Build or Test. Record that it was fixed in commit `91d9d2f`, that the fix had
    never been exercised by any CI run at the time it was recorded, and that plans 03-18 and
    03-20 respectively added the permanent guard and observed the runtime proof. Use the
    `windows append` subcommand so the table, the JSON array and the frontmatter counts all
    update together.

    **Correction 3 — the verification report's "every leg" claim.** In
    `03-VERIFICATION.md`, correct the statement that the corpus generated and verified cleanly
    on every leg it reached, in both places it appears (the `gaps:` `reason:` field and the
    `re_verification.gaps_remaining` entry). Replace it with the per-leg truth: the corpus
    steps succeeded on `x64-linux`, `arm64-linux` and `x64-windows-static-md`, and failed on
    both macOS legs at the verification step. Add a short, dated correction note under the
    report's own heading recording that all five build legs failed in that run rather than two,
    that the macOS cause was a fourth defect recorded nowhere at the time, and that the ffmpeg
    version named in the report was wrong — with the run id cited as the source. Describe the
    error rather than re-quoting the erroneous sentence, so the corrected file no longer
    contains the false claim in any form.

    **Correction 4 — close what this round actually closed.** Using the `windows fixed`
    subcommand, close entry #9 on the evidence that the `x64-windows-static-md` leg's `Build`
    step concluded success (Task 1's quoted output), and close entry #10 on the evidence that
    the blocking legs are green with the pinned corpus and the re-baselined goldens. Close the
    newly appended macOS entry on the evidence that `arm64-osx` passed both corpus steps.
    Record the run id alongside each closure in the SUMMARY. Leave entry #11 open: it is a
    non-blocking-leg credential problem, outside this round's scope, and closing it would be a
    claim nothing in this round supports.

    **Correction 5 — conditional.** If plan 03-19 implemented the designated-leg corpus policy,
    append a ledger entry recording the narrowed coverage: which byte-exact fixture-derived
    golden tests run on the designated leg only, on which legs they are excluded, and that the
    exclusion is announced in each affected leg's log and its size asserted. Leave it open — it
    is a real, accepted limitation, and the ledger is where accepted limitations live so the
    next round sees them. If 03-19 implemented the uniform policy, state in the SUMMARY that no
    such entry is owed and why.

    Do not mark any entry fixed on the strength of a committed change alone. Every closure
    above cites a specific observed step conclusion from Task 1's run.
  </action>
  <verify>
    <automated>node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status</automated>
    <fails_when>non-zero exit, or the reported total does not equal the number of rows in the markdown table plus the number of objects in the fenced JSON array (the two representations have diverged)</fails_when>
    <automated>grep -c 'installs ffmpeg 9.0.1 via apt' .planning/WINDOWS.md</automated>
    <fails_when>the printed count is anything other than 0</fails_when>
    <automated>grep -c '6.1.1-3ubuntu5' .planning/WINDOWS.md</automated>
    <fails_when>the printed count is fewer than 2 — the correction reached only one of the file's two representations</fails_when>
    <automated>grep -c 'generated and verified cleanly on every leg it reached' .planning/phases/03-probe-layer-container-size/03-VERIFICATION.md</automated>
    <fails_when>the printed count is anything other than 0</fails_when>
    <automated>grep -ci 'bash 3.2' .planning/WINDOWS.md</automated>
    <fails_when>the printed count is 0 — the macOS defect entry was never appended</fails_when>
  </verify>
  <acceptance_criteria>
    - **Source assertion** — `grep -c 'installs ffmpeg 9.0.1 via apt' .planning/WINDOWS.md` reports `0` and `grep -c '6.1.1-3ubuntu5' .planning/WINDOWS.md` reports at least `2` (the table row and the JSON object).
    - **Source assertion** — `grep -c 'generated and verified cleanly on every leg it reached' .planning/phases/03-probe-layer-container-size/03-VERIFICATION.md` reports `0`, and the file contains a dated correction note citing the run id.
    - **Source assertion** — `.planning/WINDOWS.md` contains an entry naming the macOS bash 3.2 corpus-step failure, the `arm64-osx` leg, exit 127, and commit `91d9d2f`.
    - **CLI output** — `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status` exits 0, and its reported `total_count` matches both the table row count and the JSON array length. Quote the output in the SUMMARY.
    - **Source assertion** — entries #9 and #10 and the newly appended macOS entry all read `fixed` with a `resolved_at` timestamp; entry #11 still reads `open`.
    - **Behavior assertion** — the SUMMARY names, for each closed entry, the specific step conclusion from Task 1's run that closed it. A closure with no cited observation is not acceptable.
    - **Behavior assertion** — if 03-19 chose the designated-leg policy, a new open ledger entry records the narrowed coverage naming the excluded tests and the legs; if it chose uniform, the SUMMARY states that no entry is owed.
    - **Source assertion** — `git diff --name-only` lists only `.planning/WINDOWS.md`, the verification report, and this plan's SUMMARY. No source, script, test or workflow file is touched by this task.
  </acceptance_criteria>
  <precondition>Task 1's run evidence exists in this plan's SUMMARY; every ledger closure below cites a specific observed step conclusion from it and must not be made before that evidence is recorded.</precondition>
  <done>The ledger and the verification report state what CI actually shows, every closure cites an observed step conclusion, and the non-blocking credential defect remains open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI run evidence to the release decision | The three blocking legs' aggregate status is the required check that gates a merge; what is claimed about it determines what ships. |
| defect ledger to the next verification round | `.planning/WINDOWS.md` is read as ground truth by the next round; a wrong entry propagates as a wrong conclusion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-94 | Repudiation | the SC5 closure claim | high | mitigate | A criterion closed on inference rather than observation is indistinguishable from one genuinely met, and this exact substitution already occurred once this phase. Mitigated by requiring four verbatim `Passed` result lines from two named legs' logs, per-job conclusions enumerated individually, and an explicit prohibition on satisfying a CI claim with a local run. |
| T-3-95 | Tampering | `.planning/WINDOWS.md`'s dual representation | medium | mitigate | The file holds the same records as a markdown table, a JSON array and a frontmatter count triple; a hand-edit to one leaves the ledger internally inconsistent while still looking correct. Mitigated by routing appends and closures through the `windows` subcommands, by applying in-place text corrections to both representations, and by asserting the reported total against both. |
| T-3-96 | Repudiation | premature closure of ledger entries | medium | mitigate | Marking a defect fixed because a change was committed — rather than because its effect was observed — is how #10 came to name the wrong ffmpeg version in the first place. Mitigated by requiring each closure to cite a specific observed step conclusion, and by leaving #11 open since nothing in this round observes it. |

**Package legitimacy gate:** not applicable — no npm, pip or cargo install task is introduced;
`03-RESEARCH.md`'s `## Package Legitimacy Audit` records "Not applicable this phase."
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Corrected records**
- `.planning/WINDOWS.md` entry #10 — the CI-installed ffmpeg version corrected to `6.1.1-3ubuntu5`, in both the markdown table and the fenced JSON array, with the real two-major-version magnitude stated.
- `.planning/phases/03-probe-layer-container-size/03-VERIFICATION.md` — the "every leg it reached" claim replaced with the per-leg truth, plus a dated correction note citing run `33951407521`.

**New ledger entries**
- A `.planning/WINDOWS.md` entry for the macOS bash-3.2 corpus-step failure on the blocking `arm64-osx` leg (exit 127, fixed in `91d9d2f`, guarded by 03-18, proven by 03-20), appended via `windows append` and then closed.
- Conditional: a `.planning/WINDOWS.md` entry recording narrowed byte-exact golden coverage, if plan 03-19 implemented the designated-leg policy. Left open.

**Closed ledger entries**
- `.planning/WINDOWS.md` #9 (MSVC build failure) and #10 (golden drift), each closed on a cited observed step conclusion.

**Explicitly left open**
- `.planning/WINDOWS.md` #11 — the non-blocking `arm64-linux` vcpkg NuGet credential failure.

**No new files, no new CI steps, no new scripts, no new test case names** are introduced by this plan.
</artifacts_this_phase_produces>

<verification>
- On one real run, `build (x64-linux)`, `build (arm64-osx)`, `build (x64-windows-static-md)` and `lint (ENG-16 boundary)` all conclude `success`.
- Four `integration.trust06_idempotence` result lines reading `Passed` are quoted, two from each of `x64-linux` and `arm64-osx`.
- The `x64-windows-static-md` `Build` step and the `arm64-osx` corpus steps are quoted as `success`.
- `grep -c 'installs ffmpeg 9.0.1 via apt' .planning/WINDOWS.md` reports 0; `grep -c '6.1.1-3ubuntu5' .planning/WINDOWS.md` reports at least 2.
- `grep -c 'generated and verified cleanly on every leg it reached' .planning/phases/03-probe-layer-container-size/03-VERIFICATION.md` reports 0.
- `windows status` exits 0 with a total matching both the table and the JSON array; #9, #10 and the new macOS entry read `fixed`; #11 reads `open`.
</verification>

<success_criteria>
ROADMAP SC5 is met and is closed on observed run-log evidence rather than on inference: the
encode-twice comparison runs and comes back clean on both x64-linux and arm64-osx in a run
where all three blocking legs are green. The defect ledger and the verification report state
what that run actually shows, so the next round starts from facts instead of re-deriving them.
</success_criteria>

<output>
Create `.planning/phases/03-probe-layer-container-size/03-20-SUMMARY.md` when done.
</output>
