---
phase: 03-probe-layer-container-size
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/model.h
  - src/core/rational.h
  - src/core/snapshot.cpp
  - src/compare/engine.cpp
  - src/compare/tol.cpp
  - src/report/json.cpp
  - src/report/junit.cpp
  - src/report/markdown.cpp
  - docs/schema/report-1.0.json
  - tests/unit/test_rational.cpp
  - tests/unit/test_measurement_provenance.cpp
  - tests/unit/CMakeLists.txt
  - .planning/phases/03-probe-layer-container-size/03-CHECK-ROSTER.md
  - tests/unit/test_tolerance.cpp
autonomous: false
requirements: [SIZE-01, CONT-07, PROBE-09]

estimate:
  tokens: 26000
  raw_tokens: 52000
  tasks: 4
  confidence: high

must_haves:
  truths:
    - "`SkipReason` carries `partial_scan`, `insufficient_data` and `no_timing_data`, and a build with all four consuming sites updated compiles clean under `-Wall -Wextra -Werror`."
    - statement: "The same three new `SkipReason` enumerators compile clean under MSVC `/W4 /WX` (C4062 fires on an unhandled enumerator in a switch with no `default:`)."
      verification: backstop
    - "A `Measurement` carrying `estimated: true` on either side widens the resolved tolerance by exactly 3x in `compare_tol` (D-03), and the widening is stated in the finding message."
    - "A `Measurement`'s `evidence` object reaches the emitted `Finding` and renders as a JSON object rather than `null` (closes Broken Window #1)."
    - "`mediadiff::detail::checked_div` returns false for `INT64_MIN / -1` and for a zero divisor, and never invokes UB."
    - "Two identical `compare --json` runs still produce byte-identical stdout after evidence propagation (TRUST-05 unbroken)."
  artifacts:
    - src/core/model.h
    - src/core/rational.h
    - docs/schema/report-1.0.json
    - tests/unit/test_measurement_provenance.cpp
    - .planning/phases/03-probe-layer-container-size/03-CHECK-ROSTER.md
  key_links:
    - "`SkipReason` enum -> `src/report/json.cpp::skip_reason_to_string` -> `src/report/junit.cpp::skip_reason_text` -> `docs/schema/report-1.0.json`'s closed `skip_reason` enum: all four must move together or the build/schema test fails."
    - "`Measurement.estimated` -> `src/core/snapshot.cpp` round-trip -> `src/compare/tol.cpp` tolerance widening: the marker must survive a snapshot write/read cycle or D-03 silently degrades to measured-tolerance comparison."
    - "`Measurement.evidence` -> `src/compare/engine.cpp` merge -> `Finding.evidence` -> `src/report/json.cpp`: single propagation seam, not seven comparator edits."
  prohibitions:
    - "Never add a `default:` arm to a `SkipReason` switch to silence the compiler — the exhaustive switch is the gate that makes a missing enumerator a build failure instead of a silent gap."
    - "Never widen a tolerance by converting to double — the estimated-measurement widening is an exact integer multiply on the parsed rational or it is not done at all."
    - "Never register a check id that is not on the approved roster; check ids are forever and a rename post-v1 costs an alias plus a deprecation cycle."
    - "Never let an `estimated` marker exist only in evidence text — D-03 requires it reach the comparison layer as data the comparator reads."
---

<objective>
Close the compile-time trap and land the three shared core-model primitives every later
Phase-3 plan depends on, then lock the phase's check-id roster behind a human decision.

This plan adds no user-visible capability by design: it is the prerequisite that makes the
tracer (03-02) and every check family after it compilable. It is deliberately placed ahead
of the tracer because `SkipReason` cannot be extended incrementally — `src/report/json.cpp`
and `src/report/junit.cpp` switch over it with no `default:` arm and `docs/schema/report-1.0.json`
declares a closed enum, so the enum and its three consumers move in one commit or the build
is red.

Purpose: give SIZE-01 its `skipped:partial_scan` vocabulary (D-02), CONT-07 its `estimated`
comparison attribute (D-03), PROBE-09 its evidence channel for byte offsets, and SIZE-01's
windowing math a `checked_div` primitive.
Output: extended `SkipReason`, `Measurement.estimated`, `Finding.evidence`,
`detail::checked_div`, and an approved 27-id check roster recorded on disk.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-probe-layer-container-size/03-CONTEXT.md
@claude_docs/02-container-analysis.md
@claude_docs/06-content-and-size-analysis.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Extend SkipReason across all four consuming sites in one commit</name>
  <files>src/core/model.h, src/report/json.cpp, src/report/junit.cpp, docs/schema/report-1.0.json, tests/unit/test_measurement_provenance.cpp, tests/unit/CMakeLists.txt</files>
  <read_first>
    - `src/core/model.h` lines 33-51 — `Status` and the 11-value `SkipReason` enum, plus the header comment explaining why `none` exists (ENG-14: the JSON report emits `skip_reason` unconditionally).
    - `src/report/json.cpp` lines 34-60 — `skip_reason_to_string`, exhaustive switch, **no `default:` arm**, trailing `return "none";`.
    - `src/report/junit.cpp` lines 17-43 — `skip_reason_text`, identical shape, identical obligation.
    - `docs/schema/report-1.0.json` lines 112-125 — the closed `"skip_reason"` enum with all 11 current spellings.
    - `tests/integration/test_json_schema.cpp` lines 169-195 — the test asserting `skip_reason` is present on every finding and the `json_schema_basic` golden.
    - `src/core/snapshot.cpp` lines 55-70 — `scope_kind_to_string`'s comment documenting the no-`default:`-arm-plus-trailing-return idiom this project uses deliberately.
  </read_first>
  <behavior>
    - Test 1: every `SkipReason` enumerator round-trips through `skip_reason_to_string` to its own snake_case spelling, asserted by iterating a hand-listed array of all 14 enumerators (a test that must be edited when the enum grows, by design).
    - Test 2: the set of strings `skip_reason_to_string` can return is exactly the set declared in `docs/schema/report-1.0.json`'s `skip_reason` enum — parsed from the schema file at test time, not hardcoded twice.
    - Test 3: `skip_reason_text` (junit) agrees with `skip_reason_to_string` (json) for all 14 enumerators — the two renderers may never drift.
  </behavior>
  <action>
    Add exactly three enumerators to `mediadiff::SkipReason` in `src/core/model.h`, appended
    after `no_prior_release` so no existing value's position shifts:
    `partial_scan` (D-02: a dependent check refusing to report a number computed from a
    truncated `PacketScan`), `insufficient_data` (doc 02 section 5: a single-PCR TS file has
    nothing to measure a PCR interval over), and `no_timing_data` (a stream whose packets all
    carry `AV_NOPTS_VALUE` for dts, so `size.peak_bitrate` has no axis to window on).
    Document each enumerator inline with the concrete check that emits it.

    Then update, in the SAME commit, the three sites that would otherwise break or drift:
    `src/report/json.cpp`'s `skip_reason_to_string` (add three `case` arms returning the
    identical snake_case spelling; keep the trailing `return "none";` and add no `default:`),
    `src/report/junit.cpp`'s `skip_reason_text` (same three arms, same spellings), and
    `docs/schema/report-1.0.json`'s `"skip_reason"` enum array (append the same three strings
    in the same order).

    Add `tests/unit/test_measurement_provenance.cpp` with the three behaviors above; register
    it in `tests/unit/CMakeLists.txt`. The schema-agreement test reads
    `docs/schema/report-1.0.json` via a compile definition path in the same style
    `tests/integration/test_json_schema.cpp` already uses — do not hardcode the enum list a
    second time in C++.
  </action>
  <verify>
    <automated>cmake --build build/x64-linux --target mediadiff_unit_tests 2>&amp;1 | tail -5 &amp;&amp; ctest --test-dir build/x64-linux -R "unit\." --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - **Locally provable** — `grep -c 'partial_scan\|insufficient_data\|no_timing_data' src/core/model.h src/report/json.cpp src/report/junit.cpp docs/schema/report-1.0.json` reports at least 3 for each of the four files.
    - **Locally provable** — `grep -v '^\s*//' src/report/json.cpp | grep -c 'default:'` and the same for `src/report/junit.cpp` both report `0` (the exhaustive-switch gate is intact).
    - **Locally provable** — `ctest --test-dir build/x64-linux -R "unit\." --output-on-failure` is green and the new test file contributes at least 3 assertions naming the three new enumerators.
    - **Locally provable** — `ctest --test-dir build/x64-linux -R "integration\." --output-on-failure` is green: the existing `json_schema` suite still validates a real report against the edited schema.
    - **CI-only** — the `build (x64-windows-static-md)` job compiles `src/report/json.cpp` and `src/report/junit.cpp` with `/W4 /WX` and emits no C4062/C4061; observable as that matrix leg reaching its `Test` step.
  </acceptance_criteria>
  <done>Three new `SkipReason` values exist, all four consuming sites carry them, no `default:` arm was introduced, and the full local suite plus the JSON-schema integration suite are green.</done>
  <reversibility rating="costly">Enum spellings appear in committed `--json` output and in a shipped, versioned schema; renaming one after release is a schema-compatibility break, not a refactor. Not gated by a checkpoint because the three spellings are not discretionary: `skipped:partial_scan` is named verbatim by 03-CONTEXT.md D-02 and `skipped:insufficient_data` verbatim by doc 02 section 5.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Measurement.estimated and Finding.evidence with a single propagation seam</name>
  <files>src/core/model.h, src/core/snapshot.cpp, src/compare/engine.cpp, src/report/json.cpp, src/report/junit.cpp, src/report/markdown.cpp, tests/unit/test_measurement_provenance.cpp</files>
  <read_first>
    - `src/core/model.h` lines 68-90 (`Measurement`) and lines 118-135 (`Finding`) — the two structs being extended.
    - `src/core/snapshot.cpp` lines 214-266 — the measurement read loop, including the existing `evidence` read at line 263 and the strict per-field type checks that reject a poisoned snapshot as `input_unsupported`.
    - `src/core/snapshot.cpp` lines 297-325 — the measurement write loop, including the ordered key emission (`id`, `scope`, `value`, `evidence`) that determines canonical output order.
    - `src/compare/engine.cpp` lines 190-215 — where `compare_fingerprints` builds unpaired/skipped findings and where a comparator's returned `Finding` is collected; this is the ONE seam evidence propagation belongs in.
    - `src/report/json.cpp` lines 130-160 — the `delta` and `evidence` unconditional-`nullptr` block and its comment explaining why (Broken Window #1).
    - `.planning/WINDOWS.md` — window #1's exact wording; this task is what closes it.
    - `tests/integration/test_type_poisoned_snapshot.cpp` — the untrusted-snapshot rejection convention any new snapshot field must join.
  </read_first>
  <behavior>
    - Test 1: a `Measurement` with `estimated = true` written by `write_snapshot` and read back by `read_snapshot` still has `estimated == true`; one with the field absent reads back `false`.
    - Test 2: a snapshot whose `estimated` key holds a string (not a boolean) is rejected as `ErrorKind::input_unsupported`, naming the check id — never coerced.
    - Test 3: a comparison of two measurements each carrying non-empty `evidence` produces a `Finding` whose `evidence` object holds both sides under distinct keys, and `render_json` emits it as an object rather than `null`.
    - Test 4: a comparison where neither measurement carries evidence still emits `"evidence": null`, so every Phase-2 golden is unchanged.
  </behavior>
  <action>
    In `src/core/model.h`: add `bool estimated = false;` to `Measurement` (D-03 — the marker
    must be data the comparison layer reads, not evidence prose), and add
    `nlohmann::ordered_json evidence;` to `Finding`, defaulted null. Document on `estimated`
    that it means "this value was derived from an estimate (e.g. a TS mux-rate estimate), not
    measured directly" and that `compare_tol` widens for it.

    In `src/core/snapshot.cpp`: emit `estimated` in the measurement object only when true
    (keeping every existing golden byte-identical), positioned after `value` and before
    `evidence` so the canonical key order stays fixed; on read, accept an absent key as
    `false` and reject a present-but-non-boolean key with
    `Error{ErrorKind::input_unsupported, "snapshot measurement 'estimated' is not a boolean: " + id}`,
    matching the surrounding rejection shape exactly.

    In `src/compare/engine.cpp`: after a comparator returns a `Finding`, populate
    `finding.evidence` from the paired measurements — an ordered object with `baseline` and
    `candidate` members, each present only when that side's measurement evidence is a non-null
    object; leave `finding.evidence` null when neither side has any. Do this at this ONE site,
    not in the seven comparators, so no comparator needs to change and no future comparator can
    forget.

    In `src/report/json.cpp`: replace the unconditional `j["evidence"] = nullptr;` with the
    finding's own evidence, falling back to `nullptr` when it is null. Leave `j["delta"]`
    exactly as it is — that half of window #1 stays open deliberately and its comment must be
    narrowed to say so rather than deleted. Route the evidence object through the same
    `serialize_document` canonical writer path the surrounding code uses so float formatting
    cannot diverge (D-08).

    In `src/report/junit.cpp` and `src/report/markdown.cpp`: render a finding's evidence only
    where each format already has a slot for detail text; if adding one would change an
    existing golden's structure, do not add it in this task — record that in the summary
    instead. The JSON report is the required carrier here.

    Extend `tests/unit/test_measurement_provenance.cpp` with the four behaviors above.
  </action>
  <verify>
    <automated>ctest --test-dir build/x64-linux --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - **Locally provable** — `grep -n 'bool estimated' src/core/model.h` matches exactly once inside `struct Measurement`, and `grep -n 'ordered_json evidence' src/core/model.h` matches inside both `Measurement` and `Finding`.
    - **Locally provable** — for every `src/compare/*.cpp` except `engine.cpp`, `grep -vE '^\s*(//|\*|/\*)' FILE | grep -c 'finding.evidence'` reports `0` (comment lines stripped first, so a comment naming the seam cannot invalidate the gate) — proving the single-seam rule held.
    - **Locally provable** — `ctest --test-dir build/x64-linux --output-on-failure` is green with every existing golden (`json_schema_basic`, `junit_basic`, `markdown_basic`, `tty_basic`, `inspect_basic`, `dir_worst_n`) unchanged; `git status --porcelain tests/golden/` reports no modifications.
    - **Locally provable** — a snapshot fixture with `"estimated": "yes"` makes `mediadiff inspect` exit `65` and print a message containing `estimated`.
    - **Locally provable** — running `mediadiff compare` twice on the same inputs still produces byte-identical `--json` stdout (the existing determinism test passes).
  </acceptance_criteria>
  <done>`Measurement.estimated` round-trips through a snapshot, `Finding.evidence` is populated at exactly one seam and rendered as JSON, and no committed golden changed.</done>
  <reversibility rating="costly">`estimated` becomes part of the on-disk `*.snap.json` contract and `evidence` part of the report contract; removing either later invalidates snapshots users have committed.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add checked_div to rational.h and widen tolerance for estimated measurements (D-03)</name>
  <files>src/core/rational.h, src/compare/tol.cpp, tests/unit/test_rational.cpp, tests/unit/test_tolerance.cpp</files>
  <read_first>
    - `src/core/rational.h` lines 32-101 — `detail::checked_mul` (with its MSVC `_mul128` branch), `checked_sub`, `checked_add`, `checked_negate`, and the per-function doc-comment convention: each names the real caller that needed it and why the naive form is unsafe.
    - `src/core/tolerance.h` — the `Tolerance` struct: `unit`, `num`, `den`, `is_relative`, `optional<warn_num>`, and the note that `warn_num` shares `den` with the fail threshold.
    - `src/compare/tol.cpp` lines 60-226 — where the tolerance is resolved, the `is_relative` branch's exact cross-multiplication formula, the two-threshold `within_warn`/`within_fail` logic, and the `overflow_finding` helper that turns an overflow into an honest failure rather than a fabricated verdict.
    - `.planning/phases/03-probe-layer-container-size/03-CONTEXT.md` — D-03 in full, including "the estimate's nature must reach the comparison layer, not just the evidence string".
    - `tests/unit/test_tolerance.cpp` — the existing tolerance-parsing test conventions to extend.
  </read_first>
  <behavior>
    - Test 1: `checked_div(10, 3, &out)` returns true with `out == 3`; `checked_div(x, 0, &out)` returns false for any `x`; `checked_div(INT64_MIN, -1, &out)` returns false (the one true overflow case) and does not execute the division.
    - Test 2: a `tol` check comparing two measurements with `estimated == false` on both sides uses the declared tolerance unchanged — the existing tolerance tests keep passing byte-for-byte.
    - Test 3: the same pair with `baseline.estimated == true` passes at a delta that would fail under the declared tolerance but sits inside 3x it, and the finding's message contains the substring `estimated`.
    - Test 4: a delta beyond 3x the declared tolerance still fails even when both sides are estimated — widening is bounded, not a bypass.
    - Test 5: when the tolerance magnitude is large enough that the 3x multiply would overflow int64, the comparison returns the existing overflow finding rather than a silently wrapped threshold.
  </behavior>
  <action>
    Add `inline bool checked_div(std::int64_t a, std::int64_t b, std::int64_t* out)` to
    `mediadiff::detail` in `src/core/rational.h`, immediately after `checked_negate`, following
    the same two-branch portable style as `checked_sub`/`checked_add` (no `#if` needed —
    division overflow has an exact portable test). Return false when `b == 0` and when
    `a == INT64_MIN && b == -1`; otherwise set `*out = a / b` and return true. Give it the same
    doc-comment shape as its four siblings: name SIZE-01's window-boundary computation as the
    caller that needed it and state why a bare `/` is UB for those two inputs (RESEARCH.md
    Open Question 2 — the recommendation to add it to `rational.h` rather than inline a
    one-off in `size.cpp` is adopted here).

    In `src/compare/tol.cpp`: after the tolerance is resolved and before the `is_relative`
    branch, if `baseline.estimated || candidate.estimated` (D-03), multiply the effective
    threshold magnitudes by the fixed integer factor 3 — `tolerance.num` and, when present,
    `tolerance.warn_num` — using `detail::checked_mul`, and route a failed multiply through the
    existing `overflow_finding` helper rather than falling back to the unwidened value. Define
    the factor as a single named constant (`kEstimatedToleranceFactor`) with a comment
    recording the reasoning: 3x on `container.ts.pcr_interval`'s 100 ms default yields 300 ms,
    still well under `container.ts.psi_interval`'s own 500 ms bound, so a real PCR spacing
    regression still fires while mux-rate estimation noise does not (false positives are P0).
    Append ` (estimated measurement, tolerance widened ` + factor + `x)` to the finding's
    message on every branch where the widened threshold was used, so a reader sees why the
    comparison was lenient. Never convert to double at any point.

    Extend `tests/unit/test_rational.cpp` with the `checked_div` behaviors and
    `tests/unit/test_tolerance.cpp` (or `test_compare_semantics.cpp`, whichever already builds
    a `Measurement` pair directly) with the widening behaviors.
  </action>
  <verify>
    <automated>ctest --test-dir build/x64-linux -R "unit\." --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - **Locally provable** — `grep -n 'checked_div' src/core/rational.h` matches, and the function body contains both the `b == 0` and the `INT64_MIN`/`-1` guard before any `/` operator.
    - **Locally provable** — `grep -n 'kEstimatedToleranceFactor' src/compare/tol.cpp` matches exactly once as a definition, and `grep -vE '^\s*(//|\*|/\*)' src/compare/tol.cpp | grep -c 'static_cast<double>\|(double)'` reports `0`.
    - **Locally provable** — a unit test named for D-03 asserts a pass at delta between 1x and 3x the tolerance when `estimated` is set on one side, and a fail beyond 3x with both sides estimated.
    - **Locally provable** — `ctest --test-dir build/x64-linux -R "unit\." --output-on-failure` green, including every pre-existing tolerance test unmodified (proving non-estimated comparisons are untouched).
    - **CI-only** — `checked_div` compiles under MSVC `/W4 /WX` with no C4723 (potential divide by zero) warning; observable as the `build (x64-windows-static-md)` leg reaching `Test`.
  </acceptance_criteria>
  <done>`detail::checked_div` exists with both overflow guards, `compare_tol` widens exactly 3x for estimated measurements via checked integer arithmetic, and the widening is visible in the finding message.</done>
  <reversibility rating="costly">D-03 is rated costly in CONTEXT.md: the widening factor becomes an observable comparison behavior users tune against; changing it later re-baselines every TS interval comparison in the field.</reversibility>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: Approve the Phase-3 check-id roster (ids are forever)</name>
  <files>.planning/phases/03-probe-layer-container-size/03-CHECK-ROSTER.md</files>
  <decision>Approve the full 27-id Phase-3 check roster before any plan registers a check into `src/core/checks.def`.</decision>
  <context>
CLAUDE.md states "Check IDs are forever: additions fine, renames only via alias + deprecation."
Phase 2 set the precedent for this exact gate — `src/core/checks.def`'s header records "the seed
set approved in Task 1 of 02-01-PLAN.md's checkpoint". Plans 03-02, 03-04, 03-05, 03-06, 03-08
and 03-09 each register a family from this roster; approving it once here means no later plan
re-opens an id spelling.

Three ids are additions beyond doc 02's literal table-row count, forced by the registry's
one-semantic-per-id model. They are what this checkpoint most needs a decision on:

  - `container.mp4.fragment_duration` — doc 02 section 3's `container.mp4.fragmentation` row
    declares TWO semantics in one row ("mode `exact` · fail; stats `±tol` (±20 %) · warn").
    A `CheckDef` carries one `semantic`. Split: `container.mp4.fragmentation` keeps the
    `exact`/fail mode, `container.mp4.fragment_duration` carries the ±20% median-duration
    tolerance at warn.
  - `container.ts.pmt_version_churn` — doc 02 section 5's `container.ts.psi_interval` row
    likewise carries a second semantic ("versions `exact` · warn") alongside its ±ms interval.
  - `container.ts.cc_discontinuities` — doc 02 section 5's `container.ts.cc_errors` row states
    flagged (`discontinuity_indicator`) resets are "reported separately as `info`"; a separate
    status on the same id is not representable, so it becomes its own id.

Two further declarations warrant an explicit decision rather than silent adoption:

  - `container.mkv.codec_delay` baseline severity is proposed as `fail`, not `warn`. Doc 02
    section 4 says "fail for Opus tracks, warn otherwise", but severity in this architecture is
    resolved from the registry plus policy, never per-measurement — per-codec severity is not
    representable. `fail` is proposed because for non-Opus tracks `CodecDelay` is normally
    absent/zero on both sides and the check never fires at all; a difference there is itself
    suspicious.
  - `container.mp4.edit_list` uses `[check.profile_severity]` with `strict_bitexact = "fail"`
    and `remux = "fail"` over a `warn` baseline, matching doc 02 section 3's "fail in
    `remux`/`strict`, warn in encoders".

Full roster (27 ids), each `group` / `semantic` / `unit` / `value_kind` / `severity` / `tolerance`:

TOPOLOGY (plan 03-02 registers the first, 03-04 the rest)
  container.format              container exact    none    string      fail   —
  container.track_count         container exact    none    histogram   fail   —
  container.track_types         container exact    none    string      fail   —
  container.track_order         container exact    none    string      warn   —
  container.chapters            container set      none    string_set  info   —

METADATA (plan 03-04)
  meta.tags                     meta      set      none    string_set  warn   —
  meta.tags.language            meta      exact    none    string      warn   —

MP4/MOV (plan 03-05)
  container.mp4.faststart       container exact    none    string      fail   —
  container.mp4.brands          container set      none    string_set  warn   —
  container.mp4.fragmentation   container exact    none    string      fail   —
  container.mp4.fragment_duration container tol    percent rational    warn   "20%"
  container.mp4.edit_list       container exact    none    string      warn   — (+profile_severity)
  container.mp4.timescale       container exact    none    int64       warn   —

MATROSKA/WEBM (plan 03-06)
  container.mkv.cues_placement  container exact    none    string      warn   —
  container.mkv.codec_delay     container tol      samples int64       fail   "1samples"
  container.mkv.timestamp_scale container exact    none    int64       warn   —
  container.mkv.duration_element container presence none   string      info   —

MPEG-TS (plan 03-08)
  container.ts.cc_errors        container exact    count   int64       fail   —
  container.ts.cc_discontinuities container exact  count   int64       info   —
  container.ts.pcr_interval     container tol      ms      rational    fail   "100ms"
  container.ts.psi_interval     container tol      ms      rational    warn   "500ms"
  container.ts.pmt_version_churn container exact   count   int64       warn   —
  container.ts.null_ratio       container tol      percent rational    info   "5%"

SIZE (plan 03-09)
  size.file                     size      tol      percent int64       fail   "3%,8%" (+profile_tolerance strict_bitexact/remux "0.5%")
  size.stream_bitrate           size      tol      percent rational    fail   "3%,10%"
  size.peak_bitrate             size      tol      percent rational    fail   "5%,15%"
  size.overhead                 size      tol      percent rational    info   "5%"

Two scope decisions are folded in and also need confirming:
  - `size.peak_bitrate` is PER-STREAM, not a combined muxed-container bitrate (RESEARCH.md
    Open Question 1 — doc 06 section 4 leaves it ambiguous; per-stream matches
    `size.stream_bitrate`'s explicit framing and avoids a cross-timebase merge the doc
    describes no algorithm for).
  - `container.ts.*` program-scoped measurements use `Scope{Kind::program, index}` where
    `index` carries the PSI `program_number` value read from `AVProgram::program_num` — NOT the
    `AVFormatContext::programs[]` array position. `compare_fingerprints` pairs by
    `(check_index, scope)`, so array-position indexing would mis-pair two files whose programs
    are declared in different order (CONT-08).
  </context>
  <options>
    <option id="approve-as-proposed">
      <name>Approve the roster exactly as listed</name>
      <pros>Unblocks every downstream plan; the three additions are honest consequences of the one-semantic-per-id model and doc 02's own two-semantic rows; ids are documented as additions, which CLAUDE.md explicitly permits.</pros>
      <cons>Ships 27 permanent ids, three of which have no literal counterpart in doc 02's tables.</cons>
    </option>
    <option id="approve-with-edits">
      <name>Approve with named spelling or attribute edits</name>
      <pros>Any id spelling, severity, tolerance default or scope decision can be corrected now, at zero cost, instead of via an alias plus deprecation later.</pros>
      <cons>Requires the reviewer to state each edit precisely; downstream plans read the roster file, so an unrecorded edit silently reverts.</cons>
    </option>
    <option id="collapse-additions">
      <name>Reject the three additions and fold each back into its doc-02 parent id</name>
      <pros>Exactly matches doc 02's table row count; no id exists that a reader of the design doc cannot find.</pros>
      <cons>Each folded id loses a semantic: fragment-duration drift becomes uncheckable, PMT version churn becomes evidence-only prose, and flagged CC resets can no longer be reported at `info` separately from unflagged errors at `fail` — which is precisely the distinction PROBE-07 exists to preserve.</cons>
    </option>
  </options>
  <action>
    Write the approved roster verbatim to
    `.planning/phases/03-probe-layer-container-size/03-CHECK-ROSTER.md` as a markdown table with
    the six declared columns plus a `plan` column naming which plan registers each id, and a
    short header recording the approval decision and the date. Downstream plans read this file
    as the single source of truth for id spellings — a plan that registers an id absent from
    this file is a defect.
  </action>
  <acceptance_criteria>
    - **Locally provable** — `.planning/phases/03-probe-layer-container-size/03-CHECK-ROSTER.md` exists and `grep -c '^| container\.\|^| meta\.\|^| size\.' 03-CHECK-ROSTER.md` reports the approved id count (27 unless the reviewer edited it).
    - **Locally provable** — every id in the roster matches the generator's own grammar `[a-z0-9_]+(\.[a-z0-9_]+)*` (verifiable by `grep -oP` against the id column).
    - **Locally provable** — the file records the reviewer's chosen option and the date.
  </acceptance_criteria>
  <resume-signal>Select: approve-as-proposed, approve-with-edits (state each edit), or collapse-additions</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `*.snap.json` on disk -> `read_snapshot` | A committed snapshot is attacker-influenceable in a pull request; this plan adds one new field (`estimated`) that crosses it. |
| `Measurement.evidence` -> `Finding.evidence` -> `--json` stdout | Evidence originates in analyzers reading media files; this plan opens the channel that lets file-derived JSON reach report output for the first time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-01 | Tampering | `src/core/snapshot.cpp` measurement read loop | medium | mitigate | A present-but-non-boolean `estimated` key is rejected as `ErrorKind::input_unsupported` naming the check id, never coerced — same shape as the existing `scope`/`value` rejections. Task 2 behavior test 2. |
| T-3-02 | Tampering | `src/compare/tol.cpp` estimated widening | high | mitigate | The widening factor is a fixed compile-time constant multiplied through `detail::checked_mul`; a snapshot cannot supply the factor, and an overflowing multiply routes to `overflow_finding` rather than a wrapped (arbitrarily large) threshold. A wrapped threshold would silently convert every fail into a pass — the exact false-negative class this project treats as P0. |
| T-3-03 | Information Disclosure | `Finding.evidence` -> `render_json` | medium | mitigate | Evidence is serialized through `serialize_document`, the same canonical writer that already escapes JSON string content; the terminal-rendering half of this exposure (T-2-33) is out of scope here and is closed in plan 03-11 before any evidence reaches `tty_render.cpp`. |
| T-3-04 | Denial of Service | `detail::checked_div` | medium | mitigate | Both UB inputs (`b == 0`, `INT64_MIN / -1`) are rejected before the division executes; on x86-64 either would raise SIGFPE and terminate the process, which for a CI gate is a crash, not a verdict. |
| T-3-SC | Tampering | npm/pip/cargo/vcpkg installs | low | accept | This plan installs no package. 03-RESEARCH.md's Package Legitimacy Audit records that Phase 3 introduces no new external dependency; the FFmpeg 8.1 pin is unchanged from Phase 1. |
</threat_model>

<verification>
- **Locally provable:** `ctest --test-dir build/x64-linux --output-on-failure` fully green.
- **Locally provable:** `git status --porcelain tests/golden/` is empty — no committed golden changed.
- **Locally provable:** `grep -v '^\s*//' src/report/json.cpp src/report/junit.cpp | grep -c 'default:'` reports `0`.
- **Locally provable:** `bash scripts/lint_eng16.sh && bash scripts/lint_check_id_strings.sh && bash scripts/lint_dead_code_after_fail.sh && bash scripts/lint_fixture_case_collisions.sh` all pass.
- **CI-only:** all three matrix legs (`x64-linux`, `x64-osx`/`arm64-osx`, `x64-windows-static-md`) reach and pass their `Test` step — this is where the MSVC `/W4 /WX` exhaustive-switch behavior and the AppleClang build are actually observed.
</verification>

<success_criteria>
- `SkipReason` has 14 enumerators; all four consuming sites carry all 14.
- `Measurement.estimated` survives a snapshot write/read round trip and is read by `compare_tol`.
- `Finding.evidence` is populated at exactly one seam (`src/compare/engine.cpp`) and rendered in `--json`.
- `detail::checked_div` exists with both overflow guards and is unit-tested.
- `03-CHECK-ROSTER.md` exists with the reviewer-approved id list.
- No existing golden file changed; determinism (TRUST-05) intact.
</success_criteria>

## Artifacts this phase produces (this plan's share)

**New `SkipReason` enumerators:** `partial_scan`, `insufficient_data`, `no_timing_data`

**New struct fields:** `mediadiff::Measurement::estimated` (bool), `mediadiff::Finding::evidence` (`nlohmann::ordered_json`)

**New functions:** `mediadiff::detail::checked_div(int64_t, int64_t, int64_t*) -> bool`

**New constants:** `kEstimatedToleranceFactor` (in `src/compare/tol.cpp`)

**New files:** `tests/unit/test_measurement_provenance.cpp`, `.planning/phases/03-probe-layer-container-size/03-CHECK-ROSTER.md`

**New CLI flags:** none

**New check ids:** none registered by this plan (the roster is approved here, registered downstream)

**New fixtures:** none

<output>
Create `.planning/phases/03-probe-layer-container-size/03-01-SUMMARY.md` when done.
Record: the exact `SkipReason` enumerator count after the edit, whether any golden required
refreshing (expected: none), the approved roster option, and whether `Finding.evidence` was
also wired into markdown/junit or deferred.
</output>
</content>
</invoke>
