---
phase: 03-probe-layer-container-size
plan: 04
type: execute
wave: 4
depends_on: ["03-02"]
files_modified:
  - src/analyzers/container/topology.cpp
  - src/analyzers/container/meta.cpp
  - src/analyzers/container/analyzers.h
  - src/probe/orchestrator.cpp
  - src/core/checks.def
  - docs/checks/container.track_count.md
  - docs/checks/container.track_types.md
  - docs/checks/container.track_order.md
  - docs/checks/container.chapters.md
  - docs/checks/meta.tags.md
  - docs/checks/meta.tags.language.md
  - scripts/gen_corpus.sh
  - CMakeLists.txt
  - tests/unit/test_topology_analyzer.cpp
  - tests/unit/test_meta_tags.cpp
  - tests/unit/CMakeLists.txt
  - tests/integration/test_container_topology.cpp
  - tests/integration/CMakeLists.txt
autonomous: true
requirements: [CONT-01, CONT-03, CONT-04, CONT-09]

estimate:
  tokens: 42000
  raw_tokens: 84000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "`container.track_count`, `container.track_types`, `container.track_order` and `container.chapters` each produce a measurement for every supported container, with `container.chapters` auto-skipping as `not_applicable_container` on MPEG-TS (doc 02 section 2)."
    - "Subtitle track presence and caption/timecode data track presence are each demonstrated by a dedicated fixture pair, not inferred from a generic stream count (CONT-09)."
    - "A dropped `tmcd` timecode track or a dropped caption data track is named explicitly in the `container.track_types` finding message, not merely reflected in a count."
    - "`meta.tags` compares as a set with the four built-in volatile keys (`creation_time`, `encoder`, `handler_name`, `encoding_tool`) excluded from the compared value, while their differing values are still carried in evidence (CONT-03)."
    - "`meta.tags.language` treats `und` and an absent language tag as equal (CONT-04) — a fixture pair differing only that way compares clean."
    - "Reordering two streams with identical membership renders as a `container.track_order` difference while `container.track_count` and `container.track_types` still pass (doc 02: 'a move, not add+remove')."
  artifacts:
    - src/analyzers/container/topology.cpp
    - src/analyzers/container/meta.cpp
    - docs/checks/container.track_count.md
    - docs/checks/meta.tags.md
    - tests/integration/test_container_topology.cpp
  key_links:
    - "`DemuxSession` per-stream `codecpar->codec_type` -> `Scope::Kind` mapping -> per-type counts: the mapping must cover video/audio/subtitle/data/attachment or a track type silently vanishes from the count."
    - "volatile key list -> the value `meta.tags` compares -> evidence: the ignored keys must be removed from the compared `StringSet` AND retained in evidence, or CONT-03's `-v` requirement has nothing to render."
    - "`und` normalization -> `meta.tags.language`'s emitted `Value`: normalization happens at measurement construction, not in the comparator, so a stored snapshot already carries the normalized form."
  prohibitions:
    - "Never let a subtitle or caption track's presence be covered only by a generic stream-count assertion — CONT-09 exists because that inference is what a prior phase got wrong."
    - "Never drop an ignored volatile tag from evidence; ignoring a key for comparison is not the same as hiding it."
    - "Never weaken a check or widen its tolerance to make a fixture compare clean."
    - "Never register a check id that is not on the approved 03-CHECK-ROSTER.md."
---

<objective>
Expand the tracer's single-check topology analyzer into the full container-agnostic family and
add the metadata family — doc 02 section 7 step 1 completed, and doc 02 section 2's table
implemented end to end.

This is horizontal expansion from the proven slice: same analyzer file, same seam, same
`Measurement` emit shape the tracer established. No architectural change.

Purpose: CONT-01's five topology checks, CONT-03's volatile-aware tag set, CONT-04's
`und`-equals-absent normalization, and CONT-09's explicit subtitle and caption-track coverage —
the last of which is a requirement precisely because "it falls out of the stream count" is the
inference that failed before.
Output: six new registered checks, their docs, their fixture recipes, and both a triggering and
a clean pair for each.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-probe-layer-container-size/03-CONTEXT.md
@.planning/phases/03-probe-layer-container-size/03-CHECK-ROSTER.md
@.planning/phases/03-probe-layer-container-size/03-02-SUMMARY.md
@claude_docs/02-container-analysis.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The four remaining container-agnostic topology checks (CONT-01, CONT-09)</name>
  <files>src/analyzers/container/topology.cpp, src/core/checks.def, docs/checks/container.track_count.md, docs/checks/container.track_types.md, docs/checks/container.track_order.md, docs/checks/container.chapters.md, scripts/gen_corpus.sh, tests/unit/test_topology_analyzer.cpp, tests/unit/CMakeLists.txt, tests/integration/test_container_topology.cpp, tests/integration/CMakeLists.txt</files>
  <read_first>
    - `claude_docs/02-container-analysis.md` section 2, all seven rows — extraction, semantic, default severity and the "Edge cases &amp; evidence" column for each. In particular: `container.track_count`'s "evidence lists per-type counts"; `container.track_types`' "timecode (`tmcd`) and caption data tracks called out by name in the message — their loss is the headline"; `container.track_order`'s "reorder with identical membership renders as a move, not add+remove"; `container.chapters`' "auto-`skipped:not_applicable` on TS".
    - `.planning/phases/03-probe-layer-container-size/03-CHECK-ROSTER.md` — the approved declared attributes for all four ids. Register exactly these; do not re-derive them.
    - `src/analyzers/container/topology.cpp` as plan 03-02 wrote it — the existing single-measurement emit, the `CheckId` enum reference style (never a bare id string, per 02-CONTEXT.md D-03 and `scripts/lint_check_id_strings.sh`).
    - `vcpkg/packages/ffmpeg_x64-linux/include/libavcodec/codec_par.h` — `AVCodecParameters::codec_type` (`AVMEDIA_TYPE_VIDEO`/`AUDIO`/`SUBTITLE`/`DATA`/`ATTACHMENT`) and `codec_id`.
    - `vcpkg/packages/ffmpeg_x64-linux/include/libavformat/avformat.h` — `AVChapter { int64_t id; AVRational time_base; int64_t start, end; AVDictionary *metadata; }` (one shared `time_base` for both `start` and `end`), `AVFormatContext::nb_chapters`/`chapters`.
    - `src/core/model.h` — `Scope::Kind` (`global`, `video`, `audio`, `subtitle`, `data`, `program`) — note there is NO `attachment` kind; an attachment stream's per-type count belongs in the `global` measurement's value/evidence, not in a scope that does not exist.
    - `src/core/value.h` — `Histogram { std::vector<std::pair<std::string, std::int64_t>> bins; }` (ordered pairs, structural `operator==`) and `StringSet`; `src/core/serializer.h` for how each serializes canonically.
    - `src/core/checks.def` header comment (every key documented) and the `meta.tool_version` entry as the literal `[[check]]` template.
    - `docs/checks/container.format.md` as plan 03-02 wrote it — the required heading shape to copy for four more docs.
    - `src/core/rational.h` — `Rational`/`Ticks`, for converting `AVChapter`'s `start`/`end` at the probe edge (02-CONTEXT.md D-07).
    - `scripts/gen_corpus.sh` — the recipe convention and the existing tracer recipes to append beside.
  </read_first>
  <behavior>
    - Test 1: `container.track_count` on a 1-video/1-audio/1-subtitle fixture emits a `Histogram` with bins in a fixed order (`video`, `audio`, `subtitle`, `data`, `attachment`) and counts `1,1,1,0,0`; a bin with a zero count is present, not omitted, so two files differing only by a dropped subtitle track compare as a value difference rather than a shape difference.
    - Test 2: `container.track_types` emits the ordered multiset as a single canonical string preserving both order and duplicates (e.g. `video,audio,audio,subtitle`); two files with the same types in a different order produce DIFFERENT values.
    - Test 3: `container.track_order` emits an ordered `(type, codec_id)` signature; two files with the same set of streams in a different order produce a `container.track_order` fail while `container.track_count` and `container.track_types` (as a multiset) still pass — the "move, not add+remove" behavior.
    - Test 4: a fixture that loses a `tmcd` timecode track produces a `container.track_types` finding whose message names `tmcd`; a fixture that loses a caption data track names it too (CONT-09).
    - Test 5: `container.chapters` on an MKV with two chapters emits a `StringSet` of two canonicalized chapter entries, each carrying start, end and title; chapter times are rational, never floating milliseconds.
    - Test 6: `container.chapters` on an MPEG-TS input emits no measurement at all, and the resulting finding is `skipped` with `SkipReason::not_applicable_container`.
    - Test 7: a subtitle-bearing fixture and its subtitle-stripped sibling produce a `container.track_count` fail and a `container.track_types` fail; the identical-subtitle pair produces pass on both (CONT-09's explicit pair requirement).
  </behavior>
  <action>
    Extend `src/analyzers/container/topology.cpp` to emit four more measurements alongside
    `container.format`, all at `Scope{Kind::global, 0}`, all referencing their check through
    the generated `CheckId` enum:

    `container.track_count` — a `Histogram` with five bins in a FIXED order (`video`, `audio`,
    `subtitle`, `data`, `attachment`), each present even at zero. Fixed order and present zeros
    are what make the `exact` comparison a value difference rather than a shape difference, and
    what keeps `--json` byte-identical across runs. Put the same per-type counts in evidence per
    doc 02's "evidence lists per-type counts".

    `container.track_types` — the ordered multiset of media types as one canonical string,
    comma-joined in stream index order, preserving duplicates. When building the finding's
    evidence, additionally record which streams are timecode (`tmcd`) or caption data tracks,
    identified from `codecpar->codec_type == AVMEDIA_TYPE_DATA` together with `codec_id` /
    the stream's `handler_name` metadata; doc 02 requires their loss to be "the headline", so
    name them explicitly in the evidence the message is built from rather than leaving a reader
    to infer it from a type letter.

    `container.track_order` — an ordered `(media_type, codec_id_name)` signature string in
    stream index order. Use libav's stable codec NAME (`avcodec_get_name`), never the numeric
    `AVCodecID` value, so the recorded signature does not shift when FFmpeg renumbers an enum.

    `container.chapters` — a `StringSet` where each element canonicalizes one chapter as its
    start ticks, end ticks, shared timebase and title. Convert `AVChapter::start`/`end` through
    `mediadiff::Ticks` at the probe edge; floating milliseconds appear only in rendered output,
    never in a compared value. Emit NO measurement when the container family is MPEG-TS — the
    engine's existing unpaired-measurement path produces the
    `skipped:not_applicable_container` finding doc 02 requires; verify against
    `src/compare/engine.cpp` that this is in fact the behavior for a check with no measurement
    on either side, and if it is not, emit the skip explicitly rather than assuming.

    Register all four in `src/core/checks.def` with the roster's declared attributes and write
    the four `docs/checks/<id>.md` files with all three required level-2 headings and all three
    `### Accept`/`### Tune`/`### Silence` sub-headings — `tools/gen_registry.py` fails the build
    otherwise.

    Append fixture recipes to `scripts/gen_corpus.sh` (all `-flags +bitexact -fflags +bitexact -y`):
    a three-stream MP4 (video + audio + subtitle via `-f lavfi` sources plus a small subtitle
    stream), the same file with the subtitle stream dropped, a stream-reordered variant with
    identical membership, an MP4 carrying a `tmcd` timecode track (`-timecode 00:00:00:00`) and
    its sibling without one, an MKV with two chapters and its sibling with none, and a plain
    MPEG-TS for the not-applicable case.
  </action>
  <verify>
    <automated>bash scripts/gen_corpus.sh &amp;&amp; ctest --test-dir build/x64-linux --output-on-failure</automated>
  </verify>
  <precondition>A system `ffmpeg` CLI at version 6.1 or newer is on `PATH` (or `MEDIADIFF_FFMPEG` points at one); every behavior below reads a fixture `scripts/gen_corpus.sh` synthesizes.</precondition>
  <acceptance_criteria>
    - **Locally provable** — `grep -c '^id = "container\.' src/core/checks.def` reports `5` (format plus the four added here).
    - **Locally provable** — `ls docs/checks/container.*.md | wc -l` reports `5`, and `cmake --build build/x64-linux --target <registry generation>` succeeds (the generator enforces every heading).
    - **Locally provable** — `./build/x64-linux/mediadiff compare tests/fixtures/topo_subs.mp4 tests/fixtures/topo_nosubs.mp4 --json` exits non-zero with `container.track_count` and `container.track_types` both `"status": "fail"`.
    - **Locally provable** — `./build/x64-linux/mediadiff compare tests/fixtures/topo_subs.mp4 tests/fixtures/topo_subs_copy.mp4 --json` exits `0` with both of those checks `"status": "pass"` (the clean half of the CONT-09 pair).
    - **Locally provable** — `./build/x64-linux/mediadiff compare tests/fixtures/topo_order_a.mp4 tests/fixtures/topo_order_b.mp4 --json` shows `container.track_order` `"fail"` while `container.track_count` shows `"pass"`.
    - **Locally provable** — the `tmcd` pair's `--json` output contains the literal substring `tmcd` inside the `container.track_types` finding's message or evidence.
    - **Locally provable** — `./build/x64-linux/mediadiff inspect tests/fixtures/topo_ts.ts --json` shows `container.chapters` with `"status": "skipped"` and `"skip_reason": "not_applicable_container"`.
    - **Locally provable** — `bash scripts/lint_check_id_strings.sh` passes (no bare check-id string literal in analyzer code).
    - **Locally provable** — two consecutive `mediadiff compare` runs on the same pair produce byte-identical `--json` (fixed histogram bin order holds).
  </acceptance_criteria>
  <done>Five container-agnostic topology checks are registered, documented, fixture-covered with both a triggering and a clean pair each, and subtitle plus caption/timecode presence is proven by its own dedicated pairs rather than inferred.</done>
  <reversibility rating="costly">The four permanent check ids were approved as a whole at 03-01's `checkpoint:decision` roster gate, so the ids-are-forever door was walked through deliberately there. What remains costly here is the canonical string encoding of `track_types` and `track_order`, which enters committed snapshots — changing an encoding later invalidates every stored baseline.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: meta.tags with the built-in volatile ignore list (CONT-03)</name>
  <files>src/analyzers/container/meta.cpp, src/analyzers/container/analyzers.h, src/probe/orchestrator.cpp, src/core/checks.def, docs/checks/meta.tags.md, scripts/gen_corpus.sh, CMakeLists.txt, tests/unit/test_meta_tags.cpp, tests/unit/CMakeLists.txt</files>
  <read_first>
    - `claude_docs/02-container-analysis.md` section 2's `meta.tags` row — "container + per-stream dictionaries", "`set` with volatile ignore (`creation_time, encoder, handler_name, encoding_tool`)", "ignored-but-differing shown under `-v`". Note the row's own parenthetical explicitly excludes `major_brand` from the volatile list because brands are scoped to `container.mp4.brands`.
    - `.planning/phases/03-probe-layer-container-size/03-CONTEXT.md` — the Claude's-Discretion note that the fixed list ships as the built-in baseline and user-extensibility via `mediadiff.toml` is a DEFERRED idea. Do not add a config key for it.
    - `.planning/phases/03-probe-layer-container-size/03-CHECK-ROSTER.md` — `meta.tags`' approved attributes.
    - `vcpkg/packages/ffmpeg_x64-linux/include/libavutil/dict.h` — `AVDictionary`, `av_dict_get` with `AV_DICT_IGNORE_SUFFIX` for iteration, `av_dict_iterate`; `AVFormatContext::metadata` (container level) and `AVStream::metadata` (per stream).
    - `src/core/value.h` — `StringSet` is `std::set<std::string>`, so ordering is the set's own and canonical output is deterministic for free.
    - `src/compare/set.cpp` — how the `set` semantic computes added/removed members and builds its message; this is what a `meta.tags` difference will render as.
    - `src/core/model.h` — `Measurement::evidence` (an `ordered_json`) and, from plan 03-01, `Finding::evidence`; the ignored-but-differing keys ride here.
    - `src/report/model.h` — `RenderOptions`' `-v` flags (`show_pass`, `show_chain`) and how `src/cli/options.h`'s `CliOptions::verbose` sets both. The `-v` rendering of ignored tags is completed in plan 03-11; this task's obligation is to put the data in evidence so that plan has something to render.
    - `tests/unit/test_volatile.cpp` — the existing volatile-handling test conventions.
  </read_first>
  <behavior>
    - Test 1: the compared `StringSet` for `meta.tags` excludes every key in the built-in volatile list, so two files differing ONLY in `creation_time` and `encoder` compare clean.
    - Test 2: those same two files' finding evidence contains both differing volatile keys with both sides' values — ignoring is not hiding.
    - Test 3: a file pair differing in a non-volatile tag (e.g. `title`) produces a `meta.tags` difference naming that key.
    - Test 4: `meta.tags` emits one measurement at `Scope{Kind::global, 0}` for the container dictionary and one per stream at the stream's own scope, so a per-stream tag change is attributed to that stream rather than to the file.
    - Test 5: a tag key or value containing a byte sequence that is not valid UTF-8 does not crash and does not corrupt the emitted JSON — the value is recorded in a form `serialize_document` can emit.
    - Test 6: the volatile list is exactly the four documented keys — a test asserts the constant's contents member by member, so silently adding a fifth becomes a test failure rather than a quiet policy change.
  </behavior>
  <action>
    Create `src/analyzers/container/meta.cpp` with a `meta.tags` analyzer declaring
    `required_passes = {Pass::demux_header}`.

    Define the volatile key list as a single named `constexpr` array
    (`kVolatileTagKeys = {"creation_time", "encoder", "handler_name", "encoding_tool"}`) with a
    comment recording that this ships fixed as the built-in baseline per CONTEXT.md's stated
    default, that user-extensibility via `mediadiff.toml` is explicitly DEFERRED, and that
    `major_brand` is deliberately NOT here because brands are scoped to
    `container.mp4.brands` (doc 02 section 2's own parenthetical). Match keys
    case-insensitively over ASCII only, pinning the comparison so a locale cannot change which
    keys are ignored.

    Emit one `meta.tags` measurement per dictionary: the container's at
    `Scope{Kind::global, 0}` and each stream's at that stream's own `Scope::Kind`/index. The
    `Value` is a `StringSet` of `key=value` entries with every volatile key REMOVED. The
    measurement's `evidence` carries an object listing every ignored key whose value differs
    between the two sides — since an analyzer sees only one file, record every ignored key's own
    value in evidence and let plan 03-01's engine-side evidence merge produce the both-sides view.

    Handle non-UTF-8 tag bytes: `AVDictionary` values are raw bytes with no encoding guarantee.
    Before constructing the `std::string` that enters the `StringSet` or the evidence, replace
    any byte sequence that is not valid UTF-8 with the Unicode replacement character, so
    `serialize_document` never has to emit an invalid JSON string and the canonical-output
    guarantee (SNAP-03, TRUST-05) holds. Do NOT strip C0 control bytes here — that is
    T-2-33's fix and belongs in one place, at the render boundary, in plan 03-11; doing it in
    two places would leave the compared value and the rendered value disagreeing.

    Register `meta.tags` in `src/core/checks.def` per the roster and write
    `docs/checks/meta.tags.md` with the required headings, whose `### Silence` section names the
    built-in volatile list explicitly so `--explain` teaches it.

    Append a fixture recipe pair to `scripts/gen_corpus.sh`: two otherwise-identical MP4s
    differing only in `-metadata creation_time=...` and `-metadata encoder=...` (the clean pair),
    and a pair differing in `-metadata title=...` (the triggering pair). Add a per-stream variant
    with `-metadata:s:a:0 title=...` for behavior 4.

    Add `tests/unit/test_meta_tags.cpp` for behaviors 1-6.
  </action>
  <verify>
    <automated>bash scripts/gen_corpus.sh &amp;&amp; ctest --test-dir build/x64-linux --output-on-failure</automated>
  </verify>
  <precondition>A system `ffmpeg` CLI at version 6.1 or newer is on `PATH` (or `MEDIADIFF_FFMPEG` points at one); the metadata fixture pairs are synthesized by `scripts/gen_corpus.sh`.</precondition>
  <acceptance_criteria>
    - **Locally provable** — `grep -n 'kVolatileTagKeys' src/analyzers/container/meta.cpp` matches exactly once as a definition, and the array holds exactly the four documented keys.
    - **Locally provable** — `grep -c 'major_brand' src/analyzers/container/meta.cpp` reports at least 1 and that occurrence is in comment text explaining the deliberate exclusion.
    - **Locally provable** — `./build/x64-linux/mediadiff compare tests/fixtures/tags_volatile_a.mp4 tests/fixtures/tags_volatile_b.mp4 --json` exits `0` with `meta.tags` `"status": "pass"` and a non-null `evidence` object naming `creation_time`.
    - **Locally provable** — `./build/x64-linux/mediadiff compare tests/fixtures/tags_title_a.mp4 tests/fixtures/tags_title_b.mp4 --json` exits non-zero with `meta.tags` differing and the message naming `title`.
    - **Locally provable** — a unit test asserts the volatile constant member by member (adding a fifth key breaks it).
    - **Locally provable** — the non-UTF-8 tag test passes and the emitted `--json` parses as valid JSON.
    - **Locally provable** — `bash scripts/lint_check_id_strings.sh` passes.
  </acceptance_criteria>
  <done>`meta.tags` compares container and per-stream dictionaries as sets with the four built-in volatile keys excluded from the value but retained in evidence, and non-UTF-8 tag bytes cannot corrupt canonical output.</done>
  <reversibility rating="costly">The volatile key list changes which differences are visible by default; adding or removing a key later re-baselines every user's clean run.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: meta.tags.language with und-equals-absent normalization (CONT-04)</name>
  <files>src/analyzers/container/meta.cpp, src/core/checks.def, docs/checks/meta.tags.language.md, scripts/gen_corpus.sh, tests/unit/test_meta_tags.cpp</files>
  <read_first>
    - `claude_docs/02-container-analysis.md` section 2's `meta.tags.language` row — "per-stream `language`", `exact` · warn, "`und` ↔ absent normalized equal (common muxer divergence, not a regression)".
    - `.planning/REQUIREMENTS.md` CONT-04's wording — "treats `und` and absent as equal, since that divergence is a muxer artifact and not a regression".
    - `.planning/phases/03-probe-layer-container-size/03-CHECK-ROSTER.md` — `meta.tags.language`'s approved attributes (`exact` / `none` / `string` / `warn`).
    - `src/analyzers/container/meta.cpp` as written in Task 2 — the per-stream dictionary iteration this reuses.
    - `src/compare/exact.cpp` lines 75-131 — how the `exact` comparator compares two `Value`s via `std::variant::operator==`; understanding this is what makes it clear the normalization must happen at MEASUREMENT construction, not in the comparator.
    - `src/core/snapshot.cpp` — the write/read round trip; a normalized value must be what lands on disk, or a stored snapshot and a fresh probe of the same file would disagree.
  </read_first>
  <behavior>
    - Test 1: a stream with `language=und` and a stream with no `language` tag at all both produce the identical `meta.tags.language` `Value`.
    - Test 2: a file pair differing only by `und` vs absent compares `pass` (not `warn`, not `skipped`).
    - Test 3: a file pair with `eng` vs `fra` compares as a difference at `warn` severity.
    - Test 4: a file pair with `eng` vs absent compares as a difference — absent normalizes to `und`, and `und` is not `eng`.
    - Test 5: the normalized value survives `write_snapshot`/`read_snapshot` unchanged, so a snapshot taken from a file with an absent tag compares clean against a fresh probe of a file carrying `und`.
    - Test 6: language matching is ASCII-case-insensitive (`ENG` equals `eng`) but does not attempt ISO-639-2/T-vs-B alias resolution — `fra` and `fre` are NOT normalized to each other, and a test pins that as deliberate.
  </behavior>
  <action>
    Add a `meta.tags.language` analyzer to `src/analyzers/container/meta.cpp`, emitting one
    measurement per stream at that stream's own `Scope`. Read `language` from the stream's
    `AVDictionary`; when the key is absent or empty, emit the literal string `und`. Lowercase
    the value over ASCII only.

    Perform this normalization at measurement construction, NOT in the comparator: the `exact`
    comparator compares two `Value`s through `std::variant::operator==` and has no per-check
    knowledge, and — more importantly — a snapshot written before normalization would carry the
    un-normalized value forever, so a stored baseline and a fresh probe of the same file would
    disagree. Record that reasoning in a comment.

    Deliberately do NOT resolve ISO-639-2/T versus /B aliases (`fra`/`fre`, `deu`/`ger`).
    Document why in `docs/checks/meta.tags.language.md`'s `### Tune` section: `und` versus
    absent is a muxer artifact with no semantic content, whereas a T-versus-B spelling change
    IS a real metadata change a downstream player may treat differently, and silently
    collapsing it would be the false-negative this project treats as worse than a false
    positive.

    Register the check per the roster and write its doc with all required headings.

    Append fixture recipes: `-metadata:s:a:0 language=und` versus no language flag (the clean
    pair), and `language=eng` versus `language=fra` (the triggering pair).

    Extend `tests/unit/test_meta_tags.cpp` with the six behaviors.
  </action>
  <verify>
    <automated>bash scripts/gen_corpus.sh &amp;&amp; ctest --test-dir build/x64-linux --output-on-failure</automated>
  </verify>
  <precondition>A system `ffmpeg` CLI at version 6.1 or newer is on `PATH` (or `MEDIADIFF_FFMPEG` points at one); the language fixture pairs are synthesized by `scripts/gen_corpus.sh`.</precondition>
  <acceptance_criteria>
    - **Locally provable** — `./build/x64-linux/mediadiff compare tests/fixtures/lang_und.mp4 tests/fixtures/lang_absent.mp4 --json` exits `0` with `meta.tags.language` `"status": "pass"`.
    - **Locally provable** — `./build/x64-linux/mediadiff compare tests/fixtures/lang_eng.mp4 tests/fixtures/lang_fra.mp4 --json` shows `meta.tags.language` differing at `warn`.
    - **Locally provable** — `./build/x64-linux/mediadiff snapshot tests/fixtures/lang_absent.mp4 -o /tmp/lang.snap.json` then `./build/x64-linux/mediadiff compare /tmp/lang.snap.json tests/fixtures/lang_und.mp4` exits `0` (behavior 5 end to end).
    - **Locally provable** — `grep -n 'fre\|ger\|639' docs/checks/meta.tags.language.md` matches, documenting the deliberate non-normalization.
    - **Locally provable** — `ctest --test-dir build/x64-linux -R "unit.*meta_tags" --output-on-failure` selects a non-zero number of tests and is green.
  </acceptance_criteria>
  <done>`meta.tags.language` normalizes absent to `und` at measurement construction so the normalized value survives a snapshot round trip, and ISO-639 T/B aliases are deliberately not collapsed.</done>
  <reversibility rating="costly">The normalization rule is baked into every stored snapshot; changing it later means previously-clean baselines start differing.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `AVDictionary` tag keys and values -> compared `Value`, evidence, and eventually terminal output | Tag content is fully attacker-controlled arbitrary bytes with no encoding guarantee; this is the first phase where file-CONTENT-derived strings (not just filenames) flow toward the report. |
| `AVChapter` titles -> `container.chapters` `StringSet` | Same channel, same exposure. |
| stream count and per-stream `codec_type` -> per-type histogram | A crafted file can declare many streams; each contributes a fixed-size entry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-15 | Tampering | tag/chapter strings -> `serialize_document` | high | mitigate | Invalid UTF-8 byte sequences are replaced with U+FFFD at measurement construction so the canonical JSON writer never receives a string it cannot emit; an unemittable string would break the byte-identical `--json` guarantee (TRUST-05) and could truncate a report mid-object. |
| T-3-16 | Information Disclosure | tag/chapter strings -> `tty_render.cpp` | high | mitigate | Not closed in this plan by design. T-2-33 (no control-byte filtering anywhere in `src/`) is closed once, at the single render boundary, in plan 03-11 — before any terminal-facing verbose rendering of these strings ships. Doing it here as well would leave the compared value and the rendered value disagreeing about what the tag says. Tracked as the explicit dependency from this plan to 03-11. |
| T-3-17 | Denial of Service | per-type histogram over `nb_streams` | low | mitigate | Five fixed bins regardless of stream count; the per-stream measurement list grows linearly with `nb_streams`, which libav itself bounds during `avformat_find_stream_info`. |
| T-3-18 | Denial of Service | an enormous single tag value | medium | mitigate | Tag values enter a `StringSet` whose total size is bounded by the dictionary libav already parsed and holds; no value is duplicated into evidence more than once. If a tag value exceeds a sane cap, truncate it with an explicit marker rather than carrying an unbounded string into every report format. |
| T-3-SC | Tampering | package installs | low | accept | No package installed; FFmpeg 8.1 pin unchanged (03-RESEARCH.md Package Legitimacy Audit: not applicable this phase). |
</threat_model>

<verification>
- **Locally provable:** `bash scripts/gen_corpus.sh` produces every fixture this plan's tests reference.
- **Locally provable:** `ctest --test-dir build/x64-linux --output-on-failure` fully green.
- **Locally provable:** six new ids registered, six new `docs/checks/*.md` present, build's registry generation succeeds.
- **Locally provable:** each of the six checks has both a triggering CLI invocation and a clean one, asserted in `tests/integration/test_container_topology.cpp` (DOC-03's per-check obligation, formally gated in plan 03-11).
- **Locally provable:** all four lint scripts pass.
- **CI-only:** all three matrix legs green — in particular the Windows leg, where the ASCII-only case folding and the UTF-8 replacement path meet MSVC's `/utf-8` source handling.
</verification>

<success_criteria>
- All five container-agnostic topology checks plus both metadata checks emit, compare and render.
- Subtitle and caption/timecode presence each have their own dedicated fixture pair (CONT-09).
- The four built-in volatile tag keys are excluded from comparison and retained in evidence (CONT-03).
- `und` and absent language compare equal, through a fresh probe and through a snapshot round trip (CONT-04).
- `container.chapters` auto-skips as `not_applicable_container` on MPEG-TS.
</success_criteria>

## Artifacts this phase produces (this plan's share)

**New check ids:** `container.track_count`, `container.track_types`, `container.track_order`, `container.chapters`, `meta.tags`, `meta.tags.language`

**New docs:** `docs/checks/container.track_count.md`, `container.track_types.md`, `container.track_order.md`, `container.chapters.md`, `meta.tags.md`, `meta.tags.language.md`

**New source files:** `src/analyzers/container/meta.cpp`

**New constants:** `kVolatileTagKeys` (in `src/analyzers/container/meta.cpp`)

**New fixtures:** `topo_subs.mp4`, `topo_subs_copy.mp4`, `topo_nosubs.mp4`, `topo_order_a.mp4`, `topo_order_b.mp4`, `topo_tmcd.mp4`, `topo_notmcd.mp4`, `topo_chapters.mkv`, `topo_nochapters.mkv`, `topo_ts.ts`, `tags_volatile_a.mp4`, `tags_volatile_b.mp4`, `tags_title_a.mp4`, `tags_title_b.mp4`, `lang_und.mp4`, `lang_absent.mp4`, `lang_eng.mp4`, `lang_fra.mp4`

**New tests:** `tests/unit/test_topology_analyzer.cpp`, `tests/unit/test_meta_tags.cpp`, `tests/integration/test_container_topology.cpp`

**New CLI flags:** none

<output>
Create `.planning/phases/03-probe-layer-container-size/03-04-SUMMARY.md` when done.
Record: the exact canonical encoding chosen for `container.track_types` and
`container.track_order` (later plans and any future migration depend on it), how a `tmcd` and a
caption data track were actually identified from `codecpar`, and whether the
`skipped:not_applicable_container` path for `container.chapters` fell out of the engine's
unpaired handling or needed an explicit emit.
</output>
</content>
