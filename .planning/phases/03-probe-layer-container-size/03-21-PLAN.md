---
phase: 03-probe-layer-container-size
plan: 21
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - tests/unit/test_ebml_scan.cpp
  - src/cli/main.cpp
  - scripts/lint_bash4_builtins.sh
  - scripts/install_pinned_ffmpeg.sh
  - .planning/WINDOWS.md
autonomous: true
requirements: [TRUST-06, PROBE-05]
estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "On one real GitHub Actions run of PR #3, all three blocking legs — `build (x64-linux)`, `build (arm64-osx)` and `build (x64-windows-static-md)` — conclude their `Build` step `success` and reach their `Test` step. This has never once happened across this phase's CI history."
    - "A clang syntax-only compile of `tests/unit/test_ebml_scan.cpp` under `-Werror -Wunused-const-variable` exits 0 locally, reproducing the AppleClang gate that fails `arm64-osx` today so the fix is proven before it costs a CI round trip rather than after."
    - "`src/cli/main.cpp`'s Windows-only `wmain` names the CLI diagnostic sink with its declaring namespace, matching the sibling UTF-16 conversion call five lines above it, so MSVC's unqualified-name lookup has something to find (WINDOWS.md #16)."
    - "An unknown-size Cluster inside a Segment — legal EBML that `src/probe/ebml_scan.h`'s own header comment names explicitly but that no hand-constructed test has ever exercised — completes the walk cleanly and records the Cluster's own byte offset (PROBE-05 structural edge)."
    - "`scripts/lint_bash4_builtins.sh` flags the split-flag associative-array form and the multi-option shopt form that bypassed it before (03-REVIEW.md WR-01), its self-test control clause exercises all six of its checks rather than one of six (IN-03), and it still reports the existing scripts corpus clean."
    - "`scripts/install_pinned_ffmpeg.sh`'s tar.xz branch refuses an archive member whose name resolves outside the extraction directory and writes nothing outside it (03-REVIEW.md WR-02)."
    - "Every CI defect this round newly exposes is recorded in `.planning/WINDOWS.md` with the run id that exposed it, whether or not this plan is the one that fixes it."
  prohibitions:
    - "A blocking leg is never reported green from a local build, from a partial matrix, or from its absence in a failure list."
    - "The unused constant is never silenced with a void cast, a pragma, an attribute, or a relaxation of the warning flags — warnings-as-errors is a hard project constraint and the only two honest fixes are to reference the constant or to delete it."
    - "A lint regex is never widened in a way that newly flags an existing script under scripts/; a gate that reds the required lint job on correct code is as harmful as one that misses a defect."
    - "The live zip extraction branch is never modified while hardening the dead tar.xz branch — every CI leg depends on the zip path at the ffmpeg install step, so a mistake there fails all five legs at once."
    - "A ledger entry is never marked fixed on the strength of a committed change alone; the entry closes on an observed step conclusion."
    - "A newly exposed CI defect is never left unrecorded because it belongs to another plan's file — it is recorded in the ledger even when it is fixed elsewhere or not fixed at all."
    - "The full local test suite is never left with fewer tests than it started with; a suite that shrank is a suite that stopped asserting something."
  artifacts:
    - path: "tests/unit/test_ebml_scan.cpp"
      provides: "The file-local Cluster element-ID constant referenced by a real test case instead of sitting unused"
      contains: "unknown size, stopping the walk cleanly"
    - path: "src/cli/main.cpp"
      provides: "The namespace-qualified diagnostic call inside wmain"
      contains: "mediadiff::report_cli_error("
    - path: "scripts/lint_bash4_builtins.sh"
      provides: "Widened associative-array and shopt matchers plus one known-bad self-test fixture per check"
      contains: "dotglob globstar"
    - path: "scripts/install_pinned_ffmpeg.sh"
      provides: "Member-path validation on the tar.xz extraction branch, delimited so it can be extracted and exercised standalone"
      contains: "BEGIN tar.xz extraction program"
    - path: ".planning/WINDOWS.md"
      provides: "A ledger entry for every CI defect this round newly exposes, each carrying the run id that exposed it"
  key_links:
    - from: "tests/unit/test_ebml_scan.cpp and src/cli/main.cpp"
      to: "the arm64-osx and x64-windows-static-md Build step conclusions"
      via: "one push to the PR branch producing one pull_request CI run whose per-job step conclusions are read individually"
      pattern: "build \\(arm64-osx\\)|build \\(x64-windows-static-md\\)"
---

<!-- planner-discipline-allow: report_cli_error -->
<!-- planner-discipline-allow: kClusterId -->
<!-- planner-discipline-allow: dotglob globstar -->

<objective>
Remove the two one-line source defects that keep two of the three blocking CI legs from
compiling, close the two open code-review warnings against shipped Phase-3 scripts, and drive the
matrix to the state ROADMAP SC5 needs as its precondition: **all three blocking legs conclude
`Build` success and reach `Test` on the same run.**

Purpose: SC5 says the encode-twice release blocker "comes back clean **as a CI release blocker**."
A release blocker that cannot build on two of its three required legs is not one. Three
consecutive gap-closure rounds have each fixed one blocking defect only to expose the next one
behind it — NOMINMAX, then golden drift, then the bash-3.2 array-builtin crash, then the unused
constant, then the unqualified call. This plan is written on the assumption that a sixth defect
may be behind the fifth, and Task 3 exists to close it rather than to be surprised by it.

Output: two source fixes proven locally against the exact compiler diagnostic that fails CI, two
hardened scripts, a ledger that records whatever the run exposes, and one real run in which all
three blocking legs get as far as running tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/phases/03-probe-layer-container-size/03-VERIFICATION.md
@.planning/phases/03-probe-layer-container-size/03-REVIEW.md
</context>

<flagged_assumptions>
**A1 — Spec-less edge probe: 32 rows, all accounted for.** No `03-SPEC.md` exists for this phase,
so neither an `## Edge Coverage` nor a `## Prohibitions` section was available and the
deterministic probe was run over all 23 phase requirement IDs instead. It returned **32
applicable rows, 0 resolved, 32 unresolved**, with `byVerification: {explicit: 0, backstop: 0}` —
every row came back `unclassified` or as an unfilled category probe. Per the fallback protocol an
`unclassified` row stays `unresolved` and is surfaced as a flagged assumption; it is never
auto-resolved with a backstop and never silently dropped.

Round-level accounting, stated so the equality is checkable:

| requirement | rows | categories | disposition this round |
|---|---|---|---|
| PROBE-01, PROBE-02, PROBE-04, PROBE-06, PROBE-07, PROBE-09 | 6 | unclassified (1 each) | unresolved — out of scope; SC1-SC4 independently verified satisfied |
| PROBE-05 | 1 | unclassified | **authored** — the unknown-size-Cluster edge, see `must_haves.truths` and Task 1 Fix B |
| PROBE-08 | 4 | adjacency, empty, ordering, concurrency | unresolved — out of scope |
| PROBE-10 | 3 | adjacency, empty, ordering | unresolved — out of scope |
| CONT-01, CONT-02, CONT-04, CONT-05, CONT-06, CONT-07, CONT-08, CONT-09 | 8 | unclassified (1 each) | unresolved — out of scope |
| CONT-03 | 3 | adjacency, empty, ordering | unresolved — out of scope |
| SIZE-01 | 1 | concurrency | unresolved — out of scope |
| DIR-06 | 1 | concurrency | unresolved — out of scope |
| TRUST-06 | 1 | unclassified | unresolved — the probe's own question is not answerable from a CI-wiring criterion; carried into 03-21 and 03-22 as a flagged assumption |
| TRUST-09 | 1 | unclassified | unresolved — out of scope |
| DOC-03 | 3 | adjacency, empty, ordering | unresolved — concerns DOC-03's fixture-pair semantics, not its CI wiring |

**Equality: 32 probe-surfaced rows == 1 authored into `must_haves.truths` + 31 surfaced as flagged
assumptions. No silent drops.** The 31 stay unresolved not because they are uninteresting but
because this is gap-closure round 3 on an already-executed phase in which 22 of 23 requirements
are independently verified satisfied against real code and real CI; inventing 31 acceptance
criteria for behaviours nobody is changing this round would be fabrication, not coverage. The
`prohibitions` block is authored descriptor-less per the recall protocol and disposes
flagged-unverified.

**A2 — The clang sweep is a warning-class proxy for AppleClang, not a macOS build.** At plan time
a `clang++ -fsyntax-only -Werror -Wunused-const-variable` sweep was run over all **178**
translation units in `build/x64-linux/compile_commands.json`. It produced exactly **one**
warning-as-error across the whole tree: `tests/unit/test_ebml_scan.cpp:89`. That is real evidence
that no *second* unused-constant-class defect is queued behind WINDOWS.md #13. It is **not**
evidence that `arm64-osx` will link or that its tests will pass — the sweep is syntax-only, uses
libstdc++ rather than libc++, and reports unrelated alias-template CTAD diagnostics on files that
AppleClang demonstrably compiles today (`mediadiff::unexpected` is an alias template; Clang 18
with libstdc++ rejects a deduction AppleClang accepts). Treat the sweep as a cheap pre-filter for
one warning class, never as a substitute for the macOS leg.

**A3 — The Windows leg's remaining risk is not name lookup.** `src/cli/main.cpp`'s `wmain` is the
only function in the tree at global scope inside a Windows-only preprocessor region. A plan-time
survey found **16** files carrying `_WIN32`/`_MSC_VER` regions (`src/probe/bmff_scan.cpp`,
`src/probe/ebml_scan.cpp`, `src/probe/ts_scan.cpp`, `src/core/rational.h`, `src/util/fs.h`,
`src/cli/options.cpp`, `src/cli/dir_pairing.cpp`, `src/cli/commands/compare.cpp`,
`src/cli/commands/dir.cpp`, `src/cli/commands/snapshot.cpp`, `tests/process_spawn.h`,
`tests/support/utf8_path.h`, `tests/unit/test_fs_utf8.cpp`, `tests/unit/test_golden.cpp`,
`tests/unit/test_console_vt.cpp`, and `src/cli/main.cpp` itself) and every one other than
`main.cpp` is either an include guard or sits inside a namespace. The unqualified-call defect
class is therefore closed by Task 1, but any other MSVC-only defect in those 16 files remains
invisible from a POSIX host. Task 3 exists for exactly that.

**A4 — This plan may need more than one CI round trip.** If a blocking leg is still red after a
push, the correct response is to read the failure, fix it in the file that owns it, record it in
the ledger, and push again — never to soften a criterion. Task 3 bounds this at four round trips
and states what to do when the budget runs out.
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "the three blocking legs compile" — the two one-line defects, one push, per-leg Build conclusions read</name>
  <reversibility rating="reversible">Two source edits plus one added test case; git holds the prior text and neither change alters a shipped interface.</reversibility>
  <files>tests/unit/test_ebml_scan.cpp, src/cli/main.cpp</files>
  <read_first>
    - `tests/unit/test_ebml_scan.cpp` **in full**, including its 8-line header comment. "Use it or remove it" is a judgement that requires knowing what the file was built to test: the header states that behaviours 1-4 and 7 use hand-constructed byte buffers built in-file, and behaviours 5, 6, 8, 9 use fixtures `scripts/gen_corpus.sh` synthesizes. The file-local element-ID constants live in the anonymous namespace at lines 82-90; note which of the eight are referenced more than once and which is referenced exactly once.
    - `src/probe/ebml_scan.h`, header comment lines 1-33 plus the `EbmlScanResult` struct. The header states that the reserved unknown-size VINT form "is LEGAL EBML for a Segment **or a Cluster**". The existing test at `tests/unit/test_ebml_scan.cpp:218` covers the Segment half of that sentence and the "not Segment or Cluster ends the walk" half. The Cluster half has no test at all.
    - `src/probe/ebml_scan.cpp` lines 360-445 (`walk_elements` and the `allow_none` / `allow_cluster_only` / `allow_segment_only` predicates) and lines 612-706 (`walk_segment_children` and `run_ebml_scan`). These establish the exact behaviour the new test asserts: `allow_cluster_only` permits the unknown-size form for the Cluster id only; the callback records `first_cluster_offset` and returns `ElementAction::stop`; `stop` ends the walk successfully without requiring every remaining byte to be accounted for; and `result.complete` is assigned from the walk's boolean return with `stop_offset` forced to 0 on success.
    - `tests/unit/test_ebml_scan.cpp` lines 50-95 — the `bytes_be`, `size_vint`, `unknown_size_vint` and `write_bytes` helpers the new test composes, and the width convention of the element-ID constants (a canonical 4-byte ID carries its marker bit already, so `bytes_be(id, 4)` is the wire form).
    - `src/cli/diagnostics.h` **in full** — it declares the one permitted CLI diagnostic sink inside `namespace mediadiff` at line 43 and documents that no other site under `src/cli/` may write an Error message to standard error. This is the source of truth for the declaring namespace.
    - `src/cli/main.cpp` lines 205-315 — the whole Windows-only region. Note that `namespace mediadiff` closes at line 209, that `wmain` runs 222-312 at global scope, and that the region already writes `mediadiff::enable_vt_output()`, `mediadiff::wide_to_utf8(...)` and `mediadiff::run(...)` with their namespace while line 297 does not.
    - `.planning/WINDOWS.md` entries #13 and #16 — the two defects this task closes, and the run id (33990099158) on which each was observed.
  </read_first>
  <action>
    Land the two one-line defects together in one push and read what the matrix does. This is the
    thin end-to-end slice — source edit, local proof against the same diagnostic CI uses, push,
    observed per-leg Build conclusions — with nothing else in the diff to confuse attribution.

    **Fix A: `src/cli/main.cpp` line 297.** The call to the CLI diagnostic sink inside `wmain` is
    written without its namespace. Qualify it as `mediadiff::report_cli_error(...)`, exactly
    matching the sibling `mediadiff::wide_to_utf8(...)` call five lines above it in the same loop
    body. Change nothing else about the statement — not the message text, not the `LocalFree`
    ordering, not the `return 64`. The symbol is declared in `namespace mediadiff` at
    `src/cli/diagnostics.h:43`; `main.cpp` closes that namespace at line 209, which is why MSVC
    reports `error C3861` here while GCC and Clang never see it (the block is `#ifdef _WIN32`).

    **Fix B: `tests/unit/test_ebml_scan.cpp` line 89.** The file-local `kClusterId` constant
    (value `0x1F43B675`) is declared and never referenced, tripping AppleClang's
    `-Wunused-const-variable` under `-Werror` and aborting the `arm64-osx` and `x64-osx` Build
    steps. Both honest fixes exist; **choose to reference it**, because the behaviour it is the
    missing ingredient for is documented in `src/probe/ebml_scan.h`'s own header comment and is
    currently untested by any hand-constructed buffer.

    Add one new `TEST_CASE` carrying the tag `[unit]`, placed immediately after the existing
    unknown-size test that ends around line 239, named exactly:

    `ebml_scan - a Cluster inside Segment may legally carry unknown size, stopping the walk cleanly at its own offset`

    It composes a byte buffer using the file's own helpers: a Cluster header made of the 4-byte
    canonical element ID produced from the `kClusterId` constant followed by the 1-byte reserved
    unknown-size VINT from `unknown_size_vint(1)`, wrapped in a Segment header made of the 4-byte
    Segment element ID followed by a known-size 1-byte VINT declaring exactly the Cluster header's
    own length. Write it through `write_bytes` and run `run_ebml_scan` on the resulting path.
    Assert, in this order: the call has a value; `complete` is true; `stop_offset` is 0;
    `first_cluster_offset` holds a value equal to 5 — the Segment's own 4-byte ID plus its 1-byte
    size VINT, i.e. the Cluster element's own byte offset in the file; and `cues_offset` holds no
    value. This is the exact complement of the Void case in the test above it, and it turns the
    header comment's "or a Cluster" clause into a tested claim rather than an asserted one.

    Prove Fix B locally against the same diagnostic AppleClang uses before spending a CI round
    trip. The clang syntax-only command in this task's verify block reproduces
    `-Werror,-Wunused-const-variable` on this exact translation unit and was confirmed at plan time
    to fail on the current tree with precisely the CI error text, so a clean exit is real evidence
    rather than an absence of evidence.

    Then commit both fixes, push to `gsd/phase-03-probe-layer-container-size` (PR #3 is open, so a
    push produces a `pull_request` run), wait for the run to complete with `gh run watch`, and
    record in this plan's SUMMARY as quoted command output:

    1. The run id and head SHA, so a later reader can tell which commit was proven.
    2. Every `build (...)` job's `Build` and `Test` step conclusion, enumerated individually — all
       five legs, never a summary. Two previous rounds' reports were wrong precisely because they
       summarised a five-leg matrix as two.
    3. For any blocking leg whose `Build` is not `success`, the first error line from
       `gh run view <run id> --log-failed --job <job id>`, quoted verbatim. Do **not** fix it here:
       that is Task 3's job, and keeping this task's diff to the two one-line fixes is exactly what
       makes the attribution unambiguous.
  </action>
  <verify>
    <automated>clang++ --gcc-install-dir=/usr/lib/gcc/x86_64-linux-gnu/13 -std=c++20 -fsyntax-only -Wall -Wextra -Werror -Wunused-const-variable -I tests -I src -I build/x64-linux/generated -isystem build/x64-linux/vcpkg_installed/x64-linux/include -DMEDIADIFF_FIXTURE_DIR='"x"' -DMEDIADIFF_GOLDEN_DIR='"x"' -DMEDIADIFF_REGISTRY_FIXTURES='"x"' -DMEDIADIFF_REPORT_SCHEMA='"x"' -DMEDIADIFF_GEN_REGISTRY='"x"' -DMEDIADIFF_PYTHON='"x"' tests/unit/test_ebml_scan.cpp</automated>
    <fails_when>non-zero exit. An `error: unused variable ... [-Werror,-Wunused-const-variable]` line is the exact arm64-osx CI failure and means the fix did not land. A `fatal error: 'cstddef' file not found` instead means clang never located a libstdc++ installation and therefore never examined the file — correct the `--gcc-install-dir` path against `ls /usr/lib/gcc/x86_64-linux-gnu/` rather than editing the source.</fails_when>

    <automated>for c in kSegmentId kSeekHeadId kSeekEntryId kSeekIdId kSeekPositionId kClusterId kCuesId kVoidId; do n=$(grep -c "$c" tests/unit/test_ebml_scan.cpp); [ "$n" -ge 2 ] || { echo "declared-but-unreferenced: $c (occurrences=$n)"; exit 1; }; done; echo "all 8 element-id constants are referenced beyond their declaration"</automated>
    <fails_when>a line beginning `declared-but-unreferenced:` is printed, or the trailing confirmation line is absent</fails_when>

    <automated>Q=$(grep -c 'mediadiff::report_cli_error(' src/cli/main.cpp); A=$(grep -c 'report_cli_error(' src/cli/main.cpp); echo "qualified=$Q total=$A"; [ "$Q" -ge 1 ] && [ "$Q" = "$A" ]</automated>
    <fails_when>the printed `qualified=` value is 0 (the fix did not land) or differs from the printed `total=` value (a call site in this file still omits the namespace)</fails_when>

    <automated>cmake --build --preset x64-linux && ctest --test-dir build/x64-linux -R 'unknown size, stopping the walk cleanly' --output-on-failure</automated>
    <fails_when>non-zero exit, or the ctest summary reports `No tests were found` / `Total Tests: 0` — the new test-case name did not match the filter, so nothing was actually asserted</fails_when>

    <automated>ctest --test-dir build/x64-linux -N | sed -n 's/^Total Tests: \([0-9][0-9]*\)$/\1/p'</automated>
    <fails_when>the printed number is anything other than 623 — the suite was 622 tests before this task and must grow by exactly the one new case; a smaller number means a test was lost, a larger one means an unplanned test was added</fails_when>

    <automated>ctest --test-dir build/x64-linux --output-on-failure</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than `623 tests passed`</fails_when>

    <automated>RUN=$(gh run list --branch gsd/phase-03-probe-layer-container-size --limit 1 --json databaseId --jq '.[0].databaseId'); echo "run=$RUN"; gh run view "$RUN" --json jobs --jq '[.jobs[] | select(.name | startswith("build (")) | {job: .name, build: ([.steps[] | select(.name=="Build")] | .[0].conclusion // "not reached"), test: ([.steps[] | select(.name=="Test")] | .[0].conclusion // "not reached")}]'</automated>
    <fails_when>the command prints nothing, prints `run=` with no id (no run exists for the branch, so the push never happened), or prints fewer than 5 job objects (a leg is missing from the matrix rather than merely failing). This command is evidence-gathering, not a pass gate — the three-legs-green gate is Task 3's.</fails_when>
  </verify>
  <acceptance_criteria>
    - **Source assertion** — `grep -c 'mediadiff::report_cli_error(' src/cli/main.cpp` reports at least `1`, and reports the same number as `grep -c 'report_cli_error(' src/cli/main.cpp`.
    - **Source assertion** — `grep -c 'kClusterId' tests/unit/test_ebml_scan.cpp` reports at least `2`; it must never report exactly `1`, which is the declared-and-unused state that fails AppleClang.
    - **Test command** — the clang `-Werror -Wunused-const-variable` syntax-only compile of `tests/unit/test_ebml_scan.cpp` exits 0. Quote the command and its exit status in the SUMMARY.
    - **Test command** — `ctest --test-dir build/x64-linux -N` reports `Total Tests: 623` (622 before, plus exactly the one new case).
    - **Test command** — `ctest --test-dir build/x64-linux -R 'unknown size, stopping the walk cleanly'` runs exactly 1 test and it passes.
    - **Behavior assertion** — the new test asserts `first_cluster_offset` equals 5, not merely that it holds a value; an offset assertion that only checks presence would pass against a walk that recorded the wrong element.
    - **CLI output** — the SUMMARY quotes the pushed run's id, head SHA, and all five `build (...)` jobs' `Build` and `Test` step conclusions individually.
    - **CLI output** — for every blocking leg whose `Build` is not `success`, the SUMMARY quotes the first error line from that job's `--log-failed` output verbatim, with the file and line it names.
    - **Source assertion** — `git diff --name-only` for this task's commit lists only `tests/unit/test_ebml_scan.cpp` and `src/cli/main.cpp`. No script, workflow or ledger file is touched by this task.
  </acceptance_criteria>
  <precondition>`build/x64-linux` is configured (`build/x64-linux/compile_commands.json` exists), `clang++` is installed with a GCC toolchain under `/usr/lib/gcc/x86_64-linux-gnu/`, `gh` is authenticated against this repository, and PR #3 is open for branch `gsd/phase-03-probe-layer-container-size` so that a push produces a `pull_request` run.</precondition>
  <done>Both one-line defects are fixed and committed, the new Cluster edge test passes locally, the clang warning gate exits clean, and one real CI run's per-leg Build and Test step conclusions are recorded verbatim — whatever they are.</done>
</task>

<task type="auto">
  <name>Task 2: Close the two open code-review warnings — a lint gate that can no longer be bypassed, and a tar extraction that refuses traversal</name>
  <reversibility rating="reversible">Two script edits; the widened regexes were validated against the existing corpus at plan time and the tar branch is provably unreached today.</reversibility>
  <files>scripts/lint_bash4_builtins.sh, scripts/install_pinned_ffmpeg.sh</files>
  <read_first>
    - `.planning/phases/03-probe-layer-container-size/03-REVIEW.md`, findings WR-01 and WR-02 in full, plus IN-02 and IN-03. WR-01 was confirmed by direct execution of the extracted AWK matcher against three synthetic probes; IN-03 explains why the existing self-test cannot catch it.
    - `scripts/lint_bash4_builtins.sh` **in full**. Load-bearing regions: the head comment's numbered list of six flagged constructs and its "explicitly NOT flagged" list (lines 18-38); the `# bash4-allow` per-line escape valve and the statement that this script's own pattern-definition and fixture lines carry it (lines 55-63); the `BASH4_AWK` matcher (lines 113-160); and the three-fixture self-test control clause with its three refusal branches (lines 161-217).
    - `scripts/install_pinned_ffmpeg.sh` lines 185-260 — the `case "$PIN_ARCHIVE"` block with its `zip` and `tar.xz` arms, and the *existing* post-extraction traversal guard that resolves only `ffmpeg_path` (lines 219-253). Note that the two guards defend different things: the existing one validates the binary this script will invoke; the missing one validates every file the archive writes.
    - `scripts/ffmpeg_pin.json` — confirm all four pin entries carry `"archive": "zip"`, which is what makes the `tar.xz` arm dead code today and therefore safe to change in a round whose priority is CI green.
    - `scripts/corpus_digest.sh` head comment — it documents the deliberate duplication of the SHA-256 helper (IN-04). Do not act on IN-04 in this task; see this task's deferral note.
  </read_first>
  <action>
    Close WR-01 and WR-02. Both are real, currently-open findings against shipped Phase-3 code, and
    WR-01 in particular is the failure mode this project treats as most serious: a gate that
    reports clean while the construct it exists to catch walks past it.

    **WR-01a — the shopt matcher.** The current pattern requires the bash-4-only recursive-glob
    option name to be the token *immediately* after `-s`, but `shopt -s` legally accepts a list of
    option names. Replace the sixth check's regex with
    `(^|[^A-Za-z0-9_])shopt[ \t]+-s([ \t]+[A-Za-z_][A-Za-z0-9_]*)*[ \t]+globstar([^A-Za-z0-9_]|$)`
    so the flagged option name matches anywhere in that list. Keep the existing `# bash4-allow`
    marker on the `print` line.

    **WR-01b — the associative-array matcher.** The current pattern only inspects the single flag
    token immediately after the builtin keyword, so any line that puts another flag first slips
    past even though `declare -r -A arr` is exactly equivalent to `declare -rA arr`. Replace the
    second check's regex with
    `(^|[^A-Za-z0-9_])(declare|local|typeset)([ \t]+-[A-Za-z]+)*[ \t]+-[A-Za-z]*A[A-Za-z]*([ \t]|$)`
    so any subsequent flag token containing an uppercase A is matched. Both replacement regexes
    were validated at plan time: together they flag `declare -A arr`, `declare -rA arr`,
    `declare -r -A arr`, `local -x -A map`, `shopt -s globstar` and `shopt -s dotglob globstar`;
    they leave `declare -a arr`, `declare -i count`, `declare -r -x FOO=1`, `local -r name`,
    `shopt -s extglob`, `shopt -u globstar` and `arr=(a b c)` clean; and they produce zero hits
    across the current `scripts/*.sh` corpus, so the required lint job does not newly red.

    **IN-03 — self-test coverage for all six checks.** The self-test control clause today builds
    three fixtures, all using the array-read builtin, so five of the six checks have no known-bad
    control input and a regression in any of them would still print "self-test OK". Extend the
    clause to one known-bad fixture per check, keeping the existing known-bad-in-comment and
    known-good fixtures and their refusal branches unchanged. The five new fixture contents are
    `declare -r -A arr`, `echo "${name^^}"`, `wait -n`, `coproc reader { cat; }` and
    `shopt -s dotglob globstar` — chosen so the two new fixtures also serve as permanent
    regression guards for WR-01a and WR-01b specifically. Each new fixture must produce awk exit
    status 1; a fixture that does not fire must abort the script with a refusal message in the
    same shape as the three existing branches (state which check's control input failed, and state
    that reporting the real scan clean is being refused). **Every new line whose text contains a
    flagged construct must carry the `# bash4-allow` marker**, exactly as the existing fixture
    lines do — this script is inside its own scan scope, and an unmarked fixture line makes the
    lint fail on its own source.

    Leave IN-02 unfixed and record the reason in the SUMMARY: the head comment already documents
    the line-based-approximation limitation for comment stripping, the quoted-literal
    false-positive is the same class, no file under `scripts/` currently triggers it, and the
    `# bash4-allow` escape valve is the sanctioned per-line remedy if one ever does. Likewise
    leave IN-01 (the `std::stoll` re-parse in `src/cli/options.cpp`) and IN-04 (the duplicated
    SHA-256 helper) unfixed: IN-01 is documented defence-in-depth in a file outside this round's
    scope, and IN-04's proposed fix introduces a new sourced helper on a code path all five CI
    legs execute at the ffmpeg install step — unjustifiable risk in a round whose purpose is
    getting that matrix green.

    **WR-02 — tar.xz member-path validation.** The `zip` arm is safe as written and must not be
    touched: CPython's zipfile sanitizes member names natively, and that arm is the live path every
    CI leg executes. The `tar.xz` arm calls `extractall` with no member validation, which is the
    CVE-2007-4559 class: a member with an absolute or `../`-prefixed name can write outside the
    target directory. It is dead code today (all four pin entries are zip), which is precisely why
    it is safe to change now and dangerous to leave for the round that first needs it.

    Rewrite only the `tar.xz` arm's inline python program so that, before extracting anything, it
    opens the archive, resolves each member's destination with `os.path.realpath` joined against
    the realpath of the target directory, and raises `SystemExit` with a message naming the
    offending member if the resolved path is neither the target directory itself nor a path under
    it plus `os.sep`; it must additionally refuse any symlink or hardlink member whose `linkname`
    is absolute. Only after the whole member list passes may it call `extractall`. The program is
    embedded in a double-quoted shell string, so it must contain no `$` character and no backtick.
    Wrap the python program body between two comment lines reading exactly
    `# --- BEGIN tar.xz extraction program` and `# --- END tar.xz extraction program`, placed
    inside the quoted string, so the program can be extracted with `sed` and exercised standalone
    against a hostile archive — which is what the third verify command in this task does, and
    what makes a dead branch a tested branch rather than an asserted one.
  </action>
  <verify>
    <automated>bash scripts/lint_bash4_builtins.sh</automated>
    <fails_when>non-zero exit, or the output omits the `self-test OK` line, or the output omits the `clean. Scanned` line — either omission means the gate stopped short of completing its own control clause or its real scan</fails_when>

    <automated>SD=$(mktemp -d); trap 'rm -rf "$SD"' EXIT; AWKP=$(sed -n "/^BASH4_AWK='$/,/^'$/p" scripts/lint_bash4_builtins.sh | sed '1d;$d'); test -n "$AWKP" || { echo "MATCHER NOT EXTRACTED"; exit 1; }; fail=0; while IFS= read -r s; do printf '%s\n' "$s" > "$SD/p.sh"; awk "$AWKP" "$SD/p.sh" >/dev/null; [ $? -eq 1 ] || { echo "NOT FLAGGED: $s"; fail=1; }; done <<'BAD'
declare -A arr
declare -rA arr
declare -r -A arr
local -x -A map
shopt -s globstar
shopt -s dotglob globstar
BAD
while IFS= read -r s; do printf '%s\n' "$s" > "$SD/p.sh"; awk "$AWKP" "$SD/p.sh" >/dev/null; [ $? -eq 0 ] || { echo "FALSE POSITIVE: $s"; fail=1; }; done <<'GOOD'
declare -a arr
declare -i count
declare -r -x FOO=1
local -r name
shopt -s extglob
shopt -u globstar
arr=(a b c)
GOOD
[ "$fail" -eq 0 ] && echo "matcher boundary probe: 6 known-bad flagged, 7 known-good clean"</automated>
    <fails_when>the confirmation line `matcher boundary probe: 6 known-bad flagged, 7 known-good clean` is absent, or any `NOT FLAGGED:` / `FALSE POSITIVE:` / `MATCHER NOT EXTRACTED` line is printed. A `NOT FLAGGED:` line naming a split-flag or multi-option form means the WR-01 bypass is still open; a `FALSE POSITIVE:` line means the widening went too far and will red the required lint job on correct code.</fails_when>

    <automated>SD=$(mktemp -d); trap 'rm -rf "$SD"' EXIT; mkdir -p "$SD/dest"; sed -n '/# --- BEGIN tar.xz extraction program/,/# --- END tar.xz extraction program/p' scripts/install_pinned_ffmpeg.sh | sed '1d;$d' > "$SD/extract.py"; test -s "$SD/extract.py" || { echo "EXTRACTION PROGRAM NOT DELIMITED"; exit 1; }; python3 -c "import tarfile,io,sys,os; tf=tarfile.open(os.path.join(sys.argv[1],'hostile.tar.xz'),'w:xz'); d=b'pwned'; ti=tarfile.TarInfo('../escape.txt'); ti.size=len(d); tf.addfile(ti, io.BytesIO(d)); tf.close()" "$SD"; if python3 "$SD/extract.py" "$SD/hostile.tar.xz" "$SD/dest"; then echo "GUARD DID NOT FIRE"; exit 1; fi; test ! -e "$SD/escape.txt" || { echo "GUARD FIRED BUT A FILE WAS STILL WRITTEN OUTSIDE DEST"; exit 1; }; echo "tar.xz traversal guard refused the hostile member and wrote nothing outside the destination"</automated>
    <fails_when>the confirmation line is absent, or any of `EXTRACTION PROGRAM NOT DELIMITED` (the BEGIN/END markers are missing so nothing was tested), `GUARD DID NOT FIRE` (the traversal member extracted successfully), or `GUARD FIRED BUT A FILE WAS STILL WRITTEN OUTSIDE DEST` (validation happened after extraction rather than before) is printed</fails_when>

    <automated>grep -c '"archive": "zip"' scripts/ffmpeg_pin.json</automated>
    <fails_when>the printed count is anything other than 4 — the pin file no longer describes four zip entries, so the tar.xz arm may be live and this task's zero-blast-radius assumption no longer holds</fails_when>

    <automated>git diff --name-only HEAD -- scripts/install_pinned_ffmpeg.sh | wc -l; git diff HEAD -- scripts/install_pinned_ffmpeg.sh | grep -c '^[-+].*zipfile'</automated>
    <fails_when>the second printed number is anything other than 0 — a changed line mentioning the zip module means the live extraction path every CI leg depends on was modified, which this task forbids</fails_when>
  </verify>
  <acceptance_criteria>
    - **Test command** — `bash scripts/lint_bash4_builtins.sh` exits 0 and prints both its `self-test OK` line and its `clean. Scanned N file(s)` line. Quote both in the SUMMARY.
    - **Test command** — the extracted-matcher boundary probe flags all 6 known-bad forms (including `declare -r -A arr` and `shopt -s dotglob globstar`, the two WR-01 bypasses confirmed by the reviewer) and leaves all 7 known-good forms clean.
    - **Source assertion** — `scripts/lint_bash4_builtins.sh` contains a known-bad self-test fixture for each of its six checks, and every fixture line whose text contains a flagged construct carries the `# bash4-allow` marker. Verify by `grep -c 'bash4-allow' scripts/lint_bash4_builtins.sh` increasing relative to the pre-task value and by the script passing its own scan.
    - **Behavior assertion** — deliberately corrupting any one of the six new known-bad fixtures (locally, uncommitted) makes the script abort with a refusal message naming that check, rather than printing `self-test OK`. State in the SUMMARY which fixture was used for this spot check and quote the refusal.
    - **Test command** — the tar.xz traversal probe refuses a member named `../escape.txt` and no file appears outside the destination directory.
    - **Source assertion** — `sed -n '/# --- BEGIN tar.xz extraction program/,/# --- END tar.xz extraction program/p' scripts/install_pinned_ffmpeg.sh` emits a non-empty python program containing a `SystemExit` refusal and a realpath containment comparison.
    - **Source assertion** — the diff for `scripts/install_pinned_ffmpeg.sh` touches no line mentioning the zip module; the live extraction arm is byte-identical to its prior state.
    - **Behavior assertion** — the SUMMARY records, in one sentence each, why IN-01, IN-02 and IN-04 are deliberately not fixed this round. A silently dropped review finding is not acceptable; a reasoned deferral is.
  </acceptance_criteria>
  <precondition>`scripts/ffmpeg_pin.json`'s four entries all declare the zip archive kind, which is what makes the tar.xz arm unreachable in CI and therefore safe to modify in this round; if any entry has changed to a tar archive, stop and escalate rather than editing a live extraction path.</precondition>
  <done>The bash-3.2 gate flags both spellings that previously bypassed it, its self-test exercises all six of its checks, the tar.xz branch refuses a traversal member and is proven to do so by a runnable probe, the live zip path is untouched, and every unfixed review finding carries a recorded reason.</done>
</task>

<task type="auto">
  <name>Task 3: Read the next failure and close it — drive all three blocking legs to Build success and a reached Test step</name>
  <reversibility rating="reversible">Whatever this task changes is a defect fix in the file that owns it; each is recorded in the ledger with the run id that exposed it and git holds the prior text.</reversibility>
  <files>.planning/WINDOWS.md, plus whichever source, script or workflow file each newly exposed CI failure names (declared here because it cannot be known before the run — every such file is listed by path in this plan's SUMMARY)</files>
  <read_first>
    - This plan's own SUMMARY as written by Tasks 1 and 2 — the run id, the per-leg `Build` and `Test` step conclusions, and any error lines already quoted. This task starts from that evidence, not from a fresh guess.
    - `.planning/phases/03-probe-layer-container-size/03-VERIFICATION.md`, the `## Gaps Summary` section's closing paragraphs — they name the pattern this task exists to break: three consecutive rounds in which fixing one blocking-CI defect revealed the next one behind it, and the fact that `x64-windows-static-md` has never once been observed reaching its `Test` step across runs 33951407521, 33983460934 and 33990099158.
    - `.planning/WINDOWS.md` in full, **both representations** — the markdown table and the fenced JSON array hold the same records, and the frontmatter carries `open_count` / `waived_count` / `fixed_count` / `total_count`. This is why every append goes through the `windows append` subcommand rather than a hand-edit.
    - `.github/workflows/ci.yml` lines 22-69 (the five matrix legs and which three carry `blocking: true`) and the step names whose conclusions this task reads individually: `Install the pinned ffmpeg build (D-GAP-01)`, `Generate media fixture corpus (BUILD-08 / D-08)`, `Verify the fixture corpus is complete`, `Report corpus digest (cross-platform byte-identity evidence)`, `Assert the corpus digest matches the committed pin (D-GAP-01)`, `Configure`, `Build`, `Test`.
    - This plan's flagged assumption A3 — the 16 files carrying Windows-only preprocessor regions. When the failing leg is `x64-windows-static-md` and the error names one of them, that list is where the defect class lives, and A2's caveat explains why a POSIX host could not have seen it.
    - `.planning/phases/03-probe-layer-container-size/03-19-SUMMARY.md` — the designated-leg corpus policy, so a non-designated leg's excluded-by-name test lines are recognised as expected output rather than misread as a new failure.
  </read_first>
  <action>
    Drive the matrix to the state SC5 requires as its precondition, closing whatever stands in the
    way rather than assuming the two one-line fixes were sufficient. Three consecutive rounds have
    made exactly that assumption and been wrong each time; this task is the plan's honest admission
    that a sixth defect may sit behind the fifth.

    Push Task 2's script changes if they are not already pushed, wait for the run to complete, and
    then loop:

    1. Read the per-job `Build` and `Test` step conclusions for all five `build (...)` jobs. If all
       three blocking legs read `success` for `Build` and their `Test` step conclusion is anything
       other than `skipped` (that is, `Test` actually ran), the loop is done.
    2. Otherwise, for each blocking leg that is not there yet, pull the failing step's own log with
       `gh run view <run id> --log-failed --job <job id>` and quote the first error line verbatim.
       Diagnose from the log, never from a guess about what changed.
    3. Record the defect in `.planning/WINDOWS.md` via the `windows append` subcommand **before**
       fixing it, so the ledger holds the entry even if the fix turns out to be larger than this
       round can absorb. The entry names the file, the line where known, the failing leg, the run
       id that exposed it, and — if the defect was invisible on the other legs — why (a
       `#ifdef`-gated region, a compiler-specific warning, a shell dialect).
    4. Fix it in the file that owns it, keeping the change minimal and confined to the defect. Do
       not take the opportunity to refactor; a large diff in this task destroys the attribution
       that makes the next run's evidence readable.
    5. Re-verify locally with whatever local proof applies — a full `cmake --build --preset
       x64-linux` plus `ctest`, the clang warning gate from Task 1 for an AppleClang diagnostic, or
       `bash scripts/lint_bash4_builtins.sh` for a shell-portability failure — then commit, push,
       and return to step 1.

    **Budget: four CI round trips for this task.** If the three blocking legs are still not all at
    `Build` success with `Test` reached after the fourth, stop. Do not soften any criterion, do not
    declare SC5 closed on a partial matrix, and do not disable, exclude or make non-blocking any
    leg to manufacture green. Instead record, in `.planning/WINDOWS.md` and in this plan's SUMMARY:
    the exact remaining failure with its run id and quoted error line, what was tried, and what the
    next round needs. A recorded, well-diagnosed open defect is a correct outcome for this task;
    an unearned green is not.

    Two failure shapes have specific, already-decided handling and must not be treated as new
    defects. `build (arm64-linux)` failing at `Register vcpkg NuGet feed (read-write, trusted runs
    only)` is WINDOWS.md #11, a non-blocking-leg credential problem already recorded and out of
    scope. `build (x64-osx)` failing at link with an architecture mismatch is WINDOWS.md #14, the
    documented cross-build failure class on a non-blocking leg, also out of scope. Record both
    legs' conclusions and state explicitly that neither counts against this task; do not close
    either entry, because nothing in this round observes them fixed.
  </action>
  <verify>
    <automated>RUN=$(gh run list --branch gsd/phase-03-probe-layer-container-size --limit 1 --json databaseId --jq '.[0].databaseId'); echo "run=$RUN"; gh run view "$RUN" --json jobs --jq '[.jobs[] | select(.name=="build (x64-linux)" or .name=="build (arm64-osx)" or .name=="build (x64-windows-static-md)") | .steps[] | select(.name=="Build" and .conclusion=="success")] | length'</automated>
    <fails_when>the printed number is anything other than 3, or the command prints only the `run=` line with no number (no job name matched, so the evidence was never gathered). A printed 1 is the current pre-task state: only x64-linux builds.</fails_when>

    <automated>RUN=$(gh run list --branch gsd/phase-03-probe-layer-container-size --limit 1 --json databaseId --jq '.[0].databaseId'); gh run view "$RUN" --json jobs --jq '[.jobs[] | select(.name=="build (x64-linux)" or .name=="build (arm64-osx)" or .name=="build (x64-windows-static-md)") | .steps[] | select(.name=="Test" and .conclusion!="skipped")] | length'</automated>
    <fails_when>the printed number is fewer than 3 — a blocking leg's Test step is still `skipped`, meaning Build failed before it and the leg never ran a test. `x64-windows-static-md` has never yet contributed to this count on any real run in this phase's history.</fails_when>

    <automated>RUN=$(gh run list --branch gsd/phase-03-probe-layer-container-size --limit 1 --json databaseId --jq '.[0].databaseId'); gh run view "$RUN" --json jobs --jq '[.jobs[] | select(.name | startswith("build (")) | {job: .name, build: ([.steps[] | select(.name=="Build")] | .[0].conclusion // "not reached"), test: ([.steps[] | select(.name=="Test")] | .[0].conclusion // "not reached")}]'</automated>
    <fails_when>fewer than 5 job objects are printed — a matrix leg vanished rather than merely failed, which would silently narrow the gate</fails_when>

    <automated>node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status</automated>
    <fails_when>non-zero exit, or the reported total does not equal both the number of rows in `.planning/WINDOWS.md`'s markdown table and the number of objects in its fenced JSON array — the two representations have diverged, which is how a ledger comes to look correct while disagreeing with itself</fails_when>
  </verify>
  <acceptance_criteria>
    - **CLI output** — the final run's id and head SHA are quoted in the SUMMARY, together with all five `build (...)` jobs' `Build` and `Test` step conclusions enumerated individually.
    - **CLI output** — the count of blocking legs whose `Build` step concluded `success` is `3`, and the count whose `Test` step conclusion is not `skipped` is `3`. Both numbers are quoted from the commands above, not asserted in prose.
    - **Behavior assertion** — for every CI failure this task encountered, the SUMMARY records: the run id, the leg, the failing step name, the first error line quoted verbatim, the file it named, the fix applied, and the WINDOWS.md entry id created for it. A failure that was fixed without a ledger entry is not acceptable.
    - **Source assertion** — `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status` exits 0 and its reported total matches both the markdown table row count and the JSON array length. Quote the output.
    - **Behavior assertion** — `build (arm64-linux)` and `build (x64-osx)` conclusions are recorded with their failing step, and the SUMMARY states explicitly that WINDOWS.md #11 and #14 remain open and are outside this task's claim.
    - **Behavior assertion** — the SUMMARY states how many CI round trips this task consumed. If the four-trip budget was exhausted without three green legs, the SUMMARY says so plainly, quotes the remaining failure, and does not claim the task's goal was met.
    - **Source assertion** — no change in this task's diff makes a blocking leg non-blocking, excludes a test by name beyond the existing `EXPECTED_EXCLUDED_COUNT=5` set, or relaxes a warning flag. Confirm by `git diff -- .github/workflows/ci.yml CMakeLists.txt` being empty or containing no such change, and quote the diff stat.
  </acceptance_criteria>
  <precondition>Task 1's run evidence exists in this plan's SUMMARY; this task's diagnosis starts from that run's quoted failure lines rather than from a fresh assumption about what is broken.</precondition>
  <done>One real CI run shows all three blocking legs concluding `Build` success with their `Test` step actually executed, every defect encountered on the way is recorded in the ledger with its run id, and the two non-blocking legs' known issues remain open and explicitly outside the claim — or, if the round-trip budget was exhausted first, the exact remaining failure is recorded and no green is claimed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pinned ffmpeg archive to the CI workspace | An archive downloaded over the network is unpacked into a repo-relative directory that later steps execute from; member names in the archive are attacker-influenced structure. |
| repository scripts to the required lint gate | The bash-3.2 gate is the only thing standing between a bash-4-only construct and a blocking macOS leg dying at exit 127 before any test runs. |
| CI run evidence to the release decision | The three blocking legs' aggregate status is the required check that gates a merge; what is claimed about it determines what ships. |
| defect ledger to the next verification round | `.planning/WINDOWS.md` is read as ground truth by the next round; a wrong or missing entry propagates as a wrong conclusion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-97 | Tampering | `scripts/install_pinned_ffmpeg.sh` tar.xz extraction arm | high | mitigate | `extractall` with no member validation lets an archive member with an absolute or `../`-prefixed name write outside the extraction directory (CVE-2007-4559 class), and the script's existing post-extraction guard validates only the resolved ffmpeg binary, not the files the archive wrote. Mitigated by validating every member's realpath against the realpath of the destination before any extraction occurs, refusing absolute-target link members, and proving the refusal with a runnable hostile-archive probe (Task 2). |
| T-3-98 | Tampering | `scripts/lint_bash4_builtins.sh` matcher | medium | mitigate | Two of the gate's six checks are bypassed by syntactically ordinary spellings (`declare -r -A`, `shopt -s <other> globstar`), and the self-test exercises only one check, so a bypass or a regression in the other five reports "self-test OK" forever. Mitigated by widening both regexes (validated at plan time against 6 known-bad and 7 known-good forms plus the whole existing corpus) and by adding one known-bad control fixture per check (Task 2). |
| T-3-99 | Repudiation | the "the blocking legs build" claim | high | mitigate | A leg reported green on inference rather than observation is indistinguishable from one genuinely green, and this exact substitution already occurred twice in this phase. Mitigated by reading each job's `Build` and `Test` step conclusion individually from `gh run view --json jobs`, by enumerating all five legs rather than summarising, and by an explicit prohibition on satisfying a CI claim from a local build. |
| T-3-100 | Denial of Service | manufacturing green by narrowing the gate | high | mitigate | The cheapest way to make three legs green is to stop requiring one — mark a leg non-blocking, widen the CTest exclusion regex, or relax a warning flag. Any of these produces a required check that reports green without having run what it exists to run. Mitigated by an explicit prohibition, by an acceptance criterion asserting the workflow and CMake diffs contain no such change, and by the `EXPECTED_EXCLUDED_COUNT=5` assertion already standing in the workflow. |
| T-3-101 | Repudiation | premature or missing ledger entries | medium | mitigate | A defect fixed without a ledger entry is invisible to the next round, and an entry marked fixed because a change was committed — rather than because its effect was observed — is how WINDOWS.md #10 came to name the wrong ffmpeg version. Mitigated by appending each entry through the `windows` subcommands before the fix, by requiring the run id on every entry, and by asserting the ledger's two representations and its frontmatter counts agree. |

**Package legitimacy gate:** not applicable — this plan introduces no npm, pip or cargo install
task. `03-RESEARCH.md`'s `## Package Legitimacy Audit` records "Not applicable this phase", and the
only third-party artifact this plan touches (`scripts/ffmpeg_pin.json`'s four entries) is already
pinned by URL and SHA-256 with no unpinned fallback.
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Every symbol below is created by this plan and does not exist in the tree today. The
plan-review-convergence source-grounding pass should treat each as new, not as drift.

**New test case (Catch2 `TEST_CASE` name, and therefore a new CTest name)**
- `ebml_scan - a Cluster inside Segment may legally carry unknown size, stopping the walk cleanly at its own offset`, tagged `[unit]`, in `tests/unit/test_ebml_scan.cpp`. Its CTest name is that string with the `unit.` prefix. This is the 623rd test in the suite.

**New shell fixture identifiers in `scripts/lint_bash4_builtins.sh`**
- One known-bad self-test fixture file per previously-uncovered check, added alongside the existing `KNOWN_BAD_FILE` / `KNOWN_BAD_COMMENT_FILE` / `KNOWN_GOOD_FILE`, covering the associative-array declaration, case-modification expansion, `wait -n`, `coproc` and multi-option `shopt -s` checks — each with its own exit-status variable and its own refusal branch, in the shape of the three branches already present.

**New delimiter comments in `scripts/install_pinned_ffmpeg.sh`**
- `# --- BEGIN tar.xz extraction program` and `# --- END tar.xz extraction program`, placed inside the tar.xz arm's quoted python program so the program can be extracted with `sed` and exercised standalone.

**New ledger entries in `.planning/WINDOWS.md`**
- One entry per CI defect this round newly exposes (ids continue from 17), each carrying the failing leg, the run id, and why the defect was invisible on the other legs. The count is not knowable before the run.

**Modified, not new**
- `src/cli/main.cpp` line 297 gains a namespace qualification on an existing call; no new symbol.
- Two regexes inside the existing `BASH4_AWK` matcher are widened; the matcher's name and the six check descriptions it prints are unchanged.

**No new files, no new CI steps, no new CMake targets, no new check IDs** are introduced by this plan.
</artifacts_this_phase_produces>

<verification>
- The clang `-Werror -Wunused-const-variable` syntax-only compile of `tests/unit/test_ebml_scan.cpp` exits 0 (it fails today with the exact arm64-osx error text).
- All eight file-local element-ID constants in `tests/unit/test_ebml_scan.cpp` are referenced beyond their declaration.
- `grep -c 'mediadiff::report_cli_error(' src/cli/main.cpp` reports at least 1 and equals `grep -c 'report_cli_error(' src/cli/main.cpp`.
- `ctest --test-dir build/x64-linux -N` reports `Total Tests: 623`, and the full suite passes.
- `bash scripts/lint_bash4_builtins.sh` exits 0, printing both its self-test and its clean-scan lines; the extracted matcher flags 6 known-bad forms and leaves 7 known-good forms clean.
- The tar.xz traversal probe refuses a `../escape.txt` member and writes nothing outside the destination; the live zip arm's diff is empty.
- On the final CI run, exactly 3 blocking legs' `Build` steps conclude `success` and exactly 3 blocking legs' `Test` steps have a conclusion other than `skipped`, with all five legs enumerated.
- `windows status` exits 0 with a total matching both the markdown table and the JSON array.
</verification>

<success_criteria>
The two source defects that keep `arm64-osx` and `x64-windows-static-md` from compiling are gone,
proven locally against the same compiler diagnostic CI uses rather than only in CI; the two open
code-review warnings against shipped Phase-3 scripts are closed with runnable proofs and the three
deliberately-unfixed findings carry recorded reasons; and one real CI run shows all three blocking
legs building successfully and reaching their Test step — the precondition ROADMAP SC5 has never
yet had. Every defect encountered along the way is in the ledger with the run id that exposed it,
and nothing was made green by narrowing what the gate requires.
</success_criteria>

<output>
Create `.planning/phases/03-probe-layer-container-size/03-21-SUMMARY.md` when done.
</output>