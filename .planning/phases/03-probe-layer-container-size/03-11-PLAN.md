---
phase: 03-probe-layer-container-size
plan: 11
type: execute
wave: 11
depends_on: ["03-10"]
files_modified:
  - src/util/sanitize.h
  - src/util/sanitize.cpp
  - src/cli/tty_render.cpp
  - src/cli/provenance_render.cpp
  - src/report/markdown.cpp
  - src/cli/commands/inspect.cpp
  - src/report/model.h
  - src/report/model.cpp
  - scripts/lint_control_bytes.sh
  - scripts/gen_corpus.sh
  - CMakeLists.txt
  - .github/workflows/ci.yml
  - tests/unit/test_sanitize.cpp
  - tests/unit/test_inspect_container_section.cpp
  - tests/unit/CMakeLists.txt
  - tests/integration/test_trust06_idempotence.cpp
  - tests/integration/test_doc03_coverage.cpp
  - tests/integration/CMakeLists.txt
  - tests/golden/inspect_container.txt
  - src/report/json.cpp
  - src/report/junit.cpp
  - .planning/phases/02-core-engine/02-SECURITY.md
autonomous: false
requirements: [TRUST-06, DOC-03, CONT-03]

estimate:
  tokens: 42000
  raw_tokens: 84000
  tasks: 4
  confidence: high

must_haves:
  truths:
    - "No file-derived string reaches terminal output without C0/C7 control bytes being escaped first — a metadata tag or filename containing an ANSI escape sequence cannot manipulate the terminal (closes T-2-33)."
    - "`mediadiff inspect` on an MP4, a Matroska and an MPEG-TS renders a complete container section covering generic topology, per-format mechanisms, per-program measurements and metadata tags."
    - "Volatile metadata tags that were ignored for comparison but differ are shown under `-v` and hidden without it (CONT-03's remaining half)."
    - "Encoding a fixture twice with identical settings and comparing under `sw-encoder` comes back clean, wired into CI as a release blocker (TRUST-06)."
    - "Every registered check has at least one fixture pair that triggers it and one that comes back clean, enforced by a gate that fails the build naming any uncovered check (DOC-03)."
    - statement: "The container section renders identically on a Windows console with VT processing enabled, and the control-byte escaping holds there too."
      verification: backstop
  artifacts:
    - src/util/sanitize.h
    - scripts/lint_control_bytes.sh
    - tests/integration/test_trust06_idempotence.cpp
    - tests/integration/test_doc03_coverage.cpp
    - tests/golden/inspect_container.txt
  key_links:
    - "`sanitize_for_display` -> every render path that formats file-derived text: a single choke point, enforced by a lint, or the next renderer added silently reopens T-2-33."
    - "DOC-03 gate -> the registry's own check list -> the fixture-pair manifest: the gate must enumerate checks from the registry, not from a hand-maintained list, or a newly registered check is uncovered and unnoticed."
    - "TRUST-06's encode-twice job -> `--profile sw-encoder` -> a clean exit 0: the profile is what makes an identical-settings re-encode a legitimate no-change, and a failure here means either the encoder is non-deterministic or a check is over-sensitive."
  prohibitions:
    - "Never let file-derived text reach terminal output unescaped."
    - "Never satisfy the DOC-03 gate with a hand-maintained check list that can drift from the registry."
    - "Never make the TRUST-06 job optional, conditional, or skippable — a release blocker that can skip is not a release blocker."
    - "Never weaken a check, widen a tolerance, or add a volatile-key exemption to make the TRUST-06 encode-twice comparison come back clean; a failure there is information, not an obstacle."
    - "Never set `UPDATE_GOLDENS` in CI."
---

<objective>
Close the phase: make file-derived text safe to render, render the container section
`inspect` promised, prove the encode-twice idempotence guarantee as a release blocker, and gate
on every check having both a triggering and a clean fixture pair.

Purpose: T-2-33 (carried in from Phase 2 and made more reachable by this phase's metadata and
evidence flow), the ROADMAP's success criterion 1 (a complete container section for all three
container families), CONT-03's `-v` half, TRUST-06's CI release blocker, and DOC-03's per-check
fixture-pair gate.
Output: `src/util/sanitize.{h,cpp}` with a lint enforcing its single choke point, the container
section render with a golden, a non-skippable TRUST-06 CI job, and a registry-driven DOC-03 gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-probe-layer-container-size/03-CONTEXT.md
@.planning/phases/03-probe-layer-container-size/03-CHECK-ROSTER.md
@.planning/phases/03-probe-layer-container-size/03-10-SUMMARY.md
@.planning/phases/02-core-engine/02-SECURITY.md
@claude_docs/02-container-analysis.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close T-2-33 — one control-byte escaping choke point, enforced by a lint</name>
  <files>src/util/sanitize.h, src/util/sanitize.cpp, src/cli/tty_render.cpp, src/cli/provenance_render.cpp, src/report/markdown.cpp, src/report/json.cpp, src/report/junit.cpp, scripts/lint_control_bytes.sh, CMakeLists.txt, .github/workflows/ci.yml, tests/unit/test_sanitize.cpp, tests/unit/CMakeLists.txt, .planning/phases/02-core-engine/02-SECURITY.md</files>
  <read_first>
    - `.planning/phases/02-core-engine/02-SECURITY.md` — T-2-33's exact wording: no control-byte filtering exists anywhere in `src/`, and `src/cli/tty_render.cpp` formats real filenames into terminal output unfiltered. This is the item CONTEXT.md assigns to Phase 3 because this phase makes it "more reachable, not less".
    - `src/cli/tty_render.cpp` in full — every `fmt::format` call that embeds a `Finding::id`, a scope string, a value, a message or an accept/tune/silence triple; `elide_value` (lines ~85-102, byte-length elision); `make_glyph`'s deliberate two-string plain/rendered split for width accounting (an ANSI escape is invisible on a terminal but counts toward the width budget) — the same reasoning applies to an escape sequence arriving from a tag value.
    - `src/cli/provenance_render.cpp` and `src/report/markdown.cpp` — the other two render paths that format finding text.
    - `src/report/json.cpp` — JSON string escaping already handles control bytes at the wire level via `serialize_document`; JSON output does NOT need this filter and must not double-escape.
    - `src/util/fs.h` — the existing `src/util/` conventions, and note that `scripts/lint_eng16.sh` scans `src/util` for the bare tokens `stdout`/`stderr`, so the new file must contain neither.
    - `scripts/lint_dead_code_after_fail.sh` and `scripts/lint_fixture_case_collisions.sh` — the shared lint shape: `set -euo pipefail`, a documented known-limitation section up front, a ZERO-FILE GUARD refusing to report clean on a missing scan target, a named escape-valve marker for genuine false positives, and `LC_ALL=C` pinning when correctness depends on ASCII-only behavior.
    - `.github/workflows/ci.yml` lines 370-396 — the `lint (ENG-16 boundary)` job whose `name:` must not change.
    - `tests/unit/test_tty_render.cpp` — the existing render tests and their goldens, which must not change unless the sanitization genuinely alters output for existing inputs.
  </read_first>
  <behavior>
    - Test 1: `sanitize_for_display` replaces every C0 control byte (0x00-0x1F) except tab, and DEL (0x7F), with a visible escape form; a string containing `\x1b[31m` renders as visible text, not as a colour change.
    - Test 2: the C1 range expressed as UTF-8 (U+0080 through U+009F, which some terminals also interpret as control codes) is escaped too.
    - Test 3: an embedded newline in a tag value is escaped rather than passed through — an unescaped newline would break the renderer's row-per-finding layout and could forge a fake finding line.
    - Test 4: ordinary printable ASCII and valid multi-byte UTF-8 pass through byte-for-byte unchanged, so no existing golden changes.
    - Test 5: an invalid UTF-8 byte sequence is replaced rather than emitted raw.
    - Test 6: sanitization happens before width accounting — a value whose escaped form is longer than its raw form is elided against the escaped length, so the elision budget stays honest.
    - Test 7: JSON output is NOT double-escaped: a tag value containing `\x1b` appears once-escaped in `--json` per JSON's own rules, not with the display escape applied on top.
    - Test 8: `scripts/lint_control_bytes.sh` fails when a render path formats a finding's message, value or id without routing it through `sanitize_for_display`, and its self-test control clause fires on a synthetic known-bad before every real run.
  </behavior>
  <action>
    Create `src/util/sanitize.{h,cpp}` with a single function
    `std::string sanitize_for_display(std::string_view raw)`. It escapes every C0 byte except
    horizontal tab, escapes DEL, escapes the UTF-8 encodings of U+0080..U+009F, and replaces
    invalid UTF-8 sequences with U+FFFD. Everything else passes through byte-for-byte. Use a
    visible, unambiguous escape form (a backslash-`x` hex pair) and document the choice: a reader
    must be able to tell an escaped byte from a literal backslash-x in the source text, so escape
    the backslash itself as well.

    Do NOT put a bare `stdout` or `stderr` token in this file — `scripts/lint_eng16.sh` scans
    `src/util` for exactly those and will fail the build.

    Route every file-derived string through it at the RENDER boundary — `src/cli/tty_render.cpp`,
    `src/cli/provenance_render.cpp` and `src/report/markdown.cpp` — and nowhere else. Sanitizing
    at measurement construction instead would make the compared value and the rendered value
    disagree about what the tag says, which is why plan 03-04 deliberately did not do it there.
    Apply sanitization BEFORE `elide_value`'s width accounting, for the same reason
    `make_glyph` keeps a separate plain string: a sequence that is invisible on a terminal but
    occupies bytes corrupts every elision decision downstream.

    Leave `src/report/json.cpp` and `src/report/junit.cpp` alone. JSON escapes control bytes at
    the wire level through `serialize_document`, and XML escaping in `junit.cpp` handles its own
    context; applying a display escape on top would double-escape and change committed goldens
    for no security benefit. State that reasoning in a comment at each of the two files.

    Create `scripts/lint_control_bytes.sh` following the established lint shape exactly:
    `set -euo pipefail`; a documented known-limitation section up front (it is a line-based scan,
    not a C++ dataflow analysis — it flags a render-path `fmt::format` whose argument list
    mentions a finding field without a `sanitize_for_display` call on the same or an adjacent
    line); a self-test control clause firing on a synthetic known-bad before the real run; a
    ZERO-FILE GUARD refusing to report clean when its scan target directory is missing, with the
    same explicit message shape the two existing lints use; `LC_ALL=C` pinning; and a named
    escape-valve marker (`// control-bytes-allow`) for genuine false positives rather than
    loosening the scan. Wire it into the existing `lint (ENG-16 boundary)` CI job as one more
    step, without renaming the job.

    Update `.planning/phases/02-core-engine/02-SECURITY.md`'s T-2-33 entry status as part of this
    task's commit so the carried-forward threat is visibly closed rather than quietly dropped.
  </action>
  <verify>
    <automated>bash scripts/lint_control_bytes.sh &amp;&amp; ctest --test-dir build/x64-linux --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - **Locally provable** — `grep -rl 'sanitize_for_display' src/` lists exactly five files: `src/util/sanitize.h`, `src/util/sanitize.cpp`, and the three display render paths — and for both `src/report/json.cpp` and `src/report/junit.cpp`, `grep -vE '^\s*(//|\*|/\*)' FILE | grep -c 'sanitize_for_display'` reports `0` (comment lines stripped first, since the action requires each of those two files to carry a comment explaining why it deliberately does NOT sanitize).
    - **Locally provable** — `bash scripts/lint_eng16.sh` still passes (no `stdout`/`stderr` token entered `src/util`).
    - **Locally provable** — `bash scripts/lint_control_bytes.sh` prints its self-test result before its real scan, and exits non-zero when its scan target directory is renamed away (verify once during development).
    - **Locally provable** — a fixture whose `title` tag contains a literal ESC byte, compared with `-v`, produces terminal output in which no raw `0x1b` byte appears: `./build/x64-linux/mediadiff compare <a> <b> -v | grep -c $'\x1b\[31m'` reports `0` when `--no-color` is passed.
    - **Locally provable** — `git status --porcelain tests/golden/` is empty after the change (no existing golden altered, proving behavior 4).
    - **Locally provable** — `ctest --test-dir build/x64-linux -R "unit.*sanitize" --output-on-failure` selects a non-zero number of tests and is green.
    - **CI-only** — the `lint (ENG-16 boundary)` required check runs the new script on every push, and the Windows leg's console-VT test still passes with sanitization in place.
  </acceptance_criteria>
  <done>Every file-derived string reaching terminal or markdown output is escaped at one choke point, a self-testing lint prevents the next renderer from reopening the hole, and T-2-33 is recorded closed.</done>
  <reversibility rating="costly">The escape form appears in rendered output users read and may script against; changing it later changes what every terminal report looks like.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The inspect container section, and CONT-03's -v rendering of ignored volatile tags</name>
  <files>src/cli/commands/inspect.cpp, src/report/model.h, src/report/model.cpp, tests/unit/test_inspect_container_section.cpp, tests/unit/CMakeLists.txt, tests/golden/inspect_container.txt</files>
  <read_first>
    - `.planning/ROADMAP.md` Phase 3 success criterion 1 verbatim — the complete list of what the container section must render: "generic topology including subtitle and caption track presence, per-format mechanisms (faststart, brands, edit lists, Cues placement, CodecDelay, CC errors, PCR/PSI intervals, null ratio), per-program measurements on multi-program TS, and metadata tags compared as a set with volatile keys ignored but still shown under `-v`".
    - `claude_docs/02-container-analysis.md` section 2's `meta.tags` row — "ignored-but-differing shown under `-v`".
    - `src/cli/commands/inspect.cpp` lines 100-200 — `render_inspect_text` and `render_inspect_json`, the group iteration, `value_to_json`, the `serialize_document` routing, and how `opt_flag(options.verbose)` is already threaded through.
    - `src/report/model.h` — `Group`, `kGroupOrder`, `GroupBlock`, `RenderOptions` and its `-v` flags; and `group_for`'s comment on deriving group from the check id's first segment.
    - `src/analyzers/container/meta.cpp` as plan 03-04 wrote it — the ignored-volatile-key data placed in `Measurement::evidence`, which is what this task renders.
    - `src/core/model.h` — `Finding::evidence` as plan 03-01 added it, and the engine-side merge that populates it.
    - `src/util/sanitize.h` as written in Task 1 — every rendered tag value goes through it.
    - `tests/golden/inspect_basic.txt` and `tests/unit/test_tty_render.cpp` — the existing inspect golden and render-test conventions; the new container golden is a sibling, not a replacement.
  </read_first>
  <behavior>
    - Test 1: `mediadiff inspect <mp4>` renders a container section listing every `container.*` and `meta.*` check that produced a measurement, grouped and ordered by the existing `kGroupOrder`.
    - Test 2: the same for a Matroska input and for an MPEG-TS input; the TS render shows per-program entries with their program numbers.
    - Test 3: subtitle and caption/timecode track presence appears in the rendered topology section, not only in JSON evidence.
    - Test 4: with `-v`, a comparison whose only differences are volatile metadata keys shows those keys and both sides' values; without `-v`, they are absent from the output entirely.
    - Test 5: a `skipped` finding renders with its skip reason visible — `skipped != pass` must be legible, not inferred from absence.
    - Test 6: every rendered tag value and filename passes through `sanitize_for_display`.
    - Test 7: the container section's text output is pinned by a committed golden, compared read-only.
  </behavior>
  <action>
    Extend `render_inspect_text` in `src/cli/commands/inspect.cpp` to render the container section
    per the ROADMAP criterion. Reuse the existing `ReportModel` grouping and ordering — do not
    re-derive grouping here; `src/report/model.h`'s own header comment records that four
    renderers each re-deriving grouping was the exact problem that type exists to solve.

    Render, per group, each finding's id, scope (with a program-scoped entry showing its program
    number), value, status and — when the status is `skipped` — its skip reason spelled out.
    Route every value and message through `sanitize_for_display`.

    CONT-03's `-v` half: when `opt_flag(options.verbose)` is set, render the ignored-volatile-key
    entries carried in each `meta.tags` finding's evidence, showing the key and both sides'
    values. When it is not set, omit them entirely. Record in a comment that the data has been in
    evidence since plan 03-04 precisely so this render could exist without the analyzer needing
    to know about `-v`.

    Add a committed golden `tests/golden/inspect_container.txt` via `check_golden`, capturing the
    container section for one representative fixture per family. Keep the golden scoped to the
    container and meta groups rather than the whole report, so an unrelated render change does
    not turn this into a golden nobody reads.

    Add `tests/unit/test_inspect_container_section.cpp` for behaviors 1-6 and the golden for 7.
  </action>
  <verify>
    <automated>ctest --test-dir build/x64-linux --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - **Locally provable** — `./build/x64-linux/mediadiff inspect tests/fixtures/topo_subs.mp4` prints lines containing `container.format`, `container.track_count`, `container.track_types`, `container.mp4.faststart` and `meta.tags`.
    - **Locally provable** — `./build/x64-linux/mediadiff inspect tests/fixtures/ts_multiprogram.ts` prints at least two lines whose scope text names a program number.
    - **Locally provable** — `./build/x64-linux/mediadiff compare tests/fixtures/tags_volatile_a.mp4 tests/fixtures/tags_volatile_b.mp4 -v --no-color | grep -c creation_time` reports at least `1`, and the same command WITHOUT `-v` reports `0`.
    - **Locally provable** — `./build/x64-linux/mediadiff inspect tests/fixtures/topo_ts.ts --no-color | grep -c 'not_applicable_container'` reports at least `1` (a skip's reason is legible).
    - **Locally provable** — `tests/golden/inspect_container.txt` exists, is committed, and `git status --porcelain tests/golden/` is empty after a clean run.
    - **Locally provable** — `ctest --test-dir build/x64-linux -R "unit.*inspect_container" --output-on-failure` selects a non-zero number of tests and is green.
    - **CI-only** — the Windows leg's console rendering of the container section is exercised by the published verification bundle (`mediadiff.exe`); note in the summary that Phase 2's deferred UAT test 2 (Windows console colour rendering) can now be run against the container section too.
  </acceptance_criteria>
  <done>`inspect` renders a complete container section for all three container families including per-program entries and legible skip reasons, ignored volatile tags appear only under `-v`, and the output is pinned by a committed golden.</done>
  <reversibility rating="costly">The rendered layout becomes what users read and what the golden pins; changing it means a reviewed golden refresh.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TRUST-06 — encode-twice idempotence as a non-skippable CI release blocker</name>
  <files>scripts/gen_corpus.sh, tests/integration/test_trust06_idempotence.cpp, tests/integration/CMakeLists.txt</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` TRUST-06's wording: "Encoding a fixture twice with identical settings and comparing under `sw-encoder` produces a clean result — wired into CI as a release blocker."
    - `claude_docs/06-content-and-size-analysis.md` section 6 — the "identical-encode pair (clean under `sw-encoder` — the idempotence fixture)" recipe.
    - `.planning/ROADMAP.md` Phase 3 success criterion 5 — this requirement restated as a phase-level outcome.
    - `scripts/gen_corpus.sh` — the `-flags +bitexact -fflags +bitexact` convention and the `GENERATOR_MANIFEST.json` provenance, which is what makes "identical settings" reproducible at all. Its own header comment states that bitexact makes an encoder deterministic for a GIVEN encoder build, not across builds — so the two encodes must happen in the same run on the same machine.
    - `tests/integration/test_idempotence.cpp` and `tests/integration/test_determinism.cpp` — the existing double-run harness (02-CONTEXT.md D-13's determinism-by-diff pattern) this extends to real media.
    - `.github/workflows/ci.yml` lines 220-280 — the `Test` step, its zero-test guard, and how a step failure fails the job.
    - `src/core/profiles.cpp` — the `sw_encoder` profile's own severity and tolerance table, which is what makes an identical re-encode a legitimate no-change.
  </read_first>
  <behavior>
    - Test 1: encoding the same lavfi source twice with identical settings in the same run, then comparing the two outputs under `--profile sw-encoder`, exits `0` with no `fail` and no `warn` finding.
    - Test 2: the same comparison under `--profile strict-bitexact` also exits `0` when the two encodes are byte-identical, and the test asserts which of the two situations it is (byte-identical versus merely equivalent) so a change in encoder determinism is visible rather than absorbed.
    - Test 3: if ANY check fires on the identical-encode pair, the test fails naming that check — the failure message must make it obvious whether the encoder was non-deterministic or the check is over-sensitive, since those need opposite fixes.
    - Test 4: the test fails loudly if either encode did not produce a file, naming which — never skipping.
    - Test 5: the CI job running this cannot be skipped, is not conditional on a matrix value or an event type, and its failure fails the workflow.
  </behavior>
  <action>
    Add an identical-encode pair to `scripts/gen_corpus.sh`: the same lavfi source encoded twice
    with byte-identical arguments in the same script run, to two different output names. Record in
    the recipe comment that both encodes must happen in the same run against the same ffmpeg build
    — the script's own header already explains that `+bitexact` guarantees determinism for a given
    encoder build, not across builds, so a fixture generated on one machine and compared against
    one generated on another would be testing the wrong thing.

    Add `tests/integration/test_trust06_idempotence.cpp` covering behaviors 1-4. On failure, the
    message must name every firing check and state both hypotheses explicitly — encoder
    non-determinism versus check over-sensitivity — because a maintainer reading a red CI leg
    needs to know which of the two to investigate, and the fix for each is the opposite of the
    fix for the other.

    Under NO circumstances resolve a TRUST-06 failure by loosening a check, widening a tolerance,
    or adding a volatile-key exemption. If a check fires on an identical-encode pair, either the
    check found something real about the encoder or the check is wrong — both are findings worth
    a summary entry, and silently tuning the gate until it goes green is exactly the false-negative
    this requirement exists to prevent.

    Wire it into CI as part of the existing `Test` step on the Linux leg — the full suite already
    runs on every leg with a non-zero-test-count guard, so the requirement's "wired into CI as a
    release blocker" is satisfied by the test being IN that suite and unconditional. Do NOT create
    a separate job with an `if:` condition or a `continue-on-error`; a release blocker that can
    skip is not a release blocker. State in the summary that this was the chosen wiring and why,
    so a later reader does not add a redundant conditional job.

    The test requires the system ffmpeg the corpus generator uses. The Linux leg installs one for
    the existing corpus-generator check; confirm that is true for the leg this runs on and, if it
    is not, add the install step rather than making the test conditional.
  </action>
  <verify>
    <automated>bash scripts/gen_corpus.sh &amp;&amp; ctest --test-dir build/x64-linux -R "integration.*trust06" --output-on-failure</automated>
  </verify>
  <precondition>A system `ffmpeg` CLI at version 6.1 or newer is on `PATH` (or `MEDIADIFF_FFMPEG` points at one) on the machine and on the CI leg that runs this test; both encodes must be produced by that same binary in the same run.</precondition>
  <acceptance_criteria>
    - **Locally provable** — `./build/x64-linux/mediadiff compare tests/fixtures/idem_a.mp4 tests/fixtures/idem_b.mp4 --profile sw-encoder` exits `0` and its output contains no `fail` or `warn` row.
    - **Locally provable** — `ctest --test-dir build/x64-linux -R "integration.*trust06" --output-on-failure` selects a non-zero number of tests and is green.
    - **Locally provable** — `grep -c 'continue-on-error\|if:' .github/workflows/ci.yml` does not increase for the `Test` step (no conditional was added around this test's execution).
    - **Locally provable** — temporarily corrupting one of the two encodes makes the test FAIL naming the firing check (verify once during development; do not commit the corruption).
    - **Locally provable** — `git diff --stat src/core/checks.def` shows no tolerance or severity was changed in this task's commit (the prohibition against tuning the gate to green, verifiable from the commit itself).
    - **CI-only** — the Linux leg's `Test` step runs this case on every push and pull request, unconditionally; a red result blocks the merge.
  </acceptance_criteria>
  <done>An identical-settings double encode compares clean under `sw-encoder` in an unconditional CI test whose failure message distinguishes encoder non-determinism from check over-sensitivity, with no check loosened to achieve it.</done>
  <reversibility rating="reversible">A test and a fixture recipe.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Confirm the DOC-03 per-check fixture-pair gate covers every registered check</name>
  <files>tests/integration/test_doc03_coverage.cpp, tests/integration/CMakeLists.txt</files>
  <what-built>
DOC-03's gate: every registered check now needs at least one fixture pair that TRIGGERS it and one
that comes back CLEAN, enumerated from the registry itself rather than a hand-maintained list.

`tests/integration/test_doc03_coverage.cpp` reads the built registry's full check list, runs the
declared fixture pairs, and fails naming any check for which either half is missing — the same
discipline `tests/unit/test_fail_first_coverage.cpp` already applies per semantic-by-status cell,
extended to per-check-id as DOC-03 requires.

Twenty-seven checks were registered across plans 03-02, 03-04, 03-05, 03-06, 03-08 and 03-09. Each
plan created its own pairs. This gate is where a gap becomes visible.
  </what-built>
  <how-to-verify>
1. Build and run the gate:
   `bash scripts/gen_corpus.sh && ctest --test-dir build/x64-linux -R "integration.*doc03" --output-on-failure`
2. If it is GREEN: confirm the count. The output must name how many checks it verified, and that
   number must equal the registry's own check count (3 from Phase 2 plus this phase's registered
   ids). A gate that verified fewer checks than exist is the silent-gap failure this project has
   already hit; confirm the number rather than the colour.
3. If it is RED: it names each uncovered check and which half (triggering or clean) is missing.
   For each, decide:
   - the fixture pair exists but the manifest entry is wrong (fix the manifest), or
   - the pair genuinely does not exist (add the recipe to `scripts/gen_corpus.sh`), or
   - the check cannot be triggered by any synthesizable fixture (state which check and why; that
     is a real finding worth recording, not a reason to exempt it silently).
4. Confirm no check was exempted. `grep -c 'exempt\|skip\|allow' tests/integration/test_doc03_coverage.cpp`
   should show any exemption mechanism is absent, or if one exists, that every use of it carries a
   written reason.

Report: the verified check count, the registry's check count, and whether they match.
  </how-to-verify>
  <resume-signal>Type "approved" with the two counts, or describe the uncovered checks and how you want each handled.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file-derived strings (filenames, metadata tags, chapter titles, codec names) -> terminal and markdown output | The exposure T-2-33 names, made materially more reachable by this phase: Phase 2 only put filenames here; Phase 3 puts file CONTENT here. |
| committed golden and fixture manifest -> the DOC-03 gate's own coverage claim | A gate that enumerates from a hand-maintained list can silently stop covering a newly added check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-33 | Tampering / Information Disclosure | `src/cli/tty_render.cpp` and every other display render path | high | mitigate | **This is the carried-forward Phase-2 threat, closed here.** A single `sanitize_for_display` choke point escapes C0 (except tab), DEL and the UTF-8 C1 range before any file-derived string is formatted, applied before width accounting so an invisible sequence cannot corrupt elision. A self-testing lint with a zero-file guard prevents the next renderer from reopening it. `02-SECURITY.md` is updated in the same commit. |
| T-3-53 | Tampering | an escape sequence forging a fake finding row | high | mitigate | Newline is escaped, not passed through — an unescaped newline inside a tag value would let a crafted file inject a line that looks like a `pass` finding into a terminal report a human is reading. Behavior 3. |
| T-3-54 | Repudiation | the DOC-03 gate silently under-covering | high | mitigate | The gate enumerates checks from the built registry, never from a hand-maintained list, and the human checkpoint requires confirming the verified count against the registry count rather than accepting a green result. A gate whose scope can drift from what it claims to cover is the defect this project has already been bitten by. |
| T-3-55 | Repudiation | TRUST-06 wired as a skippable job | high | mitigate | The test lives inside the existing unconditional `Test` step rather than a separate job with an `if:` or `continue-on-error`; the acceptance criterion greps the workflow to confirm no conditional was added around it. |
| T-3-56 | Tampering | resolving a TRUST-06 failure by loosening a check | high | mitigate | Stated as an explicit prohibition, and the acceptance criterion inspects `git diff --stat src/core/checks.def` for the task's own commit — a tolerance change alongside a TRUST-06 fix is visible in the commit itself. |
| T-3-57 | Denial of Service | an enormous tag value expanding under escaping | low | mitigate | Escaping expands a byte to at most four; the value is already bounded by plan 03-04's truncation cap before it reaches the renderer, and elision runs against the escaped length. |
| T-3-SC | Tampering | package installs | low | accept | No package installed; FFmpeg 8.1 pin unchanged (03-RESEARCH.md Package Legitimacy Audit: not applicable this phase). |
</threat_model>

<verification>
- **Locally provable:** `ctest --test-dir build/x64-linux --output-on-failure` fully green.
- **Locally provable:** all six lint scripts pass (`lint_eng16`, `lint_check_id_strings`, `lint_dead_code_after_fail`, `lint_fixture_case_collisions`, `lint_tsduck_goldens`, `lint_control_bytes`), each showing its self-test clause where it has one.
- **Locally provable:** `grep -rl 'sanitize_for_display' src/` lists only `src/util/sanitize.{h,cpp}` and the three display render paths as call sites (json/junit mentions are comment-only, verified by the comment-stripped grep in Task 1).
- **Locally provable:** the DOC-03 gate's verified check count equals the registry's check count.
- **Locally provable:** the TRUST-06 pair compares clean under `sw-encoder` with no check tuned.
- **Locally provable:** `git status --porcelain tests/golden/` empty; no golden altered except the newly added `inspect_container.txt`.
- **CI-only:** all three matrix legs green; the `lint (ENG-16 boundary)` required check runs both new lint scripts; the Windows console rendering of the container section is verifiable from the published artifact bundle.
</verification>

<success_criteria>
- T-2-33 is closed at one choke point, enforced by a self-testing lint, with `02-SECURITY.md` updated.
- `inspect` renders a complete container section for MP4, Matroska and MPEG-TS including per-program entries and legible skip reasons.
- Ignored volatile tags appear under `-v` and not otherwise (CONT-03 complete).
- TRUST-06's encode-twice comparison is clean in an unconditional CI test.
- DOC-03's gate enumerates from the registry and every registered check has both a triggering and a clean pair, confirmed by count.
</success_criteria>

## Artifacts this phase produces (this plan's share)

**New headers/functions:** `src/util/sanitize.h` / `sanitize.cpp` (`sanitize_for_display`)

**New scripts:** `scripts/lint_control_bytes.sh`

**New CI steps:** `scripts/lint_control_bytes.sh` in the existing `lint (ENG-16 boundary)` job (job name unchanged); TRUST-06's test inside the existing unconditional `Test` step

**New goldens:** `tests/golden/inspect_container.txt`

**New fixtures:** `idem_a.mp4`, `idem_b.mp4` (identical-settings double encode), `tags_esc_a.mp4`, `tags_esc_b.mp4` (a tag value containing a literal ESC byte)

**New tests:** `tests/unit/test_sanitize.cpp`, `tests/unit/test_inspect_container_section.cpp`, `tests/integration/test_trust06_idempotence.cpp`, `tests/integration/test_doc03_coverage.cpp`

**Threat closed:** T-2-33 (carried from Phase 2)

**Broken window closed:** WINDOWS.md #1's `evidence` half (the `delta` half remains open by design; `Finding` still carries no structured per-comparator delta)

**New check ids:** none

**New CLI flags:** none

<output>
Create `.planning/phases/03-probe-layer-container-size/03-11-SUMMARY.md` when done.
Record: the exact escape form chosen and why; the DOC-03 gate's verified count versus the
registry count; any check that could not be triggered by a synthesizable fixture and why; whether
the TRUST-06 pair was byte-identical or merely equivalent under `sw-encoder`; and the state of
WINDOWS.md #1 (evidence closed, delta still open).
</output>
</content>
