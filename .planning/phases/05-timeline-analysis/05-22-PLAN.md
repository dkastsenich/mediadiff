---
phase: 05-timeline-analysis
plan: 22
type: execute
wave: 18
depends_on: ["05-20", "05-21"]
gap_closure: true
files_modified:
  - src/analyzers/timeline/av_sync.cpp
  - src/analyzers/timeline/analyzers.h
  - scripts/gen_corpus.sh
  - tests/golden/CORPUS_DIGEST_PROVISIONAL.txt
  - tests/fixtures/GENERATOR_MANIFEST.json
  - tests/unit/test_av_drift.cpp
  - tests/integration/test_timeline_av_sync.cpp
  - tests/integration/test_timeline_start_duration.cpp
autonomous: true
requirements: [TIME-07, TIME-08, DOC-04]

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "The code implements exactly the branch recorded in 05-STEP-DESIGN.md's Decision section, and nothing from the branch not chosen (UD-1)."
    - "Branch adopt: the SC1 pair named in 05-STEP-DESIGN.md's Implementation spec reports timeline.av_drift.pattern = step, with step_time_ms in evidence within one checkpoint spacing of the join. Its whole-report declared set is asserted."
    - "Branch narrow-vocabulary: fit_drift and the string mapping can no longer produce step. The SC1 step-intended pair keeps its measured irregular classification, and its test no longer calls it step-intended."
    - "In both branches, the constant-offset and linear-drift SC1 pairs keep their classifications and declared sets. Every trajectory on a discontinuity-free file is bit-identical to before this plan, unless the Decision chose span:observed, whose recorded effects the Implementation spec lists (TIME-07, TIME-08)."
    - "When the decided span source is span:observed, the MP4-to-TS declared sets (test_timeline_start_duration.cpp Test 4 and its mirror) are re-measured. timeline.av_drift / timeline.av_drift.pattern are dropped only if the measurement passes."
    - "Any fixture bytes this plan changes are regenerated only with TZ=UTC taskset -c 0-3. The fixture name goes into CORPUS_DIGEST_PROVISIONAL.txt, and CORPUS_DIGEST.txt is never edited."
  artifacts:
    - path: src/analyzers/timeline/av_sync.cpp
      provides: "the decided checkpoint mapping (adopt) or the step-free fit and mapping (narrow)"
    - path: tests/unit/test_av_drift.cpp
      provides: "the decided branch's unit coverage, including TIME-07 boundary vectors"
    - path: tests/integration/test_timeline_av_sync.cpp
      provides: "the SC1 case asserting the decided outcome whole-report"
  key_links:
    - from: "05-STEP-DESIGN.md Decision + Implementation spec"
      to: "av_sync.cpp checkpoint mapping / fit_drift"
      via: "the executor implements the recorded spec verbatim; any divergence is a deviation, not a design choice"
    - from: "SC1 integration case"
      to: "ROADMAP Phase 5 SC1 (amended or literal)"
      via: "the declared set and pattern assertion are the executable form of SC1"
  prohibitions:
    - statement: "MUST NOT implement a design, constant or span source the Decision did not choose, and MUST NOT tune thresholds beyond the Implementation spec to reach step."
      status: resolved
      verification: judgment
    - statement: "MUST NOT write, predict or edit any hash in tests/golden/CORPUS_DIGEST.txt; hashes come only from the designated leg (05-24)."
      status: resolved
      verification: judgment
    - statement: "MUST NOT use GPL-gated filters or encoders (e.g. mpdecimate) in any recipe; the Windows ffmpeg pin is an LGPL build."
      status: resolved
      verification: judgment
---

<objective>
Implement, in product code and tests, the SC1 branch the human chose at 05-21's blocking checkpoint. This is Gap 1.
- **adopt:** implement the piecewise checkpoint mapping from 05-STEP-DESIGN.md's Implementation spec, so `step` is reachable with a step time.
- **narrow-vocabulary:** remove `step` from what fit_drift and the pattern mapping can produce.

In both branches, apply the decided span source (A2).

Purpose: all six gaps must close before Phase 5 closes, and SC1 closes only through the decided branch. This plan is the executable half of that decision. 05-23 carries the documentation, vocabulary amendments and ledger half.

**Branch selection is mechanical, not interpretive.** The first action of Task 1 reads the `## Decision` section of `.planning/phases/05-timeline-analysis/05-STEP-DESIGN.md`. Its branch token (`adopt:<design-id>` or `narrow-vocabulary`) and span token (`span:declared` or `span:observed`) select the steps below. If the section still says PENDING, or either token is missing, stop and report; do not choose.

Output: the decided av_sync.cpp change, unit and integration coverage, and, only if the spec requires it, a changed existing fixture recipe with its name on the provisional ledger.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-timeline-analysis/05-STEP-DESIGN.md
@.planning/phases/05-timeline-analysis/05-21-SUMMARY.md
@.planning/phases/05-timeline-analysis/05-CHECK-ROSTER.md
@tests/integration/timeline_findings.h
@tests/golden/CORPUS_DIGEST_PROVISIONAL.txt
</context>

## Artifacts this phase produces

| Kind | Symbol / path |
|---|---|
| Functions (adopt only) | the `detail::` mapping functions named in 05-STEP-DESIGN.md `## Implementation spec`, declared in `src/analyzers/timeline/analyzers.h`, with the spec's exact signatures |
| Constants (adopt only) | the fixed named constants listed in the spec (e.g. a discontinuity threshold), in `analyzers.h` next to `kDriftEpsilonMs` |
| Enum change (narrow only) | `DriftPattern` loses its `step` enumerator; `DriftFit` loses its step-time member |
| Evidence (narrow only) | `timeline.av_drift` evidence no longer emits `step_time_ms` |
| Tests | `av_drift - ...` TEST_CASEs for the decided branch; the SC1 integration case renamed to state the decided outcome |
| Ledger line (only if the spec changes a recipe) | `timeline_drift_step.mp4` in `tests/golden/CORPUS_DIGEST_PROVISIONAL.txt` |

<flagged_assumptions>
**A1: the spec is expected to need no fixture change.**
- 05-21's panel includes timeline_drift_step.mp4 itself as variant V3 (a content-contiguous jump).
- A design that meets soundness criterion (c) classifies it `step` as it stands.
- If the spec nonetheless requires a recipe change, only the existing `timeline_drift_step.mp4` recipe may change.
- A brand-new fixture name cannot pass `scripts/lint_corpus_digest_provenance.sh` clause 3: a ledger entry must name an existing digest line, and no digest line may be added on a workstation. A spec requiring a new name is therefore a stop-and-report deviation.
- `scripts/gen_corpus.ps1` generates no fixtures. It is the Phase 1 skeleton, unchanged since commit 77d2c48, so it stays unchanged. The SUMMARY records that.

**A2: doc 04 (claude_docs/04-timeline-analysis.md) is not in UD-1's amendment list.** Under narrow-vocabulary this plan leaves it untouched. 05-23 records the divergence in 05-CHECK-ROSTER.md's amendment rather than rewriting the normative spec.

**Perf flag:** under adopt, or under span:observed, av_sync's per-checkpoint work on the MP4 perf reference changes, so instruction counts may move. 05-24's designated-leg run confirms the ratchet. valgrind is not installed on this workstation.
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: The decided branch is wired end to end through av_sync.cpp and proven on the SC1 step-intended pair</name>
  <reversibility rating="costly">Changes the published behavior of timeline.av_drift.pattern (adopt: a new reachable value; narrow: a removed value). The one-way vocabulary choice itself was gated at 05-21's blocking-human checkpoint; this task implements it.</reversibility>
  <files>src/analyzers/timeline/av_sync.cpp, src/analyzers/timeline/analyzers.h, scripts/gen_corpus.sh, tests/golden/CORPUS_DIGEST_PROVISIONAL.txt, tests/fixtures/GENERATOR_MANIFEST.json</files>
  <read_first>
    - .planning/phases/05-timeline-analysis/05-STEP-DESIGN.md in full, especially `## Decision`, `## Implementation spec` and `## Residual MP4-to-TS drift`
    - src/analyzers/timeline/av_sync.cpp: the pattern string mapping at about lines 125-135, the checkpoint construction in run_timeline_av_sync, and fit_drift at about lines 1067-1232
    - src/analyzers/timeline/analyzers.h (DriftPattern, DriftFit, DriftCheckpoint, the kDrift* constants)
    - scripts/gen_corpus.sh, the timeline_drift_step.mp4 recipe at about lines 2037-2078 (read even if no change is expected)
    - tests/golden/CORPUS_DIGEST_PROVISIONAL.txt header (entry format: LC_ALL=C sorted, unique, names only)
  </read_first>
  <action>
    1. Read `## Decision`. Record the two tokens verbatim at the top of the SUMMARY.

    2. **Branch adopt:<design-id>.**
       - Implement the spec's mapping as the `detail::` functions it names, with the exact signatures, fixed named constants, integer/rational arithmetic and rounding rules it states (ExactInt / Int128Accum for wide products, no floating point).
       - Replace the shipped checkpoint target construction in run_timeline_av_sync with it. fit_drift's classification rules stay as they are, unless the spec explicitly changes them.
       - The `step_time_ms` evidence is emitted as today whenever fit_drift reports a step.
       - If and only if the spec requires a recipe change:
         - Edit only the timeline_drift_step.mp4 recipe in scripts/gen_corpus.sh, to the spec's exact arguments. No GPL-gated filter or encoder is allowed.
         - Regenerate with `TZ=UTC taskset -c 0-3 bash scripts/gen_corpus.sh`, then run `bash scripts/check_corpus.sh`.
         - Add `timeline_drift_step.mp4` to tests/golden/CORPUS_DIGEST_PROVISIONAL.txt, keeping it LC_ALL=C sorted and unique. Update its STATUS paragraph to say the entry awaits designated-leg transcription in 05-24.
         - Leave tests/golden/CORPUS_DIGEST.txt untouched.
         - Stage the regenerated tests/fixtures/GENERATOR_MANIFEST.json for commit.

    3. **Branch narrow-vocabulary.**
       - Remove the `step` enumerator from `DriftPattern` and the step-time member from `DriftFit`.
       - Remove fit_drift's step-detection branch. A trajectory that used to classify step now falls through to irregular, with its residual max as today.
       - Remove the "step" string from the pattern mapping, and stop emitting `step_time_ms`.
       - Do not touch fixtures or recipes.

    4. **Span source.** Under span:observed, make the checkpoint spans use the observed first-to-last packet extent plus that packet's duration wherever the spec says. Under span:declared, keep the shipped preference.

    5. Build and run the step-intended SC1 pair: `./build/x64-linux/mediadiff compare --profile sw-encoder --json tests/fixtures/timeline_start_base.mp4 tests/fixtures/timeline_drift_step.mp4`. Record the pattern, residual_max_ms and step_time_ms (if any) in the SUMMARY.
  </action>
  <verify>
    <automated>cmake --build --preset x64-linux &amp;&amp; ./build/x64-linux/mediadiff compare --profile sw-encoder --json tests/fixtures/timeline_start_base.mp4 tests/fixtures/timeline_drift_step.mp4 | python3 -c "import json,re,sys; note=open('.planning/phases/05-timeline-analysis/05-STEP-DESIGN.md').read(); dec=note.split('## Decision',1)[1]; adopt=re.search(r'adopt:[A-Za-z0-9_-]+',dec); narrow='narrow-vocabulary' in dec; sys.exit('decision unreadable') if (bool(adopt)==narrow) or 'PENDING' in dec else None; d=json.load(sys.stdin); p=[x for x in d['findings'] if x['id']=='timeline.av_drift.pattern']; c=p[0]['candidate'] if p else None; ev=json.dumps(p[0].get('evidence')) if p else ''; print('branch', 'adopt' if adopt else 'narrow', 'candidate', c); sys.exit(0 if (adopt and c=='step' and 'step_time_ms' in ev) or (narrow and c=='irregular' and 'step_time_ms' not in ev) else 1)"</automated>
    <fails_when>Non-zero exit; or "decision unreadable" (Decision still PENDING, or it names neither or both branches); or under adopt the candidate pattern is not `step` or evidence lacks step_time_ms; or under narrow the candidate is not `irregular` or evidence still carries step_time_ms.</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY starts with the Decision's two tokens, verbatim.
    - adopt: `grep -n` for each spec-named `detail::` function in src/analyzers/timeline/analyzers.h matches.
    - narrow: `grep -v '^\s*//' src/analyzers/timeline/av_sync.cpp | grep -c 'return "step"'` reports 0.
    - `git diff --stat -- tests/golden/CORPUS_DIGEST.txt` is empty.
    - If a recipe changed: `bash scripts/lint_corpus_digest_provenance.sh` exits 0, and the ledger lists exactly `timeline_drift_step.mp4`. Otherwise `git diff --stat -- scripts/gen_corpus.sh tests/golden/CORPUS_DIGEST_PROVISIONAL.txt tests/fixtures/GENERATOR_MANIFEST.json` is empty.
  </acceptance_criteria>
  <done>The decided branch runs end to end, and the step-intended pair shows the decided outcome.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit coverage for the decided branch, including TIME-07's boundary vectors</name>
  <files>tests/unit/test_av_drift.cpp</files>
  <read_first>
    - tests/unit/test_av_drift.cpp in full (the `av_drift - ` cases; "a clean 100ms step at the fourth of six checkpoints classifies step" is the case the narrow branch rewrites)
    - .planning/phases/05-timeline-analysis/05-STEP-DESIGN.md `## Implementation spec` (the unit-test vectors, including TIME-07 boundary and precision cases) and `## Decision`
  </read_first>
  <behavior>
    - adopt: every unit-test vector in the Implementation spec passes, named `av_drift - <spec vector name>`. That includes:
      - a step residual of exactly 6 ms is not a step, and 7 ms is (the "strictly greater" rule);
      - a plateau at 2 ms from its mean is flat, and 3 ms is not;
      - the spec's discontinuity threshold at, and one tick either side of, its value;
      - one rounding tie in the mapping target, resolved by the spec's rule.
    - narrow: the former step trajectory (a clean 100 ms step at the fourth of six checkpoints) classifies irregular with residual max equal to its hand-computed value. No input can produce a step classification; assert this over the existing hand-built trajectories.
    - Both branches: the existing constant-offset, linear-drift, zig-zag, determinism and overflow cases pass unchanged.
  </behavior>
  <action>
    Write the behavior cases for the branch recorded in the Decision, named with the `av_drift - ` prefix. Under narrow, rename the former step case to state that it classifies irregular. Every expected value is hand-computed in a comment, or copied from the spec's vector table and cited. Fix product defects the tests expose in av_sync.cpp only within the spec. Anything outside the spec is recorded as a deviation.
  </action>
  <verify>
    <automated>cmake --build --preset x64-linux &amp;&amp; ctest --preset x64-linux -N -R '^unit\.av_drift - ' | grep -E 'Total Tests: [1-9]' &amp;&amp; ctest --preset x64-linux -R '^unit\.(av_drift|av_sync) - ' --output-on-failure</automated>
    <fails_when>Non-zero exit; or "Total Tests: 0" for the av_drift filter; or any failing av_drift / av_sync unit test.</fails_when>
  </verify>
  <acceptance_criteria>
    - adopt: the count of `av_drift - ` TEST_CASEs grows by at least the number of vectors in the spec's table. The SUMMARY maps each vector to its TEST_CASE name.
    - narrow: `grep -c 'classifies step' tests/unit/test_av_drift.cpp` reports 0.
    - All `unit.av_drift` and `unit.av_sync` tests pass.
  </acceptance_criteria>
  <done>The decided branch is pinned at unit level, including TIME-07's boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: The SC1 integration case asserts the decided outcome whole-report, and span-affected declared sets are re-measured</name>
  <files>tests/integration/test_timeline_av_sync.cpp, tests/integration/test_timeline_start_duration.cpp</files>
  <read_first>
    - tests/integration/test_timeline_av_sync.cpp lines 300-376 (the SC1 case: constant-offset, linear-drift and "step (irregular)" sub-blocks) and lines 117-205 (the avoffset_unknown.ts mirror)
    - tests/integration/test_timeline_start_duration.cpp lines 117-215 (Test 4)
    - .planning/phases/05-timeline-analysis/05-STEP-DESIGN.md `## Implementation spec` (expected declared-set changes) and `## Decision`
  </read_first>
  <action>
    1. Re-measure the three SC1 pairs with `mediadiff compare --json` under `sw-encoder`, and paste each non-pass set into the SUMMARY:
       - timeline_start_base.mp4 vs timeline_avoffset_video_shift.mp4
       - timeline_drift_base.mp4 vs timeline_drift_linear.mp4
       - timeline_start_base.mp4 vs timeline_drift_step.mp4

    2. Rewrite the SC1 TEST_CASE so its name states the decided outcome:
       - adopt: "... and step fixtures ..."
       - narrow: "... and the spliced-trim fixture, classified irregular ..."
       Drop the old hedged wording that described the fixture by its intent rather than its outcome.
       - adopt: the step sub-block asserts candidate `step`, `step_time_ms` present and within the spec's tolerance of the join, and the measured whole-report set.
       - narrow: it asserts candidate `irregular` and the measured whole-report set.
       - The constant-offset and linear-drift sub-blocks are unchanged unless the measurement differs. A difference in those two is a regression: stop and record it.

    3. Under span:observed, re-measure timeline_start_base.mp4 vs timeline_start_shift.ts and vs timeline_avoffset_unknown.ts. Drop `timeline.av_drift` / `timeline.av_drift.pattern` from Test 4 and its mirror only if they now pass, and replace their causal comments accordingly. Record the before/after drift evidence. Under span:declared, leave both sets unchanged.

    4. Run the full suite, then `MEDIADIFF_DESIGNATED_LEG=1 ctest --preset x64-linux --output-on-failure`. Never set UPDATE_GOLDENS.
  </action>
  <verify>
    <automated>cmake --build --preset x64-linux &amp;&amp; ctest --preset x64-linux -N -R '^integration\.timeline_av_sync - ROADMAP SC1' | grep -E 'Total Tests: [1-9]' &amp;&amp; ctest --preset x64-linux --output-on-failure &amp;&amp; MEDIADIFF_DESIGNATED_LEG=1 ctest --preset x64-linux --output-on-failure</automated>
    <fails_when>Non-zero exit; or "Total Tests: 0" for the SC1 filter; or any failing test in the full or designated-leg run.</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "step-intended" tests/integration/test_timeline_av_sync.cpp` reports 0.
    - The SC1 TEST_CASE's step/trim sub-block asserts the decided candidate value and calls `expect_declared_set`.
    - Both the full and the designated-leg suites pass, and `git diff --stat -- tests/golden/` shows no change except, at most, CORPUS_DIGEST_PROVISIONAL.txt from Task 1.
    - The SUMMARY holds the three re-measured SC1 sets and, under span:observed, the Test 4 / mirror before/after evidence.
  </acceptance_criteria>
  <done>SC1's executable form asserts the decided outcome, and every affected declared set is measured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human decision record -> product behavior | the executor must implement exactly what was chosen |
| packet timestamps -> checkpoint mapping arithmetic | attacker-controlled discontinuities drive segmentation and division |
| workstation fixture bytes -> corpus digest | a local regeneration must never become a committed hash |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-89 | Repudiation | implementing a branch the human did not choose | high | mitigate | Task 1's verify parses the Decision mechanically and fails on PENDING/ambiguous; the SUMMARY records the tokens verbatim |
| T-05-90 | Denial of Service / Tampering | a crafted file with many timestamp discontinuities inflating segmentation work or overflowing mapping products (adopt) | medium | mitigate | the spec's fixed K=32 checkpoints bound the output; ExactInt/Int128Accum for wide products; overflow degrades to insufficient_data, per the spec's precision section |
| T-05-91 | Tampering | a workstation-derived hash entering CORPUS_DIGEST.txt | high | mitigate | the file is never edited here (acceptance checks an empty diff); the name goes on the provisional ledger; `lint_corpus_digest_provenance.sh` runs when a recipe changed |
| T-05-SC | Tampering | dependency supply chain | low | accept | no package added |
</threat_model>

<verification>
- `cmake --build --preset x64-linux && ctest --preset x64-linux --output-on-failure` is green.
- `MEDIADIFF_DESIGNATED_LEG=1 ctest --preset x64-linux --output-on-failure` is green.
- If a recipe changed: `bash scripts/lint_corpus_digest_provenance.sh` passes. `bash scripts/assert_corpus_digest.sh` is EXPECTED to fail locally on exactly the timeline_drift_step.mp4 line until 05-24 transcribes it; record its output.
- Perf ratchet: FLAGGED under adopt or span:observed. 05-24 confirms it on the designated leg.
</verification>

<success_criteria>
- The decided SC1 branch is implemented exactly, with unit and whole-report integration coverage.
- constant-offset and linear-drift are unregressed.
- Any fixture change is on the provisional ledger, never in the digest.
</success_criteria>

<output>
Create `.planning/phases/05-timeline-analysis/05-22-SUMMARY.md` when done.
</output>
