---
phase: 05-timeline-analysis
plan: 18
type: execute
wave: 15
depends_on: ["05-14", "05-16", "05-17"]
gap_closure: true
files_modified:
  - src/analyzers/timeline/av_sync.cpp
  - src/analyzers/timeline/jitter_vfr.cpp
  - tests/integration/test_timeline_structure.cpp
  - docs/checks/timeline.av_offset.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [TIME-02, TIME-05, TIME-06, TIME-07, DOC-04]

estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "On timeline_ts_nowrap.ts vs timeline_ts_wrap.ts under --profile remux, timeline.av_offset, timeline.av_drift and timeline.av_drift.pattern all pass. av_sync reads the epoch-aligned timeline packet views, never raw wrapped PTS (Gap 2)."
    - "On the same pair, timeline.jitter and timeline.vfr_profile pass on both streams. jitter_vfr reads the per-stream views (Gap 2, WINDOWS #27)."
    - "tests/integration/test_timeline_structure.cpp's wrap-pair test is a whole-report expect_declared_set assertion. Its complete set is measured from the real binary and carries a causal comment per member. The D-01/D-02 carve-out is gone (Gap 2 orchestrator note)."
    - "WINDOWS.md #26, #27 and #30 are fixed. av_sync.cpp's inclusion under the same root cause, plus the declared-duration corruption 05-17 fixed, is recorded as its own entry and marked fixed. #29 is untouched."
    - "Every MP4/MKV value is byte-identical, because the views are zero-copy off MPEG-TS, and the full suite is green."
  artifacts:
    - path: src/analyzers/timeline/av_sync.cpp
      provides: "av_offset / av_drift / av_drift.pattern over make_timeline_packet_views"
    - path: src/analyzers/timeline/jitter_vfr.cpp
      provides: "jitter / vfr_profile over make_timeline_packet_view"
    - path: tests/integration/test_timeline_structure.cpp
      provides: "the wrap pair's whole-report declared set (05-16's promoted-view invariant test)"
    - path: .planning/WINDOWS.md
      provides: "#26/#27/#30 fixed, plus the av_sync.cpp entry"
  key_links:
    - from: "make_timeline_packet_views (epoch-aligned)"
      to: "av_sync's first_presented_pts / detail::sorted_pts_with_span inputs for the video and audio streams"
      via: "view.packets() replaces video_stream.packets / audio_stream.packets"
    - from: "the wrap pair's expect_declared_set"
      to: "every timestamp consumer migrated in 05-16..05-18"
      via: "any raw wrapped read surfaces as an undeclared non-pass finding and fails the test"
  prohibitions:
    - statement: "MUST NOT declare a finding in the wrap pair's set whose cause is wrap corruption; every declared member needs a content or container cause that exists independently of the wrap."
      status: resolved
      verification: judgment
    - statement: "MUST NOT close WINDOWS #26/#27/#30 before the whole-report wrap assertion passes."
      status: resolved
      verification: judgment
---

<objective>
Finish Gap 2 (SC2).
- Migrate the last two raw-timestamp consumers, `av_sync.cpp` and `jitter_vfr.cpp`, onto 05-16's timeline packet views.
- Replace the wrap pair's documented carve-out with a whole-report declared-set assertion.
- Close WINDOWS #26/#27/#30, recording av_sync.cpp's inclusion.

Purpose:
- After 05-16 and 05-17, the wrap pair's remaining false findings are timeline.av_offset, timeline.av_drift, timeline.av_drift.pattern and timeline.vfr_profile ×2.
- The whole-report assertion is the invariant test chosen in 05-16's assumption-delta decision. It goes red if any consumer ever reads raw wrapped timestamps again.

Output: two migrated analyzers, the converted integration test, an av_offset doc note, and the ledger closures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/phases/05-timeline-analysis/05-VERIFICATION.md
@.planning/phases/05-timeline-analysis/05-16-SUMMARY.md
@.planning/phases/05-timeline-analysis/05-17-SUMMARY.md
@tests/integration/timeline_findings.h
</context>

## Artifacts this phase produces

| Kind | Symbol / path |
|---|---|
| Test (renamed and converted) | `timeline_structure - the wrap trigger pair declares its complete expected finding set under --profile remux, and timeline.wrap_events is the state-semantic non-pass case on both streams` in `tests/integration/test_timeline_structure.cpp` |
| Ledger entry | a new `.planning/WINDOWS.md` deviation entry for `src/analyzers/timeline/av_sync.cpp` (created via `gsd-tools windows append`, then `windows fixed`) |
| Doc section | "MPEG-TS wraparound" paragraph in `docs/checks/timeline.av_offset.md` |

No new C++ symbols: this plan migrates call sites onto 05-16's `TimelinePacketView` / `make_timeline_packet_views` / `make_timeline_packet_view`.

<flagged_assumptions>
**A1: the complete wrap-pair set after this plan is expected, not assumed, to be these 4 non-pass findings.** Task 2 measures it with the real binary before writing it.

| Finding | Cause |
|---|---|
| `timeline.start` (global) | The applied `-output_ts_offset` genuinely moves the absolute origin (D-03). |
| `timeline.wrap_events` ×2 (video, audio) | State semantic: the candidate genuinely wraps. |
| `timeline.duration.coherence` (audio) | State semantic: both files share libavformat's 3877 ms audio estimate against the 4023 ms container duration. This is the same artifact the nowrap clean-pair test already declares. |

If the measurement differs, the executor records each extra or missing member with its evidence. An extra member whose cause is the wrap is a defect to fix in this plan, not a member to declare.

**A2: both TS sides share the same audio-duration estimate.** So whatever av_drift computes from it is identical on both sides, and the drift comparison passes. Any residual MP4-vs-TS drift artifact is out of this pair's scope; 05-21's research panel covers it.
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: A/V offset and drift read the epoch-aligned views, proven end to end on the wrap pair</name>
  <reversibility rating="reversible">Call-site migration onto an existing zero-copy adapter.</reversibility>
  <files>src/analyzers/timeline/av_sync.cpp</files>
  <read_first>
    - src/analyzers/timeline/av_sync.cpp lines 480-700 (run_timeline_av_sync: the `video_stream.packets` / `audio_stream.packets` reads at about lines 541, 562, 599 and 689, and the priming conversion 05-14 added)
    - src/analyzers/timeline/unwrap.h (TimelinePacketView, make_timeline_packet_views, epoch rule)
    - src/analyzers/timeline/start_duration.cpp's run function (05-16's migration: how `is_ts` is computed and how an overflowed view skips)
  </read_first>
  <action>
    1. In `run_timeline_av_sync`, build `make_timeline_packet_views(packet_scan, is_ts)` once.
       - Replace every timestamp read of the primary video stream's and the compared audio stream's raw packet vectors with the corresponding `views[idx].packets()`. These are the `first_presented_pts` calls and both `detail::sorted_pts_with_span` calls, at about lines 541, 562, 599 and 689.
       - Use the multi-stream builder, not the per-stream one: av_offset compares first PTS across streams, so both streams must be on the same epoch (05-16's rule).
       - An overflowed view on either the primary video or the compared audio stream skips that audio stream's timeline.av_offset, timeline.av_drift and timeline.av_drift.pattern with `SkipReason::insufficient_data`.
       - `tb`, declared durations, priming and scopes are unchanged.
    2. Verify on the wrap pair, and record in the SUMMARY each side's av_offset `raw_offset_ms` and av_drift `end_delta_ms`/`residual_max_ms`.
  </action>
  <verify>
    <automated>cmake --build --preset x64-linux &amp;&amp; ./build/x64-linux/mediadiff compare --profile remux --json tests/fixtures/timeline_ts_nowrap.ts tests/fixtures/timeline_ts_wrap.ts | python3 -c "import json,sys; d=json.load(sys.stdin); f=[(x['id'],x['status']) for x in d['findings'] if x['id'] in ('timeline.av_offset','timeline.av_drift','timeline.av_drift.pattern')]; print(f); sys.exit(0 if f and all(s=='pass' for _,s in f) else 1)"</automated>
    <fails_when>Non-zero exit; or the printed list is empty; or any printed status is not `pass`.</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' src/analyzers/timeline/av_sync.cpp | grep -c "_stream\.packets"` reports 0.
    - `grep -n "make_timeline_packet_views" src/analyzers/timeline/av_sync.cpp` matches.
    - The wrap-pair check in `<verify>` exits 0.
    - `ctest --preset x64-linux --output-on-failure` is green.
  </acceptance_criteria>
  <done>av_offset/av_drift are wrap-transparent.</done>
</task>

<task type="auto">
  <name>Task 2: jitter/vfr_profile read the per-stream view, and the wrap pair becomes a whole-report assertion</name>
  <files>src/analyzers/timeline/jitter_vfr.cpp, tests/integration/test_timeline_structure.cpp</files>
  <read_first>
    - src/analyzers/timeline/jitter_vfr.cpp lines 440-518 (run_timeline_jitter_vfr: `stream_packets` built from `stream_scan.packets` at about line 476)
    - tests/integration/test_timeline_structure.cpp lines 1-83 (header, helpers, the `timeline_structure - ` naming), lines 84-200 (the dts_backward whole-report test: the causal-comment style to copy) and lines 318-395 (the wrap carve-out test, and the nowrap clean pair that already declares the shared coherence artifact)
    - tests/integration/timeline_findings.h (expect_declared_set: occurrence-counted; info counts as non-pass)
  </read_first>
  <action>
    1. In `run_timeline_jitter_vfr`, build `make_timeline_packet_view(stream_scan, is_ts)` per stream and set `stream_packets` to `view.packets()`. An overflowed view skips both ids for that stream with `SkipReason::insufficient_data`, following the file's existing "both ids skip for the same reason" rule.

    2. Measure the wrap pair's complete non-pass set with `./build/x64-linux/mediadiff compare --profile remux --json tests/fixtures/timeline_ts_nowrap.ts tests/fixtures/timeline_ts_wrap.ts`. List every finding whose status is neither pass nor skipped, with scope and evidence. Paste the list into the SUMMARY before editing the test. Compare it against flagged assumption A1, and stop on any member whose cause is the wrap.

    3. Rewrite the wrap-pair TEST_CASE:
       - Rename it to the name in "Artifacts this phase produces", keeping the `timeline_structure - the wrap trigger pair` prefix.
       - Add `expect_declared_set` with the measured complete set, one causal comment per member in the file's existing style.
       - Keep the existing per-finding wrap_events assertions (baseline `no_wrap`, candidate `ts_33bit_wrap`, count 2).
       - Delete the comment text that justified asserting only wrap_events on this pair.
       - No platform condition and no designated-leg scoping.
  </action>
  <verify>
    <automated>cmake --build --preset x64-linux &amp;&amp; ctest --preset x64-linux -N -R '^integration\.timeline_structure - the wrap trigger pair declares' | grep -E 'Total Tests: [1-9]' &amp;&amp; ctest --preset x64-linux --output-on-failure</automated>
    <fails_when>Non-zero exit; or "Total Tests: 0" for the renamed wrap test; or any failing test in the full suite, including the new declared set.</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "make_timeline_packet_view" src/analyzers/timeline/jitter_vfr.cpp` matches.
    - `grep -A3 "the wrap trigger pair declares its complete" tests/integration/test_timeline_structure.cpp` shows a TEST_CASE whose body calls `expect_declared_set`.
    - `grep -ci "carve-out" tests/integration/test_timeline_structure.cpp` reports 0.
    - The SUMMARY holds the measured non-pass list, and it equals the declared set.
  </acceptance_criteria>
  <done>The wrap pair is asserted whole-report, and every timestamp consumer is wrap-transparent.</done>
</task>

<task type="auto">
  <name>Task 3: Close WINDOWS #26/#27/#30, record av_sync.cpp's inclusion, and document the av_offset wrap rule</name>
  <files>.planning/WINDOWS.md, docs/checks/timeline.av_offset.md</files>
  <read_first>
    - .planning/WINDOWS.md entries #26, #27, #29 and #30 (lines 43-47)
    - docs/checks/timeline.av_offset.md in full (05-14's priming paragraph is already there)
  </read_first>
  <action>
    1. Add a paragraph titled "MPEG-TS wraparound" to docs/checks/timeline.av_offset.md. On MPEG-TS, both first presented timestamps are taken over 33-bit-unwrapped, epoch-aligned timestamps. A file whose wrap falls between its streams' first packets therefore reports its true offset.

    2. Run the full suite, and then the designated-leg suite: `MEDIADIFF_DESIGNATED_LEG=1 ctest --preset x64-linux --output-on-failure`. Never set UPDATE_GOLDENS.

    3. Only after both pass, update the ledger through gsd-tools, never by hand-editing the table:
       - `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows fixed 26`, then the same for 27 and 30.
       - `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows append --kind deviation --phase 05 --file src/analyzers/timeline/av_sync.cpp --description "..."`. The description states:
         - timeline.av_offset/timeline.av_drift/timeline.av_drift.pattern read raw, un-unwrapped PTS on MPEG-TS, the same root cause as #26, found by 05-VERIFICATION.md Gap 2 and not previously filed.
         - libavformat's declared durations were also corrupted under correct_ts_overflow=0 on a wrapping file, fixed by 05-17's overflow-corrected re-probe.
         - Both are fixed by 05-16/05-17/05-18, and the whole-report wrap assertion guards them.
       - Read the new entry id from the command output, then run `windows fixed` on that id.
       - Do not touch #29.

    4. Run `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status` and paste its output into the SUMMARY.
  </action>
  <verify>
    <automated>MEDIADIFF_DESIGNATED_LEG=1 ctest --preset x64-linux --output-on-failure &amp;&amp; grep -E '^\| (26|27|30) \|' .planning/WINDOWS.md | grep -c '| fixed |' | grep -qx 3 &amp;&amp; grep -E '^\| 29 \|' .planning/WINDOWS.md | grep -q '| open |' &amp;&amp; grep -c 'src/analyzers/timeline/av_sync.cpp' .planning/WINDOWS.md</automated>
    <fails_when>Non-zero exit: a designated-leg test fails; or fewer than 3 of #26/#27/#30 carry `fixed`; or #29 no longer shows `open`; or no WINDOWS.md line names av_sync.cpp (grep -c prints 0 and exits 1).</fails_when>
  </verify>
  <acceptance_criteria>
    - #26, #27 and #30 show status `fixed` in .planning/WINDOWS.md, and #29 still shows `open`.
    - A new entry naming `src/analyzers/timeline/av_sync.cpp` exists with status `fixed`.
    - docs/checks/timeline.av_offset.md contains `MPEG-TS wraparound`.
    - The SUMMARY holds the `windows status` output.
  </acceptance_criteria>
  <done>Gap 2 is closed in code, test and ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TS timestamps -> A/V sync and cadence arithmetic | wrapped values produced nonsensical drift magnitudes on content-identical files |
| defect ledger -> /gsd-ship gate | closing an entry that is not actually fixed would let a known false positive ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-77 | Tampering | false A/V-sync fails on a wrapping TS | high | mitigate | av_sync reads epoch-aligned views (Task 1); the whole-report wrap assertion (Task 2) |
| T-05-78 | Repudiation | a WINDOWS entry closed without the fix being proven | medium | mitigate | Task 3 closes entries only after both the full and the designated-leg suites pass, and only through gsd-tools |
| T-05-79 | Tampering | a declared set enshrining a wrap-caused finding | high | mitigate | measure first; A1's rule forbids declaring a wrap-caused member; per-member causal comments |
| T-05-SC | Tampering | dependency supply chain | low | accept | no package added |
</threat_model>

<verification>
- `cmake --build --preset x64-linux && ctest --preset x64-linux --output-on-failure` is green.
- `MEDIADIFF_DESIGNATED_LEG=1 ctest --preset x64-linux --output-on-failure` is green.
- Perf ratchet: MP4 reference, zero-copy views, so a negligible change is expected. No flag.
</verification>

<success_criteria>
- Gap 2 closed: every consumer is wrap-transparent, the wrap pair is asserted whole-report, and WINDOWS #26/#27/#30 are fixed with av_sync.cpp recorded.
</success_criteria>

<output>
Create `.planning/phases/05-timeline-analysis/05-18-SUMMARY.md` when done.
</output>
