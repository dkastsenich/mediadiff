---
phase: 05-timeline-analysis
plan: 24
type: execute
wave: 20
depends_on: ["05-23"]
gap_closure: true
files_modified:
  - tests/golden/CORPUS_DIGEST.txt
  - tests/golden/CORPUS_DIGEST_PROVISIONAL.txt
  - tests/golden/PERF_BASELINE.txt
autonomous: false
requirements: [DOC-04, PERF-05]

estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Before anything leaves the workstation, every gate the designated x64-linux leg runs is exercised locally with a known expected outcome. Those gates are the corpus completeness check, the provenance lint, the digest assert, and the full and designated-leg suites. The assert passes if and only if the provisional ledger is empty."
    - "The branch is pushed only after an explicit blocking-human authorization. Pushing updates the already-open pull request #6 for gsd/phase-05-timeline-analysis; no other outward-facing action is taken."
    - "From the designated leg's own job log, the five byte-exact golden tests are confirmed to have run by name, with no ::error:: skip diagnostic, and the timeline perf ratchet is confirmed to have printed a measurement."
    - "Every fixture name on the provisional ledger (only timeline_drift_step.mp4 can be there, and only if 05-22 changed its recipe) has its CORPUS_DIGEST.txt line replaced byte for byte with the designated leg's hash, transcribed from the log and never computed locally. The ledger returns to EMPTY with a new TRANSCRIBED-FROM-DESIGNATED-LEG marker."
    - "If the perf ratchet failed, or printed a changed baseline the human chooses to adopt, the pasteable PERF_BASELINE.txt line goes into its own separate commit, so 05-25's push checkpoint can accept or drop it on its own (D-14/D-15)."
  artifacts:
    - path: tests/golden/CORPUS_DIGEST.txt
      provides: "designated-leg hash for any fixture this gap closure changed (unchanged otherwise)"
    - path: tests/golden/CORPUS_DIGEST_PROVISIONAL.txt
      provides: "EMPTY ledger with the new marker (unchanged if it was already empty)"
    - path: tests/golden/PERF_BASELINE.txt
      provides: "the designated leg's pasteable line, in a separate commit, only when needed"
  key_links:
    - from: "designated-leg job log (gh run view --log)"
      to: "CORPUS_DIGEST.txt / PERF_BASELINE.txt"
      via: "verbatim transcription, re-read character for character after the edit"
    - from: "local pre-flight (Task 1)"
      to: "the first push"
      via: "no push happens until the local gates show their expected outcomes"
  prohibitions:
    - statement: "MUST NOT push, comment on, or change the state of any pull request without the explicit blocking-human authorization in Task 2."
      status: resolved
      verification: judgment
    - statement: "MUST NOT compute, predict or regenerate a CORPUS_DIGEST.txt hash locally, and MUST NOT touch any line not named on the provisional ledger besides the recomputed CORPUS_DIGEST_SUMMARY= line."
      status: resolved
      verification: judgment
    - statement: "MUST NOT accept a green designated leg that skipped the byte-exact goldens or the perf step."
      status: resolved
      verification: judgment
---

<objective>
Confirm the gap-closure work on the designated x64-linux CI leg. This is the only place three things can be proven:
- the five byte-exact goldens still pass;
- the perf ratchet, which 05-19 and possibly 05-22 were flagged as able to move, is within tolerance;
- any fixture whose bytes 05-22 changed has its hash on record.

This plan follows 05-13's pattern:
1. A local pre-flight tracer.
2. A blocking-human push authorization.
3. Capture and transcription from the job log.
The second push and final confirmation are 05-25.

Purpose:
- Hard constraints 2, 6 and 7 require hashes and perf baselines from the designated leg only.
- They also require every outward-facing action to sit inside a blocking-human checkpoint.

Output: the captured run evidence and, only where needed, transcribed golden lines. If the ledger is empty and the ratchet passes, this plan commits nothing, and 05-25 records that no second push is needed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/phases/05-timeline-analysis/05-13-PLAN.md
@.planning/phases/05-timeline-analysis/05-13-SUMMARY.md
@tests/golden/CORPUS_DIGEST_PROVISIONAL.txt
@tests/golden/PERF_BASELINE.txt
</context>

## Artifacts this phase produces

| Kind | Symbol / path |
|---|---|
| Record | the pre-flight results, run URL, job id, the verbatim digest lines for ledger names, the five golden test lines, and the perf output, in this plan's SUMMARY |
| Transcription (conditional) | the `tests/golden/CORPUS_DIGEST.txt` line for each provisional-ledger name, plus the recomputed `CORPUS_DIGEST_SUMMARY=` line |
| Ledger marker (conditional) | a new `# TRANSCRIBED-FROM-DESIGNATED-LEG: run=<id> job=<id> commit=<40-hex> date=<date>` line in `tests/golden/CORPUS_DIGEST_PROVISIONAL.txt` |
| Perf line (conditional, separate commit) | the pasteable `tests/golden/PERF_BASELINE.txt` replacement line(s) |

<flagged_assumptions>
**A1: the provisional ledger holds zero or one name.** It is empty today (three TRANSCRIBED markers), and only 05-22 may add `timeline_drift_step.mp4`. Task 3 transcribes exactly what the ledger lists: no more, no fewer.

**A2: the perf ratchet may fail.**
- 05-19 adds per-interval work to jitter/vfr_profile on every input.
- 05-22 may change av_sync's mapping.
- valgrind is not installed locally, so the first measurement is on CI.
- Under D-14 a regression past 2% fails the step and prints a pasteable line. Adopting it is a human decision in 05-25, so the line goes in its own commit.

**A3: pull request #6 is already open for this branch.** Pushing the branch updates it. No new PR is created and no PR state is changed.
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: Local pre-flight runs every designated-leg gate end to end, with the expected outcome stated for each</name>
  <reversibility rating="reversible">Read-only local checks; no push, no file change.</reversibility>
  <files>(no repository files are modified by this task)</files>
  <read_first>
    - .github/workflows/ci.yml lines 167-221 (the corpus steps and the assert) and lines 360-500 (the designated-leg ctest guards and the perf step)
    - tests/golden/CORPUS_DIGEST_PROVISIONAL.txt (the ledger names, if any)
    - .planning/phases/05-timeline-analysis/05-22-SUMMARY.md and 05-23-SUMMARY.md (whether a recipe changed; the final local check results)
  </read_first>
  <action>
    Run, and record each command's output tail in the SUMMARY:
    1. `bash scripts/check_corpus.sh`
    2. `bash scripts/lint_corpus_digest_provenance.sh`
    3. `bash scripts/assert_corpus_digest.sh`. Expected to pass when the ledger has no entries. Expected to fail when it lists `timeline_drift_step.mp4`, and then only on that one line.
    4. `ctest --preset x64-linux --output-on-failure`
    5. `MEDIADIFF_DESIGNATED_LEG=1 ctest --preset x64-linux --output-on-failure`

    Also:
    - Record `git status --porcelain`, which must show no uncommitted change to tracked files, and `git log --oneline origin/gsd/phase-05-timeline-analysis..HEAD`: the commits the push will carry.
    - State in the SUMMARY that the perf ratchet cannot be run locally because valgrind is not installed.
    - Do not push.
  </action>
  <verify>
    <automated>bash scripts/check_corpus.sh &amp;&amp; bash scripts/lint_corpus_digest_provenance.sh &amp;&amp; ctest --preset x64-linux --output-on-failure &amp;&amp; MEDIADIFF_DESIGNATED_LEG=1 ctest --preset x64-linux --output-on-failure &amp;&amp; names=$(grep -v '^[[:space:]]*#' tests/golden/CORPUS_DIGEST_PROVISIONAL.txt | grep -v '^[[:space:]]*$' || true); if bash scripts/assert_corpus_digest.sh; then test -z "$names"; else test "$names" = "timeline_drift_step.mp4"; fi</automated>
    <fails_when>Non-zero exit: the corpus check, the lint, or either suite fails; or the digest assert passes while the ledger lists a name; or the assert fails while the ledger is empty or lists anything other than exactly timeline_drift_step.mp4.</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records all five command outcomes, the ledger contents, and the commit list the push will carry.
    - `git status --porcelain` shows no modified tracked file.
  </acceptance_criteria>
  <done>The local state is exactly what the designated leg is expected to see.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Authorize pushing the gap-closure branch so the designated leg runs</name>
  <files>(no repository files are modified by this task)</files>
  <decision>Authorize pushing gsd/phase-05-timeline-analysis to the remote. This updates the open pull request #6 and triggers CI, including the designated x64-linux leg.</decision>
  <context>
Task 1's pre-flight shows the local state.
- Only the designated leg can confirm the five byte-exact goldens and the perf ratchet (05-19 and possibly 05-22 were flagged as able to move instruction counts).
- It also computes the hash of any fixture 05-22 changed.

Pushing is outward-facing and updates a pull request reviewers can see. `gate="blocking-human"` is set so an unattended run cannot decide it; the same discipline applied in 05-13 and 04-20.
  </context>
  <options>
    <option id="push-branch">
      <name>Push the branch (updates open PR #6); no other PR action</name>
      <pros>Runs the designated leg with the smallest footprint beyond the existing PR.</pros>
      <cons>PR #6's reviewers see the new commits immediately.</cons>
    </option>
    <option id="decline-for-now">
      <name>Decline; keep the work local</name>
      <pros>Nothing leaves the machine.</pros>
      <cons>The phase cannot close: no local action can confirm the designated leg's goldens, perf ratchet or hashes.</cons>
    </option>
  </options>
  <read_first>
    - This plan's SUMMARY, as written by Task 1 (pre-flight results and the commit list)
    - .planning/phases/05-timeline-analysis/05-13-PLAN.md Task 1 (the precedent)
  </read_first>
  <resume-signal>Reply `push-branch` or `decline-for-now`. Authorization covers exactly one `git push` of this branch. 05-25 asks again before any second push.</resume-signal>
  <acceptance_criteria>
    - The reply is recorded verbatim in the SUMMARY, with the exact push command run on its behalf.
    - No push happens before the reply.
  </acceptance_criteria>
  <done>The first outward-facing action is explicitly authorized or declined.</done>
</task>

<task type="auto">
  <name>Task 3: Capture the designated leg's evidence, and transcribe only what the ledger and the ratchet require</name>
  <files>tests/golden/CORPUS_DIGEST.txt, tests/golden/CORPUS_DIGEST_PROVISIONAL.txt, tests/golden/PERF_BASELINE.txt</files>
  <read_first>
    - .planning/phases/05-timeline-analysis/05-13-PLAN.md Task 2 in full (the capture, confirm-before-transcribe, and ledger-emptying procedure this task repeats)
    - tests/golden/CORPUS_DIGEST_PROVISIONAL.txt in full (the STATUS paragraph and marker conventions to preserve)
    - scripts/lint_corpus_digest_provenance.sh lines 1-60 (all clauses) and scripts/corpus_digest.sh (how CORPUS_DIGEST_SUMMARY= is computed)
    - tests/golden/PERF_BASELINE.txt (line format)
  </read_first>
  <action>
    1. Run only the push Task 2 authorized. Wait for the run on this branch's head commit.

    2. Capture from the designated x64-linux job via `gh run view <run-id> --log`, and record in the SUMMARY:
       - the run URL and job id;
       - every "Assert the corpus digest matches the committed pin" output line;
       - the five byte-exact golden test result lines, by name;
       - any `::error::` lines;
       - the perf step's full output, including the absolute PERF-03 ratios and any pasteable line.

    3. Confirm before transcribing:
       - the five golden tests appear by name and passed;
       - no `::error::` skip diagnostic from either guard;
       - the perf step printed a measurement.
       If any is missing, stop and record what was missing.

    4. When the ledger lists `timeline_drift_step.mp4`:
       - Replace only that fixture's line in tests/golden/CORPUS_DIGEST.txt with the hash the designated leg printed, byte for byte.
       - Recompute only the trailing `CORPUS_DIGEST_SUMMARY=` line, with the same tool chain scripts/corpus_digest.sh uses.
       - Empty the ledger entry. Add the new `TRANSCRIBED-FROM-DESIGNATED-LEG` marker (run, job, 40-hex commit, date). Update the STATUS paragraph to name this plan and the run, preserving the historical paragraphs.
       - Re-read the log, and confirm the transcribed hash character for character.
       - Commit these two files together.
       When the ledger is empty, touch neither file.

    5. When the perf step failed the ratchet, or printed a replacement line that differs from the committed baseline: paste the printed line(s) into tests/golden/PERF_BASELINE.txt exactly as printed, and commit that file ALONE, in a separate commit whose message names D-14/D-15 and the run. Otherwise leave it untouched.

    6. Commit with explicit paths only. End every message with `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Never use --no-verify. Do not push; 05-25 authorizes the second push.
  </action>
  <verify>
    <automated>bash scripts/lint_corpus_digest_provenance.sh &amp;&amp; test -z "$(grep -v '^[[:space:]]*#' tests/golden/CORPUS_DIGEST_PROVISIONAL.txt | grep -v '^[[:space:]]*$')" &amp;&amp; bash scripts/assert_corpus_digest.sh &amp;&amp; ctest --preset x64-linux --output-on-failure</automated>
    <fails_when>Non-zero exit: the provenance lint fails a clause; or the ledger still lists a name; or the local digest assert fails (a transcribed hash differs from the CI-equivalent local bytes, i.e. a transcription error or non-CI-equivalent local fixture); or a test fails.</fails_when>
    <human-check>Read the designated leg's job log yourself. Confirm the five byte-exact golden tests appear by name and passed, that the perf step printed a measurement, and, if a hash was transcribed, that it matches the log character for character. A skipped gate reads like a passed one in any summary.</human-check>
  </verify>
  <precondition>Task 2's reply was `push-branch`, and `gh auth status` succeeds for this repository.</precondition>
  <acceptance_criteria>
    - The SUMMARY holds the run URL, job id, the five golden result lines, the perf output, and any digest lines, all verbatim.
    - `git diff HEAD~1 --stat` for any transcription commit touches only CORPUS_DIGEST.txt and CORPUS_DIGEST_PROVISIONAL.txt. A perf commit, if any, touches only PERF_BASELINE.txt.
    - If no transcription and no perf change was needed, `git log origin/gsd/phase-05-timeline-analysis..HEAD` is empty, and the SUMMARY says so.
  </acceptance_criteria>
  <done>The designated leg's evidence is on record, and every required transcription is done without a locally computed hash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local working tree -> remote / PR #6 | pushing is outward-facing and visible to reviewers |
| CI job log -> committed golden files | correctness-critical values are copied by hand |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-95 | Elevation of Privilege | a push or PR change without human consent | high | mitigate | Task 2 is `checkpoint:decision gate="blocking-human"`; authorization covers exactly one push; 05-25 asks again |
| T-05-96 | Tampering | a workstation-derived or mistyped hash in CORPUS_DIGEST.txt | high | mitigate | transcription only from the log; re-read character for character; the local assert against CI-equivalent bytes and the provenance lint in `<verify>` |
| T-05-97 | Repudiation | a green run that skipped the byte-exact goldens or the perf step | high | mitigate | confirm-before-transcribe (Task 3 step 3), with tests by name and no `::error::` skip lines |
| T-05-98 | Tampering | a perf regression silently baked into the baseline | medium | mitigate | the perf line goes in its own commit; 05-25's blocking checkpoint lets the human accept or drop it (D-14/D-15) |
| T-05-SC | Tampering | dependency supply chain | low | accept | no package added |
</threat_model>

<verification>
- Local: `bash scripts/lint_corpus_digest_provenance.sh`, `bash scripts/assert_corpus_digest.sh`, `ctest --preset x64-linux --output-on-failure`.
- Designated leg: the five byte-exact goldens RUN and pass, and the perf step RUNS, all read from the job log.
</verification>

<success_criteria>
- The designated leg has run the gap-closure branch, with every leg-only gate confirmed to have run.
- Any required transcription is committed, with no locally computed hash.
- Any perf baseline change is isolated in its own commit for 05-25's decision.
</success_criteria>

<output>
Create `.planning/phases/05-timeline-analysis/05-24-SUMMARY.md` when done.
</output>
