---
phase: 05-timeline-analysis
plan: 25
type: execute
wave: 21
depends_on: ["05-24"]
gap_closure: true
files_modified:
  - tests/golden/PERF_BASELINE.txt
autonomous: false
requirements: [DOC-04, PERF-05]

estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Every commit 05-24 made is re-verified locally before any second push. The provenance lint and the digest assert pass, and so do the full and designated-leg suites."
    - "A second push happens only after an explicit blocking-human authorization. In that same decision, the human may drop a separate PERF_BASELINE.txt commit and not adopt the baseline change (D-14/D-15)."
    - "After the second push, or after 05-24's first run when there is nothing to push, the designated x64-linux leg is confirmed green from its job log. That covers the digest assert, the five byte-exact goldens by name, the provenance lint and the perf ratchet. Every other leg is green, or its failure is attributed to an existing WINDOWS.md entry."
  artifacts:
    - path: tests/golden/PERF_BASELINE.txt
      provides: "unchanged, or with 05-24's perf commit dropped by the human's choice (the only file this plan may modify)"
  key_links:
    - from: "the human's push decision"
      to: "the confirming designated-leg run"
      via: "the confirmation is read from that run's job log, not from a summary"
  prohibitions:
    - statement: "MUST NOT push, or change pull request #6's state, without the blocking-human authorization in Task 2."
      status: resolved
      verification: judgment
    - statement: "MUST NOT declare the phase's gaps closed on a run that skipped a leg-only gate."
      status: resolved
      verification: judgment
---

<objective>
Close the provenance loop and confirm the phase gate state.
- Re-verify locally whatever 05-24 committed: a transcription, a perf line, or nothing.
- Get a blocking-human authorization for the second push, including whether to keep or drop the perf-baseline commit.
- Confirm the designated leg green from its log.

Purpose: 05-13's Task 3 pattern. A transcription or baseline change is not proven until the leg that asserts it re-runs against it, and every push is its own human decision.

Output: a recorded confirmation that the designated leg is green, with every leg-only gate run. When 05-24 committed nothing, the output is the recorded fact that 05-24's run was already the confirming run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/phases/05-timeline-analysis/05-24-SUMMARY.md
@.planning/phases/05-timeline-analysis/05-13-PLAN.md
</context>

## Artifacts this phase produces

| Kind | Symbol / path |
|---|---|
| Record | the human's push decision, the confirming run URL and job id, and the verbatim gate lines, in this plan's SUMMARY |
| Possible change | removal of 05-24's separate `tests/golden/PERF_BASELINE.txt` commit, only when the human chooses to drop it; done by reverting that commit with a new explicit-path commit, never a history rewrite |

No code symbol is created.

<flagged_assumptions>
**A1: when 05-24 committed nothing, there is nothing to push.** 05-24's single run is then the confirming run. Task 2 still records a human acknowledgement: the confirmation is itself a phase-gate claim.

**A2: non-designated legs may fail for reasons already on the ledger** (e.g. open Windows entries from earlier phases). Such a failure is attributed by entry id, not investigated here. A failure matching no entry is recorded as a new WINDOWS.md entry by the orchestrator, not fixed in this plan.
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: The committed transcription and baseline state re-verify locally, end to end, before any second push</name>
  <reversibility rating="reversible">Read-only local checks.</reversibility>
  <files>(no repository files are modified by this task)</files>
  <read_first>
    - .planning/phases/05-timeline-analysis/05-24-SUMMARY.md in full (what was committed, the run evidence, any perf line)
    - tests/golden/CORPUS_DIGEST_PROVISIONAL.txt (must be empty with its markers)
  </read_first>
  <action>
    Run, and record each outcome in the SUMMARY:
    - `bash scripts/lint_corpus_digest_provenance.sh`
    - `bash scripts/assert_corpus_digest.sh`
    - `ctest --preset x64-linux --output-on-failure`
    - `MEDIADIFF_DESIGNATED_LEG=1 ctest --preset x64-linux --output-on-failure`

    Then list `git log --oneline origin/gsd/phase-05-timeline-analysis..HEAD` and classify each commit as transcription, perf baseline, or other. An "other" commit is unexpected: stop and record it. When the list is empty, record "nothing to push; 05-24's run is the confirming run".
  </action>
  <verify>
    <automated>bash scripts/lint_corpus_digest_provenance.sh &amp;&amp; bash scripts/assert_corpus_digest.sh &amp;&amp; ctest --preset x64-linux --output-on-failure &amp;&amp; MEDIADIFF_DESIGNATED_LEG=1 ctest --preset x64-linux --output-on-failure</automated>
    <fails_when>Non-zero exit from the lint, the digest assert, or either suite.</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY lists every unpushed commit with its classification, or says "nothing to push".
    - All four commands pass.
  </acceptance_criteria>
  <done>The local state that would be pushed is verified.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Authorize the second push (keep or drop the perf commit), and confirm the designated leg green</name>
  <files>tests/golden/PERF_BASELINE.txt</files>
  <decision>Choose how the phase's CI confirmation completes:
  - push the transcription and perf commits;
  - push the transcription, dropping the perf-baseline commit;
  - acknowledge that 05-24's run already confirms green (nothing to push);
  - hold.</decision>
  <context>
05-24 captured the designated leg's evidence and may have made two separate commits: a digest transcription, and a PERF_BASELINE.txt line.
- Under D-14, a regression past 2% blocks the merge until a human adopts a new baseline in a reviewed commit (D-15).
- Keeping that commit adopts the new instruction count. Dropping it keeps the old baseline, so the ratchet stays failing until the regression is addressed.

Pushing updates open PR #6. `gate="blocking-human"` is set so an unattended run cannot decide it.

After any push, confirm on the designated x64-linux leg's job log:
- `assert_corpus_digest.sh` passes;
- the five byte-exact golden tests appear by name and pass, with no `::error::` skip diagnostic;
- `lint_corpus_digest_provenance.sh` passes;
- the perf ratchet ran, and passed against the committed baseline, with the printed PERF-03 ratios recorded;
- every other leg is green, or its failure matches an existing WINDOWS.md entry.
  </context>
  <options>
    <option id="push-all">
      <name>Push every 05-24 commit (transcription and perf baseline, if any)</name>
      <pros>Closes the loop in one push; adopts the measured instruction count.</pros>
      <cons>Adopts any perf regression 05-19/05-22 introduced as the new baseline.</cons>
    </option>
    <option id="push-without-perf">
      <name>Revert the perf-baseline commit (new explicit-path revert commit), then push</name>
      <pros>Keeps the old baseline; a regression stays visible and blocking.</pros>
      <cons>The designated leg's perf step stays red until the regression is fixed or later adopted.</cons>
    </option>
    <option id="nothing-to-push">
      <name>Acknowledge 05-24's run as the confirming run (valid only when Task 1 found no unpushed commit)</name>
      <pros>No further outward-facing action.</pros>
      <cons>None, when it applies. Choosing it with unpushed commits leaves them unconfirmed.</cons>
    </option>
    <option id="hold">
      <name>Hold; investigate a captured discrepancy first</name>
      <pros>Nothing questionable becomes a committed assertion.</pros>
      <cons>The phase stays open.</cons>
    </option>
  </options>
  <read_first>
    - This plan's SUMMARY, as written by Task 1, and 05-24-SUMMARY.md (the perf output and absolute ratios)
    - .github/workflows/ci.yml lines 360-500 (the skip guards and the perf step)
    - .planning/WINDOWS.md (for attributing any non-designated-leg failure)
  </read_first>
  <resume-signal>Reply `push-all`, `push-without-perf`, `nothing-to-push`, or `hold`. After a push, paste the confirming run URL, or let the executor read it with `gh run view --log`. The confirmation is recorded from the log itself.</resume-signal>
  <acceptance_criteria>
    - The reply is recorded verbatim in the SUMMARY.
    - For `push-without-perf`, the revert commit touches only tests/golden/PERF_BASELINE.txt, uses explicit paths, and carries the Co-Authored-By trailer; no history is rewritten.
    - The SUMMARY records, from the confirming run's log:
      - the digest assert line, the five golden test lines by name, the lint result, and the perf ratchet output with ratios;
      - the status of each non-designated leg, with any failure mapped to a WINDOWS.md entry id.
    - No `::error::` skip diagnostic appears in the confirming designated-leg log.
  </acceptance_criteria>
  <done>The designated leg is confirmed green with every leg-only gate run, or the human has explicitly held the phase open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local branch -> remote / PR #6 | the second outward-facing action |
| CI log -> phase-closure claim | a skipped gate must not be read as a passed gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-99 | Elevation of Privilege | a second push without consent | high | mitigate | Task 2 is `checkpoint:decision gate="blocking-human"` |
| T-05-100 | Tampering | a perf regression adopted as the baseline without a decision | medium | mitigate | `push-all` vs `push-without-perf` is an explicit human choice (D-14/D-15) |
| T-05-101 | Repudiation | phase closure claimed on a run that skipped leg-only gates | high | mitigate | acceptance requires the tests by name and no `::error::` skip lines, read from the log |
| T-05-SC | Tampering | dependency supply chain | low | accept | no package added |
</threat_model>

<verification>
- Local: `bash scripts/lint_corpus_digest_provenance.sh && bash scripts/assert_corpus_digest.sh && ctest --preset x64-linux --output-on-failure`.
- Designated leg: every leg-only gate RUNS and passes, read from the job log.
</verification>

<success_criteria>
- The designated leg is confirmed green on the final gap-closure state, or the human has explicitly held it.
- Every push and any perf-baseline adoption happened behind its own human decision.
</success_criteria>

<output>
Create `.planning/phases/05-timeline-analysis/05-25-SUMMARY.md` when done.
</output>
