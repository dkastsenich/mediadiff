---
phase: 05-timeline-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-timeline-analysis/05-CHECK-ROSTER.md
  - src/analyzers/timeline/analyzers.h
  - src/analyzers/timeline/start_duration.cpp
  - src/probe/orchestrator.cpp
  - src/core/checks.def
  - docs/checks/timeline.start.md
  - CMakeLists.txt
  - scripts/gen_corpus.sh
  - tests/golden/CORPUS_DIGEST.txt
  - tests/golden/CORPUS_DIGEST_PROVISIONAL.txt
  - tests/integration/timeline_findings.h
  - tests/integration/test_timeline_start_duration.cpp
  - tests/integration/test_video_yuvj.cpp
  - tests/integration/test_doc03_coverage.cpp
  - tests/integration/CMakeLists.txt
autonomous: false
requirements: [TIME-01, TIME-03, DOC-04]

estimate:
  tokens: 60000
  raw_tokens: 120000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "`05-CHECK-ROSTER.md` exists and is approved before any `timeline.*` id is written to `src/core/checks.def`; every id later registered by plans 05-04 through 05-11 appears in it verbatim (D-04 and D-10 are rated one-way for exactly this reason)."
    - "`timeline.start` emits ONE `Scope::Kind::global` measurement holding the file's earliest presentation time, plus ONE `Scope::Kind::{video,audio,...}` measurement per timestamped stream holding that stream's first presentation PTS MINUS the global origin (D-03) — never a per-stream absolute PTS."
    - "A whole-file shift (an MP4 stream-copied to MPEG-TS, which the TS muxer delays by its 1.4 s default) produces ONE non-pass `timeline.start` finding at global scope, not one per stream (D-03's entire purpose)."
    - "A stream whose packets all carry `AV_NOPTS_VALUE` emits `skipped:no_timing_data` with `Absent{}` — the sentinel is never coerced to 0 and never placed at the origin (TIME-01)."
    - "Two packets carrying the exact same presentation PTS are both retained as distinct records and the earliest-PTS origin is the same value whichever is read first; an `absent` timestamp never compares equal to a real `0` (TIME-01 adjacency edge)."
    - "An empty packet array, a single-packet stream, and a zero-stream input each produce an explicit `skipped:` measurement or no measurement at all — never a fabricated `0 ms` start (TIME-01 empty edge)."
    - "When two packets carry equal timestamps the derivation's internal index-view sort is stable, so `timeline.start` is byte-identical across repeated runs of the same file (TIME-01 ordering edge; the same discipline `derive_cadence` already pins with its shuffled-read-order test)."
    - "`tests/integration/timeline_findings.h` holds the ONE `count_non_pass` definition in the repository; `tests/integration/test_video_yuvj.cpp` includes it instead of carrying its own copy, so D-01's 'the counter is the one that file already uses' stays literally true."
    - "Every timeline fixture assertion declares its complete expected finding-id set and asserts `count_non_pass(report) == declared_set.size()` over the WHOLE report — all families, `info` included, never filtered by group or id (D-01/D-02)."
    - statement: "`tests/integration/test_doc03_coverage.cpp`'s count-equality REQUIRE stays green after `timeline.start` is registered, because this plan ships its triggering AND clean fixture pair in the same commit as the registry entry."
      verification: backstop
  artifacts:
    - .planning/phases/05-timeline-analysis/05-CHECK-ROSTER.md
    - src/analyzers/timeline/analyzers.h
    - src/analyzers/timeline/start_duration.cpp
    - docs/checks/timeline.start.md
    - tests/integration/timeline_findings.h
    - tests/integration/test_timeline_start_duration.cpp
  key_links:
    - "`05-CHECK-ROSTER.md` -> `src/core/checks.def`: a check id registered without a roster line is a permanent id nobody approved (CLAUDE.md: check IDs are forever)."
    - "`Pass::packet_scan`'s shared `PacketScanResult` -> `timeline_start_duration_analyzer()`'s `required_passes` -> `ProbeResults` by const reference: copying the packet array would double the footprint Phase 3's D-01 budget bounded."
    - "the global-origin measurement -> every per-stream relative value: if the origin is computed per stream rather than once per file, a whole-file shift fires on every stream and D-03's false-positive fix is silently undone."
    - "`timeline_findings.h`'s `count_non_pass` -> every timeline integration test -> DOC-04: a second, local copy of the predicate is how a fixture quietly starts counting something narrower."
  prohibitions:
    - "Never narrow the DOC-04 count by group, id or severity to make a fixture pass; narrow the FIXTURE instead (D-01's own rule, and the comment test_video_yuvj.cpp already carries)."
    - "Never register a `timeline.*` id that is absent from `05-CHECK-ROSTER.md` — check IDs are forever and a rename costs an alias plus a deprecation cycle."
    - "Never regenerate an existing line of `tests/golden/CORPUS_DIGEST.txt` on a workstation; new fixture hashes are provisional until a designated-leg run supplies them (`scripts/lint_corpus_digest_provenance.sh` is the executable guard)."
    - "Never use a GPL-gated filter or encoder in a fixture recipe; the Windows pin is a `win64-lgpl` build and a recipe that works locally can produce nothing there."
    - "Never coerce `AV_NOPTS_VALUE` to 0 anywhere in the timeline family — a fabricated origin is a confidently wrong number, which this project treats as P0."
---

<objective>
The phase's tracer: one production-quality path from a real fixture, through the packet sweep phases 3
and 4 already ship, into a registered `timeline.start` check a user can see in `mediadiff compare
--json` — plus the frozen, approved check-id roster every later plan registers from, and the shared
no-others harness DOC-04 is measured by.

Purpose: prove the riskiest architectural claim in this phase end-to-end on the first commit — that
D-03's global-origin-plus-per-stream-relative scoping actually collapses a whole-file shift into ONE
finding on a real MP4-to-TS remux — rather than discovering after nine analyzer plans that the scope
split was wrong. `timeline.start` is the thinnest check that genuinely needs D-03 and has a real,
LGPL-clean fixture pair.
Output: `05-CHECK-ROSTER.md`, `src/analyzers/timeline/{analyzers.h,start_duration.cpp}`, one
registered check with its `--explain` doc and its DOC-03 fixture pair, and the single `count_non_pass`
definition every later timeline test reads.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-timeline-analysis/05-CONTEXT.md
@.planning/phases/05-timeline-analysis/05-RESEARCH.md
@.planning/phases/05-timeline-analysis/05-PATTERNS.md
@claude_docs/04-timeline-analysis.md
@src/probe/pass.h
@src/probe/packet_scan.h
</context>

## Artifacts this phase produces

Created by THIS plan (every symbol is new unless marked *extended*):

| Kind | Symbol / path |
|---|---|
| Doc artifact | `.planning/phases/05-timeline-analysis/05-CHECK-ROSTER.md` (the 16-id frozen roster) |
| Check id | `timeline.start` |
| Header | `src/analyzers/timeline/analyzers.h` — `timeline_start_duration_analyzer()` |
| Source | `src/analyzers/timeline/start_duration.cpp` — file-local `scope_kind_for_stream`, `push_skip`, `detail::global_origin_ticks` |
| Doc | `docs/checks/timeline.start.md` |
| Test header | `tests/integration/timeline_findings.h` — `count_non_pass`, `expect_declared_set` |
| Test | `tests/integration/test_timeline_start_duration.cpp` |
| Fixtures | `tests/fixtures/timeline_start_base.mp4`, `tests/fixtures/timeline_start_base_copy.mp4`, `tests/fixtures/timeline_start_shift.ts` |
| Registration | `all_analyzers()` entry in `src/probe/orchestrator.cpp`; `libmediadiff` source/header list entries in `CMakeLists.txt`; integration-test source entry in `tests/integration/CMakeLists.txt` |
| *Extended* | `tests/integration/test_video_yuvj.cpp` (local `count_non_pass` replaced by the shared header), `tests/integration/test_doc03_coverage.cpp` (one `declared_pairs()` row, running total 61 → 62) |

<flagged_assumptions>
**A1 — Spec-less edge probe, phase-level accounting.** This phase has no `SPEC.md`, so the
deterministic edge probe ran over all 15 requirement IDs and returned **24 applicable edges, all
`unresolved` with `verification: null`**. Seventeen are authored as `must_haves.truths` across the
twelve plans in this phase; **seven are `unclassified` rows and therefore stay `unresolved`** — they
are surfaced as flagged assumptions, never auto-resolved with a backstop and never dropped. The three
TIME-01 edges (adjacency / empty / ordering) are authored above. The `unclassified` row owned by this
plan's requirement set is **TIME-03** (see A2). `17 authored + 7 flagged == 24`; the phase-level
equality is restated in the planner's return.

**A2 — TIME-03 edge row is `unclassified` and stays unresolved.** The probe could not classify an
edge category for "`timeline.start` and `timeline.duration` work, with the duration triple
cross-checked internally". The planner's reading is that the real edge is *which of the three
declared durations is missing on a given container* (Matroska frequently has no `AVStream->duration`),
which plan 05-03 handles explicitly with a per-member `absent` state rather than a fabricated value.
That reading is an assumption, not a resolved edge, and is recorded as such.

**A3 — the doc-04 tolerance rows carry two thresholds; `CheckDef` carries one.**
`src/core/tolerance.cpp:25-44` accepts the two-threshold form `"<warn>,<fail><suffix>"`, so doc 04's
"±5 ms warn / ±20 ms fail" is expressible verbatim as `"5ms,20ms"`. This was read and confirmed during
planning; the roster below depends on it.

**A4 — the 1.4 s MPEG-TS muxer delay.** `05-CONTEXT.md` records it as a real, reproducible artifact of
the TS muxer's default `-muxdelay`, not a fixture quirk. The tracer fixture pair is built on it. If the
pinned generator produces a different delay, Task 2's assertion fails loudly with the measured number
rather than silently passing — the expected value is asserted as "non-zero and identical across every
stream", not as a hardcoded 1.4 s.
</flagged_assumptions>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Approve the Phase-5 `timeline.*` check-id roster (ids are forever)</name>
  <files>.planning/phases/05-timeline-analysis/05-CHECK-ROSTER.md</files>
  <decision>Approve the full 16-id Phase-5 `timeline.*` roster, its five additions beyond doc 04's literal table rows, and the two snapshot-contract shapes D-04 and D-10 lock, before any plan in this phase registers a check into `src/core/checks.def`.</decision>
  <context>
CLAUDE.md: "Check IDs are forever: additions fine, renames only via alias + deprecation." `05-CONTEXT.md`
rates **D-04 one-way** ("check IDs are forever; a rename needs an alias plus a deprecation cycle") and
**D-10 one-way** ("the stored shape is the snapshot and `--json` contract"). Both are check-id /
stored-shape decisions, so this single roster gate is where they are approved — the same shape Phase 3
(`03-CHECK-ROSTER.md`, 27 ids) and Phase 4 (`04-CHECK-ROSTER.md`, 31 ids) both used. Ten plans in this
phase register from this roster; approving it once here means no later plan re-opens an id spelling.

Write the approved result to `05-CHECK-ROSTER.md` in `04-CHECK-ROSTER.md`'s format.

**Proposed roster (16 ids)** — `group` is `timeline` for every row:

| id | semantic | unit | value_kind | severity | tolerance | plan |
|---|---|---|---|---|---|---|
| `timeline.start` | tol | ms | rational | fail | `"5ms,20ms"` (+profile_tolerance strict_bitexact/remux = `"1ms"`) | 05-01 |
| `timeline.duration` | tol | ms | rational | fail | `"20ms,40ms"` | 05-04 |
| `timeline.duration.coherence` | state | none | string | info | — (flagged_values = `container_vs_stream`, `container_vs_computed`, `stream_vs_computed`) | 05-04 |
| `timeline.dts_monotonic` | tol | count | int64 | fail | `"0"` | 05-05 |
| `timeline.pts_unique` | tol | count | int64 | fail | `"0"` | 05-05 |
| `timeline.gaps` | span | ms | span_list | fail | — | 05-06 |
| `timeline.wrap_events` | state | none | string | info | — (flagged_values = `ts_33bit_wrap`) | 05-06 |
| `timeline.discontinuities` | span | ms | span_list | fail | — | 05-07 |
| `timeline.discontinuities.flagged` | span | ms | span_list | info | — | 05-07 |
| `timeline.jitter` | tol | ms | rational | fail | `"0.5ms,2ms"` | 05-08 |
| `timeline.vfr_profile` | dist | percent | histogram | warn | `"2%"` | 05-08 |
| `timeline.av_offset` | tol | ms | rational | fail | `"5ms,20ms"` | 05-09 |
| `timeline.av_drift` | tol | ms_per_min | rational | fail | `"0.2ms/min"` | 05-10 |
| `timeline.av_drift.pattern` | exact | none | string | fail | — | 05-10 |
| `timeline.timecode` | presence | none | string | info | — | 05-11 |
| `timeline.timecode.value` | exact | none | string | info | — | 05-11 |

**Five ids are additions beyond doc 04's eleven table rows.** Each is forced by the same constraint
Phase 3 and Phase 4 both already ruled on: a `CheckDef` carries exactly ONE `semantic` and ONE status,
so a design-doc row declaring two comparison behaviours becomes two ids.

  - `timeline.duration.coherence` — doc 04's `timeline.duration` row declares BOTH a pairwise
    cross-file tolerance AND an "internal triple disagreement > 1 frame raises an `info` incoherence
    note even when both files share it". A shared incoherence has no delta to hang on the tolerance
    check, so it becomes its own `state`-semantic `info` id — exactly `video.hdr.coherence`'s shape and
    Phase 4 D-10's precedent, which `05-CONTEXT.md`'s Discretion section names explicitly.
  - `timeline.wrap_events` — doc 04 §1.2: "Wrap events themselves are recorded (`info`) — a candidate
    that wraps where baseline didn't usually means a start-offset change upstream." Same reasoning:
    visible when both sides wrap, no delta to hang on another check. `05-CONTEXT.md`'s Discretion
    section says TS wrap events "follow the same reasoning if they need to be visible when both sides
    wrap" — this roster says they do.
  - `timeline.discontinuities.flagged` — doc 04 §2's `timeline.discontinuities` row declares TWO
    statuses on one row: "TS: packets under `discontinuity_indicator=1` are *flagged* structure →
    `info`, unflagged → gating". A single id carries a single severity, so the flagged spans become
    their own `info` id and the gating id carries only the unflagged spans. This is verbatim the split
    Phase 3 already shipped for `container.ts.cc_errors` versus `container.ts.cc_discontinuities`, and
    `src/core/checks.def`'s own record of that decision reads: "doc 02 section 5's
    `container.ts.cc_errors` row states flagged resets are 'reported separately as `info`'; a separate
    status on the same id is not representable, so it becomes its own id."
  - `timeline.timecode.value` — doc 04 §2's `timeline.timecode` row asks for "presence + start value"
    under a single `presence` semantic, but `src/compare/presence.cpp`'s own header states the
    resolution this project already adopted in Phase 4: "a value-level comparison, if wanted, is a
    separate check on the same extraction". `timeline.timecode` keeps presence; `timeline.timecode.value`
    compares the rendered SMPTE string exactly, including the drop-frame punctuation, so a start
    timecode that moves from `00:00:10:00` to `00:00:20:00` is actually reported. This is verbatim the
    split Phase 4 shipped for `video.hdr.mdcv` versus `video.hdr.mdcv.luminance`.
  - `timeline.av_drift.pattern` — **D-04, locked and one-way.** `timeline.av_drift` compares the rate
    in ms/min; `timeline.av_drift.pattern` compares the class exactly. Listed here for spelling
    confirmation only, not for re-litigation.

**Four Discretion items are decided in this roster and need confirming:**

  - **`timeline.jitter` is ONE id, not two.** σ is the compared value; max|dev| rides in evidence.
    `05-CONTEXT.md` leaves this to the planner "bearing in mind D-01 counts each firing separately" —
    two ids would make a single jittery stream report two non-pass findings, which is exactly the
    noise D-02's declared-set discipline exists to keep honest.
  - **Primary-stream selection** for `av_offset`/`av_drift`: the first video stream that is not an
    attached picture, with one measurement per audio stream. No audio, or no video, produces an
    explicit `skipped:insufficient_data` rather than silence (`skipped != pass` is load-bearing).
  - **`timeline.gaps`/`timeline.discontinuities` stream scope**: every stream carrying timestamps,
    EXCEPT `Scope::Kind::subtitle`. `05-RESEARCH.md` Open Question 3 flags that a subtitle track's
    intentionally-sparse PTS pattern would trigger false-positive gaps under a 2x-nominal rule tuned
    for audio/video, and a false positive is P0 here. The exclusion is recorded in
    `docs/checks/timeline.gaps.md` rather than left implicit.
  - **No S12M-specific id.** `05-RESEARCH.md` verified against the linked FFmpeg 8.1 that
    `AV_PKT_DATA_S12M_TIMECODE` is set only by `libavdevice/decklink_dec.cpp`, which this build does
    not link, and that MPEG-2 GOP timecode is decode-only. A separately-triggerable S12M id could never
    have a DOC-03 trigger fixture, so `timeline.timecode` covers `tmcd` and reports
    `skipped:requires_decode` for the other two sources.

**Two tolerance spellings differ from doc 04's literal wording and need confirming:**

  - **`timeline.duration` is `"20ms,40ms"`, not "±1 frame".** Doc 04 says "±1 frame fail". A
    frame-derived threshold is frame-rate-dependent, which makes the same delta gate differently on two
    files that differ only in rate — a data-dependent threshold, which D-08 rules out for exactly this
    class of reason. 40 ms is one frame at 25 fps and is a fixed, documented constant. If you prefer
    the literal frame form, `unit = "frames"` with the duration expressed in frames is the alternative,
    and the value shape changes with it.
  - **`timeline.dts_monotonic`/`timeline.pts_unique` are `tol`/`count` with tolerance `"0"`, not
    `exact`.** Doc 04 says "count `exact 0` · fail". A zero-magnitude absolute tolerance behaves
    identically to exact equality while leaving `--tol timeline.dts_monotonic=2` meaningful for a
    pipeline with known, accepted noise — the identical reasoning Phase 4 recorded for
    `video.frame_count`'s `"0frames"`.
  </context>
  <options>
    <option id="approve-as-proposed">
      <name>Approve the roster exactly as listed (16 ids)</name>
      <pros>Unblocks every downstream plan in one decision; the five additions are the same one-semantic-per-id consequence Phases 3 and 4 already ruled on; every id gets a real `--explain` doc and a real DOC-03 fixture pair in the plan that registers it.</pros>
      <cons>Ships 16 permanent ids, five of which a reader of `claude_docs/04-timeline-analysis.md` cannot find as a literal table row.</cons>
    </option>
    <option id="approve-with-edits">
      <name>Approve with named spelling, severity, tolerance or scope edits</name>
      <pros>Any id spelling or attribute is correctable now at zero cost rather than via an alias plus a deprecation cycle later.</pros>
      <cons>Every edit must be stated precisely; plans 05-04 through 05-11 read this file as the single source of truth, so an unrecorded edit silently reverts.</cons>
    </option>
    <option id="collapse-additions">
      <name>Reject the five additions and fold each back into its doc-04 parent id</name>
      <pros>Exactly matches doc 04's table row count; no id exists that a reader of the design doc cannot locate.</pros>
      <cons>The duration-triple incoherence note and the TS wrap note both become invisible when both files share them (there is no delta to report), and D-04's locked rate/pattern split would have to be reopened — a locked, one-way decision.</cons>
    </option>
  </options>
  <resume-signal>Reply `approve-as-proposed`, or `approve-with-edits` followed by the exact id/attribute changes, or `collapse-additions`. The chosen result is written verbatim into `05-CHECK-ROSTER.md` before any registration happens.</resume-signal>
  <read_first>
    - `.planning/phases/04-video-analysis/04-CHECK-ROSTER.md` in full — the file format, the per-id attribute table, and how Phase 4 recorded the attribute decisions folded into its roster.
    - `src/core/checks.def` lines 1-60 — the header's own account of how the seed roster was approved, the `[check.profile_severity]` / `[check.profile_tolerance]` sub-table syntax, and the `flagged_values` requirement for the `state` semantic.
    - `src/core/checks.def` at `video.hdr.coherence` (line ~1036) — the exact `state` / `info` / `flagged_values` block shape the two new coherence-style ids copy.
    - `src/core/tolerance.cpp` lines 14-50 — the tolerance grammar, including the two-threshold `"<warn>,<fail><suffix>"` form and the `count`-unit suffix rule.
    - `tools/gen_registry.py` lines 114-125 — the `SEMANTICS` / `VALUE_KINDS` / `UNITS` / `SEVERITIES` / `PROFILES` vocabularies every roster row must spell correctly.
    - `claude_docs/04-timeline-analysis.md` §2 — the eleven normative check rows this roster is derived from.
    - `.planning/phases/05-timeline-analysis/05-CONTEXT.md` D-04, D-08, D-10 and the whole `### Claude's Discretion` block.
    - `.planning/phases/05-timeline-analysis/05-RESEARCH.md` Pattern 4 and Pitfall 6 — why no S12M id exists.
  </read_first>
  <acceptance_criteria>
    - `.planning/phases/05-timeline-analysis/05-CHECK-ROSTER.md` exists, lists every approved id with all six attributes, and records the approving decision verbatim including any edits.
    - Every id in the roster is spelled with `group = "timeline"` and a `semantic`/`unit`/`value_kind`/`severity` drawn from `tools/gen_registry.py`'s own vocabularies.
    - The file names which plan registers each id, so a later plan registering an unlisted id is visible as a diff against this file.
  </acceptance_criteria>
  <done>The roster is approved and committed; no `timeline.*` id may be written to `src/core/checks.def` by any plan in this phase unless it appears in this file.</done>
  <reversibility rating="one-way">Check IDs are forever (PROJECT.md). This gate is where D-04's rate/pattern split and D-10's stored snapshot shape are approved; after this, a change costs an alias plus a deprecation cycle.</reversibility>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: `timeline.start` end to end — one global origin, per-stream relative values, one finding for a whole-file shift</name>
  <files>src/analyzers/timeline/analyzers.h, src/analyzers/timeline/start_duration.cpp, src/probe/orchestrator.cpp, src/core/checks.def, docs/checks/timeline.start.md, CMakeLists.txt, scripts/gen_corpus.sh, tests/golden/CORPUS_DIGEST.txt, tests/golden/CORPUS_DIGEST_PROVISIONAL.txt, tests/integration/test_doc03_coverage.cpp</files>
  <read_first>
    - `src/analyzers/video/gop.cpp` in full — the file-local `push_skip`, the file-local `scope_kind_for_stream` / `compute_stream_scopes` pair, the `#if defined(__GNUC__) && !defined(__clang__)` `-Wmaybe-uninitialized` bracket and its own comment on verifying the suppression is actually needed before copying it, and the order in which `partial_scan` is checked ahead of every other skip reason.
    - `src/analyzers/video/analyzers.h` in full — the named-accessor convention and the doc-comment density (owning requirement, shaping decision, `Scope::Kind`, `required_passes`) every declaration carries.
    - `src/probe/pass.h` lines 90-165 — `ProbeResults`' "held by value in `std::optional`, handed out only as `const ProbeResults&`, never copied" contract, and `AnalyzerSpec`'s four fields.
    - `src/probe/packet_scan.h` lines 96-160 — `PacketRecord`'s field list, the `AV_NOPTS_VALUE`-preserved-verbatim contract, `StreamPacketScan::tb`, and the `partial` flag's meaning.
    - `src/core/model.h` lines 39-90 — the full `SkipReason` vocabulary (`no_timing_data`, `insufficient_data`, `partial_scan` are the three this task uses) and `Scope::Kind`, which already carries `global`.
    - `src/core/value.h` lines 30-60 — `RationalValue{num, den, tb}` and `Absent`.
    - `src/core/rational.h` — `detail::checked_sub` / `checked_add` and the tick-comparison helpers; every subtraction in this task goes through them.
    - `src/core/checks.def` at `video.frame_rate.measured` (line ~652) — the comment-block-then-`[[check]]` shape, and the `[check.profile_tolerance]` sub-table form.
    - `docs/checks/video.frame_rate.measured.md` in full — the three required level-2 headings and the `### Accept` / `### Tune` / `### Silence` level-3 split `tools/gen_registry.py` enforces.
    - `scripts/gen_corpus.sh` lines 90-130 — the recipe-block shape, the literal `$OUT_DIR/<name>` token convention `scripts/check_corpus.sh` extracts mechanically, and the never-GPL convention.
    - `scripts/ffmpeg_pin.json` — confirm the Windows pin is the `win64-lgpl` build before choosing any filter.
    - `tests/integration/test_doc03_coverage.cpp` lines 1-70 and its `declared_pairs()` table — the four `CoveragePair` fields, the running-total-in-comment convention, and the count-equality `REQUIRE`.
    - `CMakeLists.txt` lines 160-245 — the `libmediadiff` source list and header list new files join.
    - `.planning/phases/05-timeline-analysis/05-PATTERNS.md` section `src/analyzers/timeline/start_duration.cpp` — the scope-derivation and mechanism-evidence-not-re-adjustment patterns.
  </read_first>
  <behavior>
    - Test 1: on a two-stream MP4, `timeline.start` emits exactly one measurement at `Scope{Kind::global, 0}` and one per timestamped stream — total measurement count equals 1 + (number of streams carrying at least one real timestamp).
    - Test 2: the global measurement's value equals the minimum first-presentation PTS across all streams, converted to ms as an exact `RationalValue` (never pre-divided, never a `double`).
    - Test 3: each per-stream measurement's value equals that stream's first presentation PTS minus the global origin; for the stream that owns the earliest PTS this is exactly zero.
    - Test 4: comparing `timeline_start_base.mp4` against `timeline_start_shift.ts` (the same content stream-copied to MPEG-TS, which the TS muxer delays) produces a non-pass `timeline.start` finding at GLOBAL scope, and every per-stream `timeline.start` finding is `pass` — one cause, one finding (D-03).
    - Test 5: comparing `timeline_start_base.mp4` against `timeline_start_base_copy.mp4` produces `timeline.start` at `pass` on every scope.
    - Test 6: a stream whose packets all carry `AV_NOPTS_VALUE` emits `SkipReason::no_timing_data` with `Absent{}` — never a `0`.
    - Test 7: when `PacketScanResult::partial` is set, every `timeline.start` measurement emits `SkipReason::partial_scan` with no value, ahead of every other skip reason.
    - Test 8: running the same file twice produces byte-identical `timeline.start` measurements, including when two packets share an identical PTS (the index-view sort must be stable).
  </behavior>
  <action>
    Create `src/analyzers/timeline/analyzers.h` mirroring `src/analyzers/video/analyzers.h` verbatim in
    shape and comment density: one `const AnalyzerSpec&`-returning declaration per analyzer file, each
    with a doc comment naming the owning TIME-NN requirement, the shaping D-NN decision, its
    `Scope::Kind` usage and its `required_passes`. This task declares only
    `timeline_start_duration_analyzer()`; later plans append siblings.

    Create `src/analyzers/timeline/start_duration.cpp`. Copy `gop.cpp`'s file-local `push_skip` and
    `scope_kind_for_stream` / `compute_stream_scopes` pair verbatim — this project's convention is a
    file-local copy, never a shared export, and each copy cites its sibling in a comment. Verify on the
    current compiler whether the `-Wmaybe-uninitialized` pragma bracket is actually needed in THIS file
    before copying it; `color.cpp`'s own comment records that an unneeded suppression was removed rather
    than reflexively carried, and `scripts/lint_pragma_scope.sh` is the gate.

    Implement D-03 in a file-local `detail::` helper: walk every stream's `packets`, take the first
    record whose `pts` is not `AV_NOPTS_VALUE` in presentation order (sort a LOCAL index view, never the
    caller's array — `cadence.cpp`'s own discipline), rescale each candidate to a common comparison
    basis with `core/rational.h`'s checked helpers, and take the minimum as the file's global origin.
    Emit ONE measurement at `Scope{Kind::global, 0}` carrying that origin, then ONE measurement per
    timestamped stream carrying `first_pts - origin`. Both values are `RationalValue` in the check's
    declared `ms` unit, held as an exact num/den pair — never divided, never converted to a floating-point
    type anywhere in this file. State D-03's reasoning in a comment above the helper: a whole-file shift is
    one finding, a single stream moving is one finding on that stream.

    Skip-reason order, stated in the code in this order: `partial_scan` first whenever
    `PacketScanResult::partial` or that stream's own `partial` is set (Phase 3 D-02 — a value from a
    truncated sweep is a confidently wrong number); then `no_timing_data` when the stream carries no
    real timestamp on either axis; then `insufficient_data` for any other case where a real answer
    cannot be computed, including an overflow in the rescale arithmetic. Never emit a value alongside a
    skip reason.

    Evidence: cite the container mechanism, never re-derive an adjustment from it. Record the raw first
    PTS in native ticks, the stream timebase, the axis used, and — when `ProbeResults::bmff` or
    `::ebml` is populated — the `elst[0]` entry or `CodecDelay` value as a mechanism string, exactly as
    `05-PATTERNS.md` prescribes. `05-RESEARCH.md` Pitfall 5 is explicit that reading the
    edit list's own media-time field and applying it arithmetically would re-apply libav's own
    edit-list adjustment a second time; cite the mechanism, do not apply it.

    Register `timeline_start_duration_analyzer()` in `all_analyzers()` in `src/probe/orchestrator.cpp`,
    after the video analyzers, with `required_passes = {Pass::demux_header, Pass::packet_scan}` and
    `scope = ContainerFamily::other` (a timeline check applies to every container). Add
    `src/analyzers/timeline/start_duration.cpp` to `libmediadiff`'s source list and
    `src/analyzers/timeline/analyzers.h` to its header list in `CMakeLists.txt`.

    Register `timeline.start` in `src/core/checks.def` exactly as the approved roster declares it, with
    a comment block above it citing this plan, TIME-01/TIME-03, D-03, and the skip-reason vocabulary
    this check uses. Write `docs/checks/timeline.start.md` with all three required level-2 headings and
    the three level-3 sub-headings; the `### Tune` section must explain the global-versus-per-stream
    split in user terms (a whole-file shift is one finding at global scope) and name the
    `strict_bitexact`/`remux` one-tick profile tolerance.

    Add three `gen_corpus.sh` recipe blocks in the established shape, each with a comment citing this
    plan and the LGPL-only convention: `$OUT_DIR/timeline_start_base.mp4` from
    `-f lavfi -i "testsrc2=size=320x240:rate=25:duration=4" -f lavfi -i "sine=frequency=440:duration=4"
    -c:v mpeg4 -c:a aac -flags +bitexact -fflags +bitexact`; `$OUT_DIR/timeline_start_base_copy.mp4` as
    a `cp` of the first (mirroring `tracer_a_copy.mp4`'s existing precedent); and
    `$OUT_DIR/timeline_start_shift.ts` as a `-c copy` remux of the first to MPEG-TS, which carries the
    muxer's own default delay on every stream. Use no filter that is not confirmed present in the
    `win64-lgpl` Windows pin.

    New fixture hashes are PROVISIONAL: append the three new names to
    `tests/golden/CORPUS_DIGEST_PROVISIONAL.txt` under its existing marker convention and regenerate
    `tests/golden/CORPUS_DIGEST.txt` — but do NOT rewrite any pre-existing hash line.
    `scripts/lint_corpus_digest_provenance.sh` is the executable guard and must stay green; plan 05-12
    transcribes the designated-leg values.

    Add the `timeline.start` entry to `declared_pairs()` in `tests/integration/test_doc03_coverage.cpp`
    — trigger `timeline_start_base.mp4` / `timeline_start_shift.ts`, clean `timeline_start_base.mp4` /
    `timeline_start_base_copy.mp4` — and increment the running-total comment from sixty-one to
    sixty-two. Verify the pair empirically against the real binary before committing, exactly as that
    file's own header requires.
  </action>
  <verify>
    <automated>bash scripts/gen_corpus.sh &amp;&amp; bash scripts/check_corpus.sh &amp;&amp; bash scripts/lint_corpus_digest_provenance.sh &amp;&amp; cmake --build build/x64-linux 2>&amp;1 | tail -5 &amp;&amp; ctest --test-dir build/x64-linux --output-on-failure</automated>
    <fails_when>Non-zero exit; or the build prints a warning-as-error from `start_duration.cpp`; or `check_corpus.sh` reports any of the three new fixture names missing or zero-length; or `lint_corpus_digest_provenance.sh` prints a rewritten-pre-existing-line diagnostic; or `integration.doc03_coverage` lists `timeline.start` under `uncovered_no_pair` / `uncovered_trigger_did_not_fire`; or the count-equality REQUIRE in that test reports `verified_count != registry.size()`.</fails_when>
  </verify>
  <precondition>The pinned generator ffmpeg is installed at `.ffmpeg-pinned/&lt;runner-key&gt;/ffmpeg` (run `bash scripts/install_pinned_ffmpeg.sh` if absent) or a system ffmpeg &gt;= 6.1 is on `PATH`; all three `timeline_start_*` recipes depend on it.</precondition>
  <acceptance_criteria>
    - **Locally provable** — `./build/x64-linux/mediadiff compare tests/fixtures/timeline_start_base.mp4 tests/fixtures/timeline_start_shift.ts --profile remux --json` shows exactly one `timeline.start` finding whose status is neither `pass` nor `skipped`, and its `scope.kind` is `global`.
    - **Locally provable** — the same command shows every per-stream `timeline.start` finding (`scope.kind` of `video` or `audio`) at `"pass"`.
    - **Locally provable** — `./build/x64-linux/mediadiff compare tests/fixtures/timeline_start_base.mp4 tests/fixtures/timeline_start_base_copy.mp4 --profile remux --json` shows every `timeline.start` finding at `"pass"`.
    - **Locally provable** — `./build/x64-linux/mediadiff inspect tests/fixtures/timeline_start_base.mp4 --json` run twice produces byte-identical output (`diff <(...) <(...)` is empty).
    - **Locally provable** — `./build/x64-linux/mediadiff explain timeline.start` prints all three sections.
    - **Locally provable** — `grep -v '^ *//' src/analyzers/timeline/start_duration.cpp | grep -c 'double\|float\|std::sqrt'` reports `0`.
    - **Locally provable** — `grep -c 'av_read_frame' src/analyzers/timeline/start_duration.cpp` reports `0`: the analyzer reads the shared array and opens nothing.
    - **Locally provable** — `bash scripts/lint_check_id_strings.sh`, `bash scripts/lint_bash4_builtins.sh` and `bash scripts/lint_pragma_scope.sh` all exit zero.
  </acceptance_criteria>
  <done>A real fixture pair flows through the existing packet sweep into a registered `timeline.start` check a user can see in `mediadiff compare --json`; a whole-file TS-remux shift reports ONE global finding and zero per-stream findings; its `--explain` doc exists; and DOC-03's count-equality gate is still green.</done>
  <reversibility rating="costly">D-03's global-plus-relative scoping and its values enter committed snapshots and the `--json` contract; changing the shape later re-baselines every stored fingerprint that carries a `timeline.start`.</reversibility>
</task>

<task type="auto">
  <name>Task 3: The DOC-04 no-others harness — one `count_non_pass`, declared finding sets, proven on the tracer pair</name>
  <files>tests/integration/timeline_findings.h, tests/integration/test_timeline_start_duration.cpp, tests/integration/test_video_yuvj.cpp, tests/integration/CMakeLists.txt</files>
  <read_first>
    - `tests/integration/test_video_yuvj.cpp` lines 80-140 — `count_non_pass` verbatim (the predicate D-01 names as "the one that file already uses"), its own comment about narrowing the fixture rather than weakening the assertion, and the `TEST_CASE` naming style.
    - `tests/integration/cli_harness.h` in full — `run_cli` / `CliResult`, and the header-only convention a shared integration-test helper follows.
    - `tests/integration/CMakeLists.txt` lines 1-70 and 180-200 — the `add_executable` source list, the per-file comment convention naming the owning plan and requirement, and `catch_discover_tests(... TEST_PREFIX "integration.")`, which makes every ctest name `integration.<TEST_CASE name>`.
    - `tests/integration/test_container_ts.cpp` — a real example of a multi-check integration test built on `run_cli` plus a parsed `nlohmann::ordered_json` report.
    - `.planning/phases/05-timeline-analysis/05-CONTEXT.md` D-01 and D-02 in full — the counter's scope (all families, `info` included, never filtered) and the declared-set rule (every member beyond the first carries a written causal reason).
    - `support/fixture_paths.h` (via `tests/integration/test_doc03_coverage.cpp`'s include) — how a test resolves `tests/fixtures/<name>`.
  </read_first>
  <behavior>
    - Test 1: `count_non_pass` over a synthetic report counts every finding whose status is neither `pass` nor `skipped`, across every group including `info`-severity findings, and returns zero for an all-`pass` report.
    - Test 2: `expect_declared_set(report, {...})` fails when the report contains a non-pass finding whose id is absent from the declared set, naming the unexpected id in the failure message.
    - Test 3: `expect_declared_set` fails when a declared id is absent from the report's non-pass findings, naming the missing id.
    - Test 4: the tracer pair `timeline_start_base.mp4` vs `timeline_start_shift.ts` under `--profile remux` declares its complete expected set and `count_non_pass(report)` equals that set's size exactly.
    - Test 5: the clean pair `timeline_start_base.mp4` vs `timeline_start_base_copy.mp4` declares the empty set and `count_non_pass(report)` is `0`.
  </behavior>
  <action>
    Create `tests/integration/timeline_findings.h` as a header-only helper following `cli_harness.h`'s
    convention. Move `count_non_pass` into it VERBATIM from `test_video_yuvj.cpp` — same body, same
    status predicate, same comment about narrowing the fixture rather than the assertion — and make
    `test_video_yuvj.cpp` include the header and delete its local copy, so the repository holds exactly
    one definition and D-01's "the counter is the one `test_video_yuvj.cpp` already uses" stays
    literally true rather than becoming two drifting copies.

    Add `expect_declared_set(const nlohmann::ordered_json& report, const std::vector&lt;std::string&gt;&amp;
    declared_ids)`: it asserts `count_non_pass(report) == declared_ids.size()` AND that every declared
    id appears among the report's non-pass findings, reporting unexpected and missing ids by name in
    the failure message. The header's own comment must state D-02's rule in the project's words: a
    declared set may hold more than one id when ONE cause legitimately moves several facts, and every
    member beyond the first carries a written causal reason at its call site. The helper must never
    filter by group, id or severity — a helper that could filter is the mechanism by which the gate
    quietly stops gating.

    Create `tests/integration/test_timeline_start_duration.cpp` and register it in
    `tests/integration/CMakeLists.txt`'s source list with the established per-file comment naming this
    plan and TIME-01/TIME-03/DOC-04. Name every `TEST_CASE` in the file with the literal prefix
    `timeline_start_duration - ` so `ctest -R "integration\.timeline_start_duration"` selects exactly
    this file's cases (`TEST_PREFIX "integration."` makes the ctest name
    `integration.&lt;TEST_CASE name&gt;`; a filter that matches nothing exits ZERO and reports "No tests
    were found", which is why the prefix matters).

    Write the tracer pair's assertion using `expect_declared_set`. The MP4-to-TS declared set is
    expected to hold `timeline.start` at global scope plus any cross-container finding the remux
    legitimately moves; each additional member carries an inline comment giving its causal reason, per
    D-02. If the count does not match, NARROW THE FIXTURE — never add a filter, never relax the
    predicate. Record in the test's own header comment that this is the rule.
  </action>
  <verify>
    <automated>cmake --build build/x64-linux 2>&amp;1 | tail -5 &amp;&amp; ctest --test-dir build/x64-linux -R "integration\.(timeline_start_duration|video_yuvj)" --output-on-failure</automated>
    <fails_when>Non-zero exit; or the output contains "No tests were found" (the regex selected nothing, which ctest reports with exit code 0 — a silently-passing gate); or fewer than two distinct test names appear in the run list; or a `count_non_pass` mismatch is reported naming an unexpected or missing finding id.</fails_when>
  </verify>
  <acceptance_criteria>
    - **Locally provable** — `grep -rc 'std::size_t count_non_pass' tests/integration/ | grep -v ':0$'` names exactly one file, `tests/integration/timeline_findings.h`.
    - **Locally provable** — `grep -c 'timeline_findings.h' tests/integration/test_video_yuvj.cpp` reports at least `1`.
    - **Locally provable** — `ctest --test-dir build/x64-linux -N -R "integration\.timeline_start_duration"` lists at least one test name (an empty list means the prefix convention was not followed).
    - **Locally provable** — `grep -c 'group\|severity' tests/integration/timeline_findings.h` shows no filtering logic on either token inside the `count_non_pass` body (read the body; the predicate compares `status` only).
  </acceptance_criteria>
  <done>One `count_non_pass` definition exists repository-wide, `expect_declared_set` enforces D-02's declared-set rule over the whole report, and both the tracer pair and the clean pair assert through it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file bytes → `DemuxSession`/libav | every timestamp this plan reads originates in an attacker-controllable container |
| `PacketScanResult` → `timeline.start` arithmetic | untrusted int64 tick values cross into subtraction and rescale arithmetic |
| fixture recipe → committed corpus digest | a recipe change silently alters bytes that a byte-exact golden asserts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Denial of Service | `detail::global_origin_ticks` rescale of crafted extreme PTS values | medium | mitigate | every subtraction and rescale goes through `core/rational.h`'s `detail::checked_sub`/`checked_mul`; a failed check yields `SkipReason::insufficient_data`, never a wrapped value or a crash — `cadence.cpp`'s own established precedent |
| T-05-02 | Tampering | a crafted file whose `AV_NOPTS_VALUE` sentinels are coerced to 0, fabricating an origin at zero and hiding a real start-time change | high | mitigate | the sentinel is preserved verbatim by `PacketScan` and tested for explicitly; Test 6 asserts `SkipReason::no_timing_data` with `Absent{}`, and the no-float acceptance criterion forbids any coercion path |
| T-05-03 | Information Disclosure | mechanism evidence echoing raw container strings (`elst[0]…`) into report output | low | accept | `scripts/lint_control_bytes.sh` and the existing `sanitize_for_display` choke point already govern every rendered string; no new rendering path is introduced by this plan |
| T-05-04 | Tampering | a workstation-regenerated `CORPUS_DIGEST.txt` line replacing a designated-leg hash, silently disabling a byte-exact gate | high | mitigate | new names go to `CORPUS_DIGEST_PROVISIONAL.txt` only; `scripts/lint_corpus_digest_provenance.sh` runs in this task's `<verify>` and fails on any rewritten pre-existing line |
| T-05-SC | Tampering | dependency supply chain | low | accept | this phase adds no npm/pip/cargo/vcpkg package (`05-RESEARCH.md` § Package Legitimacy Audit: "not applicable — this phase introduces no new external package"), so the package-legitimacy gate has nothing to audit; the only new system package in the phase is `valgrind`, introduced and dispositioned in plan 05-11 |
</threat_model>

<verification>
- `bash scripts/gen_corpus.sh && bash scripts/check_corpus.sh && bash scripts/lint_corpus_digest_provenance.sh && bash scripts/lint_bash4_builtins.sh`
- `cmake --build build/x64-linux 2>&1 | tail -5 && ctest --test-dir build/x64-linux --output-on-failure`
- `./build/x64-linux/mediadiff explain timeline.start`
</verification>

<success_criteria>
- `05-CHECK-ROSTER.md` exists, is approved, and names every `timeline.*` id this phase will register.
- `timeline.start` is registered, documented, covered by a DOC-03 trigger/clean pair, and visible in `mediadiff compare --json`.
- An MP4-to-TS remux reports ONE global `timeline.start` finding and zero per-stream ones.
- `count_non_pass` has exactly one definition repository-wide and both this plan's fixture pairs assert through `expect_declared_set`.
- The full suite is green and every corpus/provenance lint exits zero.
</success_criteria>

<output>
Create `.planning/phases/05-timeline-analysis/05-01-SUMMARY.md` when done.
</output>
