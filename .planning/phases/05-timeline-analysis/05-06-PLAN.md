---
phase: 05-timeline-analysis
plan: 06
type: execute
wave: 5
depends_on: ["05-05"]
files_modified:
  - src/analyzers/timeline/analyzers.h
  - src/analyzers/timeline/monotonic.cpp
  - src/core/checks.def
  - docs/checks/timeline.gaps.md
  - docs/checks/timeline.wrap_events.md
  - scripts/gen_corpus.sh
  - tests/golden/CORPUS_DIGEST.txt
  - tests/golden/CORPUS_DIGEST_PROVISIONAL.txt
  - tests/integration/test_timeline_structure.cpp
  - tests/integration/test_doc03_coverage.cpp
autonomous: true
requirements: [TIME-02, TIME-04, DOC-04]

estimate:
  tokens: 50000
  raw_tokens: 100000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "`timeline.gaps` reports a `span_list` of spans where the interval between consecutive presentation timestamps exceeds `max(2 x nominal, declared_duration + 1 tick)`, with nominal taken from the SHARED `derive_cadence` mode interval — never a second interval computation (doc 04 §2, PROBE-10)."
    - "Both the `2x` multiplier and the one-tick allowance are fixed named constants (D-08); neither is configurable in v1, because a detection parameter changes the measured value and would require fingerprint recording plus mismatch-skip machinery that is deliberately deferred."
    - "The span list enters the fingerprint so `compare` against a snapshot retains full fidelity for delta algebra, and each span's start/end is an exact `RationalValue` in the check's `ms` unit — never a pre-divided float."
    - "`timeline.wrap_events` fires at `info` with the flagged value `ts_33bit_wrap` when a stream's raw timestamps wrapped, EVEN WHEN BOTH FILES WRAP — it is a `state`-semantic check, so `pass` means neither side was flagged (doc 04 §1.2: 'wrap events themselves are recorded (info)')."
    - "Raw timestamps are preserved in evidence alongside the unwrapped ones on any stream that wrapped: the wrap count, the first wrapping packet index and its raw and unwrapped values are all published, so a reader can reconstruct the correction (TIME-02)."
    - "A 33-bit wrap occurring mid-file produces ZERO `timeline.gaps` spans and ZERO `timeline.dts_monotonic` violations attributable to the wrap — a wrap is unwrapped, never mistaken for a backward discontinuity (TIME-02, doc 04 §5's TS-wrap acceptance criterion)."
    - "`timeline.gaps` runs on every stream carrying timestamps EXCEPT `Scope::Kind::subtitle`, per the scope decision recorded in `05-CHECK-ROSTER.md`, and the exclusion plus its reason appears in `docs/checks/timeline.gaps.md`."
    - "A truncated packet scan emits `skipped:partial_scan` on both ids ahead of every other skip reason — a gap list from an incomplete sweep is missing exactly the gaps at the truncation point."
  artifacts:
    - docs/checks/timeline.gaps.md
    - docs/checks/timeline.wrap_events.md
    - tests/fixtures/timeline_gap.mp4
    - tests/fixtures/timeline_ts_wrap.ts
    - tests/fixtures/timeline_ts_nowrap.ts
    - tests/fixtures/timeline_ts_nowrap_copy.ts
  key_links:
    - "`derive_cadence` mode interval -> the gap rule's `nominal`: computing nominal locally would be a second interval statistic over the same array, which PROBE-10 exists to prevent."
    - "`unwrap_ts_timestamps` -> gap detection on a TS stream -> `timeline.wrap_events`: the unwrap feeds the gap analysis AND produces the wrap count the state check publishes; running gap detection on raw TS timestamps turns every wrap into a false gap."
    - "the `span_list` value -> `Fingerprint::measurements` -> snapshot round trip: doc 01 §3's delta algebra reads these spans, so a lossy or re-ordered list breaks snapshot comparison fidelity."
  prohibitions:
    - "Never compute a second interval statistic inside the timeline family; call the shared `derive_cadence`."
    - "Never make the gap multiplier or the discontinuity threshold configurable in v1 (D-08)."
    - "Never run gap detection on raw, un-unwrapped MPEG-TS timestamps."
    - "Never declare a `timeline.wrap_events` CLEAN pair whose two files both wrap — under the `state` semantic `pass` means NEITHER side flagged, so a both-wrap pair is a trigger, not a clean pair."
    - "Never narrow the DOC-04 whole-report count to make a fixture pass; narrow the fixture."
---

<objective>
`timeline.gaps` and `timeline.wrap_events`: the span list of real timeline holes, and the MPEG-TS 33-bit
wrap made visible as an `info` note rather than silently corrected — with the fixture that proves a
mid-file wrap produces no false gap and no false backward discontinuity.

Purpose: doc 04 §5 names the TS-wrap fixture's acceptance criterion directly — "assert clean unwrap and
no false discontinuity". This is the plan where plan 05-02's pure unwrap function meets real bytes, and
where the gap rule's `nominal` is proven to come from the one shared cadence derivation rather than a
second implementation.
Output: two registered checks with `--explain` docs, four TS/MP4 fixtures, and the DOC-04 declared sets.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-timeline-analysis/05-CONTEXT.md
@.planning/phases/05-timeline-analysis/05-RESEARCH.md
@.planning/phases/05-timeline-analysis/05-PATTERNS.md
@.planning/phases/05-timeline-analysis/05-CHECK-ROSTER.md
@claude_docs/04-timeline-analysis.md
@src/analyzers/timeline/unwrap.h
@src/probe/cadence.h
</context>

## Artifacts this phase produces

| Kind | Symbol / path |
|---|---|
| Check ids | `timeline.gaps`, `timeline.wrap_events` |
| Constants | `kGapNominalMultiplier` (2), `kGapDeclaredDurationSlackTicks` (1) in `src/analyzers/timeline/monotonic.cpp` |
| Evidence keys | `nominal_interval_ticks`, `gap_count`, `wrap_count`, `first_wrap_index`, `first_wrap_raw`, `first_wrap_unwrapped`, `unwrapped` |
| Docs | `docs/checks/timeline.gaps.md`, `docs/checks/timeline.wrap_events.md` |
| Fixtures | `tests/fixtures/timeline_gap.mp4`, `tests/fixtures/timeline_ts_wrap.ts`, `tests/fixtures/timeline_ts_nowrap.ts`, `tests/fixtures/timeline_ts_nowrap_copy.ts` |
| *Extended* | `src/analyzers/timeline/monotonic.cpp`, `tests/integration/test_timeline_structure.cpp`, `tests/integration/test_doc03_coverage.cpp` (running total 66 → 68) |

<flagged_assumptions>
**A1 — the `-output_ts_offset` wrap recipe is doc-prescribed but not fixture-tested upstream.** Doc 04
§5 prescribes "starting ~30 s before `2^33/90000` s so the wrap occurs mid-file"; `05-RESEARCH.md`
assumption A3 records that the crafted-timestamp recipes were license-verified but not generated. Task 1
must GENERATE the fixture and PROVE the wrap landed — read the packet timestamps back with the pinned
`ffprobe` and show a raw PTS sequence that crosses `2^33` mid-file — before any test asserts on it. A
recipe that produces a file which never wraps is a fixture that tests nothing.

**A2 — the clean pair for a `state`-semantic check must be two UNFLAGGED files.** This is why
`timeline_ts_nowrap.ts` and `timeline_ts_nowrap_copy.ts` both exist: under `src/compare/state.cpp`'s
post-WR-02 semantics `pass` means neither side was flagged, so `timeline_ts_wrap.ts` compared against a
copy of itself would be a TRIGGER, not a clean pair. Getting this backwards is how a DOC-03 clean pair
silently stops proving anything.

**A3 — the TS fixtures carry the 1.4 s muxer delay** that `05-CONTEXT.md` records as a real,
reproducible artifact. Every declared finding set involving a TS fixture accounts for it with a written
causal reason (D-02) rather than filtering it out.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: The gap and TS-wrap fixtures, with the wrap proven to have landed mid-file</name>
  <files>scripts/gen_corpus.sh, tests/golden/CORPUS_DIGEST.txt, tests/golden/CORPUS_DIGEST_PROVISIONAL.txt</files>
  <read_first>
    - `claude_docs/04-timeline-analysis.md` §5 — the gap and TS-wrap recipes and the TS-wrap acceptance criterion ("assert clean unwrap and no false discontinuity").
    - `claude_docs/04-timeline-analysis.md` §1.2 — the 33-bit domain (`2^33` ticks at 90 kHz is 95443.7 s), which fixes the `-output_ts_offset` value.
    - `scripts/gen_corpus.sh` lines 90-140 and the never-GPL convention at lines 96-107.
    - `scripts/ffmpeg_pin.json` — the `win64-lgpl` Windows pin; `setts` and `-output_ts_offset` are both confirmed LGPL-clean in `05-RESEARCH.md`, `mpdecimate` is not.
    - `scripts/resolve_pinned_ffmpeg.sh` — generator and `ffprobe` resolution.
    - `tests/golden/CORPUS_DIGEST_PROVISIONAL.txt` and `scripts/lint_corpus_digest_provenance.sh` — the provisional-listing convention and the never-rewrite rule.
    - `.planning/phases/05-timeline-analysis/05-CONTEXT.md` § Specific Ideas — the note that the 1.4 s MPEG-TS muxer delay is real and rides on every TS fixture in this phase.
  </read_first>
  <action>
    Add four `gen_corpus.sh` recipe blocks in the established shape, each with a comment citing this plan,
    doc 04 §5, and the LGPL-only convention.

    `$OUT_DIR/timeline_gap.mp4`: the `timeline_start_base.mp4` source and encode, with a `setts`
    expression that advances the presentation timestamps of every packet after a chosen index by roughly
    three nominal frame intervals, creating ONE span whose interval exceeds `2 x nominal`. The comment
    must state why three intervals and not two — the rule is strictly greater than the maximum of
    `2 x nominal` and `declared duration + 1 tick`, so a two-interval hole sits exactly on the boundary
    and would make the fixture's verdict depend on the tie-break.

    `$OUT_DIR/timeline_ts_wrap.ts`: a `-c copy` MPEG-TS remux of `timeline_start_base.mp4` with
    `-output_ts_offset` set so the 90 kHz timeline crosses `2^33` ticks mid-file — approximately two
    seconds before `95443.7` s for a four-second source. Write the offset as an explicit value with a
    comment showing the arithmetic (`2^33 / 90000 == 95443.7...` seconds), never as an unexplained
    magic number.

    `$OUT_DIR/timeline_ts_nowrap.ts`: the same `-c copy` remux with NO output timestamp offset, and
    `$OUT_DIR/timeline_ts_nowrap_copy.ts` as a `cp` of it — together these are the `state`-semantic clean
    pair (see A2). Mirror `tracer_a_copy.mp4`'s existing `cp` precedent.

    All four with `-flags +bitexact -fflags +bitexact`.

    PROVE the wrap landed: run the pinned `ffprobe -show_packets -select_streams v:0` over
    `timeline_ts_wrap.ts`, confirm the raw PTS sequence rises toward `2^33` and then restarts near zero
    mid-file, and paste the surrounding packet lines into this plan's SUMMARY. Do the same for
    `timeline_gap.mp4`, showing the single enlarged interval. Also confirm `timeline_ts_nowrap.ts` does
    NOT wrap — a clean pair that silently wraps would invert the check's meaning.

    Append all four names to `tests/golden/CORPUS_DIGEST_PROVISIONAL.txt` and regenerate
    `tests/golden/CORPUS_DIGEST.txt`; never rewrite a pre-existing hash line.
  </action>
  <verify>
    <automated>bash scripts/gen_corpus.sh &amp;&amp; bash scripts/check_corpus.sh &amp;&amp; bash scripts/lint_bash4_builtins.sh &amp;&amp; bash scripts/lint_corpus_digest_provenance.sh</automated>
    <fails_when>Non-zero exit; or `check_corpus.sh` reports any of the four new fixture names missing or zero-length; or `lint_bash4_builtins.sh` names a bash-4 builtin in the new blocks; or `lint_corpus_digest_provenance.sh` prints a rewritten-pre-existing-line diagnostic.</fails_when>
  </verify>
  <precondition>The pinned generator ffmpeg AND ffprobe are available via `scripts/resolve_pinned_ffmpeg.sh` (run `bash scripts/install_pinned_ffmpeg.sh` if absent); the read-back proof depends on `ffprobe`.</precondition>
  <acceptance_criteria>
    - **Locally provable** — the SUMMARY contains the `ffprobe -show_packets` transcript for `timeline_ts_wrap.ts` showing raw PTS values crossing `2^33` mid-file, and a transcript for `timeline_ts_nowrap.ts` showing no such crossing.
    - **Locally provable** — the SUMMARY contains a transcript for `timeline_gap.mp4` showing exactly one interval larger than twice the nominal.
    - **Locally provable** — `grep -v "^ *#" scripts/gen_corpus.sh | grep -c 'mpdecimate\|libx264\|libx265'` reports `0` (comment lines are stripped first, since a recipe comment may legitimately NAME the forbidden filter as forbidden).
    - **Locally provable** — `grep -c 'timeline_gap\|timeline_ts_wrap\|timeline_ts_nowrap' tests/golden/CORPUS_DIGEST_PROVISIONAL.txt` reports `4`.
  </acceptance_criteria>
  <done>Four fixtures exist, each proven by a read-back transcript to carry (or not carry) the anomaly it is named for.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `timeline.gaps` and `timeline.wrap_events` over the unwrapped presentation timeline</name>
  <files>src/analyzers/timeline/analyzers.h, src/analyzers/timeline/monotonic.cpp, src/core/checks.def, docs/checks/timeline.gaps.md, docs/checks/timeline.wrap_events.md</files>
  <read_first>
    - `src/analyzers/timeline/monotonic.cpp` as landed by plan 05-05 — the per-stream loop, the sentinel-exclusion view, the TS unwrap call site, the `unwrapped` evidence flag and the skip-reason ordering this task extends rather than duplicates.
    - `claude_docs/04-timeline-analysis.md` §2's `timeline.gaps` row and §1.2's wrap-recording sentence — both normative, quoted into the respective `docs/checks/<id>.md`.
    - `claude_docs/04-timeline-analysis.md` §1.3 — frame end is `pts + duration`; the reconstructed-duration rule plan 05-04 already implements is what "declared duration + 1 tick" refers to.
    - `src/probe/cadence.h` as amended by plan 05-03 — `mode_interval_ticks` is the `nominal` the gap rule uses; `ideal_interval_num/den` is NOT (nominal is the mode, per doc 04's own wording).
    - `src/core/value.h` lines 56-70 — `Span{start, end}` and `SpanList{spans}`, and their exact field names.
    - `src/core/snapshot.cpp` — how a `span_list` value round-trips, so the fingerprint fidelity claim is grounded rather than assumed.
    - `src/core/checks.def` at `video.hdr.coherence` — the `state` / `info` / `flagged_values` block for `timeline.wrap_events`.
    - `src/compare/state.cpp` — `pass` means NEITHER side flagged (Phase 4's WR-02 correction), which is why the clean pair is two unflagged files.
    - `src/compare/span.cpp` (or the `span` semantic's comparator, wherever it lives) — what "introduced gates" means for the span semantic, so the doc's `### Accept` text is accurate.
    - `.planning/phases/05-timeline-analysis/05-CHECK-ROSTER.md` — approved attributes and the subtitle-scope exclusion.
  </read_first>
  <behavior>
    - Test 1: on `timeline_gap.mp4`, `timeline.gaps` reports exactly one span whose start and end bracket the enlarged interval, expressed in ms as exact rationals.
    - Test 2: on `timeline_start_base.mp4`, `timeline.gaps` reports an empty span list — not a skip, and not a fabricated zero-length span.
    - Test 3: an interval exactly equal to `2 x nominal` is NOT a gap; an interval one tick larger IS. Both asserted from hand-constructed input.
    - Test 4: `nominal` comes from `derive_cadence`'s mode interval; a stream whose cadence reports `insufficient_data` makes `timeline.gaps` emit `skipped:insufficient_data` rather than picking a fallback nominal.
    - Test 5: on `timeline_ts_wrap.ts`, `timeline.wrap_events` carries the flagged value and evidence holds the wrap count, the first wrapping packet index, and that packet's raw AND unwrapped values.
    - Test 6: on `timeline_ts_wrap.ts`, `timeline.gaps` reports NO span attributable to the wrap and `timeline.dts_monotonic` reports no violation attributable to it — doc 04 §5's own acceptance criterion.
    - Test 7: comparing two files that BOTH wrap still produces a non-pass `timeline.wrap_events` finding at `info`; comparing two unflagged files produces `pass`.
    - Test 8: a truncated scan emits `skipped:partial_scan` on `timeline.gaps` ahead of every other skip reason.
    - Test 9: no `timeline.gaps` measurement is emitted at subtitle scope.
  </behavior>
  <action>
    Extend `src/analyzers/timeline/monotonic.cpp` — the same translation unit as the other two TIME-04
    counts, since all four read the same per-stream axis view and splitting them would force a second
    walk. Extend the existing `timeline_monotonic_analyzer()` rather than registering a second
    `AnalyzerSpec`; add `Pass::ts_scan` to its `required_passes` only if plan 05-07's seam needs it —
    this plan reads the wrap purely from `unwrap_ts_timestamps`, so `{Pass::demux_header,
    Pass::packet_scan}` remains sufficient. Update the declaration's doc comment in
    `src/analyzers/timeline/analyzers.h` to name the two new ids and their requirements.

    Add two named constants with comments citing doc 04 §2 and D-08:
    `kGapNominalMultiplier = 2` and `kGapDeclaredDurationSlackTicks = 1`. State in the comment that D-08
    keeps both fixed in v1 because a detection parameter changes the measured value — unlike a tolerance,
    which only changes the verdict — so making one configurable would require recording it in the
    fingerprint and skipping across mismatched values, machinery deliberately deferred rather than
    improvised.

    `timeline.gaps`: for each non-subtitle stream, take `nominal` from the SHARED `derive_cadence`'s
    `mode_interval_ticks` (never a local interval tally), and walk the presentation-sorted, sentinel-free
    index view. A span is emitted when the interval between consecutive presentation timestamps STRICTLY
    EXCEEDS `max(kGapNominalMultiplier * nominal, declared_duration + kGapDeclaredDurationSlackTicks)`,
    using `detail::checked_mul`/`checked_add` for both magnitudes and a cross-multiplication comparison
    rather than a division. `declared_duration` is the preceding packet's duration, reconstructed per doc
    04 §1.3 by the helper plan 05-04 already landed — call it, do not re-implement it. Build a
    `SpanList` of `Span{start, end}` with both bounds as exact `RationalValue` in ms. Publish
    `nominal_interval_ticks` and `gap_count` in evidence. When `derive_cadence` reports anything other
    than `ok`, emit `skipped:insufficient_data` — never a fallback nominal.

    `timeline.wrap_events`: publish a `state`-semantic string per stream. On a `ContainerFamily::ts`
    input, use the `UnwrapResult` the existing unwrap call site already produces: a non-zero
    `wrap_events` count emits the flagged value `ts_33bit_wrap`; zero emits the unflagged value. Evidence
    carries the wrap count, the first wrapping packet's index, and that packet's RAW and UNWRAPPED values
    — TIME-02's "raw values are preserved in evidence" is satisfied here, concretely. On a non-TS input
    the check emits `skipped:not_applicable_container`. Map `UnwrapResult::overflowed` to
    `skipped:insufficient_data`.

    Register both ids in `src/core/checks.def` exactly as `05-CHECK-ROSTER.md` approves — including the
    non-empty `flagged_values` list for `timeline.wrap_events`, which the generator requires for the
    `state` semantic — each with a comment block citing this plan, TIME-02/TIME-04, D-08, and the
    skip-reason vocabulary.

    Write both `docs/checks/<id>.md` files with all three level-2 headings and the three level-3
    sub-headings. `docs/checks/timeline.gaps.md` must state the subtitle-scope exclusion AND its reason
    (a subtitle track's intentionally-sparse timing would trigger false gaps under a rule tuned for audio
    and video), and its `### Tune` must explain that the multiplier is a detection constant rather than a
    tolerance, so `--tol` cannot widen it. `docs/checks/timeline.wrap_events.md` must explain, in user
    terms, that a candidate wrapping where the baseline did not usually means a start-offset change
    upstream — doc 04 §1.2's own sentence — and that the note fires even when both files wrap.
  </action>
  <verify>
    <automated>cmake --build build/x64-linux 2>&amp;1 | tail -5 &amp;&amp; ctest --test-dir build/x64-linux --output-on-failure</automated>
    <fails_when>Non-zero exit; or the build emits a warning-as-error from `monotonic.cpp`; or the registry generator rejects `timeline.wrap_events` for an empty `flagged_values` list or a missing `docs/checks/<id>.md`; or `integration.doc03_coverage`'s count-equality REQUIRE fails because the registry grew without a declared pair (Task 3 adds those rows in the same commit).</fails_when>
  </verify>
  <acceptance_criteria>
    - **Locally provable** — `./build/x64-linux/mediadiff inspect tests/fixtures/timeline_gap.mp4 -v --json` shows a `timeline.gaps` measurement whose span list holds exactly one span and whose evidence carries `nominal_interval_ticks`.
    - **Locally provable** — `./build/x64-linux/mediadiff inspect tests/fixtures/timeline_start_base.mp4 -v --json` shows `timeline.gaps` with an EMPTY span list and no skip reason.
    - **Locally provable** — `./build/x64-linux/mediadiff inspect tests/fixtures/timeline_ts_wrap.ts -v --json` shows `timeline.wrap_events` carrying the flagged value with `wrap_count`, `first_wrap_raw` and `first_wrap_unwrapped` in evidence, AND `timeline.gaps` with no span attributable to the wrap, AND `timeline.dts_monotonic` at `0`.
    - **Locally provable** — `grep -c 'derive_cadence' src/analyzers/timeline/monotonic.cpp` reports at least `1`, and `grep -c 'mode_interval_ticks' src/analyzers/timeline/monotonic.cpp` reports at least `1`.
    - **Locally provable** — `grep -v '^ *//' src/analyzers/timeline/monotonic.cpp | grep -c 'std::sqrt'` reports `0`.
    - **Locally provable** — `./build/x64-linux/mediadiff explain timeline.gaps` and `... explain timeline.wrap_events` each print all three sections.
    - **Locally provable** — `bash scripts/lint_check_id_strings.sh` exits zero.
  </acceptance_criteria>
  <done>A mid-file 33-bit wrap is visible as an `info` note with its raw values preserved, and produces no false gap and no false monotonicity violation — doc 04 §5's own TS-wrap acceptance criterion, met on a real file.</done>
</task>

<task type="auto">
  <name>Task 3: The DOC-03 rows and the DOC-04 declared sets for gaps and wrap events</name>
  <files>tests/integration/test_timeline_structure.cpp, tests/integration/test_doc03_coverage.cpp</files>
  <read_first>
    - `tests/integration/test_timeline_structure.cpp` as landed by plan 05-05 — the `timeline_structure - ` TEST_CASE prefix and the existing declared-set call shape.
    - `tests/integration/timeline_findings.h` — `count_non_pass`, `expect_declared_set`, and D-02's causal-reason rule.
    - `tests/integration/test_doc03_coverage.cpp` — `CoveragePair`'s four fields, the running-total comment (sixty-six after plan 05-05), and the empirical-verification requirement in its header.
    - `src/compare/state.cpp` — one more time, to keep the clean-pair reasoning straight: `pass` means neither side flagged.
    - `.planning/phases/05-timeline-analysis/05-CONTEXT.md` D-02 — a declared set may hold more than one id when one cause moves several facts, each with a written causal reason.
  </read_first>
  <action>
    Extend `tests/integration/test_timeline_structure.cpp` with declared-set assertions:
      - `timeline_start_base.mp4` vs `timeline_gap.mp4`: the declared set holds `timeline.gaps` plus every
        id the `setts` shift legitimately moves (the duration triple among them, since shifting the tail
        changes the computed duration), each with an inline causal reason per D-02. If the whole-report
        count does not match, NARROW THE FIXTURE — never add a filter.
      - `timeline_ts_nowrap.ts` vs `timeline_ts_wrap.ts`: the declared set holds `timeline.wrap_events`
        plus every id the output-timestamp offset legitimately moves, each with a causal reason. The TS
        muxer delay that `05-CONTEXT.md` records rides on both sides here, so it should cancel; if it does
        not, record what moved and why.
      - `timeline_ts_nowrap.ts` vs `timeline_ts_nowrap_copy.ts`: the declared set is empty and
        `count_non_pass` is `0` — the `state`-semantic clean pair, both sides unflagged.
      - a case asserting doc 04 §5's own criterion directly: on the wrap pair, neither `timeline.gaps` nor
        `timeline.dts_monotonic` appears among the non-pass findings.

    Before writing the DOC-03 rows, RUN each candidate pair against the real binary and confirm the check
    genuinely fires and the clean pair genuinely reports `pass` rather than merely never running. Then add
    two `declared_pairs()` entries and increment the running-total comment from sixty-six to sixty-eight.
  </action>
  <verify>
    <automated>cmake --build build/x64-linux 2>&amp;1 | tail -5 &amp;&amp; ctest --test-dir build/x64-linux -R "integration\.(timeline_structure|doc03_coverage)" --output-on-failure</automated>
    <fails_when>Non-zero exit; or the output contains "No tests were found"; or `integration.doc03_coverage` lists either new id under `uncovered_no_pair` / `uncovered_trigger_did_not_fire`; or an `expect_declared_set` assertion names an unexpected or missing finding id; or the wrap-pair case reports `timeline.gaps` or `timeline.dts_monotonic` among the non-pass findings.</fails_when>
  </verify>
  <acceptance_criteria>
    - **Locally provable** — `./build/x64-linux/mediadiff compare tests/fixtures/timeline_ts_nowrap.ts tests/fixtures/timeline_ts_wrap.ts --json` shows a `timeline.wrap_events` finding at `info` whose status is not `pass`, and no `timeline.gaps` finding among the non-pass results.
    - **Locally provable** — `./build/x64-linux/mediadiff compare tests/fixtures/timeline_ts_nowrap.ts tests/fixtures/timeline_ts_nowrap_copy.ts --json` reports a whole-report non-pass count of `0`.
    - **Locally provable** — `./build/x64-linux/mediadiff compare tests/fixtures/timeline_start_base.mp4 tests/fixtures/timeline_gap.mp4 --json` shows a `timeline.gaps` finding whose status is neither `pass` nor `skipped`.
    - **Locally provable** — `grep -c 'sixty-eight' tests/integration/test_doc03_coverage.cpp` reports at least `1`.
  </acceptance_criteria>
  <done>Both ids have a proven triggering and clean pair, the clean pair for the `state` check is two unflagged files, and doc 04 §5's TS-wrap criterion is asserted as its own test case.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw MPEG-TS timestamps → unwrap → gap detection | a crafted wrap sequence can be engineered to look like a gap, or a real gap made to look like a wrap |
| packet durations → the gap threshold | a crafted declared duration participates directly in `max(2 x nominal, duration + 1 tick)` |
| span list → fingerprint → snapshot comparison | an unbounded span list is a memory-growth surface and a snapshot-size surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-23 | Tampering | crafted TS wrap sequences engineered to suppress a real gap (or manufacture a false one) by shifting the unwrap offset | high | mitigate | the unwrap is doc 04 §1.2's asymmetric rule, unit-proven in plan 05-02; the wrap count and the first wrapping packet's raw and unwrapped values are published in evidence so the correction is auditable, and Test 6 asserts no gap is attributable to a wrap |
| T-05-24 | Tampering | a crafted declared packet duration overflowing `declared_duration + 1 tick` and inverting the gap threshold | high | mitigate | both magnitudes are computed with `detail::checked_mul`/`checked_add` and compared by cross-multiplication; any failure yields `skipped:insufficient_data`, never a threshold of the wrong sign |
| T-05-25 | Denial of Service | a crafted stream whose every interval qualifies as a gap, producing a span list with millions of entries in the fingerprint | medium | mitigate | the span list is bounded by the per-stream packet ceiling `kMaxPacketsPerStream`, which is itself bounded by the accounted-byte budget Phase 3's D-01 established; the `gap_count` evidence field makes an implausible count visible rather than silent |
| T-05-26 | Tampering | a `timeline.wrap_events` clean pair in which both files wrap, silently inverting the check's meaning so the gate proves nothing | high | mitigate | the clean pair is two deliberately-unflagged files (`timeline_ts_nowrap.ts` and its copy); A2 records the reasoning and Task 3's acceptance criterion asserts a zero non-pass count on it |
| T-05-27 | Tampering | a locally regenerated pre-existing digest line replacing a designated-leg hash | high | mitigate | `scripts/lint_corpus_digest_provenance.sh` runs in Task 1's `<verify>`; new names go to the provisional ledger only |
| T-05-SC | Tampering | dependency supply chain | low | accept | no npm/pip/cargo/vcpkg package is added by this plan (`05-RESEARCH.md` § Package Legitimacy Audit: not applicable to this phase) |
</threat_model>

<verification>
- `bash scripts/gen_corpus.sh && bash scripts/check_corpus.sh && bash scripts/lint_bash4_builtins.sh && bash scripts/lint_corpus_digest_provenance.sh`
- `cmake --build build/x64-linux 2>&1 | tail -5 && ctest --test-dir build/x64-linux --output-on-failure`
- `./build/x64-linux/mediadiff explain timeline.gaps && ./build/x64-linux/mediadiff explain timeline.wrap_events`
</verification>

<success_criteria>
- `timeline.gaps` emits an exact-rational span list whose `nominal` comes from the one shared cadence derivation.
- `timeline.wrap_events` makes a 33-bit wrap visible at `info` with raw values preserved in evidence.
- A mid-file wrap produces no false gap and no false monotonicity violation, asserted as its own test case.
- Both ids have a proven DOC-03 pair (the `state` check's clean pair being two unflagged files) and a DOC-04 declared set.
- The full suite is green and every corpus/provenance lint exits zero.
</success_criteria>

<output>
Create `.planning/phases/05-timeline-analysis/05-06-SUMMARY.md` when done.
</output>
