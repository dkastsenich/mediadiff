---
phase: 05-timeline-analysis
plan: 23
type: execute
wave: 19
depends_on: ["05-22"]
gap_closure: true
files_modified:
  - docs/checks/timeline.av_drift.pattern.md
  - docs/checks/timeline.av_drift.md
  - .planning/phases/05-timeline-analysis/05-CHECK-ROSTER.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - src/core/checks.def
  - .planning/WINDOWS.md
autonomous: true
requirements: [TIME-07, DOC-04]

estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "`mediadiff explain timeline.av_drift.pattern` (docs are compiled in) states that a seamlessly re-timestamped trim is undetectable from timestamps alone until Phase 6's audio decode path exists. It also states the timestamp-gap ambiguity (dropout vs sync step) and which reading the shipped mapping assumes (UD-1)."
    - "The published vocabulary in docs/checks/timeline.av_drift.pattern.md, 05-CHECK-ROSTER.md, the checks.def comment, ROADMAP Phase 5 SC1 and REQUIREMENTS TIME-07 matches what the code can produce after 05-22."
    - "Under narrow-vocabulary, SC1, TIME-07 and the roster row are amended in place, with a dated amendment citing UD-1 and 05-STEP-DESIGN.md's Decision. doc 04's divergence is recorded in the roster amendment rather than by editing the normative spec."
    - "Under adopt, none of the vocabulary files changes, because they already list step. The docs gain the chosen design's description, and its assumption about timestamp discontinuities."
    - "The residual MP4-to-TS av_drift outcome is recorded in WINDOWS.md exactly as the span decision dictates. Under span:declared it is appended and waived with the human's recorded reason. Under span:observed it is appended open only if the MP4-to-TS av_drift still fails."
  artifacts:
    - path: docs/checks/timeline.av_drift.pattern.md
      provides: "vocabulary, seamless-trim limitation, discontinuity-ambiguity statement"
    - path: .planning/phases/05-timeline-analysis/05-CHECK-ROSTER.md
      provides: "the roster row and dated amendment (narrow only)"
    - path: .planning/WINDOWS.md
      provides: "the residual MP4-to-TS drift record per the span decision"
  key_links:
    - from: "docs/checks/timeline.av_drift.pattern.md"
      to: "mediadiff explain output"
      via: "tools/gen_registry.py compiles docs/checks/*.md into check_explain.cpp at build time"
    - from: "05-STEP-DESIGN.md Decision"
      to: "roster / SC1 / TIME-07 / WINDOWS.md edits"
      via: "each edit is selected mechanically by the recorded tokens"
  prohibitions:
    - statement: "MUST NOT rename, remove or re-attribute any check id in checks.def; only the explanatory comment for timeline.av_drift.pattern may change (IDs are forever)."
      status: resolved
      verification: judgment
    - statement: "MUST NOT rewrite ROADMAP.md or REQUIREMENTS.md wholesale; each is changed by a scoped edit of the single SC1 / TIME-07 line."
      status: resolved
      verification: judgment
---

<objective>
Close Gap 1's documentation and contract half, per UD-1. Make every published statement of timeline.av_drift.pattern's vocabulary match the code 05-22 shipped.

In both branches:
- Document that a seamlessly re-timestamped trim is undetectable from timestamps alone until Phase 6.
- Document the discontinuity ambiguity.
- Record the residual MP4-to-TS drift outcome in the defect ledger, as the span decision dictates.

Purpose:
- UD-1 requires the seamless-trim statement whichever branch is taken.
- Under narrow-vocabulary it also requires amending SC1, TIME-07, 05-CHECK-ROSTER.md and docs/checks/timeline.av_drift.pattern.md.
- A vocabulary claim the code cannot honor is itself a false promise to users matching on it.

Branch selection is mechanical. Read `## Decision` in `.planning/phases/05-timeline-analysis/05-STEP-DESIGN.md` and the tokens recorded at the top of 05-22-SUMMARY.md. They must agree; if they do not, stop and report.

Output: updated check docs (compiled into `explain`), vocabulary amendments where decided, a comment-only checks.def update where decided, and the ledger record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/phases/05-timeline-analysis/05-STEP-DESIGN.md
@.planning/phases/05-timeline-analysis/05-22-SUMMARY.md
@.planning/phases/05-timeline-analysis/05-CHECK-ROSTER.md
</context>

## Artifacts this phase produces

| Kind | Symbol / path |
|---|---|
| Doc sections | `## Limits of timestamp-only detection` in `docs/checks/timeline.av_drift.pattern.md` (seamless re-timestamped trim; gap-vs-jump ambiguity); a span-source note in `docs/checks/timeline.av_drift.md` when span:observed was decided |
| Roster amendment (narrow only) | `## Amendments` section in `.planning/phases/05-timeline-analysis/05-CHECK-ROSTER.md` with a dated UD-1 entry |
| Ledger entry (per span decision) | a `.planning/WINDOWS.md` deviation entry for the residual MP4-to-TS av_drift outcome, created with `gsd-tools windows append` (and `windows waive` under span:declared) |

No C++ symbol is created. checks.def changes only in a comment, and only under narrow-vocabulary.

<flagged_assumptions>
**A1: doc 04 stays unedited.** UD-1 lists four artifacts to amend under narrow-vocabulary, and claude_docs/04-timeline-analysis.md is not one of them. Under narrow, the roster's dated amendment records that doc 04 section 3 point 4 and section 5's "Step" recipe are superseded for Phase 5 until audio decode (Phase 6) can disambiguate. The SUMMARY flags doc 04 for the human, as a follow-up edit they may choose to make.

**A2: under span:declared the residual MP4-to-TS drift finding is the human's accepted outcome, not an open defect.** It is appended and waived with the reason the human gave in the Decision, so /gsd-ship is not blocked by a decision already taken. Under span:observed, a still-failing MP4-to-TS av_drift is a real open false positive: it is appended open, and the SUMMARY says it blocks /gsd-ship.
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: The compiled-in explain text states the timestamp-only limits and the decided vocabulary, proven end to end through the build</name>
  <reversibility rating="reversible">Documentation compiled into explain; freely editable.</reversibility>
  <files>docs/checks/timeline.av_drift.pattern.md, docs/checks/timeline.av_drift.md</files>
  <read_first>
    - docs/checks/timeline.av_drift.pattern.md and docs/checks/timeline.av_drift.md in full
    - .planning/phases/05-timeline-analysis/05-STEP-DESIGN.md sections `## Ambiguity analysis`, `## Seamless re-timestamped trim`, `## Residual MP4-to-TS drift`, `## Implementation spec` (adopt) and `## Decision`
    - .planning/phases/05-timeline-analysis/05-22-SUMMARY.md (the tokens and the measured SC1 outcomes)
    - tools/gen_registry.py (how docs/checks/*.md are compiled, and any section structure it requires)
  </read_first>
  <action>
    1. Read the Decision tokens, and confirm they match those at the top of 05-22-SUMMARY.md.

    2. In docs/checks/timeline.av_drift.pattern.md:
       - Under adopt, rewrite the `step` bullet to describe the adopted mapping's behavior: what counts as a discontinuity, the fixed threshold, and what step_time_ms names.
       - Under narrow, delete the `step` bullet, and change "one of exactly four values" to three. Name the three values. Add one sentence saying a spliced trim reports `irregular` with its residual max.
       - In both branches, add the section `## Limits of timestamp-only detection`, stating:
         (a) a seamlessly re-timestamped trim, with no timestamp discontinuity, is undetectable from timestamps alone until Phase 6's audio decode path exists;
         (b) a timestamp gap is ambiguous between a dropout (content missing, sync preserved) and a sync step (content contiguous, timestamps jumped);
         (c) which reading the shipped mapping assumes, and what it reports for the other.

    3. In docs/checks/timeline.av_drift.md, only under span:observed, add a paragraph: checkpoint spans use the observed packet extent where the container declares no per-stream duration, and why (05-STEP-DESIGN.md A2).

    4. Build, then capture `./build/x64-linux/mediadiff explain timeline.av_drift.pattern` into the SUMMARY.
  </action>
  <verify>
    <automated>cmake --build --preset x64-linux &amp;&amp; ./build/x64-linux/mediadiff explain timeline.av_drift.pattern | grep -c "undetectable from timestamps alone"</automated>
    <fails_when>Non-zero exit; or grep prints 0 and exits 1 because the compiled explain text lacks the seamless-trim statement.</fails_when>
  </verify>
  <acceptance_criteria>
    - `./build/x64-linux/mediadiff explain timeline.av_drift.pattern` contains "Limits of timestamp-only detection" and "undetectable from timestamps alone".
    - narrow: the explain output's vocabulary lists exactly `constant-offset`, `linear-drift` and `irregular`. adopt: it lists all four.
    - `ctest --preset x64-linux --output-on-failure` passes, including any registry/doc test.
  </acceptance_criteria>
  <done>Users reading explain see the decided vocabulary and the honest limits.</done>
</task>

<task type="auto">
  <name>Task 2: SC1, TIME-07, the roster and the checks.def comment match the decided vocabulary</name>
  <files>.planning/phases/05-timeline-analysis/05-CHECK-ROSTER.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, src/core/checks.def</files>
  <read_first>
    - .planning/phases/05-timeline-analysis/05-CHECK-ROSTER.md (the timeline.av_drift.pattern row at about line 39, and item 5 at about line 56)
    - .planning/ROADMAP.md Phase 5 success criterion 1 (about line 342)
    - .planning/REQUIREMENTS.md TIME-07 (about line 132)
    - src/core/checks.def lines 1430-1450 (the timeline.av_drift.pattern comment block)
  </read_first>
  <action>
    **Branch adopt.**
    - Change nothing in these four files.
    - Confirm each still names `step`: run `grep -c step` on the four regions, and record the output in the SUMMARY.

    **Branch narrow-vocabulary.** Edit each file with scoped edits only:
    1. 05-CHECK-ROSTER.md:
       - In the timeline.av_drift.pattern row and item 5, change the vocabulary to `constant-offset` / `linear-drift` / `irregular`.
       - Append a `## Amendments` section, placed before `## Status`, with a dated entry citing UD-1, 05-21, and 05-STEP-DESIGN.md's Decision. The entry states:
         - the removed value, and why (timestamp-only ambiguity);
         - that the check id and its exact/fail semantics are unchanged;
         - that doc 04 section 3 point 4 and section 5's Step recipe are superseded for Phase 5 until Phase 6's audio decode path can disambiguate.
    2. ROADMAP.md: amend Phase 5 SC1's clause "a spliced 100 ms trim is reported as `step` with the step time". It becomes: a spliced trim with a timestamp discontinuity is reported as a non-pass `irregular` pattern with its residual max, and a seamlessly re-timestamped trim is documented as undetectable from timestamps alone until Phase 6. Append "(amended <date>, UD-1)". Use a scoped Edit of that one line; never a whole-file Write.
    3. REQUIREMENTS.md: amend TIME-07's pattern list to `constant-offset` / `linear-drift` / `irregular`, and append "(amended <date>, UD-1: `step` withdrawn until audio decode)". One-line scoped Edit.
    4. checks.def: in the timeline.av_drift.pattern comment only, change the four spellings to the three. Check ids, attributes and every non-comment line stay unchanged.
  </action>
  <verify>
    <automated>cmake --build --preset x64-linux &amp;&amp; ctest --preset x64-linux -N -R '^unit\.registry' | grep -E 'Total Tests: [1-9]' &amp;&amp; ctest --preset x64-linux --output-on-failure &amp;&amp; git diff -U0 -- src/core/checks.def | grep -E '^[-+][^-+]' | grep -v '^[-+][[:space:]]*#' | wc -l | grep -qx 0</automated>
    <fails_when>Non-zero exit; or "Total Tests: 0" for the registry filter; or any failing test; or the checks.def diff contains a changed line that is not a comment, so the final `wc -l` is not 0.</fails_when>
  </verify>
  <acceptance_criteria>
    - adopt: `git diff --stat` for the four files is empty.
    - narrow:
      - `grep -c "## Amendments" .planning/phases/05-timeline-analysis/05-CHECK-ROSTER.md` reports 1.
      - The ROADMAP SC1 line and the REQUIREMENTS TIME-07 line each contain "amended" and "UD-1".
      - `git diff -- .planning/ROADMAP.md | grep -c '^-[^-]'` reports 1.
      - The checks.def diff is comment-only.
    - The full suite passes, including the registry tests that pin the checks.def id count.
  </acceptance_criteria>
  <done>Every published vocabulary statement matches the code.</done>
</task>

<task type="auto">
  <name>Task 3: Record the residual MP4-to-TS drift outcome per the span decision, and verify the whole phase state</name>
  <files>.planning/WINDOWS.md</files>
  <read_first>
    - .planning/phases/05-timeline-analysis/05-STEP-DESIGN.md `## Decision` (the span token, and the reason for span:declared) and `## Residual MP4-to-TS drift`
    - .planning/phases/05-timeline-analysis/05-22-SUMMARY.md (Test 4 / mirror before-after drift evidence)
    - .planning/WINDOWS.md header and the last five entries (id numbering, status literals)
  </read_first>
  <action>
    1. **span:declared.**
       - Run `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows append --kind deviation --phase 05 --file src/analyzers/timeline/av_sync.cpp --description "..."`.
       - The description states that on a lossless MP4 to MPEG-TS stream copy, timeline.av_drift fails, and timeline.av_drift.pattern flips to irregular. It gives the cause: the checkpoint span uses libavformat's estimated TS audio stream duration (3877 ms against 4023 ms), which the container does not declare. It records that the human kept the declared-duration span at 05-21.
       - Read the new id from the output, then run `windows waive <id> "<the human's recorded reason>"`.

    2. **span:observed.**
       - If 05-22-SUMMARY.md shows the MP4-to-TS av_drift now passes, append nothing.
       - If it still fails, append the same entry with the measured cause, and leave it open. The SUMMARY states that /gsd-ship will block on it.

    3. Run `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status`, and paste the output. Confirm #26, #27, #28 and #30 show fixed, and #29 is untouched.

    4. Final local phase check, recording each result in the SUMMARY:
       - `ctest --preset x64-linux --output-on-failure`
       - `MEDIADIFF_DESIGNATED_LEG=1 ctest --preset x64-linux --output-on-failure`
       - `bash scripts/lint_corpus_digest_provenance.sh`
       - `bash scripts/assert_corpus_digest.sh`. It is expected to fail only on a timeline_drift_step.mp4 line if 05-22 changed that recipe; otherwise it passes.
  </action>
  <verify>
    <automated>node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status &amp;&amp; for n in 26 27 28 30; do grep -E "^\| $n \|" .planning/WINDOWS.md | grep -q '| fixed |' || { echo "entry $n not fixed"; exit 1; }; done &amp;&amp; ctest --preset x64-linux --output-on-failure &amp;&amp; MEDIADIFF_DESIGNATED_LEG=1 ctest --preset x64-linux --output-on-failure &amp;&amp; bash scripts/lint_corpus_digest_provenance.sh</automated>
    <fails_when>Non-zero exit; or "entry N not fixed" is printed for any of 26/27/28/30; or any failing test in either suite; or the provenance lint reports a failed clause.</fails_when>
  </verify>
  <acceptance_criteria>
    - span:declared: WINDOWS.md contains a new entry naming the MP4-to-TS av_drift cause, with status `waived`.
    - span:observed: either no new entry and a passing MP4-to-TS av_drift (per 05-22-SUMMARY.md), or a new `open` entry together with a SUMMARY line saying it blocks /gsd-ship.
    - The SUMMARY records the four final-check commands and their results, including the `windows status` output.
  </acceptance_criteria>
  <done>The phase's local state is verified, and every residual is recorded exactly as decided.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published docs/roster -> users matching pattern values | a promised value the code cannot produce misleads CI rules |
| defect ledger -> /gsd-ship gate | a residual false positive must be visible, as a decided waiver or as open |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-92 | Repudiation | docs promising `step` when the code cannot produce it, or omitting the seamless-trim limit | medium | mitigate | Task 1 verifies the compiled explain text; Task 2 aligns roster/SC1/TIME-07 with the code |
| T-05-93 | Tampering | a check id or attribute changed while editing checks.def | high | mitigate | the verify asserts the checks.def diff is comment-only; registry tests pin the id count |
| T-05-94 | Repudiation | a residual false positive silently dropped from the ledger | medium | mitigate | Task 3 appends it as a waiver with the human's reason or as open, per the recorded span token |
| T-05-SC | Tampering | dependency supply chain | low | accept | no package added |
</threat_model>

<verification>
- `cmake --build --preset x64-linux && ctest --preset x64-linux --output-on-failure` is green.
- `MEDIADIFF_DESIGNATED_LEG=1 ctest --preset x64-linux --output-on-failure` is green.
- `bash scripts/lint_corpus_digest_provenance.sh` passes.
- Perf ratchet: docs and comments only. No flag.
</verification>

<success_criteria>
- UD-1's seamless-trim statement is compiled into explain.
- The published vocabulary matches the code in every artifact UD-1 names.
- The residual MP4-to-TS drift outcome is recorded exactly as decided.
</success_criteria>

<output>
Create `.planning/phases/05-timeline-analysis/05-23-SUMMARY.md` when done.
</output>
