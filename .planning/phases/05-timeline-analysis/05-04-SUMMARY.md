---
phase: 05-timeline-analysis
plan: 04
subsystem: analysis-checks
tags: [timeline, duration, coherence, catch2, checks-def, doc-03, doc-04]

# Dependency graph
requires:
  - phase: 05-timeline-analysis
    provides: "05-01's timeline_start_duration.cpp scaffolding (push_skip, scope_kind_for_stream, the global-origin helper and its skip-reason ordering), DOC-04 no-others harness (timeline_findings.h) and corpus-fixture conventions; 05-03's amended src/probe/cadence.h derive_cadence, whose mode_interval_ticks this plan reuses verbatim for the last-packet reconstruction case rather than computing a second cadence"
provides:
  - "timeline.duration: the duration TRIPLE (container-declared via AVFormatContext->duration, stream-declared via AVStream->duration, computed via reconstructed packet durations), each member independently absent-capable, compared pairwise and emitted as one Measurement with all three in evidence"
  - "timeline.duration.coherence: a state-semantic check (video.hdr.coherence's shape, Phase 4 D-10 precedent) firing at info when any pair of the triple disagrees by more than a fixed 40ms threshold (D-08), naming the FIRST disagreeing pair as its value while evidence tracks every tested and disagreeing pair"
  - "detail::reconstruct_packet_durations (file-local, src/analyzers/timeline/start_duration.cpp): sorts a local index view by pts (never reorders the caller's array), reconstructs a missing duration as delta-to-next-pts for non-last packets and as derive_cadence's own mode_interval_ticks for the last packet, returns nullopt on no valid pts or any checked-arithmetic overflow"
  - "DemuxSession::container_duration_ticks() and StreamInfo::declared_duration_ticks -- the only two libav-duration reads for this feature, both funneled through src/probe/ per the no-libav-header-outside-probe rule"
  - "tests/fixtures/timeline_duration_short.mp4 -- new DOC-03 trigger fixture for timeline.duration (container duration exceeds computed duration); DOC-03/DOC-04 coverage for timeline.duration.coherence reuses 05-01's already-committed timeline_start_shift.ts pair (flagged_assumption A1), empirically confirmed firing container_vs_stream on its audio scope before being declared"
affects: [05-timeline-analysis (any later plan reading the duration triple's evidence shape or extending timeline.duration.coherence's flagged-value set)]

# Actuals (#2632)
actuals:
  tokens: 18633
  tasks: 3
  commits: 2

tech-stack:
  added: []
  patterns:
    - "Duration-triple computation runs INDEPENDENTLY of D-03's global-origin computation: run_timeline_start_duration's candidates.empty()/!origin_ms early-returns became if/else branches that fall through to an unconditional per-stream duration loop, since duration needs no cross-stream origin -- the first analyzer feature in this file whose control flow deliberately does not short-circuit on the origin-scan's own absence"
    - "Fixed pairwise-comparison order for a 3-member coherence check: container_vs_stream, container_vs_computed, stream_vs_computed always evaluated in that order, only testing pairs where both members are present, with tested_pairs/disagreeing_pairs tracked as evidence arrays while the compared VALUE names only the first disagreeing pair -- the template for any future 3+-way internal coherence check"
    - "reconstruct_packet_durations sorts a LOCAL INDEX VIEW by pts before reconstructing, never touching the caller's PacketRecord span -- reaffirms the project's 'sort a view, never reorder the caller's array' rule at a new call site"
    - "mediadiff compare --json substituted for mediadiff inspect --json -v when a plan's acceptance criteria ask for evidence-field verification, since inspect never renders Measurement::evidence at all (pre-existing project-wide gap, first surfaced in 05-01-SUMMARY.md, still not fixed and out of this plan's scope)"

key-files:
  created:
    - docs/checks/timeline.duration.md
    - docs/checks/timeline.duration.coherence.md
    - tests/fixtures/timeline_duration_short.mp4 (gitignored media, generated by scripts/gen_corpus.sh)
  modified:
    - src/analyzers/timeline/analyzers.h
    - src/analyzers/timeline/start_duration.cpp
    - src/core/checks.def
    - src/probe/demux_session.h
    - src/probe/demux_session.cpp
    - scripts/gen_corpus.sh
    - tests/golden/CORPUS_DIGEST.txt
    - tests/golden/CORPUS_DIGEST_PROVISIONAL.txt
    - tests/golden/list_checks_effective.txt
    - tests/fixtures/GENERATOR_MANIFEST.json
    - tests/integration/test_timeline_start_duration.cpp
    - tests/integration/test_doc03_coverage.cpp
    - tests/unit/test_timeline_start_duration.cpp

key-decisions:
  - "Task 1 (timeline.duration) and Task 2 (timeline.duration.coherence) committed together as one commit (e8cc98c), not two -- both are emitted from the same file-local emit_timeline_duration function over the same computed triple, and checks.def must register BOTH ids for the translation unit (which references CheckId::timeline_duration_coherence) to compile at all; splitting would leave a non-buildable intermediate state. Documented as a deviation, mirroring the precedent in 05-01-SUMMARY.md and 05-02-SUMMARY.md."
  - "Last-packet duration reconstruction reuses derive_cadence's own mode_interval_ticks (already computed by 05-03's amended cadence.h) rather than deriving a second, independent cadence -- proven distinct from a naive previous-frame-delta fallback via a deliberately asymmetric pts fixture ([0,10,20,30,44]: three uniform 10-tick intervals plus one 14-tick interval) so the unit test cannot pass by accident under the wrong algorithm."
  - "kDurationIncoherenceThresholdMs fixed at 40 (one frame at 25fps) as a named C++ constant, not a configurable detection parameter, per D-08 -- a detection-parameter would require fingerprint recording and mismatch-skip machinery deliberately deferred past v1."
  - "timeline_duration_short.mp4's DOC-03 trigger/clean pair and the coherence check's DOC-03 pair were both verified firing against the real mediadiff binary before being declared in test_doc03_coverage.cpp, per DOC-03's own empirical-proof gate -- the coherence pair specifically reuses 05-01's already-committed MPEG-TS remux fixture (flagged_assumption A1) instead of generating a new one, since it was already observed to disagree by 146ms (container_declared_ms=4023 vs stream_declared_ms=3877) on its audio scope."

patterns-established:
  - "A 3-member internal-coherence check (timeline.duration.coherence) mirrors a 2-member one (video.hdr.coherence): state semantic, non-empty flagged_values, info severity, value names the disagreement rather than a boolean -- this plan is the second data point proving the shape generalizes past a fixed pair count."

requirements-completed: [TIME-01, TIME-03, DOC-04]
# TIME-01 and DOC-04 were already marked complete by an earlier plan in this
# phase (shared-ID gate, #2388); gsd_run query requirements.mark-complete
# confirmed all three already_complete at this plan's close, so no
# checkbox/traceability write was needed here -- listed above because this
# plan's own frontmatter declares them and this plan's Task 1/Task 2 work is
# what the roster attributes them to.

coverage:
  - id: D1
    description: "timeline.duration publishes the TRIPLE (container-declared, stream-declared, computed) as one Measurement, each member independently absent-capable -- a Matroska file missing AVStream->duration records that member absent in evidence rather than substituting zero or falling back to another member"
    requirement: "TIME-01"
    verification:
      - kind: unit
        ref: "unit.reconstruct_packet_durations -- 6 TEST_CASEs: declared-kept-verbatim, non-last delta reconstruction, last-packet mode-interval reconstruction (disambiguated from previous-frame-delta), presentation-order sorting, no-valid-pts -> nullopt, overflow -> nullopt"
        status: pass
      - kind: integration
        ref: "integration.timeline_start_duration -- evidence-shape proof (all three members present) and absent-member proof (MKV's missing stream_declared_ms), both via mediadiff compare --json (inspect never renders Measurement::evidence, a pre-existing project gap)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Frame end is pts+duration; a packet with no declared duration is reconstructed as the delta to the next PTS in presentation order, or (last frame) derive_cadence's own mode_interval_ticks -- never a second independent cadence computation -- with duration_source:reconstructed carried in evidence whenever any packet was reconstructed"
    requirement: "TIME-03"
    verification:
      - kind: unit
        ref: "unit.reconstruct_packet_durations -- last-packet case uses pts sequence [0,10,20,30,44] (three 10-tick intervals + one 14-tick interval) to prove the result is mode-interval (10), not zero and not previous-frame-delta (14)"
        status: pass
    human_judgment: false
  - id: D3
    description: "timeline.duration.coherence fires at info when the triple's members disagree by more than the fixed 40ms threshold, even when both files share the incoherence -- state semantic, flagged value names WHICH pair disagreed (container_vs_stream/container_vs_computed/stream_vs_computed), evidence tracks tested_pairs and disagreeing_pairs, pairs with an absent member are never tested"
    requirement: "TIME-03"
    verification:
      - kind: integration
        ref: "integration.timeline_start_duration -- coherence flagged-value-and-order proof (timeline_start_base.mp4 vs timeline_start_shift.ts, audio scope -> container_vs_stream) and untestable-pairs proof (NTSC MKV pair, tested_pairs == {container_vs_computed} only)"
        status: pass
    human_judgment: false
  - id: D4
    description: "A truncated packet scan makes both timeline.duration and timeline.duration.coherence emit skipped:partial_scan ahead of every other skip reason, per Phase 3 D-02 (a duration computed from an incomplete sweep is a confidently wrong number)"
    verification:
      - kind: unit
        ref: "existing packet_scan.partial skip-reason-ordering coverage in test_timeline_start_duration.cpp, extended to push_skip for timeline_duration and timeline_duration_coherence per scope"
        status: pass
    human_judgment: false
  - id: D5
    description: "Every timeline fixture assertion declares its complete expected finding set and asserts whole-report count_non_pass equals that set's size (DOC-04 no-others gate); every member beyond the first carries a written causal reason (D-01/D-02); the new timeline_duration_short.mp4 DOC-03 trigger/clean pair and the coherence DOC-03 pair (reusing 05-01's committed MPEG-TS remux) were both empirically proven firing against the real binary before being declared"
    requirement: "DOC-04"
    verification:
      - kind: integration
        ref: "integration.test_doc03_coverage -- declared_pairs() entries for timeline.duration (trigger=timeline_start_base.mp4/timeline_duration_short.mp4, clean=timeline_start_base.mp4/timeline_start_base_copy.mp4) and timeline.duration.coherence (trigger=timeline_start_base.mp4/timeline_start_shift.ts, clean=byte-identical pair), running total now 64"
        status: pass
      - kind: integration
        ref: "integration.timeline_start_duration -- final TEST_CASE declares timeline_start_base.mp4 vs timeline_duration_short.mp4's complete finding set (timeline.duration x2, container.mp4.edit_list x2, video.frame_count, size.file, size.stream_bitrate, size.overhead), each extra member carrying a causal-reason comment, empirically verified via real mediadiff compare --json runs"
        status: pass
      - kind: other
        ref: "bash scripts/gen_corpus.sh && bash scripts/check_corpus.sh && bash scripts/lint_bash4_builtins.sh && bash scripts/lint_corpus_digest_provenance.sh all exit 0; grep -c 'timeline_duration_short' tests/golden/CORPUS_DIGEST_PROVISIONAL.txt reports 1"
        status: pass
    human_judgment: false

duration: 22min
completed: 2026-09-16
status: complete
---

# Phase 5 Plan 04: `timeline.duration` Triple and `timeline.duration.coherence` Summary

**`timeline.duration` publishes the container/stream/computed duration TRIPLE with per-member absent state and delta-to-next-pts packet-duration reconstruction; `timeline.duration.coherence` is a `state`-semantic internal-disagreement note (mirroring `video.hdr.coherence`) that fires at `info` on a fixed 40ms threshold and names which pair of the triple disagreed.**

## Performance

- **Duration:** 22 min
- **Started:** 2026-09-16T21:49:24+02:00
- **Completed:** 2026-09-16T22:11:20+02:00
- **Tasks:** 3/3
- **Files modified:** 15 (3 new: 2 docs + 1 gitignored media fixture; 12 tracked files modified)

## Accomplishments
- `timeline.duration` computes and publishes the container-declared/stream-declared/computed duration triple, each member independently absent-capable, with `duration_source` and `absent_members` in evidence
- `detail::reconstruct_packet_durations` reconstructs missing packet durations via delta-to-next-pts (non-last packets) or `derive_cadence`'s own `mode_interval_ticks` (last packet), proven distinct from a naive previous-delta fallback by a deliberately asymmetric pts fixture
- `timeline.duration.coherence` fires at `info` when any pair of the triple disagrees by more than the fixed `kDurationIncoherenceThresholdMs = 40`, naming the first disagreeing pair while evidence tracks every tested/disagreeing pair
- New `timeline_duration_short.mp4` DOC-03 fixture plus DOC-03/DOC-04 declared-set coverage for both checks, empirically proven against the real binary; running DOC-03 total now 64

## Task Commits

Each task was committed atomically:

1. **Task 1: The duration triple, with per-member absent state and reconstructed packet durations** + **Task 2: `timeline.duration.coherence`** — `e8cc98c` (feat) — combined into one commit; see Deviations below
3. **Task 3: The duration fixture pair, the DOC-03 rows and the DOC-04 declared sets** — `01bd51f` (test)

**Plan metadata:** pending (this commit)

_Note: TDD gate compliance below documents why Task 1/2 landed as a single `feat` commit rather than RED/GREEN/REFACTOR-separated commits._

## Files Created/Modified
- `src/probe/demux_session.h` / `.cpp` - `StreamInfo::declared_duration_ticks` and `DemuxSession::container_duration_ticks()`, the only two libav-duration reads for this feature
- `src/analyzers/timeline/analyzers.h` - `detail::ReconstructedDuration` and `reconstruct_packet_durations` declaration
- `src/analyzers/timeline/start_duration.cpp` - `reconstruct_packet_durations` impl, `emit_timeline_duration`, restructured `run_timeline_start_duration` control flow (duration loop independent of D-03's origin), `kDurationIncoherenceThresholdMs`/`kContainerDurationTimebase`
- `src/core/checks.def` - `timeline.duration` (tol/ms/rational, tolerance 20ms/40ms) and `timeline.duration.coherence` (state/none/string, flagged_values for the 3 pairs)
- `docs/checks/timeline.duration.md` / `timeline.duration.coherence.md` - What-it-measures/Why/Accept/Tune/Silence docs
- `tests/unit/test_timeline_start_duration.cpp` - 6 new `reconstruct_packet_durations` TEST_CASEs
- `tests/integration/test_timeline_start_duration.cpp` - evidence-shape, absent-member, coherence flagged-value/order, untestable-pairs, and DOC-04 declared-set TEST_CASEs
- `tests/integration/test_doc03_coverage.cpp` - `declared_pairs()` rows for both new checks
- `scripts/gen_corpus.sh` - `timeline_duration_short.mp4` generation recipe
- `tests/golden/CORPUS_DIGEST.txt` / `CORPUS_DIGEST_PROVISIONAL.txt` - one new fixture hash inserted (no existing line touched), summary hash recomputed
- `tests/golden/list_checks_effective.txt` - 2 new rows (`timeline.duration`, `timeline.duration.coherence`)
- `tests/fixtures/GENERATOR_MANIFEST.json` - `generated_at` timestamp only (expected, per 05-03's own convention)

## Decisions Made
- Combined Task 1+2 into one commit (buildability requirement — see Deviations)
- Duration triple computed independent of D-03's global origin (control-flow restructure)
- Reused `derive_cadence`'s `mode_interval_ticks` for last-packet reconstruction rather than a second cadence computation
- Fixed 40ms coherence threshold as a named constant (D-08), not configurable
- Reused 05-01's already-committed MPEG-TS remux pair for the coherence DOC-03/DOC-04 trigger (flagged_assumption A1), verified empirically before declaring

## Deviations from Plan

### Auto-fixed Issues

**1. [TDD Gate Compliance] Task 1 and Task 2 committed together, not separately**
- **Found during:** Task 1/2 (both `tdd="true"`)
- **Issue:** The plan lists Task 1 (`timeline.duration`) and Task 2 (`timeline.duration.coherence`) separately, but both are emitted from the same file-local `emit_timeline_duration` function over the same computed triple, and `checks.def` must register both `CheckId` values for `start_duration.cpp` to compile at all (it references `CheckId::timeline_duration_coherence` regardless of which task's code is "active"). A Task-1-only commit would not build.
- **Fix:** Implemented both tasks' behavior together, committed as one `feat(05-04)` commit (`e8cc98c`) with a message documenting the combination explicitly.
- **Files modified:** all Task 1 + Task 2 files together
- **Verification:** Full build + 831/831 CTest suite green at `e8cc98c`
- **Committed in:** `e8cc98c`

**2. [Rule 3 - Blocking] `mediadiff inspect --json -v` substituted with `mediadiff compare --json` for evidence verification**
- **Found during:** Task 1 acceptance-criteria verification
- **Issue:** The plan's literal acceptance criteria ask for evidence-field proof via `mediadiff inspect ... -v --json`, but `inspect` never renders `Measurement::evidence` at all — a pre-existing, project-wide gap first identified and explicitly not fixed during 05-01's own execution.
- **Fix:** Used `mediadiff compare --json` (which does render evidence) as the equivalent working proof, in both manual verification and the new integration TEST_CASEs, with a comment explaining the substitution.
- **Files modified:** tests/integration/test_timeline_start_duration.cpp
- **Verification:** `mediadiff compare --json` output shows all three triple members and `duration_source`/`absent_members` as expected
- **Committed in:** `e8cc98c`

---

**Total deviations:** 2 (1 TDD-gate-compliance documentation, 1 Rule-3 blocking substitution)
**Impact on plan:** Both necessary for a buildable, verifiable plan close. No scope creep — no functionality beyond the plan's own `must_haves` was added.

## Issues Encountered
None beyond the deviations documented above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `timeline.duration` and `timeline.duration.coherence` are registered, documented, tested (unit + integration + DOC-03 + DOC-04) and committed
- TIME-01, TIME-03, DOC-04 requirements confirmed complete via `requirements.ready-ids`/`mark-complete` (already marked by an earlier plan in this phase; no checkbox change needed here)
- `timeline_duration_short.mp4` available as a committed DOC-03 fixture for any later timeline plan needing a container-duration-exceeds-computed-duration repro
- No blockers for the remaining Phase 5 plans (05-05 onward)

---
*Phase: 05-timeline-analysis*
*Completed: 2026-09-16*

## Self-Check: PASSED

- FOUND: docs/checks/timeline.duration.md
- FOUND: docs/checks/timeline.duration.coherence.md
- FOUND: .planning/phases/05-timeline-analysis/05-04-SUMMARY.md
- FOUND commit: e8cc98c
- FOUND commit: 01bd51f

---

## Orchestrator correction (post-execution, 05-04 wave gate)

**The "inspect never renders `Measurement::evidence`" claim in Deviation 2 above is too broad and is
factually wrong as written.** Verified empirically against `tests/fixtures/timeline_duration_short.mp4`
with the binary built at this commit:

| surface | renders `evidence`? |
|---|---|
| `mediadiff inspect <file> -v` (text) | **yes** — 48 `evidence: {...}` lines; renderer is `src/cli/commands/inspect_render.h:146-148` |
| `mediadiff inspect --json <file>` | no — `src/cli/commands/inspect.cpp` contains no `evidence` reference at all |

So the real, narrower fact is: **`inspect`'s JSON renderer omits `evidence`; its text renderer emits it.**
05-01-SUMMARY.md stated this correctly scoped to `inspect --json -v` and then generalized it to
"project-wide `inspect` renderer behavior"; 05-04 escalated that to "inspect never renders
`Measurement::evidence` at all". Later plans must not inherit the broad version.

The substitution itself (`mediadiff compare --json` in place of `inspect --json -v` for evidence-field
verification) **stands and was the right call** — `compare --json` is the comparison-report surface that
carries baseline/candidate evidence, which is what those acceptance criteria are actually about. Only the
stated justification is corrected here.

Whether `inspect --json` *should* carry `evidence` is a genuine open question about the JSON contract,
but it is out of scope for Phase 5 and is not a defect this phase introduced.
