---
phase: 05-timeline-analysis
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/core/rational.h
  - src/analyzers/timeline/unwrap.h
  - src/analyzers/timeline/unwrap.cpp
  - CMakeLists.txt
  - tests/unit/test_rational_wide.cpp
  - tests/unit/test_timeline_unwrap.cpp
  - tests/unit/CMakeLists.txt
autonomous: true
requirements: [TIME-01, TIME-02]

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "`core/rational.h` gains a 128-bit-safe ACCUMULATOR (not merely overflow detection): summing thirty-two products of magnitude ~(6.48e8)^2 — a two-hour file at a 90 kHz timebase — produces the exact mathematical sum, where a naive `int64_t` accumulation overflows by roughly a factor of 46."
    - "The accumulator uses the SAME `#if defined(_MSC_VER)` / `#else` split `detail::checked_mul` already establishes: `_mul128` + `_addcarry_u64` on MSVC, `__int128` elsewhere — one shared extension in `core/rational.h`, never a bespoke bignum beside an analyzer."
    - "Narrowing a 128-bit accumulator back to `int64_t` is RANGE-CHECKED and reports failure to the caller; it never silently truncates, and a caller that cannot narrow reports `insufficient_data` rather than a wrapped value (`cadence.cpp`'s own established precedent)."
    - "`unwrap_ts_timestamps` implements doc 04 §1.2 exactly: a consecutive delta below `-kTsPtsWrapHalfRange` (-2^32) adds `kTsPtsWrapModulus` (2^33) to a running offset and counts one wrap event; a backward jump LARGER than half range in the other direction is a genuine discontinuity and does NOT adjust the offset."
    - "Raw values survive the unwrap unmodified — the function returns unwrapped values alongside the wrap-event count and never mutates or re-sorts its input span (TIME-02's 'raw values are preserved in evidence')."
    - "Every arithmetic step in the unwrap goes through `detail::checked_sub`/`checked_add`; an overflow on the running offset itself is reported to the caller rather than silently wrapping, so a crafted stream cannot grow the offset without bound."
    - "Both new units are pure, deterministic and header-declared with no libav include and no `double`/`float`/`std::sqrt` anywhere — the same no-float-in-compared-values rule `cadence.{h,cpp}` already carries."
    - statement: "Repeated calls with an identical input span produce byte-identical output, which is what makes every downstream timeline value satisfy the idempotence guarantee."
      verification: backstop
  artifacts:
    - src/analyzers/timeline/unwrap.h
    - src/analyzers/timeline/unwrap.cpp
    - tests/unit/test_rational_wide.cpp
    - tests/unit/test_timeline_unwrap.cpp
  key_links:
    - "`Int128Accum` -> `av_sync.cpp`'s least-squares sums (plan 05-09): without genuine 128-bit accumulation the flagship drift check reports `insufficient_data` on ordinary two-hour content, which is a defect, not a safety net."
    - "`unwrap_ts_timestamps` -> `timeline.gaps`/`timeline.discontinuities`/`timeline.wrap_events` (plan 05-06): unwrap runs BEFORE any other timeline statistic on a TS stream, or a wrap is mistaken for a backward discontinuity."
    - "the range-checked narrowing -> `SkipReason::insufficient_data`: a silent truncation here becomes a confidently wrong ms/min rate downstream."
  prohibitions:
    - "Never widen the accumulator by moving the sums into `double` — PROJECT.md's rational-everywhere rule is absolute for compared values, and a double accumulator loses integer precision exactly where correctness matters."
    - "Never treat a failed `checked_*` as 'close enough' and continue with a partial sum; report the failure so the caller can skip."
    - "Never adjust the running unwrap offset on a forward jump; doc 04 §1.2 makes the asymmetry normative and a symmetric rule would erase genuine discontinuities."
    - "Never mutate or sort the caller's span in place; sort a local index view, exactly as `cadence.cpp` does."
---

<objective>
The two arithmetic primitives every later timeline plan depends on, landed and unit-tested before any
check consumes them: a 128-bit-safe accumulator in `core/rational.h`, and doc 04 §1.2's 33-bit MPEG-TS
timestamp unwrap as a pure function.

Purpose: `05-RESEARCH.md`'s worked overflow analysis shows the least-squares `K·Σx²` term exceeds
`int64_t` by ~46x on an ordinary two-hour 90 kHz file — so the flagship `av_drift` check would report
`insufficient_data` on real content if it were built on `checked_mul` alone. Landing the accumulator
and the unwrap first, with their own fail-first unit tests, means plans 05-06 and 05-09 consume proven
primitives instead of discovering the bound mid-implementation.
Output: `Int128Accum` (or the approved spelling) in `core/rational.h`,
`src/analyzers/timeline/unwrap.{h,cpp}`, and two new unit-test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-timeline-analysis/05-CONTEXT.md
@.planning/phases/05-timeline-analysis/05-RESEARCH.md
@.planning/phases/05-timeline-analysis/05-PATTERNS.md
@claude_docs/04-timeline-analysis.md
@src/core/rational.h
@src/probe/cadence.h
</context>

## Artifacts this phase produces

| Kind | Symbol / path |
|---|---|
| Core type | `mediadiff::detail::Int128Accum` in `src/core/rational.h` — `add_product(int64_t, int64_t)`, `add(int64_t)`, `try_narrow(int64_t* out) -> bool` |
| Constants | `kTsPtsWrapModulus` (2^33), `kTsPtsWrapHalfRange` (2^32) in `src/analyzers/timeline/unwrap.h` |
| Struct | `mediadiff::UnwrapResult { std::vector<std::int64_t> unwrapped; std::int64_t wrap_events; bool overflowed; }` |
| Function | `mediadiff::unwrap_ts_timestamps(std::span<const std::int64_t>) -> UnwrapResult` |
| Tests | `tests/unit/test_rational_wide.cpp`, `tests/unit/test_timeline_unwrap.cpp` |
| Registration | `libmediadiff` source/header entries in `CMakeLists.txt`; two source entries in `tests/unit/CMakeLists.txt` |

<flagged_assumptions>
**A1 — TIME-02's probe edge row is `unclassified` and stays unresolved.** The deterministic edge probe
returned one row for TIME-02 with `category: unclassified`, `status: unresolved`, `verification: null`.
Per the spec-less fallback protocol an `unclassified` row is never auto-resolved with a backstop and
never dismissed, so it is surfaced here as a flagged assumption. The planner's reading is that the real
edge is **a wrap occurring on the very first pair of timestamps in the stream, and a wrap occurring at
the exact half-range boundary** — Task 2's behaviour list covers both explicitly (`delta ==
-kTsPtsWrapHalfRange` is NOT a wrap; `delta == -kTsPtsWrapHalfRange - 1` is). That reading is an
assumption, not a resolved edge.

**A2 — MSVC 128-bit composition is not compiled locally.** `_mul128` and `_addcarry_u64` are verified
present in `05-RESEARCH.md`'s sketch and `core/rational.h` already includes `<intrin.h>` for the
existing `checked_mul` MSVC branch, but the MSVC arm of the new accumulator is first exercised on the
Windows CI leg, not on this workstation. The unit test must therefore be written so that BOTH arms are
compiled from the same test source with the same expected values, and the Windows leg is the proof.
`.planning/WINDOWS.md` is the ledger if it fails there.

**A3 — canon-referral breadcrumb (prohibition recall).** Two Stage-1 candidates — "never introduce
integer-overflow undefined behaviour" and "never allow unbounded resource growth from crafted input" —
are canon security items already owned by `core/rational.h`'s checked-arithmetic primitives and by
`kMaxPacketsPerStream`. They are DROPPED from `must_haves.prohibitions` per the canon-referral rule
rather than minted as new project prohibitions; they appear instead as T-05-05 and T-05-06 in this
plan's threat register, where they carry a severity and a disposition.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A 128-bit-safe accumulator in `core/rational.h`, with range-checked narrowing</name>
  <files>src/core/rational.h, tests/unit/test_rational_wide.cpp, tests/unit/CMakeLists.txt</files>
  <read_first>
    - `src/core/rational.h` in full — especially `detail::checked_mul` (the `#if defined(_MSC_VER)` `_mul128` branch and the `__builtin_mul_overflow` `#else` branch), `checked_add`/`checked_sub`/`checked_div`/`checked_negate`, and `compare_ticks`' cross-multiplication-not-division discipline. The new accumulator extends this file's existing split one step further; it does not introduce a new conditional-compilation idiom.
    - `tests/unit/test_rational.cpp` in full — the existing fail-first style for this file's helpers: hand-computed expected values, boundary cases at `INT64_MAX`/`INT64_MIN`, and the `TEST_CASE("<name> - <behaviour>", "[unit]")` naming convention.
    - `tests/unit/CMakeLists.txt` lines 1-60 and 310-325 — the `add_executable` source list with its per-file comment convention naming the owning plan and requirement, and `catch_discover_tests(mediadiff_unit_tests TEST_PREFIX "unit.")`, which makes every ctest name `unit.<TEST_CASE name>`.
    - `.planning/phases/05-timeline-analysis/05-RESEARCH.md` — the "128-bit-safe least-squares accumulation" code example and the Pitfall 2 magnitude analysis (`Σx²` ≈ 1.34e19 over 32 terms, `K·Σx²` ≈ 4.3e20).
    - `src/probe/cadence.cpp` lines 100-115 — the comment "an overflow anywhere in this arithmetic yields insufficient_data, never a wrapped value", the precedent the narrowing contract follows.
  </read_first>
  <behavior>
    - Test 1: `add_product(a, b)` over thirty-two terms of `a == b == 648'000'000` yields exactly `32 * 648000000^2` — asserted against the hand-computed decimal literal, with a sibling assertion showing that the same sum accumulated in `int64_t` via `checked_mul`/`checked_add` FAILS (proving the test would not pass without the widening).
    - Test 2: a sum whose true value fits in `int64_t` narrows successfully via `try_narrow` and equals the value an `int64_t` accumulation would have produced.
    - Test 3: a sum whose true value exceeds `INT64_MAX` returns `false` from `try_narrow` and leaves the out-parameter untouched — never a truncated low word.
    - Test 4: negative products accumulate correctly, including a sum that crosses zero from positive to negative and back (the MSVC carry path's sign handling is where a hand-rolled 128-bit add goes wrong).
    - Test 5: `INT64_MIN * INT64_MIN` — the extreme single product — is representable in the accumulator and `try_narrow` reports `false` for it.
    - Test 6: an accumulator that has had nothing added narrows to exactly `0`.
  </behavior>
  <action>
    Add the accumulator to `src/core/rational.h` in the `detail` namespace, immediately after
    `checked_mul`, with a comment block that (a) names plan 05-09's least-squares fit as the consumer
    that forces it to exist, (b) states the concrete magnitude bound from `05-RESEARCH.md` (a two-hour
    file at 90 kHz gives `x_max ≈ 6.48e8`, so `K·Σx²` ≈ 4.3e20, roughly 46x `INT64_MAX`), and (c) states
    explicitly that this is ACCUMULATION, not overflow detection — `checked_mul` correctly detects
    64-bit overflow, but returning "cannot compute" on an ordinary two-hour movie is itself a defect.

    The type exposes three operations and nothing else: `add_product(std::int64_t a, std::int64_t b)`,
    `add(std::int64_t v)`, and `bool try_narrow(std::int64_t* out) const`. Keep the surface minimal —
    this is not a general bignum, and a wider surface invites a second consumer to depend on semantics
    nobody tested.

    Implement with the SAME conditional-compilation shape the file already uses. On the `#else` (GCC /
    Clang / AppleClang) arm hold a single `__int128` and accumulate directly. On the
    `#if defined(_MSC_VER)` arm hold `{std::int64_t hi; std::uint64_t lo;}` and compose `_mul128` with
    `_addcarry_u64`, sign-extending the product's high word before adding it to `hi`. `<intrin.h>` is
    already included at the top of this file for `checked_mul`; do not add a second include guard for it.

    `try_narrow` is range-checked: it succeeds only when the 128-bit value is exactly representable as
    `std::int64_t`, and on failure it returns `false` WITHOUT writing `*out`. State in the comment that
    every caller maps a `false` return to `SkipReason::insufficient_data`, citing `cadence.cpp`'s own
    precedent by name.

    C++20 toolchain-parity note to honour: no `std::format`, no modules, no coroutines, no `std::jthread`
    — the file's existing constraints are unchanged. `__int128` is a GCC/Clang extension, not a standard
    type, which is exactly why the MSVC arm exists; do not use it outside the `#else` branch.

    Create `tests/unit/test_rational_wide.cpp` and register it in `tests/unit/CMakeLists.txt`'s source
    list with the established per-file comment naming this plan and TIME-01/TIME-02. Name every
    `TEST_CASE` in the file with the literal prefix `rational_wide - ` so
    `ctest -R "unit\.rational_wide"` selects exactly this file's cases. Every expected value is computed
    BY HAND from the constructed input before the implementation is written — never captured from what
    the implementation currently produces.
  </action>
  <verify>
    <automated>cmake --build build/x64-linux --target mediadiff_unit_tests 2>&amp;1 | tail -5 &amp;&amp; ctest --test-dir build/x64-linux -R "unit\.(rational_wide|rational)" --output-on-failure</automated>
    <fails_when>Non-zero exit; or the output contains "No tests were found" (the `rational_wide - ` TEST_CASE prefix was not used, and ctest exits 0 on an empty selection); or fewer than six `unit.rational_wide` cases are listed in the run; or the build emits a warning-as-error from `core/rational.h`.</fails_when>
  </verify>
  <acceptance_criteria>
    - **Locally provable** — `ctest --test-dir build/x64-linux -N -R "unit\.rational_wide" | grep -c 'Test *#'` reports at least `6`.
    - **Locally provable** — `grep -c '__int128' src/core/rational.h` reports at least `1`, and every occurrence is inside the `#else` arm (read the `#if defined(_MSC_VER)` block to confirm none appears in the MSVC arm).
    - **Locally provable** — `grep -c '_addcarry_u64' src/core/rational.h` reports at least `1`.
    - **Locally provable** — `grep -v '^ *//' src/core/rational.h | grep -c 'std::sqrt'` reports `0`.
    - **Locally provable** — `./build/x64-linux/mediadiff --version` still runs (the header is included by nearly every translation unit; a broken conditional block breaks the whole build).
  </acceptance_criteria>
  <done>`core/rational.h` carries a 128-bit accumulator with range-checked narrowing on both toolchain arms, proven by a unit test whose sibling assertion demonstrates that 64-bit accumulation fails on the same input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `unwrap_ts_timestamps` — doc 04 §1.2's 33-bit unwrap, wrap distinguished from a genuine backward discontinuity</name>
  <files>src/analyzers/timeline/unwrap.h, src/analyzers/timeline/unwrap.cpp, CMakeLists.txt, tests/unit/test_timeline_unwrap.cpp, tests/unit/CMakeLists.txt</files>
  <read_first>
    - `claude_docs/04-timeline-analysis.md` §1.2 in full — the normative rule, quoted verbatim into the header comment.
    - `src/probe/cadence.h` lines 1-50 and the `derive_cadence` doc block — the pure-function-over-a-caller-owned-span contract, the named-constant discipline (no bare literals), and the "sort a local index view, never the source array" rule this file copies.
    - `src/probe/cadence.cpp` lines 80-115 — the actual index-view sort and the overflow-yields-`insufficient_data` comment.
    - `src/core/rational.h` — `detail::checked_sub` and `detail::checked_add`, used for every delta and every offset update.
    - `src/probe/packet_scan.h` lines 96-120 — `PacketRecord`'s read-order contract and the `AV_NOPTS_VALUE`-preserved-verbatim rule, so the caller's responsibility for filtering sentinels is stated correctly in this header's comment.
    - `tests/unit/test_ts_continuity.cpp` in full — the table-driven pure-function unit-test shape (`PacketSpec`/`Tally`) and its `TEST_CASE("ts_continuity - <behaviour>", "[unit]")` naming convention, which this file mirrors.
    - `.planning/phases/05-timeline-analysis/05-RESEARCH.md` — the "33-bit TS unwrap" code example and the crafted-TS denial-of-service threat pattern in the Security Domain section.
    - `CMakeLists.txt` lines 160-245 — the `libmediadiff` source and header lists.
  </read_first>
  <behavior>
    - Test 1: a monotonically increasing sequence well inside 33 bits is returned unchanged with `wrap_events == 0`.
    - Test 2: a sequence that steps from just below `2^33 - 1` down to a small value (a delta below `-2^32`) is unwrapped by adding `2^33`, producing a monotonically increasing output, with `wrap_events == 1`.
    - Test 3: two successive wraps in one stream produce `wrap_events == 2` and an output that increases across both.
    - Test 4: a backward jump of exactly `-kTsPtsWrapHalfRange` is NOT treated as a wrap (the rule is strictly below); a jump of `-kTsPtsWrapHalfRange - 1` IS.
    - Test 5: a modest backward jump (e.g. -90000 ticks, one second at 90 kHz) is a genuine discontinuity: the offset is unchanged, `wrap_events == 0`, and the output preserves the backward step so a downstream discontinuity check can see it.
    - Test 6: a large FORWARD jump (greater than half range) does not adjust the offset in either direction.
    - Test 7: an empty span returns an empty result with `wrap_events == 0`; a single-element span returns that element unchanged.
    - Test 8: a crafted sequence engineered to force repeated offset growth reports `overflowed == true` once `checked_add` on the running offset fails, and stops adjusting rather than wrapping silently.
    - Test 9: the input span is unchanged after the call (asserted by comparing a copy taken before).
  </behavior>
  <action>
    Create `src/analyzers/timeline/unwrap.h` and `unwrap.cpp`. The header's top comment quotes doc 04
    §1.2 verbatim as the normative source, then states the same three contracts `cadence.h` states about
    itself: pure, deterministic, integer-only; operates on a caller-owned span and never mutates or
    re-sorts it; no second `av_read_frame` sweep and nothing pre-computed during the scan.

    Declare two named constants, never bare literals:
    `inline constexpr std::int64_t kTsPtsWrapModulus = std::int64_t{1} &lt;&lt; 33;` and
    `inline constexpr std::int64_t kTsPtsWrapHalfRange = std::int64_t{1} &lt;&lt; 32;`, each with a comment
    naming doc 04 §1.2 as its source and the 90 kHz PTS domain it belongs to.

    `UnwrapResult` carries `std::vector&lt;std::int64_t&gt; unwrapped` (same length as the input),
    `std::int64_t wrap_events` and `bool overflowed`. Document that `unwrapped` is the ONLY value
    downstream analysis consumes and that the caller keeps the raw span for evidence — TIME-02's "raw
    values are preserved in evidence" is the caller's job, and this function makes it possible by never
    destroying them.

    `unwrap_ts_timestamps(std::span&lt;const std::int64_t&gt; raw_in_read_order)` walks consecutive pairs.
    Compute the delta with `detail::checked_sub`; when the delta is strictly below
    `-kTsPtsWrapHalfRange`, add `kTsPtsWrapModulus` to the running offset via `detail::checked_add` and
    increment `wrap_events`. A failed `checked_add` on the offset sets `overflowed` and stops adjusting
    — a crafted stream must not grow the offset without bound (the Denial-of-Service threat
    `05-RESEARCH.md` names). The symmetric forward guard does NOT adjust the offset: state in a comment
    that doc 04 makes the asymmetry normative and that a symmetric rule would erase genuine
    discontinuities, which is precisely what TIME-02 asks to distinguish.

    Apply the offset to each raw value with `detail::checked_add`; a failure there also sets
    `overflowed` and the caller maps that to `SkipReason::insufficient_data`.

    Add `src/analyzers/timeline/unwrap.cpp` to `libmediadiff`'s source list and
    `src/analyzers/timeline/unwrap.h` to its header list in `CMakeLists.txt`.

    Create `tests/unit/test_timeline_unwrap.cpp` in `test_ts_continuity.cpp`'s table-driven shape and
    register it in `tests/unit/CMakeLists.txt` with the per-file comment naming this plan and TIME-02.
    Name every `TEST_CASE` with the literal prefix `timeline_unwrap - ` so
    `ctest -R "unit\.timeline_unwrap"` selects exactly this file. Every expected value is hand-computed
    from the constructed tick sequence before the implementation exists.
  </action>
  <verify>
    <automated>cmake --build build/x64-linux --target mediadiff_unit_tests 2>&amp;1 | tail -5 &amp;&amp; ctest --test-dir build/x64-linux -R "unit\.timeline_unwrap" --output-on-failure</automated>
    <fails_when>Non-zero exit; or the output contains "No tests were found" (the `timeline_unwrap - ` TEST_CASE prefix was not used); or fewer than nine `unit.timeline_unwrap` cases are listed in the run; or the build emits a warning-as-error from `unwrap.cpp`.</fails_when>
  </verify>
  <acceptance_criteria>
    - **Locally provable** — `ctest --test-dir build/x64-linux -N -R "unit\.timeline_unwrap" | grep -c 'Test *#'` reports at least `9`.
    - **Locally provable** — `grep -v '^ *//' src/analyzers/timeline/unwrap.cpp src/analyzers/timeline/unwrap.h | grep -c '8589934592\|4294967296'` reports `0`: the two magnitudes appear only as the named constants' definitions (`1 << 33` / `1 << 32`), never as a bare decimal literal at a use site.
    - **Locally provable** — `grep -v '^ *//' src/analyzers/timeline/unwrap.cpp | grep -c 'double\|float \|std::sqrt'` reports `0`.
    - **Locally provable** — `grep -c 'checked_add\|checked_sub' src/analyzers/timeline/unwrap.cpp` reports at least `3`: the delta, the offset update and the value application all go through checked arithmetic.
    - **Locally provable** — `grep -c 'libav\|AVPacket\|avformat' src/analyzers/timeline/unwrap.h` reports `0`: this is a pure integer unit with no libav dependency.
  </acceptance_criteria>
  <done>`unwrap_ts_timestamps` implements doc 04 §1.2 exactly, distinguishes a wrap from a genuine backward discontinuity in both directions, reports overflow rather than wrapping silently, and is proven by nine hand-computed unit cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demuxed tick values → `unwrap_ts_timestamps` | every int64 crossing this boundary originates in an attacker-controllable MPEG-TS stream |
| 128-bit sums → `int64_t` narrowing | the only place a wide value re-enters the ordinary integer domain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Denial of Service | crafted MPEG-TS with pathological PTS sequences driving unbounded growth of the running unwrap offset | high | mitigate | every offset update goes through `detail::checked_add`; a failure sets `overflowed` and the walk stops adjusting, so the offset is bounded by `INT64_MAX` by construction and the caller degrades to `insufficient_data` (Test 8 pins it) |
| T-05-06 | Tampering | a crafted high-timebase/long-duration input overflowing the least-squares sums, producing a fabricated pass/fail verdict from a wrapped slope | high | mitigate | genuine 128-bit accumulation plus a range-checked `try_narrow` that refuses rather than truncating (Tests 1, 3, 5); plan 05-09 maps a refused narrowing to `SkipReason::insufficient_data` |
| T-05-07 | Tampering | a symmetric wrap rule silently converting a genuine backward discontinuity into an "explained" wrap, hiding a real timeline break | high | mitigate | the forward guard explicitly does not adjust the offset; Tests 5 and 6 assert both directions, and the asymmetry is stated in the header as normative |
| T-05-08 | Denial of Service | an enormous input span making the unwrap walk unbounded | low | accept | the only production caller is `StreamPacketScan::packets`, already bounded by `kMaxPacketsPerStream = 5'000'000` (`src/probe/packet_scan.h:63`); the walk is O(n) with no allocation per element beyond the reserved output |
| T-05-SC | Tampering | dependency supply chain | low | accept | no npm/pip/cargo/vcpkg package is added by this plan (`05-RESEARCH.md` § Package Legitimacy Audit: not applicable to this phase) |
</threat_model>

<verification>
- `cmake --build build/x64-linux 2>&1 | tail -5 && ctest --test-dir build/x64-linux --output-on-failure`
- `ctest --test-dir build/x64-linux -N -R "unit\.(rational_wide|timeline_unwrap)"` lists both new suites
</verification>

<success_criteria>
- `core/rational.h` carries a 128-bit accumulator on both toolchain arms with range-checked narrowing.
- `unwrap_ts_timestamps` exists as a pure, libav-free, float-free function honouring doc 04 §1.2's asymmetric rule.
- Fifteen or more hand-computed unit cases across the two new files are green, and the full suite still passes.
</success_criteria>

<output>
Create `.planning/phases/05-timeline-analysis/05-02-SUMMARY.md` when done.
</output>
