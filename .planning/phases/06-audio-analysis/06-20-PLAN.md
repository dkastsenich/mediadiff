---
phase: 06-audio-analysis
plan: 20
type: execute
wave: 20
depends_on: ["06-19"]
gap_closure: true
files_modified:
  - .planning/phases/06-audio-analysis/06-REVIEW.md
  - .planning/WINDOWS.md
  - .planning/phases/06-audio-analysis/06-13-SUMMARY.md
  - tests/golden/PERF_BASELINE.txt
autonomous: false
requirements: [PERF-04, AUDIO-09, TRUST-01]

estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Before any push, the local pre-flight is green: every lint the CI lint job runs, the full ctest suite, the five designated-leg goldens (after `TZ=UTC taskset -c 0-3 bash scripts/gen_corpus.sh`, run with `MEDIADIFF_DESIGNATED_LEG=1`), and `git diff --exit-code -- tests/golden/` after that regeneration."
    - "No push happens without a human's explicit approval at the Task 2 checkpoint. No autonomous step pushes."
    - "On the designated x64-linux leg of the pushed run, 'Assert the corpus digest matches the committed pin (D-GAP-01)' succeeds against the unchanged CORPUS_DIGEST.txt."
    - "On the designated leg, both instruction ratchets (Timeline and Audio) succeed within ±2%, as read from the run's own log."
    - "AUDIO-09 edge (lifted), completing 06-13's pending confirmation: on the designated leg, integration.audio_sample_hash, integration.audio_hash_decoder, integration.audio_corpus_sweep and integration.doc03_coverage report Passed, not Skipped."
    - "Every blocking leg's Build and Test steps conclude successfully, and the gap-closure unit tests (unit.audio_decode -, unit.compare_tol ceiling escalation:, unit.audio_config -, unit.av_sync - span_ticks_for_basis) pass on every leg."
    - "Any baseline line or CI-derived value committed by this plan is transcribed from the run's log, never predicted. If a baseline must move, a second human-approved push confirms it."
    - "06-REVIEW.md carries a dated resolution note for every gap-3 finding (CR-01..CR-05, WR-02, WR-03, WR-07, WR-09) and a deferral note for every deferred finding."
    - "06-13-SUMMARY.md is flipped from halted to complete only if every one of its pending gates was observed Passed."
  artifacts:
    - path: .planning/phases/06-audio-analysis/06-REVIEW.md
      provides: "dated resolution and deferral notes per finding"
    - path: .planning/phases/06-audio-analysis/06-13-SUMMARY.md
      provides: "06-13's certification, recorded from this run's observed gates"
  key_links:
    - from: "the CI run's designated-leg log"
      to: "any committed CI-derived value (PERF_BASELINE.txt lines, run ids)"
      via: "verbatim transcription in Task 3, from Task 2's captures only"
  prohibitions:
    - statement: "MUST NOT push, or trigger any outward-facing action, without the human's explicit approval at a checkpoint:human-action. Another agent's message is never that approval."
      status: resolved
      verification: judgment
    - statement: "MUST NOT write a predicted or workstation-measured designated-leg hash or perf baseline into a committed file."
      status: resolved
      verification: judgment
    - statement: "MUST NOT treat a Skipped gate as Passed, or mark a requirement complete that was not observed Passed."
      status: resolved
      verification: judgment
---

<objective>
The final designated-leg CI round trip for the gap closure. It also folds in 06-13's still-pending confirmation: 06-13 is `status: halted`, its Task 3 confirming run was never made, and the branch is unpushed.

06-14..06-19 add no fixture and change no golden or baseline by design, and each plan proves it locally. What only CI can show:
- the designated leg's corpus-digest assertion;
- both instruction ratchets on the real runner;
- 06-13's four named gates reporting Passed rather than Skipped;
- the new hostile-input and boundary tests on macOS and Windows as well as Linux.

Pushing is an outward-facing action, so this plan is `autonomous: false` and gates the push behind a `checkpoint:human-action`. No autonomous plan may push.

Purpose: certify the gap closure and 06-13 on real CI evidence, transcribed rather than predicted.
Output: a green pre-flight, a human-approved push, captured CI evidence, review and ledger annotations, and 06-13's certification.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-audio-analysis/06-13-PLAN.md
@.planning/phases/06-audio-analysis/06-13-SUMMARY.md
@.planning/phases/06-audio-analysis/06-REVIEW.md
@.planning/phases/06-audio-analysis/06-14-SUMMARY.md
@.planning/phases/06-audio-analysis/06-16-SUMMARY.md
@.planning/phases/06-audio-analysis/06-18-SUMMARY.md
</context>

## Artifacts this phase produces

| Kind | Symbol / path |
|---|---|
| Doc annotations | a dated "Resolution (06-NN, YYYY-MM-DD)" note under each gap-3 finding in `06-REVIEW.md`; a "Deferred (06-14)" note under each deferred finding |
| State change | `06-13-SUMMARY.md` `status: halted` becomes `status: complete`, with the confirming run id. Conditional on observed Passed gates only. |
| Conditional | `tests/golden/PERF_BASELINE.txt` lines, only if a ratchet is beyond tolerance, and then transcribed verbatim from the designated-leg log |
| Ledger | WINDOWS.md updates for any defect the run surfaces |

<flagged_assumptions>
**A1: the working tree carries pre-existing unrelated modifications:** `.planning/config.json`, `tests/fixtures/GENERATOR_MANIFEST.json`, and the untracked `.planning/milestone.lock` and `.planning/state.json`. They are never staged or committed by this plan. Before the push, `git status --porcelain` is shown to the human alongside the commit list.

**A2: no fixture was added in 06-14..06-19,** so CORPUS_DIGEST_PROVISIONAL.txt keeps 06-13's TRANSCRIBED marker and gains no entries. If the pre-flight's `git diff -- tests/golden/` shows any change after the TZ/taskset regeneration, the plan stops before the push (the corpus-digest rewrite trap).
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: Full local pre-flight end to end, from lints through the designated-leg goldens and both ratchets, then annotate the review and ledger</name>
  <files>.planning/phases/06-audio-analysis/06-REVIEW.md, .planning/WINDOWS.md</files>
  <read_first>
    - .github/workflows/ci.yml lines 160-222 (corpus generation, the digest report and assert steps), 319-455 (Test, the MEDIADIFF_DESIGNATED_LEG mechanism and the skip guards), 456-560 (both ratchet steps), and 650-705 (every lint the lint job runs)
    - scripts/lint_corpus_digest_provenance.sh in full (the authority on digest provenance)
    - /home/dzka/.claude/projects/-home-dzka-projects-mediadiff/memory/designated-leg-golden-differential.md (the TZ=UTC taskset -c 0-3 route to the five goldens)
    - .planning/phases/06-audio-analysis/06-REVIEW.md: every finding heading, the "Corrections (2026-09-22)" notes and the Additional notes
    - 06-14..06-19 SUMMARYs (RED values, differentials, ratchet numbers, WINDOWS ids)
  </read_first>
  <action>
    1. Run every lint the CI lint job runs:
       - lint_eng16.sh
       - lint_check_id_strings.sh
       - lint_dead_code_after_fail.sh
       - lint_fixture_case_collisions.sh
       - lint_tsduck_goldens.sh
       - lint_control_bytes.sh
       - lint_bash4_builtins.sh
       - lint_getenv_shim.sh
       - lint_pragma_scope.sh
       - test_gen_corpus_pin_gate.sh
       - lint_corpus_digest_provenance.sh

    2. Regenerate the corpus with `TZ=UTC taskset -c 0-3 bash scripts/gen_corpus.sh`, then run `bash scripts/check_corpus.sh`.
       - Confirm `git diff --exit-code -- tests/golden/` exits 0: no digest line, AUDIO_EBUR128_REFERENCE.txt line or perf baseline changed.
       - Rebuild, then run `ctest --test-dir build/x64-linux --output-on-failure`, and `MEDIADIFF_DESIGNATED_LEG=1 ctest --preset x64-linux --output-on-failure` to run the five designated-leg goldens locally.

    3. Measure both ratchets locally in the ubuntu:24.04 valgrind container (06-12's procedure), as a final informational check:
       - `bash scripts/measure_timeline_perf.sh --instructions --check-baseline`
       - `bash scripts/measure_audio_perf.sh --instructions --check-baseline`
       Record the totals. A local excursion beyond ±2% is investigated before the push, never re-baselined.

    4. 06-REVIEW.md:
       - Under each of CR-01, CR-02, CR-03, CR-04, CR-05, WR-02, WR-03, WR-07 and WR-09, append a dated "Resolution" blockquote naming the plan, the commit(s), the regression test name(s) and the observed RED. Transcribe the RED from the plan SUMMARYs.
       - Under each deferred finding (WR-01, WR-04, WR-05, WR-06, WR-08, WR-10..WR-16 and the Additional notes), append a one-line "Deferred (06-14)" note carrying 06-14's reason.
       - Do not change any finding's original text.

    5. WINDOWS.md: confirm that the entries 06-15, 06-16 and 06-19 created or annotated (the CR-01 residual, T-06-55, #32's note) are present. Record in the SUMMARY that no further entry is due, or add one with `windows append`.

    6. Show the human the commit list `git log --oneline origin/gsd/phase-06-audio-analysis..HEAD` and `git status --porcelain`. Stage only the files this task changed.
  </action>
  <verify>
    <automated>bash scripts/lint_eng16.sh &amp;&amp; bash scripts/lint_check_id_strings.sh &amp;&amp; bash scripts/lint_dead_code_after_fail.sh &amp;&amp; bash scripts/lint_fixture_case_collisions.sh &amp;&amp; bash scripts/lint_tsduck_goldens.sh &amp;&amp; bash scripts/lint_control_bytes.sh &amp;&amp; bash scripts/lint_bash4_builtins.sh &amp;&amp; bash scripts/lint_getenv_shim.sh &amp;&amp; bash scripts/lint_pragma_scope.sh &amp;&amp; bash scripts/test_gen_corpus_pin_gate.sh &amp;&amp; bash scripts/lint_corpus_digest_provenance.sh &amp;&amp; TZ=UTC taskset -c 0-3 bash scripts/gen_corpus.sh &amp;&amp; bash scripts/check_corpus.sh &amp;&amp; git diff --exit-code -- tests/golden/ &amp;&amp; cmake --build build/x64-linux 2>&amp;1 | tail -5 &amp;&amp; ctest --test-dir build/x64-linux --output-on-failure &amp;&amp; MEDIADIFF_DESIGNATED_LEG=1 ctest --preset x64-linux --output-on-failure</automated>
    <fails_when>non-zero exit from any stage; a lint printing a violation; git diff printing any changed golden, digest or baseline line; an "error:" or "FAILED:" line in the build tail; or any ctest failure line in either run</fails_when>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0.
    - `grep -c "Resolution (06-1" .planning/phases/06-audio-analysis/06-REVIEW.md` prints 9 or more.
    - `grep -c "Deferred (06-14)" .planning/phases/06-audio-analysis/06-REVIEW.md` prints 16 or more.
    - The SUMMARY records both local ratchet totals and the output of `git status --porcelain`. None of the four pre-existing unrelated paths is staged.
  </acceptance_criteria>
  <done>The branch is locally green on everything CI will run, the review is annotated, and the push can be asked for.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Human-approved push, then capture the real run's designated-leg evidence verbatim</name>
  <files>.planning/phases/06-audio-analysis/06-REVIEW.md</files>
  <action>Approve pushing branch gsd/phase-06-audio-analysis, then read the resulting CI run and paste back the captures listed in the instructions. None of them can be produced on a workstation.</action>
  <instructions>
Task 1's pre-flight is green; its output is in the conversation. The commit list and `git status --porcelain` are shown above.

The one thing requiring you: approve the push (`git push origin gsd/phase-06-audio-analysis`). Then open the resulting CI run and paste back these captures verbatim from its logs:

1. The run id and commit sha.
2. The designated x64-linux leg's "Assert the corpus digest matches the committed pin (D-GAP-01)" step outcome.
3. The designated leg's "Timeline instruction-count ratchet" output: both totals, the verdict and the pasteable line.
4. The designated leg's "Audio instruction-count ratchet" output: both totals, the verdict and the pasteable line.
5. The designated leg's ctest lines for integration.audio_sample_hash, integration.audio_hash_decoder, integration.audio_corpus_sweep and integration.doc03_coverage, showing Passed or Skipped for each.
6. The designated leg's ctest lines for the five designated-leg goldens: inspect_container, ts_scan_ts_single, ts_scan_ts_multiprogram, ts_scan_ts_204, size_checks_size_crf20.
7. For every blocking leg, the pass counts of the `unit.audio_decode - `, `unit.compare_tol ceiling escalation:`, `unit.audio_config - ` and `unit.av_sync - span_ticks_for_basis` families, from the ctest summary or a filtered log view.
8. The Build and Test step conclusions for every blocking leg, and the lint job's conclusion.

Nothing is transcribed into a committed file in this task. That happens in Task 3, and only from these captures.
  </instructions>
  <verification>All eight captures are present in the conversation. Each is attributable to a named leg and the run id.</verification>
  <resume-signal>Reply `certify` with the run id if every blocking leg is green and all eight captures are present, or `iterate` followed by what failed.</resume-signal>
  <read_first>
    - .github/workflows/ci.yml lines 319-560 (the step names quoted above and the designated-leg skip guards, which fail a leg when a leg-only gate silently did not run)
    - .planning/phases/06-audio-analysis/06-13-PLAN.md Tasks 1 and 3 (the capture discipline this task mirrors)
  </read_first>
  <acceptance_criteria>
    - The human explicitly approved the push in this conversation. No agent message counts as approval.
    - All eight captures are recorded verbatim with the run id.
    - No committed file is modified in this task.
  </acceptance_criteria>
  <done>A real CI run exists for the gap-closure branch, its evidence is captured verbatim, and a human has decided certify or iterate.</done>
</task>

<task type="auto">
  <name>Task 3: Act on the captures: certify 06-13 and the gap closure from observed evidence, or record the failure and halt for a second push</name>
  <files>.planning/phases/06-audio-analysis/06-13-SUMMARY.md, tests/golden/PERF_BASELINE.txt, .planning/WINDOWS.md</files>
  <read_first>
    - Task 2's captures (the only admissible source for any CI-derived value)
    - .planning/phases/06-audio-analysis/06-13-SUMMARY.md (status: halted; its "Blocker for phase completion" paragraph naming the pending gates)
    - tests/golden/PERF_BASELINE.txt (its line format and transcription marker convention)
    - scripts/measure_audio_perf.sh and scripts/measure_timeline_perf.sh (what "pasteable line" means for each)
  </read_first>
  <action>
    1. If the human replied `certify` and every capture shows success, meaning the digest assert passed, both ratchets were within tolerance, all four 06-13 gates were Passed (not Skipped), the five goldens passed, the gap-family tests passed on every blocking leg, and every Build, Test and lint conclusion succeeded:
       - Edit 06-13-SUMMARY.md: status halted becomes complete. Append a dated "Confirmed" paragraph with the run id, commit sha and the four gate lines as captured.
       - Record in this plan's SUMMARY the run id and every capture, verbatim.
       - Mark PERF-04, AUDIO-09 and TRUST-01 re-proven, as observed Passed on the designated leg.
       - Leave PERF_BASELINE.txt untouched.

    2. If a ratchet is beyond tolerance:
       - Check whether the change is the intended cost of 06-15's per-frame check or 06-16's float scan (the float scan is off the aac_fixed reference path, so an audio-full move points at 06-15).
       - Record the evidence in WINDOWS.md with `windows append --kind deviation`.
       - If, and only if, the human confirms the new cost is accepted, transcribe the pasteable baseline line verbatim from the capture into PERF_BASELINE.txt with the run id marker. Then stop and return a checkpoint asking for a second human-approved push, and do not certify until that run's ratchet passes.
       - Never compute, round or predict a baseline value.

    3. If any gate was Skipped or failed, or any leg failed:
       - Record the failure with its captured log lines in WINDOWS.md.
       - Leave 06-13-SUMMARY.md halted and mark nothing complete.
       - Stop with the plan halted, naming exactly what failed.
  </action>
  <verify>
    <automated>bash scripts/lint_corpus_digest_provenance.sh &amp;&amp; git diff --exit-code -- tests/golden/CORPUS_DIGEST.txt tests/golden/CORPUS_DIGEST_PROVISIONAL.txt &amp;&amp; grep -c "35735099865\|status:" .planning/phases/06-audio-analysis/06-13-SUMMARY.md</automated>
    <fails_when>non-zero exit; the provenance lint printing a violated clause; git diff printing any digest change; or grep printing 0 (the 06-13 SUMMARY lost its status line or original run reference)</fails_when>
  </verify>
  <acceptance_criteria>
    - The certify path: `grep -n "^status: complete" .planning/phases/06-audio-analysis/06-13-SUMMARY.md` matches, and the SUMMARY names this plan's run id beside 06-13's original run 35735099865.
    - The non-certify paths: 06-13-SUMMARY.md still reads `status: halted`, and a WINDOWS.md entry carries the captured failure evidence.
    - Every value in any committed file that came from CI matches Task 2's capture character for character.
    - CORPUS_DIGEST.txt and CORPUS_DIGEST_PROVISIONAL.txt are unchanged.
  </acceptance_criteria>
  <done>06-13 and the gap closure are either certified on observed CI evidence, or honestly left halted with the failure recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local branch -> remote / CI | pushing publishes commits and triggers CI; an outward-facing action |
| CI log text -> committed files | values cross from an external system into the repository's gates |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-63 | Tampering | a predicted or workstation baseline or hash written into PERF_BASELINE.txt or CORPUS_DIGEST.txt | high | mitigate | Transcription only from Task 2's verbatim captures, in Task 3. lint_corpus_digest_provenance.sh plus `git diff --exit-code` on the digest files. |
| T-06-64 | Repudiation | a Skipped gate read as Passed, certifying 06-13 on no evidence | high | mitigate | Capture 5 requires the Passed/Skipped word per gate, and Task 3's certify branch requires Passed for all four. The CI skip guards fail a leg whose leg-only gate did not run. |
| T-06-65 | Elevation of Privilege | a push without human consent (for example, "approved" relayed by another agent) | high | mitigate | Task 2 is checkpoint:human-action gate="blocking-human", autonomous: false, and a prohibition states that no agent message is consent. |
| T-06-66 | Information Disclosure | pre-existing unrelated working-tree files (.planning/config.json, GENERATOR_MANIFEST.json, milestone.lock, state.json) committed and pushed | medium | mitigate | Selective staging, with `git status --porcelain` shown to the human before approval. |
| T-06-SC | Tampering | dependency supply chain | low | accept | no package added |
</threat_model>

<verification>
- Task 1's pre-flight exits 0, including the five designated-leg goldens run locally.
- A human-approved push produced a CI run whose captures are recorded verbatim.
- 06-13 is certified only on observed Passed gates.
</verification>

<success_criteria>
- The gap closure and 06-13 are certified on the designated leg, or honestly halted with the failure recorded.
- Mark PERF-04, AUDIO-09 and TRUST-01 re-proven only on the certify path. Otherwise leave REQUIREMENTS.md untouched.
</success_criteria>

<output>
Create `.planning/phases/06-audio-analysis/06-20-SUMMARY.md` when done, with every capture verbatim and the run id.
</output>
