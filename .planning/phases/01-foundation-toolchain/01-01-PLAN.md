---
phase: 01-foundation-toolchain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitmodules
  - vcpkg/
  - vcpkg.json
  - CMakePresets.json
  - CMakeLists.txt
  - src/util/version.h
  - src/util/version.cpp
  - src/util/expected.h
  - src/cli/main.cpp
  - tests/CMakeLists.txt
  - tests/unit/CMakeLists.txt
  - tests/unit/test_license.cpp
  - tests/unit/test_expected.cpp
  - .planning/PROJECT.md
autonomous: true
requirements: [BUILD-01, BUILD-02, BUILD-03, BUILD-07, BUILD-10, CLI-05]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`cmake --preset x64-linux` configures from a clean checkout with no system FFmpeg installed and exits 0"
    - "`./build/x64-linux/mediadiff --version` exits 0 and writes to stdout the tool version, per-library libavcodec/libavformat/libavutil versions, the FFmpeg license string, and the enabled-feature list"
    - "`ctest --preset x64-linux -R unit` exits 0 with test_license and test_expected both reporting Passed"
    - "The license assertion fails when it is pointed at a non-LGPL license string — it is exact-positive-match, not a `GPL` substring test, so it cannot pass vacuously"
    - "`mediadiff::expected<T, E>` is usable from a translation unit that never names the backing library type"
    - "The FFmpeg version pin and the expected<T,E> library choice each appear as a dated row in .planning/PROJECT.md Key Decisions with the pinning mechanism and rationale"
    - "Configure fails with a FATAL_ERROR naming the offending codec if a GPL-only codec artifact resolved into the vcpkg install tree"
  artifacts:
    - path: "vcpkg.json"
      provides: "Pinned dependency manifest — decode-only LGPL FFmpeg subset (D-01, D-03)"
      contains: "overrides"
    - path: "CMakePresets.json"
      provides: "Five configure+build presets, vcpkg toolchain wired via cacheVariables (D-06)"
      contains: "CMAKE_TOOLCHAIN_FILE"
    - path: "CMakeLists.txt"
      provides: "libmediadiff/mediadiff target split, FFMPEG link wiring, Apple framework block, warnings-as-errors, MEDIADIFF_WITH_VMAF option, configure-time GPL fast-fail (D-03, D-07)"
      contains: "add_library(libmediadiff"
    - path: "src/util/version.h"
      provides: "mediadiff::compose_version_string / enabled_features_csv declarations"
      exports: ["compose_version_string", "enabled_features_csv"]
    - path: "src/util/version.cpp"
      provides: "CLI-05 --version composition from runtime libav* version + license APIs"
      min_lines: 25
    - path: "src/util/expected.h"
      provides: "mediadiff::expected<T,E> alias — the single file permitted to name the backing library (D-02)"
      contains: "namespace mediadiff"
    - path: "src/cli/main.cpp"
      provides: "Process entry point, CLI11 app, --version flag, exit-code production"
      min_lines: 20
    - path: "tests/unit/test_license.cpp"
      provides: "BUILD-03 exact-positive LGPL assertion across avutil/avcodec/avformat"
      min_lines: 10
    - path: "tests/unit/test_expected.cpp"
      provides: "BUILD-07 alias smoke test exercising and_then/transform/or_else"
      min_lines: 15
  key_links:
    - from: "CMakePresets.json"
      to: "vcpkg/scripts/buildsystems/vcpkg.cmake"
      via: "CMAKE_TOOLCHAIN_FILE cache variable set before project() is parsed"
      pattern: "vcpkg/scripts/buildsystems/vcpkg\\.cmake"
    - from: "CMakeLists.txt"
      to: "vcpkg-installed FFmpeg"
      via: "find_package(FFMPEG REQUIRED) then FFMPEG_INCLUDE_DIRS / FFMPEG_LIBRARY_DIRS / FFMPEG_LIBRARIES on libmediadiff"
      pattern: "find_package\\(FFMPEG"
    - from: "src/cli/main.cpp"
      to: "src/util/version.h"
      via: "CLI11 set_version_flag callback invokes mediadiff::compose_version_string()"
      pattern: "compose_version_string"
    - from: "tests/unit/test_license.cpp"
      to: "linked libavutil"
      via: "avutil_license() runtime self-report, exact-match asserted"
      pattern: "avutil_license"
  prohibitions:
    - statement: "MUST NOT ship a compliance gate that can pass vacuously — the FFmpeg license assertion must be demonstrated to go RED against a known-bad license string before its green is trusted; an assertion that cannot fail is worse than no assertion because it manufactures false confidence"
      category: transparency
      requirement_id: BUILD-03
      status: unresolved
      verification: null
      resolution: null
      reason: null
    - statement: "MUST NOT weaken the license assertion to an absence test (asserting some token is not present) — the allowed and forbidden license strings share a common substring, so absence testing silently accepts the exact builds it exists to reject"
      category: safety
      requirement_id: BUILD-03
      status: unresolved
      verification: null
      resolution: null
      reason: null
  flagged_assumptions:
    - "A-BUILD-01 (unclassified, unresolved): 'builds from a clean checkout' is taken to mean a fresh `git clone --recurse-submodules` with no pre-populated vcpkg binary cache and no system FFmpeg present; the compiler-floor requirement (GCC >= 12 / Clang >= 15 / AppleClang Xcode 15+ / MSVC v143) is enforced by a CMAKE_CXX_COMPILER_VERSION guard in CMakeLists.txt rather than left as prose in a README."
    - "A-BUILD-02 (unclassified, unresolved): 'two machines resolve the same dependency set' is proven in Phase 1 by re-resolving on one machine and diffing the resolved package list, not by provisioning a second physical machine. `overrides` pins only ffmpeg; every other package floats to `builtin-baseline`, so reproducibility is scoped to (baseline SHA + overrides), which is what gets committed."
    - "A-BUILD-03 (unclassified, unresolved): 'no GPL component is linked' is satisfied only by an exact positive match on the single allowed license string, asserted on all three of avutil/avcodec/avformat. The nonfree string and the LGPL-version-3 string are treated as failures too, not just the GPL-version-N forms."
    - "A-BUILD-07 (unclassified, unresolved): 'pinned as an explicit dependency' is taken to require BOTH a manifest entry AND the single-alias-header discipline — no translation unit outside src/util/expected.h may name the backing library type. The discipline half is enforced by grep in Plan 04's lint, not by review."
    - "A-BUILD-10 (unclassified, unresolved): BUILD-10 is satisfied by the PROJECT.md Key Decisions row carrying the pinned version, the pinning mechanism and the rationale. The pin merely existing in vcpkg.json does NOT satisfy it — locked decision D-01 states this explicitly."
    - "A-CLI-05 (unclassified, unresolved): `--version` is assumed to exit 0 and write to stdout (never stderr), with each of the four required fields independently matchable on its own line, so a partially-composed string cannot pass the assertion by matching one concatenated blob."
---

<objective>
Prove the entire Phase 1 toolchain chain end-to-end on ONE triplet before it is widened to three platforms and CI: vcpkg manifest resolves a pinned, decode-only LGPL FFmpeg -> FFmpeg links statically -> the libmediadiff/mediadiff target split builds -> the binary runs and identifies itself -> CTest discovers and runs a Catch2 test that asserts the shipped license.

This plan owns every load-bearing build decision the rest of the phase inherits: the FFmpeg 8.1 pin via vcpkg `overrides` (D-01), the `mediadiff::expected<T,E>` alias over `tl-expected` (D-02), the runtime license self-report assertion (D-03), the five-preset triplet layout (D-06), and the two-target library boundary (D-07).

Purpose: an architectural dead-end in the build system is discovered after one commit, not after ten already-committed layers. Every later plan in this phase expands outward from the slice proven here.
Output: a working `mediadiff --version` on `x64-linux`, a green `ctest --preset x64-linux -R unit`, and two new rows in PROJECT.md Key Decisions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-toolchain/01-CONTEXT.md
@.planning/phases/01-foundation-toolchain/01-RESEARCH.md
@.planning/phases/01-foundation-toolchain/01-VALIDATION.md
@claude_docs/00-design-and-requirements.md
</context>

<artifacts_produced>
## Artifacts this plan produces

Every symbol below is created by this plan and does not exist anywhere in the repository beforehand. Do not treat any of these as an unresolved reference to pre-existing code.

**Files:** `.gitmodules`, `vcpkg/` (git submodule), `vcpkg.json`, `CMakePresets.json`, `CMakeLists.txt`, `src/util/version.h`, `src/util/version.cpp`, `src/util/expected.h`, `src/cli/main.cpp`, `tests/CMakeLists.txt`, `tests/unit/CMakeLists.txt`, `tests/unit/test_license.cpp`, `tests/unit/test_expected.cpp`

**CMake targets:** `libmediadiff` (STATIC library), `mediadiff` (executable), `mediadiff_unit_tests` (test executable)

**CMake options / cache variables:** `MEDIADIFF_WITH_VMAF` (BOOL, default OFF), `MEDIADIFF_VERSION` (from `project(VERSION ...)`)

**CMake configure presets:** `base` (hidden), `x64-linux`, `arm64-osx`, `x64-windows-static-md`, `arm64-linux`, `x64-osx`

**CMake build presets:** `x64-linux`, `arm64-osx`, `x64-windows-static-md`, `arm64-linux`, `x64-osx`

**CMake test presets:** `x64-linux`, `arm64-osx`, `x64-windows-static-md`

**Preprocessor macros:** `MEDIADIFF_VERSION` (string, injected via `target_compile_definitions`), `MEDIADIFF_WITH_VMAF` (defined only when the option is ON)

**C++ symbols (namespace `mediadiff`):** `compose_version_string()`, `enabled_features_csv()`, `expected<T, E>` (alias template), `unexpected<E>` (alias template), `run(int argc, char** argv)`

**CTest test names:** `test_license`, `test_expected` (both registered through `catch_discover_tests`, so the discovered names are the Catch2 TEST_CASE names)

**Phase-wide artifact roll-up (all five plans)** — the complete set of symbols Phase 1 introduces, for source-grounding drift exclusion:

- Plan 01 (above)
- Plan 02: `mediadiff_integration_tests` target; CTest tests `version_output`, `vmaf_absent`, `static_link`; files `tests/integration/CMakeLists.txt`, `tests/integration/cli_harness.h`, `tests/integration/test_version_output.cpp`, `tests/integration/check_static_link.cmake`
- Plan 03: `mediadiff::utf8_to_wide()`, `mediadiff::wide_to_utf8()`, `mediadiff::fopen_utf8()`, `mediadiff::enable_vt_output()`; files `src/util/fs.h`, `app.manifest`, `tests/unit/test_fs_utf8.cpp`; CTest test `test_fs_utf8`
- Plan 04: files `scripts/lint_eng16.sh`, `scripts/gen_corpus.sh`, `scripts/gen_corpus.ps1`, `.clang-format`, `LICENSE`, `NOTICE`, `.gitignore`, and `.gitkeep` markers under `src/core/`, `src/config/`, `src/probe/`, `src/analyzers/{container,video,timeline,audio,content,size}/`, `src/compare/`, `src/report/`, `docs/checks/`; generated artifact `tests/fixtures/GENERATOR_MANIFEST.json`
- Plan 05: file `.github/workflows/ci.yml`; workflow jobs `build`, `lint`; matrix leg names `x64-linux`, `arm64-osx`, `x64-windows-static-md`, `arm64-linux`, `x64-osx`; CI env var `VCPKG_BINARY_SOURCES`; repository secret `VCPKG_PAT_TOKEN`
</artifacts_produced>

<tasks>

<task type="tracer">
  <name>Task 01-01-T1: End-to-end "mediadiff identifies itself" — one triplet, one path</name>
  <reversibility rating="costly">D-01 FFmpeg 8.1 pin: a later bump moves the vcpkg baseline, invalidates every cached binary across the 3-OS matrix, and changes any decode-path signature recorded in committed snapshots.</reversibility>
  <reversibility rating="costly">D-07 libmediadiff/mediadiff target split: retrofitting the boundary after Phase 2's 48 requirements land means relocating code between targets and untangling stdout calls from engine logic.</reversibility>
  <read_first>
    - `.planning/phases/01-foundation-toolchain/01-RESEARCH.md` -> "Standard Stack -> Installation" (the exact vcpkg.json shape, the `overrides` mechanism, the live-verified git-tree note, and the instruction to RE-FETCH the baseline SHA at execution time)
    - `.planning/phases/01-foundation-toolchain/01-RESEARCH.md` -> "Architecture Patterns -> Pattern 1" (CMakePresets schema v6 shape with all five triplets)
    - `.planning/phases/01-foundation-toolchain/01-RESEARCH.md` -> "Architecture Patterns -> Pattern 2" (FindFFMPEG module-mode consumption, the macOS Apple-framework gap, the Windows CRT note)
    - `.planning/phases/01-foundation-toolchain/01-RESEARCH.md` -> "Architecture Patterns -> Pattern 3" (configure-time forbidden-codec fast-fail, and why the vcpkg status file must not be parsed)
    - `.planning/phases/01-foundation-toolchain/01-RESEARCH.md` -> "Code Examples -> CLI11 minimal --version-only wiring" (the `set_version_flag` callback shape, `require_subcommand(0,1)`, and the version.h/.cpp composition)
    - `.planning/phases/01-foundation-toolchain/01-RESEARCH.md` -> "Open Questions" item 1 and "Assumptions Log" A2, A3, A4
    - `.planning/phases/01-foundation-toolchain/01-CONTEXT.md` -> `<decisions>` D-01, D-03, D-06, D-07
    - `claude_docs/00-design-and-requirements.md` sections 4, 5, 7 (build requirements, manifest, repository layout)
    - `.planning/research/STACK.md` -> "Version Compatibility" and "6. Static linking realities per OS"
  </read_first>
  <files>.gitmodules, vcpkg/, vcpkg.json, CMakePresets.json, CMakeLists.txt, src/util/version.h, src/util/version.cpp, src/cli/main.cpp</files>
  <action>
Wire ONE entry point through every layer of the phase's build stack, on the `x64-linux` triplet only. No second triplet, no CI, no corpus script, no Windows branch.

Add vcpkg as a git submodule at `vcpkg/` from `https://github.com/microsoft/vcpkg.git` and check it out at an explicit commit SHA. Do not accept the SHA quoted in RESEARCH.md — it is documented there as guaranteed-stale; re-fetch the current `microsoft/vcpkg` master HEAD at execution time and use that same SHA for both the submodule checkout and `builtin-baseline`. Record the SHA you actually used in the plan SUMMARY.

Author `vcpkg.json` with `name` mediadiff, `version-string` 0.1.0, the re-fetched `builtin-baseline`, and an `overrides` array pinning ffmpeg per D-01. Use `overrides` — NOT an old `builtin-baseline` commit — because baseline-hunting freezes every other dependency's version alongside ffmpeg. Resolve the A3 open question explicitly before writing the entry: three distinct pinnable vcpkg entries exist under the 8.1 line (the bare `8.1` port entry and the later `8.1.1` / `8.1.2` patch entries), each with a different git-tree hash. Default to the bare `8.1` entry, which matches D-01's literal wording; confirm the entry resolves by running the configure step, and record in the SUMMARY which literal version string and port-version you locked. Declare ffmpeg with `default-features` false and exactly the six features avcodec, avformat, swscale, swresample, dav1d, zlib. Add cli11, fmt, nlohmann-json, tomlplusplus, xxhash, libebur128, catch2, tl-expected as plain dependencies, and a `vmaf` feature whose dependency is libvmaf. Every one of these packages carries an OK verdict in RESEARCH.md's Package Legitimacy Audit — no install checkpoint is required, but do not add any package absent from that table.

Author `CMakePresets.json` at schema `version` 6 with `cmakeMinimumRequired` 3.25. Define a hidden `base` preset using the Ninja generator, `binaryDir` of `${sourceDir}/build/${presetName}`, and cacheVariables setting `CMAKE_TOOLCHAIN_FILE` to `${sourceDir}/vcpkg/scripts/buildsystems/vcpkg.cmake` plus `CMAKE_EXPORT_COMPILE_COMMANDS` ON. Derive five configure presets from it, each setting only `VCPKG_TARGET_TRIPLET`: x64-linux, arm64-osx, x64-windows-static-md (the three D-06 blocking triplets) and arm64-linux, x64-osx (the two non-blocking ones). Add matching buildPresets for all five and testPresets for the three blocking ones. Never call `set(CMAKE_TOOLCHAIN_FILE ...)` inside CMakeLists.txt — that overrides the cache value the preset supplies and silently unhooks vcpkg.

Author the root `CMakeLists.txt` complete and final for this phase — later plans extend the tests subtree and add an `app.manifest` source, but the structural shape is written once here. It must: require CMake 3.25; call `project()` with a VERSION and CXX language; set the C++ standard to 20 with extensions off and standard required; fail with a FATAL_ERROR if `CMAKE_CXX_COMPILER_VERSION` is below the documented floor for the detected `CMAKE_CXX_COMPILER_ID` (GNU 12, Clang 15, AppleClang 15, MSVC 19.30); declare `option(MEDIADIFF_WITH_VMAF ...)` defaulting OFF; run the configure-time forbidden-codec fast-fail from Pattern 3 (a `find_library` loop over the GPL-only codec names x264, x265, xvidcore scoped with NO_DEFAULT_PATH to the vcpkg installed lib dir, FATAL_ERROR naming the offender on a hit) and do NOT parse any vcpkg internal status file, which manifest mode does not reliably produce; `find_package(FFMPEG REQUIRED)`; `add_library(libmediadiff STATIC ...)` over the `src/util/` sources; attach FFMPEG include dirs, link dirs and libraries to it PUBLIC; add an `if(APPLE)` block linking the AppKit, AVFoundation, CoreImage, AudioToolbox, VideoToolbox, CoreMedia, CoreVideo, CoreFoundation and Security frameworks explicitly, because the vcpkg FindFFMPEG module has no framework pass-through while the portfile enables those FFmpeg components unconditionally on macOS; find and link cli11 and fmt; inject `MEDIADIFF_VERSION` as a compile definition from the project version; set warnings-as-errors on first-party targets only (`/W4 /WX` under MSVC, `-Wall -Wextra -Werror` otherwise) applied to `libmediadiff` and `mediadiff` and the test targets, never globally, and consume vcpkg include directories as SYSTEM so a third-party header's own warning cannot fail our build; `add_executable(mediadiff src/cli/main.cpp)` linking `libmediadiff` PRIVATE; call `enable_testing()` and `add_subdirectory(tests)`.

Author `src/util/version.h` declaring `mediadiff::compose_version_string()` returning std::string and `mediadiff::enabled_features_csv()` returning std::string. Author `src/util/version.cpp` implementing both inside libmediadiff so they are unit-testable without spawning the CLI. `compose_version_string` composes with fmt and must emit, each on its own line: the tool version from the `MEDIADIFF_VERSION` macro; one line each for libavcodec, libavformat and libavutil giving the RUNTIME version decoded from the packed integer returned by `avcodec_version()` / `avformat_version()` / `avutil_version()` via the AV_VERSION_MAJOR, AV_VERSION_MINOR and AV_VERSION_MICRO macros, alongside the compile-time version macro the headers supplied; a license line carrying `avutil_license()` verbatim; and a features line carrying `enabled_features_csv()`. `enabled_features_csv` reports vmaf only when the `MEDIADIFF_WITH_VMAF` macro is defined, and must never report cuda, which is v2 scope. Include the libav headers under `extern "C"`. RESEARCH.md flags the packed-integer decode idiom as assumption A2 — verify the decoded numbers look like a real 8.1-era library version rather than trusting the idiom, since a wrong shift silently yields plausible garbage.

Author `src/cli/main.cpp` with a `mediadiff::run(int, char**)` function holding the CLI11 wiring and a plain `main` that forwards to it and returns its value. Construct `CLI::App` with the description "media-aware regression diff" and name mediadiff; call `require_subcommand(0, 1)` now so Phase 2's implicit-compare positional handling does not have to touch this line; register the version flag via `set_version_flag` with the flag name `--version` and a callable that returns `mediadiff::compose_version_string()`, so the libav version APIs are only touched when the flag fires; parse with the CLI11_PARSE macro; return 0. Add no other flag or subcommand — every other CLI surface is Phase 2 scope. Leave the Windows entry-point branch out entirely; Plan 03 owns it, and inventing a stub for it here would be a placeholder that rots.

Do not create `src/core/`, `src/probe/` or any other empty subtree here, and do not write placeholder headers with invented APIs — Plan 04 owns the directory tree, and D-07 is explicit that empty directories are honest while fake interfaces mislead Phase 2's planner.
  </action>
  <verify>
    <automated>cmake --preset x64-linux && cmake --build --preset x64-linux && ./build/x64-linux/mediadiff --version && ./build/x64-linux/mediadiff --version | grep -Eq '^mediadiff [0-9]+\.[0-9]+\.[0-9]+' && ./build/x64-linux/mediadiff --version | grep -q 'libavcodec' && ./build/x64-linux/mediadiff --version | grep -q 'libavformat' && ./build/x64-linux/mediadiff --version | grep -q 'libavutil' && ./build/x64-linux/mediadiff --version | grep -q 'LGPL version 2.1 or later' && ./build/x64-linux/mediadiff --version | grep -Eiq 'features'</automated>
  </verify>
  <acceptance_criteria>
    - `git submodule status vcpkg` prints a commit SHA and the path `vcpkg`
    - `node -e "const m=require('./vcpkg.json'); if(!m.overrides || !m.overrides.some(o=>o.name==='ffmpeg')) process.exit(1)"` exits 0
    - `node -e "const m=require('./vcpkg.json'); const f=m.dependencies.find(d=>d.name==='ffmpeg'); if(f['default-features']!==false) process.exit(1); const want=['avcodec','avformat','swscale','swresample','dav1d','zlib']; if(JSON.stringify([...f.features].sort())!==JSON.stringify([...want].sort())) process.exit(1)"` exits 0
    - `node -e "const m=require('./vcpkg.json'); if(typeof m['builtin-baseline']!=='string'||m['builtin-baseline'].length!==40) process.exit(1)"` exits 0
    - `node -e "const m=require('./vcpkg.json'); const d=m.dependencies.map(x=>typeof x==='string'?x:x.name); for(const p of ['cli11','fmt','nlohmann-json','tomlplusplus','xxhash','libebur128','catch2','tl-expected']) if(!d.includes(p)) process.exit(1)"` exits 0
    - `node -e "const p=require('./CMakePresets.json'); if(p.version!==6) process.exit(1); const n=p.configurePresets.map(x=>x.name); for(const t of ['x64-linux','arm64-osx','x64-windows-static-md','arm64-linux','x64-osx']) if(!n.includes(t)) process.exit(1)"` exits 0
    - `grep -c 'vcpkg/scripts/buildsystems/vcpkg.cmake' CMakePresets.json` returns at least 1
    - `grep -v '^#' CMakeLists.txt | grep -c 'set(CMAKE_TOOLCHAIN_FILE'` returns 0
    - `grep -v '^#' CMakeLists.txt | grep -c 'find_package(FFMPEG'` returns at least 1
    - `grep -v '^#' CMakeLists.txt | grep -c 'if(APPLE)'` returns at least 1
    - `grep -v '^#' CMakeLists.txt | grep -c 'add_library(libmediadiff'` returns at least 1
    - `grep -v '^#' CMakeLists.txt | grep -c 'add_executable(mediadiff'` returns at least 1
    - `grep -v '^#' CMakeLists.txt | grep -c 'MEDIADIFF_WITH_VMAF'` returns at least 1
    - `grep -v '^#' CMakeLists.txt | grep -Ec 'x264|x265|xvidcore'` returns at least 1 (configure-time GPL fast-fail present)
    - `cmake --preset x64-linux` exits 0 from a tree with no system FFmpeg installed
    - `cmake --build --preset x64-linux` exits 0 with no warning emitted as an error from first-party sources
    - `./build/x64-linux/mediadiff --version` exits 0
    - `./build/x64-linux/mediadiff --version 1>/dev/null 2>/tmp/mdstderr.txt; test ! -s /tmp/mdstderr.txt` — version output goes to stdout, nothing to stderr
    - `./build/x64-linux/mediadiff --version` output matches `/LGPL version 2\.1 or later/`
    - `./build/x64-linux/mediadiff --version` output does NOT match `/cuda/i`
    - The SUMMARY records the exact vcpkg commit SHA used and the exact ffmpeg version/port-version locked in `overrides`
  </acceptance_criteria>
  <done>A single static `mediadiff` binary builds from a clean checkout on x64-linux with no system FFmpeg present, runs, and prints tool version, three per-library FFmpeg versions, the LGPL license string and the feature list — the whole build chain is proven on one path and committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-01-T2: Catch2 test harness with the license and expected<T,E> assertions</name>
  <reversibility rating="reversible">D-02 tl-expected behind a mediadiff::expected alias: swapping to the standard-library type on a future C++23 bump touches exactly one header.</reversibility>
  <behavior>
    - test_license: `avutil_license()` returns exactly the allowed LGPL-2.1 string — an exact equality assertion, never a search for an absent token
    - test_license: `avcodec_license()` and `avformat_license()` assert identically, so a hypothetical future build where per-library licensing diverges is caught
    - test_license: the assertion is demonstrated to be discriminating — a helper predicate applied to the known-bad GPL and nonfree strings returns false, proving the check has teeth
    - test_expected: `mediadiff::expected<int, std::string>` holding a value is truthy and yields that value
    - test_expected: `mediadiff::expected<int, std::string>` holding an error is falsy and yields that error
    - test_expected: chaining `and_then` on a value propagates through and produces the transformed value
    - test_expected: chaining `and_then` on an error short-circuits and preserves the original error
    - test_expected: `mediadiff::unexpected<std::string>` constructs an error-state expected
  </behavior>
  <read_first>
    - `.planning/phases/01-foundation-toolchain/01-RESEARCH.md` -> "Code Examples -> Catch2 v3 + CTest wiring" (find_package(Catch2 3 REQUIRED), Catch2::Catch2WithMain, catch_discover_tests — v3 API, not v2)
    - `.planning/phases/01-foundation-toolchain/01-RESEARCH.md` -> "Architecture Patterns -> Pattern 3" (the full FFMPEG_LICENSE value table and why an absence test is the wrong shape)
    - `.planning/phases/01-foundation-toolchain/01-RESEARCH.md` -> "Common Pitfalls -> Pitfall 2" (the warning sign that the assertion is not discriminating)
    - `.planning/phases/01-foundation-toolchain/01-CONTEXT.md` -> `<decisions>` D-02 and D-03
    - `.planning/research/STACK.md` -> "What NOT to Use" (Catch2 v2 single-header is not available in the vcpkg port)
    - `CMakeLists.txt` and `src/util/version.h` as written in Task 01-01-T1
  </read_first>
  <files>src/util/expected.h, tests/CMakeLists.txt, tests/unit/CMakeLists.txt, tests/unit/test_license.cpp, tests/unit/test_expected.cpp</files>
  <action>
Author `src/util/expected.h` as the single file in the repository permitted to name the backing library type, per D-02. Inside `namespace mediadiff`, define an alias template `expected` over the tl-expected class template with the same two type parameters, and an alias template `unexpected` over its error wrapper. Add a short header comment stating that no other file may name the backing type directly and that this indirection is what makes a future move to the standard-library type a one-header change. Do not re-implement any monadic operation — the whole point of D-02 is not owning `and_then` / `transform` / `or_else`.

Author `tests/CMakeLists.txt` as the tests subtree entry point. In this plan it adds only the unit subdirectory; Plan 02 extends it with the integration subdirectory, so keep the file to explicit `add_subdirectory` calls with no globbing that would silently pick up a sibling before its CMakeLists exists.

Author `tests/unit/CMakeLists.txt` using the Catch2 v3 integration path: `find_package(Catch2 3 REQUIRED)`, an executable named `mediadiff_unit_tests` over the two test sources, linking `libmediadiff` and `Catch2::Catch2WithMain`, then `include(Catch)` and `catch_discover_tests` so each TEST_CASE becomes its own CTest entry. Apply the same warnings-as-errors treatment the first-party targets get. Do not use the v2-era helper module or the v2 amalgamated single header — the vcpkg port is v3 and neither exists.

Author `tests/unit/test_license.cpp` including `<catch2/catch_test_macros.hpp>` (not the v2 header) and the libav headers under `extern "C"`. Write a TEST_CASE named so that it is selectable by the pattern `license`. Introduce a small file-local constant holding the one allowed license string and a file-local predicate that returns true only on exact equality with it. Assert the predicate holds for each of `avutil_license()`, `avcodec_license()` and `avformat_license()`. Then, in the same TEST_CASE, assert the predicate returns false for each of the four disallowed license strings enumerated in RESEARCH.md Pattern 3's value table (the two GPL forms, the LGPL-version-3 form, and the nonfree form) — this is the fail-first proof that the gate has teeth, satisfying the plan's first prohibition, and it is the reason an absence-style test is unacceptable: the allowed and the GPL strings share a common trailing substring, so an absence test accepts precisely the builds this assertion exists to reject.

Author `tests/unit/test_expected.cpp` including the v3 Catch2 test macros header and `util/expected.h` — and nothing from the backing library. Cover every case listed in this task's behavior block. Keep the file free of any direct mention of the backing library's namespace so that the alias discipline is demonstrated by the test itself, not merely asserted elsewhere.
  </action>
  <verify>
    <automated>cmake --preset x64-linux && cmake --build --preset x64-linux && ctest --preset x64-linux -R unit --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `ctest --preset x64-linux --output-on-failure -R license` exits 0 and reports at least 1 test passed
    - `ctest --preset x64-linux --output-on-failure -R expected` exits 0 and reports at least 1 test passed
    - `ctest --preset x64-linux -N -R unit` lists at least 2 discovered tests
    - `grep -c 'catch2/catch_test_macros.hpp' tests/unit/test_license.cpp` returns at least 1
    - `grep -c 'catch2/catch.hpp' tests/unit/test_license.cpp` returns 0
    - `grep -c 'catch2/catch.hpp' tests/unit/test_expected.cpp` returns 0
    - `grep -c 'find_package(Catch2 3' tests/unit/CMakeLists.txt` returns at least 1
    - `grep -c 'catch_discover_tests' tests/unit/CMakeLists.txt` returns at least 1
    - `grep -c 'avutil_license' tests/unit/test_license.cpp` returns at least 1
    - `grep -c 'avcodec_license' tests/unit/test_license.cpp` returns at least 1
    - `grep -c 'avformat_license' tests/unit/test_license.cpp` returns at least 1
    - `grep -c 'GPL version 2 or later' tests/unit/test_license.cpp` returns at least 1 (the known-bad string is asserted to be REJECTED by the predicate — this is the fail-first proof, not a shipped tolerance)
    - `grep -c 'nonfree and unredistributable' tests/unit/test_license.cpp` returns at least 1
    - `grep -rl 'tl::expected' src/ tests/ | tr -d '\n'` outputs exactly `src/util/expected.h`
    - `grep -c 'namespace mediadiff' src/util/expected.h` returns at least 1
  </acceptance_criteria>
  <done>`ctest --preset x64-linux -R unit` exits 0; the license assertion is an exact positive match proven to reject all four disallowed license strings; `mediadiff::expected` works with its monadic operations and the backing library type is named in exactly one file.</done>
</task>

<task type="auto">
  <name>Task 01-01-T3: Record the FFmpeg baseline and expected<T,E> decisions in PROJECT.md</name>
  <read_first>
    - `.planning/PROJECT.md` -> the Key Decisions table (read its existing column shape and row format before appending; match it exactly rather than inventing a new one)
    - `.planning/phases/01-foundation-toolchain/01-CONTEXT.md` -> `<decisions>` D-01 in full (both reasons, the known cost, and the explicit instruction that BUILD-10 is satisfied by the recording rather than by the pin existing) and D-02 in full
    - `.planning/phases/01-foundation-toolchain/01-CONTEXT.md` -> `<canonical_refs>` -> "Project-level" (states D-01 and D-02 must be appended to that table during this phase)
    - `.planning/REQUIREMENTS.md` -> BUILD-10 verbatim
  </read_first>
  <files>.planning/PROJECT.md</files>
  <action>
Append two rows to the Key Decisions table in `.planning/PROJECT.md`, matching the table's existing column shape exactly. Use `Edit` with a scoped replacement targeting the table — never `Write` on this file, which would destroy content outside the edit window.

The first row records the FFmpeg baseline decision per D-01. It must carry four things, because BUILD-10 is satisfied by the recording and not by the pin: the exact version string and port-version locked in `overrides` during Task 01-01-T1 (read the value you actually committed to `vcpkg.json`, do not restate the version from RESEARCH.md); the pinning mechanism, naming `overrides` and stating that it pins one package without freezing every other dependency to a historical baseline; the rationale, which is that Phase 1's only job is a green matrix and that building the foundation on a days-old major release makes a red build ambiguous between an upstream port problem and a mediadiff problem, plus that holding back turns the eventual bump into a live exercise of the cross-release idempotence and path-signature-guard requirements rather than spending that dogfooding opportunity; and the known cost, which is that FFmpeg 9.0 rewrote swscale from float to exact-rational math so Phase 7's perceptual baselines will shift across the eventual bump — intended behavior, since the path-signature guards exist to make that bump produce a skip rather than a false failure.

The second row records the expected<T,E> decision per D-02: the library chosen, the alias header that is the single permitted naming site, and the reason the alias rather than the library is the load-bearing part — a future move to the standard-library type on a C++23 bump touches one header.

Also append a row, or extend the first row's note, recording the A3 resolution from Task 01-01-T1: which of the three distinct 8.1-line vcpkg entries was locked and why, so a later reader does not have to re-derive it.

Do not modify any other section of PROJECT.md. Do not restate the roadmap or the requirements.
  </action>
  <verify>
    <automated>grep -c 'overrides' .planning/PROJECT.md && grep -ci 'ffmpeg' .planning/PROJECT.md && grep -c 'expected' .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains a Key Decisions row naming the pinned FFmpeg version and the string `overrides`
    - `grep -c 'swscale' .planning/PROJECT.md` returns at least 1 (the known cost of the eventual bump is recorded, not just the pin)
    - `.planning/PROJECT.md` contains a Key Decisions row naming `src/util/expected.h`
    - The version string recorded in PROJECT.md is byte-identical to the `version` value in `vcpkg.json`'s ffmpeg `overrides` entry — verify with `node -e "const fs=require('fs');const v=require('./vcpkg.json').overrides.find(o=>o.name==='ffmpeg').version;if(!fs.readFileSync('.planning/PROJECT.md','utf8').includes(v))process.exit(1)"`
    - `git diff --stat .planning/PROJECT.md` shows insertions only in the Key Decisions region, with no unrelated section rewritten
  </acceptance_criteria>
  <done>BUILD-10 is satisfied: the FFmpeg major-version baseline is an explicitly recorded decision in PROJECT.md carrying version, mechanism, rationale and known cost — not an incidental consequence of the vcpkg baseline. The expected<T,E> choice is recorded alongside it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vcpkg registry / upstream source tarballs → build tree | Third-party C/C++ source enters the shipped static binary. This is the only boundary in Phase 1 across which code the project did not author becomes code the project distributes. |
| developer / CI shell → `mediadiff` argv | Untrusted-ish path strings cross into the process. In this plan the only consumer is CLI11's `--version` flag; Plan 03 hardens the encoding half of this boundary. |
| linked FFmpeg → license posture of the shipped artifact | The build's legal claim is only as true as what actually linked, not what the manifest requested. |

## STRIDE Threat Register

ASVS level 1; block on high severity. Built from `01-RESEARCH.md` § Security Domain — this phase has no network, auth, session or multi-user surface, so V2/V3/V4/V6 do not apply and no threat is minted for them.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | Resolved FFmpeg feature set / linked libav* in `mediadiff` | high | mitigate | Two independent layers, neither sufficient alone: (1) configure-time `find_library` scan of the vcpkg installed lib dir for GPL-only codec artifacts, FATAL_ERROR naming the offender — fast-fail only, and deliberately NOT a parse of any vcpkg internal status file, which manifest mode does not reliably generate; (2) the mandatory runtime assertion in `tests/unit/test_license.cpp` exact-matching `avutil_license()`, `avcodec_license()` and `avformat_license()` against the single allowed string, with all four disallowed strings asserted to be rejected so the gate is proven discriminating. Layer 1 tests what was requested; layer 2 tests what actually shipped. |
| T-01-03 | Tampering | `vcpkg.json` dependency resolution, `vcpkg/` submodule | high | mitigate | Dependencies pinned by a 40-character `builtin-baseline` SHA plus an `overrides` entry for ffmpeg; vcpkg itself pinned as a submodule at that same SHA; the official `microsoft/vcpkg` registry used exclusively with no custom or third-party registry added; every package present in RESEARCH.md's Package Legitimacy Audit with an OK verdict, and no package added that is absent from that table. |
| T-01-SC | Tampering | vcpkg package installs (`vcpkg.json` dependencies) | high | mitigate | Package legitimacy gate satisfied: `01-RESEARCH.md` § Package Legitimacy Audit covers all ten packages (ffmpeg, cli11, fmt, nlohmann-json, tomlplusplus, xxhash, libebur128, catch2, libvmaf, tl-expected) with verdict OK and disposition Approved. Zero `[SLOP]`, zero `[SUS]`, zero `[ASSUMED]` — each was checked against its live vcpkg port page and upstream GitHub repository, so no blocking human legitimacy checkpoint is required before install. The audit was performed by manual authoritative-source fallback rather than the automated seam; re-run the automated seam if it becomes available, and treat any package added outside that table as un-audited. |
| T-01-06 | Information Disclosure | `mediadiff::compose_version_string()` output | medium | mitigate | `--version` composes only the tool version, decoded runtime library version numbers, the license string and the feature list. The raw configure-line string returned by `avutil_configuration()` is NOT emitted in this plan's default output — on a vcpkg build it embeds buildtree absolute paths, and `--version` is explicitly intended to be pasted into public bug reports. Any later surfacing of it belongs behind a verbose flag with the paths handled deliberately. |
| T-01-07 | Spoofing | Compile-time vs runtime libav* version reporting | low | accept | For a fully static binary the compile-time and runtime versions cannot diverge. Both are reported anyway so a future dynamically-linked build (explicitly out of scope per REQUIREMENTS.md) would surface the divergence rather than hide it. Accepted: no realistic exploit path for a local single-user CLI. |
</threat_model>

<flagged_assumptions>
## Spec-less probe fallback — flagged assumptions (phase-wide roll-up)

No SPEC.md exists for this phase, so the spec-less probe fallback applied. The deterministic edge probe returned:

`{"applicable":19,"resolved":0,"unresolved":19,"byVerification":{"explicit":0,"backstop":0}}`

All 19 applicable edges are **unresolved**. Per the fallback protocol an `unclassified` row is never auto-resolved with a backstop and is never silently dropped — every one is surfaced below as an explicit flagged assumption. No-silent-drop equality holds: 19 surfaced, 19 authored.

**Judgment note kept honest:** this is build and toolchain work, and the probe's categories (empty input, ordering, adjacency, concurrency) are tuned for data-processing requirements. A low classification rate here is expected and is not evidence that the requirements are underspecified. The assumptions below are stated as they actually are; none were manufactured to make the coverage look better, and none were dismissed.

| # | Requirement | Category | Status | Authored into |
|---|-------------|----------|--------|---------------|
| 1 | BUILD-01 | unclassified | unresolved | Plan 01 `must_haves.flagged_assumptions` |
| 2 | BUILD-02 | unclassified | unresolved | Plan 01 |
| 3 | BUILD-03 | unclassified | unresolved | Plan 01 |
| 4 | BUILD-07 | unclassified | unresolved | Plan 01 |
| 5 | BUILD-10 | unclassified | unresolved | Plan 01 |
| 6 | CLI-05 | unclassified | unresolved | Plan 01 |
| 7 | BUILD-04 | unclassified | unresolved | Plan 02 |
| 8 | BUILD-09 | unclassified | unresolved | Plan 02 |
| 9 | CLI-09 | adjacency | unresolved | Plan 03 |
| 10 | CLI-09 | empty | unresolved | Plan 03 |
| 11 | CLI-09 | ordering | unresolved | Plan 03 |
| 12 | CLI-09 | concurrency | unresolved | Plan 03 |
| 13 | BUILD-08 | adjacency | unresolved | Plan 04 |
| 14 | BUILD-08 | empty | unresolved | Plan 04 |
| 15 | BUILD-08 | ordering | unresolved | Plan 04 |
| 16 | BUILD-05 | adjacency | unresolved | Plan 05 |
| 17 | BUILD-05 | empty | unresolved | Plan 05 |
| 18 | BUILD-05 | ordering | unresolved | Plan 05 |
| 19 | BUILD-06 | unclassified | unresolved | Plan 05 |

The full text of each assumption lives in the `must_haves.flagged_assumptions` block of the plan named in the last column.

## Prohibition recall — canon-referral breadcrumbs (phase-wide)

Kept prohibitions are authored descriptor-less into each plan's `must_haves.prohibitions`. The following surfaced candidates are canon security/compliance items owned by dedicated tooling and were NOT minted as prohibitions:

- `gen_corpus` writing outside its output directory is path-traversal canon — covered by `/gsd-secure-phase`; not minted here.
- CI cache-feed write access from fork pull requests is access-control/supply-chain canon — covered by `/gsd-secure-phase` and tracked as threat `T-01-04` in Plan 05; not minted here.
- Known-CVE exposure in the pinned FFmpeg release is dependency-scanning canon — covered by `/gsd-secure-phase`; not minted here.

Not double-counted: "the build must not ship a binary whose linked FFmpeg is GPL" is already hard requirement BUILD-03 and lives in `must_haves.truths`, not in `prohibitions`.
</flagged_assumptions>

<verification>
Run from the repository root on a Linux host with no system FFmpeg installed:

1. `cmake --preset x64-linux` — exits 0. The first run builds FFmpeg from source and takes 15–40 minutes; that is expected and is exactly what Plan 05's binary cache later amortizes.
2. `cmake --build --preset x64-linux` — exits 0 with no first-party warning escalated to an error.
3. `./build/x64-linux/mediadiff --version` — exits 0, writes only to stdout, and its output carries all four CLI-05 fields on separate lines.
4. `ctest --preset x64-linux --output-on-failure` — exits 0; `test_license` and `test_expected` both pass.
5. `grep -rl 'tl::expected' src/ tests/` — outputs exactly `src/util/expected.h` and nothing else.
6. `.planning/PROJECT.md` Key Decisions contains the FFmpeg baseline row and the expected<T,E> row, and the recorded version matches `vcpkg.json`.

Negative control (proves the license gate is discriminating, not vacuous): `tests/unit/test_license.cpp` asserts that its own equality predicate returns false for each of the four disallowed license strings. If that half of the TEST_CASE is removed, the gate becomes untrustworthy regardless of whether it still reports green.
</verification>

<success_criteria>
- A clean checkout on `x64-linux` configures, builds and produces a running `mediadiff` binary with no system FFmpeg present (BUILD-01, BUILD-04 partial)
- Dependencies resolve from a manifest carrying a 40-character `builtin-baseline` and an ffmpeg `overrides` pin, with vcpkg itself pinned as a submodule at the same SHA (BUILD-02)
- The linked FFmpeg self-reports the allowed LGPL license string, asserted by exact positive match on three libraries and proven to reject all four disallowed strings; a configure-time GPL-only codec scan fails the build ahead of it (BUILD-03)
- `mediadiff::expected<T, E>` is available and exercised, with the backing library named in exactly one file (BUILD-07)
- The FFmpeg baseline is a recorded decision in PROJECT.md carrying version, mechanism, rationale and known cost (BUILD-10)
- `mediadiff --version` prints tool version, three per-library FFmpeg versions, the license string and the feature list, with vmaf absent and cuda never mentioned (CLI-05)
</success_criteria>

<output>
Create `.planning/phases/01-foundation-toolchain/01-01-SUMMARY.md` when done.

The SUMMARY must record, because later plans and later phases depend on these exact values:
- the vcpkg submodule commit SHA (also the `builtin-baseline`)
- the literal ffmpeg `version` and `port-version` locked in `overrides`, and which of the three 8.1-line entries that corresponds to (the A3 resolution)
- the decoded runtime libavcodec / libavformat / libavutil version numbers the binary actually reported
- whether the A2 packed-integer decode idiom produced plausible values or needed correction
- whether `-ldl` had to be added to the link line (assumption A4)
</output>
