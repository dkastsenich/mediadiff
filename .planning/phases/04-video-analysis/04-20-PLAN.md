---
phase: 04-video-analysis
plan: 20
type: execute
wave: 5
depends_on: ["04-13", "04-14", "04-15", "04-16", "04-17", "04-18", "04-19"]
gap_closure: true
files_modified:
  - .planning/phases/04-video-analysis/04-20-SUMMARY.md
autonomous: false
requirements: [BUILD-05, BUILD-08]

estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "`gsd/phase-04-video-analysis` is pushed to the remote and a DRAFT pull request against `main` exists, each preceded by an explicit human confirmation obtained immediately before the action."
    - "The designated x64-linux leg's own `scripts/corpus_digest.sh` listing from that run is captured verbatim into this plan's SUMMARY — the only artefact that can tell this project what the designated leg computes for the fixtures Phase 4 added."
    - "The first run is EXPECTED to fail at the digest-assertion step, because the 58 Phase-4 fixture lines (57 from plans 04-01 through 04-12, plus 04-16's new pair member) are still workstation-derived. The listing is printed by the preceding step, so the failure does not prevent the capture."
    - "Every pre-existing fixture line in the captured listing is compared against `git show 8caf1f1:tests/golden/CORPUS_DIGEST.txt`, and any divergence is recorded as a FINDING with the fixture named — never overwritten, never averaged away."
    - "No commit, no push and no pull request is created without a human saying so first; nothing in this plan may take an outward-facing action autonomously."
  artifacts:
    - .planning/phases/04-video-analysis/04-20-SUMMARY.md
  key_links:
    - "`.github/workflows/ci.yml`'s step order — Generate corpus, Verify corpus, Report corpus digest, THEN Assert the corpus digest — is why a run that fails the assertion still prints the listing this plan exists to capture."
    - "`DESIGNATED_LEG=\"x64-linux\"` in the assertion step -> which leg's listing is authoritative: the pinned ffmpeg builds do not produce byte-identical fixtures across platforms (03-19), so only x64-linux's listing may be transcribed."
    - "`scripts/assert_corpus_digest.sh`'s three excluded lines (`mkv_opus_a.webm`, `mkv_opus_b.webm`, `CORPUS_DIGEST_SUMMARY=`) -> the two fixtures whose captured hashes are expected to be unstable even on the designated leg, and must not be treated as findings."
  prohibitions:
    - "Never push, never create a pull request, and never create a commit that is part of either action without an explicit human confirmation obtained immediately beforehand. `workflow.auto_advance` does not apply to these checkpoints."
    - "Never open a non-draft pull request; the branch is not ready to merge and the run is being used as a measuring instrument."
    - "Never edit `tests/golden/CORPUS_DIGEST.txt` in this plan. Capture only; 04-21 transcribes."
    - "Never re-run `scripts/gen_corpus.sh` and treat this workstation's output as a substitute for the CI listing — that substitution is the original defect."
    - "Never dismiss, skip or work around the failing digest-assertion step in CI; its failure on the first run is the expected, informative outcome."
---

<objective>
Get the designated CI leg to compute and print the fixture corpus's real hashes, behind two
explicit human confirmations.

Purpose: 04-VERIFICATION.md's third gap says plainly that this cannot be resolved locally or by
human judgment alone — it needs an actual designated-leg run. Human Decision 4 fixes the order:
restore main's lines first (04-13), finish all other gap closure (04-14 through 04-19), then push
and open a draft PR with the orchestrator confirming with the user immediately before the push and
again before the PR.
Output: a pushed branch, a draft PR, and the x64-linux leg's verbatim digest listing recorded in
this plan's SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-video-analysis/04-VERIFICATION.md
@tests/golden/README.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Decide whether to publish this branch to capture the designated leg's digest</name>
  <files>(none — a routing decision taken before any outward-facing action)</files>
  <decision>
Phase 4's gap closure is complete locally (04-13 through 04-19). `tests/golden/CORPUS_DIGEST.txt`
carries main's designated-leg hashes for all 80 pre-existing fixtures and 58 workstation-derived
hashes for the fixtures Phase 4 added, every one of them named in
`tests/golden/CORPUS_DIGEST_PROVISIONAL.txt`.

Those 58 hashes cannot be made correct on this machine. The pinned ffmpeg's SIMD dispatch is
host-dependent, so a workstation encode and the x64-linux CI runner's encode produce different bytes
for every fixture with non-trivial encoded pictures. 04-VERIFICATION.md states it plainly: this
cannot be resolved locally or by human judgment alone; it needs an actual designated-leg run.

Publishing the branch and opening a draft pull request is how that run is obtained. Both are
one-way doors: a branch on a shared remote and a pull request against `main` are visible to every
collaborator and to any automation watching the repository the instant they exist, and closing or
force-pushing afterwards does not unsend that.

The decision is whether to walk through those doors now.
  </decision>
  <options>
**A. Publish now (the planned route, Human Decision 4).** Push `gsd/phase-04-video-analysis`
(Task 2), then open a DRAFT pull request against `main` (Task 3). CI runs; the x64-linux leg is
EXPECTED to fail at "Assert the corpus digest matches the committed pin (D-GAP-01)" before vcpkg
bootstrap. The step immediately before it prints the listing this phase needs, so the failure does
not prevent the capture. 04-21 then transcribes that listing and a second run goes green.
Consequence: a red CI run and a draft PR are visible on the repository for as long as this takes.

**B. Hold.** The branch stays local. Phase 4 cannot close: the 58 provisional hashes stay wrong for
the designated leg, `scripts/assert_corpus_digest.sh` will fail the first time anyone pushes this
branch, and the five designated-leg-only goldens stay unexercised against Phase 4's changes.
Consequence: every other gap is closed but the phase is not shippable, and this plan and 04-21 stay
open.

**C. Publish differently.** Name the route you want — a different branch name, a different base, an
existing PR to reuse, or a way to trigger the workflow without a PR. Tasks 2 and 3 are rewritten to
match before anything is pushed.
  </options>
  <resume-signal>Say A, B or C. Nothing leaves this machine until you answer.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Confirm, then push gsd/phase-04-video-analysis</name>
  <files>(none — an outward-facing action, not a file change)</files>
  <reversibility rating="one-way">Publishing a branch to a shared remote cannot be silently taken back; the history becomes visible to every collaborator and to CI, and a force-push to undo it is itself a disruptive act.</reversibility>
  <precondition>Task 1 returned option A (or a C route the user specified), plans 04-13 through 04-19 are complete and committed, the working tree is clean, and `git remote -v` names a remote this machine can push to.</precondition>
  <action>
Push the branch `gsd/phase-04-video-analysis` to the remote — and only after a human has confirmed,
in this task, immediately beforehand. The agent performs the push; the human authorises it. No
commit is created here: everything is already committed by the preceding plans.
  </action>
  <instructions>
Present the local state, then ask, then push.

1. `git status --porcelain` — must be empty. Show it.
2. `git log --oneline 8caf1f1..HEAD` — show the Phase 4 commit list, including the gap-closure commits.
3. `ctest --preset x64-linux --output-on-failure` — must pass in full (758 or more passing, 6 skipped by design). Show the summary line.
4. `bash scripts/lint_corpus_digest_provenance.sh` and `bash scripts/lint_pragma_scope.sh` — both must exit 0. Show their output.
5. `bash scripts/assert_corpus_digest.sh` — EXPECTED to fail on this workstation. Show it and say, out loud, that this failure is 04-13's restore working rather than a defect.

Then ask the human, in one sentence: push `gsd/phase-04-video-analysis` to the remote, yes or no?

State what happens on yes, so the red run is not a surprise: CI runs, and the x64-linux leg is
expected to fail at "Assert the corpus digest matches the committed pin (D-GAP-01)" — before vcpkg
bootstrap, before any build, before any test. That failure is the point. The step immediately before
it, "Report corpus digest (cross-platform byte-identity evidence)", prints the listing this phase
needs. The build and test steps will not run on that leg in this round; they run after 04-21
transcribes the listing.

On an explicit yes, run `git push` for this branch. On anything else, stop and report.
  </instructions>
  <verification>
`git ls-remote --heads origin gsd/phase-04-video-analysis` returns a ref whose sha equals local
`HEAD`. Record both shas in the SUMMARY, together with the human's verbatim confirmation.
  </verification>
  <resume-signal>Say "push" to proceed, or name what you want checked first. No branch leaves this machine until you say so.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Confirm, then open a DRAFT pull request and capture the designated leg's listing</name>
  <files>(none — an outward-facing action plus a capture recorded in this plan's SUMMARY)</files>
  <reversibility rating="one-way">A pull request against `main` is visible to every collaborator and to any automation watching the repository the moment it opens; closing it later does not unsend the notifications.</reversibility>
  <precondition>Task 2's push succeeded and `gh auth status` reports an authenticated account with permission to open a pull request on this repository.</precondition>
  <action>
Open a DRAFT pull request from `gsd/phase-04-video-analysis` to `main` — after a second, separate
human confirmation obtained immediately beforehand — then capture the x64-linux leg's corpus digest
listing from the resulting run and record it verbatim in this plan's SUMMARY.
  </action>
  <instructions>
Ask first, act second, capture third.

1. Ask the human, in one sentence: open a DRAFT pull request from `gsd/phase-04-video-analysis` to
   `main`, yes or no? Say why it is a draft: the branch is not being proposed for merge, it is being
   used as a measuring instrument, and a draft is what makes CI run the full matrix against `main`.
2. On an explicit yes, run `gh pr create --draft --base main` with a body stating that this is a
   Phase 4 gap-closure branch opened to capture the designated leg's corpus digest, that the digest
   assertion is expected to fail on the first run, and that the branch is not ready for review.
3. Find the run: `gh run list --branch gsd/phase-04-video-analysis --limit 5`.
4. Pull the log: `gh run view <run-id> --log`, and locate the "Report corpus digest (cross-platform
   byte-identity evidence)" step's output on the `x64-linux` leg SPECIFICALLY. Every leg prints its
   own listing; only x64-linux's may be used, because the pinned ffmpeg builds do not produce
   byte-identical fixtures across platforms — that is why the designated-leg policy exists at all.
5. Record that listing VERBATIM in this plan's SUMMARY, including its `CORPUS_DIGEST_SUMMARY=` line,
   with the run id and the commit sha it ran against. 04-21 transcribes from this record; a
   paraphrase is worthless.
6. Record the digest-assertion step's failure output too, so the expected-failure claim is evidenced
   rather than asserted.
7. Cross-check NOW, before 04-21 writes anything: for every fixture name in
   `git show 8caf1f1:tests/golden/CORPUS_DIGEST.txt`, compare the captured hash against main's. They
   must match. `mkv_opus_a.webm`, `mkv_opus_b.webm` and the `CORPUS_DIGEST_SUMMARY=` line are the
   three exclusions `scripts/assert_corpus_digest.sh` already names; a divergence there is expected
   and is not a finding. A divergence on ANY OTHER pre-existing fixture means that recipe's output
   changed during this phase — record it as a FINDING naming the fixture, both hashes and the run
   id, and STOP.

If the run never reaches the digest steps (a runner outage, a checkout failure, an auth problem),
say so: the capture is unavailable and 04-21 cannot proceed.
  </instructions>
  <verification>
`gh pr view --json isDraft,baseRefName,headRefName` reports `isDraft: true`, `baseRefName: main` and
`headRefName: gsd/phase-04-video-analysis`. This plan's SUMMARY contains a verbatim
`&lt;sha256&gt;  &lt;name&gt;` listing from the x64-linux leg with its `CORPUS_DIGEST_SUMMARY=` line, the run
id, the commit sha, the digest-assertion failure output, and the cross-check verdict.
  </verification>
  <resume-signal>Say "open the draft PR" to proceed. After the run completes, confirm the captured x64-linux listing and the cross-check result (clean, or the named divergences).</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan creates no source file, symbol, CLI flag, check id or fixture. Its output is a remote
branch, a draft pull request, and the designated leg's verbatim corpus digest listing recorded in
`.planning/phases/04-video-analysis/04-20-SUMMARY.md`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this machine -> a shared remote and a public pull request | Both actions are irreversible in effect: they publish history and notify collaborators, and no local undo exists. |
| a CI run log -> the project's record of what the designated leg computes | The log is the only evidence available; a transcription error here becomes a committed golden nobody can trace back. |
| a non-designated leg's listing -> the committed digest | Every leg prints a listing; taking the wrong one would commit hashes the designated leg will never reproduce. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-87 | Elevation of Privilege | an agent pushing or opening a PR without human consent | high | mitigate | Both actions sit behind `checkpoint:human-action` with `gate="blocking-human"`, which `workflow.auto_advance` does not auto-approve, and the plan's prohibitions forbid the action outright without a confirmation obtained immediately beforehand. |
| T-4-88 | Spoofing | a non-designated leg's listing captured as authoritative | high | mitigate | The capture step names the `x64-linux` leg explicitly, states why the other legs' listings are unusable, and requires the run id and commit sha to be recorded alongside. |
| T-4-89 | Tampering | a changed pre-existing recipe laundered into the new digest | high | mitigate | The cross-check against `8caf1f1`'s lines runs BEFORE any transcription, names the three known-unstable exclusions explicitly so the exclusion cannot widen, and stops on any other divergence with the fixture named. |
| T-4-90 | Repudiation | a paraphrased or partial listing recorded in the SUMMARY | medium | mitigate | The listing must be recorded verbatim including its summary line; 04-21's transcription reads from this SUMMARY and its own acceptance criteria re-derive the summary hash, which a paraphrase would fail. |
| T-4-SC | Tampering | npm/pip/cargo installs | low | accept | No package manager is invoked and no dependency manifest changes; `gh` and `git` are pre-existing developer tooling. |
</threat_model>

<verification>
- `git ls-remote --heads origin gsd/phase-04-video-analysis` returns a ref after Task 1.
- `gh pr view --json isDraft,baseRefName` reports `isDraft: true` and `baseRefName: main` after Task 2.
- This plan's SUMMARY contains a verbatim `<sha256>  <name>` listing from the x64-linux leg with its `CORPUS_DIGEST_SUMMARY=` line, plus the run id and commit sha.
- This plan's SUMMARY records the digest-assertion step's failure output from that run.
- This plan's SUMMARY records the cross-check verdict against `8caf1f1`: clean, or a named list of diverging pre-existing fixtures (excluding the two Opus fixtures and the summary line).
- `git diff --stat -- tests/golden/CORPUS_DIGEST.txt` is empty — this plan captures, it does not transcribe.
</verification>

<success_criteria>
- Two outward-facing actions were taken, each after an explicit human confirmation obtained immediately beforehand.
- The designated leg's listing exists verbatim in this project's own records.
- The pre-existing lines in that listing were compared against main's before anything was written.
</success_criteria>

<output>
Create `.planning/phases/04-video-analysis/04-20-SUMMARY.md` when done.
</output>
