---
phase: 04-video-analysis
plan: 14
type: execute
wave: 2
depends_on: ["04-13"]
gap_closure: true
files_modified:
  - src/analyzers/video/stream_params.cpp
  - tests/unit/test_video_stream_params.cpp
  - docs/checks/video.sar.md
autonomous: true
requirements: [VIDEO-01, VIDEO-04]

estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "`detail::resolve_sar` never returns an `EffectiveSar` with `den <= 0`, so no non-positive denominator can reach `video.sar` or `video.sar.conflict` as a compared value or as evidence — the contract `src/analyzers/video/analyzers.h:159-172` already documents is now true of the code."
    - "A malformed `pasp`/VUI pair with a non-zero numerator and a zero or negative denominator resolves to the same `1:1`, `unset=true` shape a `0/den` pair already resolves to, rather than passing through verbatim with `unset=false`."
    - "`render_level_value` renders an AV1 spelling only for `seq_level_idx` values the AV1 specification actually defines (0-23); 24-31 are reserved and fall through to the raw decimal fallback every other unresolved level already uses."
    - "`docs/checks/video.sar.md` describes the unset rule the code implements — a zero NUMERATOR in either position regardless of the denominator — not the narrower `0/1`-only rule it describes today."
    - "Each of the three fixes carries a unit test that has been observed failing with the fix reverted."
  artifacts:
    - src/analyzers/video/stream_params.cpp
    - tests/unit/test_video_stream_params.cpp
    - docs/checks/video.sar.md
  key_links:
    - "`detail::resolve_sar` -> `emit_sar`/`emit_sar_conflict` (same file, lines ~183-246): the resolved `EffectiveSar` is written straight into a `RationalValue`, so the guard has to live at the resolver, not at each emission site."
    - "`detail::compute_dar`, `detail::quantize_chromaticity` and `hdr.cpp`'s luminance checks -> the shape of this guard: every sibling extraction in this phase already refuses a non-positive denominator, and `resolve_sar` is the one that does not. Match their refusal shape rather than inventing a new one."
    - "`core/serializer.cpp`'s `rational_value_to_json` defensive `rv.den != 0` check -> why this is an oversight and not an accepted risk: the downstream guard exists precisely because upstream was assumed to hold."
  prohibitions:
    - "Never edit `src/analyzers/video/analyzers.h` in this plan. Its `EffectiveSar` contract comment is already correct; the code is being brought into line with it, and plan 04-15 owns that header this wave."
    - "Never widen the AV1 level bound back out, and never invent a spelling for a reserved `seq_level_idx` — the file's own stated philosophy is to fall through rather than guess at a spelling this project has never verified."
    - "Never change a registered check id, its semantic, its severity or its tolerance; `video.sar` and `video.sar.conflict` are permanent ids and this plan touches neither."
    - "Never add or regenerate a fixture in this plan, and never touch `tests/golden/CORPUS_DIGEST.txt`."
  flagged_assumptions:
    - "Edge-probe row VIDEO-01 (category `unclassified`) and row VIDEO-04 (category `unclassified`) are surfaced here rather than resolved: the deterministic probe could not classify what edge they name, and no gap in 04-VERIFICATION.md constrains them. VIDEO-01's stream-parameter family and VIDEO-04's SAR-conflict recording are both verified Complete in 04-VERIFICATION.md's requirements table; this plan hardens one resolver inside them and does not re-open either requirement."
---

<objective>
Close the one real contract violation this phase's code review found — `resolve_sar` letting a
non-positive denominator through — plus the AV1 level over-acceptance beside it, and make
`docs/checks/video.sar.md` describe the rule the code actually implements.

Purpose: 04-REVIEW.md WR-01 (warning, confirmed by the verifier's own direct read), IN-01 and
IN-02. Human Decision 3 in 04-VERIFICATION.md: fix all of them now, and every changed test must be
shown able to fail.
Output: a guarded resolver, a tightened AV1 bound, a corrected doc, and three mutation-proven tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-video-analysis/04-REVIEW.md
@.planning/phases/04-video-analysis/04-VERIFICATION.md
@src/analyzers/video/analyzers.h
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard resolve_sar against a non-positive denominator (WR-01)</name>
  <files>src/analyzers/video/stream_params.cpp, tests/unit/test_video_stream_params.cpp</files>
  <read_first>
    - src/analyzers/video/stream_params.cpp lines 500-560 (`render_level_value`, `detail::resolve_sar` at ~516-521, and `detail::compute_dar` immediately below it — `compute_dar`'s `width &lt;= 0 || height &lt;= 0 || sar_num &lt;= 0 || sar_den &lt;= 0` refusal is the sibling shape to match)
    - src/analyzers/video/stream_params.cpp lines 180-250 (`emit_sar` and `emit_sar_conflict`: where the resolved `EffectiveSar` becomes a `RationalValue` compared value and an evidence field)
    - src/analyzers/video/analyzers.h lines 155-175 (the `EffectiveSar` contract this fix restores — READ ONLY, this plan does not edit that file)
    - tests/unit/test_video_stream_params.cpp (the existing `resolve_sar` cases, for the file's TEST_CASE naming convention and its `[unit]` tag)
    - .planning/phases/04-video-analysis/04-REVIEW.md, the WR-01 section (its suggested fix is the decision, not a suggestion, per Human Decision 3)
  </read_first>
  <behavior>
    - `resolve_sar(0, 1)` -> `{1, 1, unset=true}` (unchanged, the existing rule).
    - `resolve_sar(0, 5)` -> `{1, 1, unset=true}` (unchanged; any zero numerator is unset regardless of denominator).
    - `resolve_sar(4, 0)` -> `{1, 1, unset=true}` (NEW: a zero denominator can no longer escape).
    - `resolve_sar(4, -3)` -> `{1, 1, unset=true}` (NEW: a negative denominator can no longer escape).
    - `resolve_sar(1, 1)` -> `{1, 1, unset=false}` (unchanged; an explicit square ratio stays distinguishable from an absent one).
    - `resolve_sar(4, 3)` -> `{4, 3, unset=false}` (unchanged; a real declared ratio passes through).
    - For every `(num, den)` pair in a table spanning zero, negative, and large-magnitude inputs, the returned `den` is strictly greater than zero.
  </behavior>
  <action>
    Extend `detail::resolve_sar`'s existing early return so it fires for a non-positive denominator
    as well as a zero numerator, exactly as 04-REVIEW.md WR-01's suggested fix writes it: the single
    condition covering `raw_num == 0` and `raw_den &lt;= 0` returns the `1:1`, unset shape, and only a
    pair that survives both tests is returned verbatim.

    Note in a short comment beside the guard why the folded case is `unset=true` rather than a third
    state: WR-01 explicitly offered either, and folding keeps `EffectiveSar` a two-field-plus-flag
    value type whose documented invariant ("num/den are always a valid, positive-denominator
    rational usable directly for comparison") holds unconditionally. A degenerate ratio is not a
    meaningfully declared one, so reporting it as "the source declared nothing" is the honest
    reading, and the RAW values remain visible in `video.sar.conflict`'s own evidence.

    Add the behaviour table above to `tests/unit/test_video_stream_params.cpp` as unit cases driving
    `detail::resolve_sar` directly (it is already exposed in `analyzers.h` for exactly this). Include
    a table-driven case asserting the returned denominator is positive for every pair, so a future
    third state cannot reintroduce the escape without failing.

    Then prove the new cases are load-bearing: temporarily revert the guard to its pre-fix form, run
    the unit suite, observe the new zero/negative-denominator cases FAIL by name, restore the guard,
    and re-run to green. Record both observed outputs in the SUMMARY.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; cmake --build --preset x64-linux 2>&amp;1 | tail -5 &amp;&amp; ctest --test-dir build/x64-linux -R "unit\.video_stream_params" --output-on-failure</automated>
    <fails_when>non-zero exit from the build or from ctest; or ctest's summary line reports "0 tests passed" / "No tests were found", which means the `-R` filter matched nothing rather than that the cases passed.</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/analyzers/video/stream_params.cpp` contains a single early-return in `resolve_sar` whose condition tests both the numerator and the denominator, and the function has exactly one other return statement.
    - Unit assertions exist for `resolve_sar(4, 0)` and `resolve_sar(4, -3)` and both expect `num == 1`, `den == 1`, `unset == true`.
    - A unit assertion exists that the returned `den` is `> 0` across a table of at least six pairs including zero and negative denominators.
    - The pre-existing `resolve_sar(0, den)` and `resolve_sar(4, 3)` assertions are unchanged and still pass.
    - The SUMMARY quotes the ctest failure output observed with the guard reverted, naming the failing TEST_CASE, and the ctest pass output after restoring it.
    - `git diff --stat -- src/analyzers/video/analyzers.h` is empty.
  </acceptance_criteria>
  <done>`video.sar`/`video.sar.conflict` cannot be handed a rational with a non-positive denominator, and a test that fails without the guard proves it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound the AV1 level spelling to the indices the spec defines (IN-01)</name>
  <files>src/analyzers/video/stream_params.cpp, tests/unit/test_video_stream_params.cpp</files>
  <read_first>
    - src/analyzers/video/stream_params.cpp lines 490-515 (`render_level_value`: the HEVC branch's tighter `level % 3 == 0` guard immediately above the AV1 branch is the in-file precedent for refusing to spell an undefined value, and the trailing `fmt::format("{}", level)` is the fallback every unresolved case already takes)
    - tests/unit/test_video_stream_params.cpp (the existing `render_level_value` cases, including the AV1 `seq_level_idx` 8 -> "4.0" worked example from doc 03)
    - .planning/phases/04-video-analysis/04-REVIEW.md, the IN-01 section
  </read_first>
  <behavior>
    - `render_level_value("av1", 0)` -> "2.0" (lowest defined index, unchanged).
    - `render_level_value("av1", 8)` -> "4.0" (doc 03's own worked example, unchanged).
    - `render_level_value("av1", 23)` -> "7.3" (highest DEFINED index, unchanged).
    - `render_level_value("av1", 24)` -> "24" (NEW: reserved, falls through to the raw decimal).
    - `render_level_value("av1", 31)` -> "31" (NEW: reserved, falls through — no fabricated "9.3").
    - `render_level_value("av1", -1)` -> "-1" (unchanged; the existing non-negative guard still applies).
    - `render_level_value("hevc", 120)` -> "4.0" and `render_level_value("h264", 31)` are unchanged — this task touches only the AV1 branch.
  </behavior>
  <action>
    Tighten `render_level_value`'s AV1 branch upper bound from the full 5-bit `seq_level_idx` range
    to the range the AV1 specification actually defines, per 04-REVIEW.md IN-01's suggested fix:
    indices 0 through 23 map to levels 2.0 through 7.3; 24 through 31 are reserved and have no
    defined spelling, so they must reach the existing raw-decimal fallback instead of the
    `2 + (level / 4)` / `level % 4` formula.

    Extend the comment above the branch with the bound's justification — the AV1 spec defines levels
    for `seq_level_idx` 0-23 only — so the number is not a bare literal a later reader would widen
    back out for symmetry with the 5-bit field width.

    Add the behaviour table above to `tests/unit/test_video_stream_params.cpp`, including the two
    unchanged non-AV1 spot checks so a mis-scoped edit to the HEVC or H.264 branch fails here.

    Then prove the new cases are load-bearing: temporarily widen the bound back to its pre-fix
    value, run the unit suite, observe the index-24 and index-31 cases FAIL by name (they will
    report the fabricated spellings), restore the bound, re-run to green, and record both outputs in
    the SUMMARY.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; cmake --build --preset x64-linux 2>&amp;1 | tail -5 &amp;&amp; ctest --test-dir build/x64-linux -R "unit\.video_stream_params" --output-on-failure</automated>
    <fails_when>non-zero exit from the build or from ctest; or ctest reports "No tests were found" / "0 tests passed", meaning the filter matched nothing.</fails_when>
  </verify>
  <acceptance_criteria>
    - Unit assertions exist for AV1 indices 23, 24 and 31, expecting "7.3", "24" and "31" respectively.
    - The pre-existing AV1 index-8 -> "4.0" assertion is unchanged and still passes.
    - Unit assertions for at least one HEVC and one H.264 level still pass unchanged.
    - The SUMMARY quotes the ctest failure output observed with the bound widened back, showing the fabricated spelling the old bound produced for index 31, plus the green run after restoring.
  </acceptance_criteria>
  <done>`video.level` never renders an AV1 spelling for an index the specification leaves reserved.</done>
</task>

<task type="auto">
  <name>Task 3: Correct video.sar.md's unset rule to the one the code implements (IN-02)</name>
  <files>docs/checks/video.sar.md</files>
  <read_first>
    - docs/checks/video.sar.md (the whole file, especially the unset-rule paragraph at lines ~15-20)
    - src/analyzers/video/stream_params.cpp (`detail::resolve_sar` AFTER Task 1 — the doc must describe the post-fix rule, including the new non-positive-denominator case)
    - docs/checks/video.sar.conflict.md (the sibling doc, for the shared vocabulary and section shape both files use)
    - .planning/phases/04-video-analysis/04-REVIEW.md, the IN-02 section
  </read_first>
  <action>
    Rewrite the unset-rule paragraph in `docs/checks/video.sar.md` so it describes what
    `detail::resolve_sar` actually does after Task 1, in the wording 04-REVIEW.md IN-02 recommends
    plus the new case Task 1 introduces:

    - A sample aspect ratio whose NUMERATOR is zero, in either position and regardless of the
      denominator, means the source declared nothing at all. Say "zero numerator", not the literal
      ratio spelling the paragraph uses today, because the code has never restricted itself to that
      one denominator.
    - A ratio whose denominator is zero or negative is structurally degenerate — a malformed `pasp`
      box or a corrupt VUI — and is treated the same way: nothing was meaningfully declared.
    - Both cases resolve to a square ratio for comparison purposes, and the `unset` boolean rides in
      evidence so a stream that explicitly declares a square ratio stays distinguishable from one
      that declared nothing.
    - The RAW values are still visible through `video.sar.conflict`'s own evidence, so folding for
      comparison does not hide what the file said.

    Keep the file's existing section structure, heading order and Accept/Tune/Silence shape
    untouched — this is a correction to one paragraph's claim, not a rewrite.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; grep -qi 'zero numerator' docs/checks/video.sar.md &amp;&amp; grep -qiE 'denominator is (zero|0) or negative|negative or zero denominator|non-positive denominator' docs/checks/video.sar.md &amp;&amp; echo SAR_DOC_UPDATED</automated>
    <fails_when>non-zero exit, or SAR_DOC_UPDATED absent from stdout — one of the two required claims (the zero-numerator rule, the degenerate-denominator rule) is not present in the file.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; cmake --build --preset x64-linux 2>&amp;1 | tail -5 &amp;&amp; ctest --test-dir build/x64-linux -R "integration\.doc03_coverage|unit\.video_stream_params" --output-on-failure</automated>
    <fails_when>non-zero exit from the build or ctest; or ctest reports "No tests were found" — the DOC-01/DOC-02 build-enforced doc gate and the DOC-03 coverage gate must both still be satisfied after editing a shipped check document.</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/checks/video.sar.md` states the unset rule in terms of a zero numerator regardless of denominator.
    - `docs/checks/video.sar.md` states that a zero or negative denominator is also treated as nothing declared.
    - The file still contains its pre-existing `## Accept / Tune / Silence` section and its heading order is unchanged (`git diff -- docs/checks/video.sar.md` shows changes confined to the unset-rule paragraph).
    - `ctest -R "integration\.doc03_coverage"` passes — the doc is still registered and paired.
  </acceptance_criteria>
  <done>The shipped `video.sar` document and the shipped `resolve_sar` code describe the same rule.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan adds no new symbol, file, CLI flag or check id. It modifies the bodies of two existing
`mediadiff::detail` functions (`resolve_sar`, `render_level_value`) in
`src/analyzers/video/stream_params.cpp`, adds TEST_CASEs to the existing
`tests/unit/test_video_stream_params.cpp`, and edits one paragraph of the existing
`docs/checks/video.sar.md`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a file-supplied `pasp` box / bitstream VUI -> `EffectiveSar` | The numerator and denominator are entirely attacker-chosen and cross a type whose documented invariant downstream consumers are entitled to trust. |
| a file-supplied `seq_level_idx` -> a rendered level string | Five attacker-controlled bits select a formula branch that produces a user-facing spelling. |
| a shipped check document -> an operator's interpretation of a verdict | A doc that overstates or understates a rule is acted on as if it were the rule. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-62 | Tampering | a non-positive denominator reaching `RationalValue` via `resolve_sar` | medium | mitigate | The resolver refuses it at the single seam every emission site reads, and a table-driven unit case asserts the returned denominator is positive across zero, negative and large-magnitude inputs. |
| T-4-63 | Spoofing | a degenerate ratio reported as an explicitly declared one | medium | mitigate | The folded case sets `unset=true`, so evidence distinguishes "declared nothing" from "declared a square ratio", and the raw values stay visible through `video.sar.conflict`. |
| T-4-64 | Spoofing | a fabricated AV1 level spelling for a reserved `seq_level_idx` | low | mitigate | The branch bound is tightened to the spec-defined indices and reserved values take the existing raw-decimal fallback; unit cases pin indices 23, 24 and 31. |
| T-4-65 | Repudiation | a check document describing a rule the code does not implement | medium | mitigate | The unset-rule paragraph is rewritten against the post-fix resolver, and the DOC-03 coverage gate is re-run after the edit. |
| T-4-SC | Tampering | npm/pip/cargo installs | low | accept | No package manager is invoked and no dependency manifest changes. |
</threat_model>

<verification>
- `cmake --build --preset x64-linux` completes with no new warning on GCC (the project builds warnings-as-errors; a changed comparison in a branch condition is the classic place a signed/unsigned warning appears).
- `ctest --test-dir build/x64-linux -R "unit\.video_stream_params" --output-on-failure` passes.
- `ctest --test-dir build/x64-linux -R "integration\.doc03_coverage" --output-on-failure` passes.
- `ctest --preset x64-linux --output-on-failure` passes in full, with no drop from the 758 passing / 6 skipped baseline recorded in 04-VERIFICATION.md.
- `git diff -- tests/golden/CORPUS_DIGEST.txt` is empty (this plan touches no fixture).
</verification>

<success_criteria>
- No `EffectiveSar` with a non-positive denominator can be produced, proven by a test observed failing without the guard.
- AV1 `seq_level_idx` 24-31 render as raw decimals, proven by a test observed failing with the old bound.
- `docs/checks/video.sar.md` and `resolve_sar` agree.
</success_criteria>

<output>
Create `.planning/phases/04-video-analysis/04-14-SUMMARY.md` when done.
</output>
