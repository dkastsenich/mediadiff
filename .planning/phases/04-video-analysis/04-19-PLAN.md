---
phase: 04-video-analysis
plan: 19
type: execute
wave: 4
depends_on: ["04-16", "04-18"]
gap_closure: true
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [PROBE-03, VIDEO-03, VIDEO-09]

estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "PROBE-03's traceability row in `.planning/REQUIREMENTS.md` reads exactly `Deferred`, so `phase.complete` — which flips Pending / In Progress / Gaps Found to Complete and leaves Out / Deferred / Blocked alone — cannot mark an unmet overhead target Complete when Phase 4 closes."
    - "VIDEO-09's traceability row reads exactly `Deferred` for the same reason."
    - "ROADMAP.md Success Criterion 5 for Phase 4 no longer asserts the under-10% parser-overhead target as a Phase 4 outcome; it asserts the measurement harness, and names PERF-03/PERF-05 in Phase 5 as the owner of the target (D-11/D-12)."
    - "ROADMAP.md Success Criterion 4 for Phase 4 scopes the recorded extraction source to the stream-level `coded_side_data` arm, and the first-frame side-data arm is explicitly recorded as Phase 7 scope — mirroring the existing VIDEO-11 placement note rather than inventing a new mechanism."
    - "VIDEO-09's split placement and PROBE-03's deferred target each have a row in ROADMAP.md's Cross-cutting requirement placements table, so a reader who asks why finds an answer where every other such answer lives."
    - "REQUIREMENTS.md's VIDEO-03 text describes the behaviour the code implements and the tests prove: two spellings folding to the same state produce ZERO findings, and a genuine range flip produces exactly ONE, on `video.color.range`."
    - "The Coverage table's invariant — every v1 requirement maps to exactly one phase, 138 total — still holds after the amendments."
  artifacts:
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
  key_links:
    - "`phase.complete`'s status transition rule -> why `Deferred` and not `Pending`: a Pending Phase 4 row is flipped to Complete at phase close, which would state in the project's own traceability table that an unmet target was met."
    - "ROADMAP.md's VIDEO-11 row in the Cross-cutting requirement placements table -> the shape VIDEO-09's row must take: Phase 4 ships the arm that needs no decode, Phase 7 makes the other arm real."
    - "04-16's rewritten `integration.video_yuvj` Test 2 -> VIDEO-03's corrected text: the requirement is being amended to match a behaviour that now has a test which fails without it, not to match an opinion."
  prohibitions:
    - "Never mark PROBE-03 or VIDEO-09 `Complete`. Neither is."
    - "Never leave either row `Pending`; `phase.complete` would flip it to Complete when Phase 4 closes, which is precisely the failure Human Decision 1 and 2 exist to prevent."
    - "Never add VIDEO-09 to Phase 7's `**Requirements**:` line. Every v1 requirement maps to exactly one phase and the Coverage table's 138 total depends on that; the split belongs in the Cross-cutting placements table and in prose, exactly as VIDEO-11's does."
    - "Never implement the first-frame HDR extraction arm, and never change `HdrSourceKind::requires_decode`'s behaviour in `src/analyzers/video/hdr.cpp` — Human Decision 1 defers it to Phase 7."
    - "Never measure or optimise parser overhead in this plan; Human Decision 2 assigns the target to Phase 5 and this plan is documentation only."
    - "Never renumber Phase 4's or Phase 7's success criteria; amend the text of the existing numbered items in place."
  flagged_assumptions:
    - "Edge-probe row PROBE-03 (category `unclassified`) is surfaced rather than resolved: the deterministic probe could not classify what edge it names. PROBE-03's fused ParserScan is functioning and verified in 04-VERIFICATION.md; only its overhead clause is deferred, and this plan changes documentation only."
    - "Edge-probe row VIDEO-09 (category `unclassified`) is surfaced rather than resolved for the same reason. VIDEO-09's stream-level arm is real and tested; the first-frame arm is deferred to Phase 7 by Human Decision 1 and is not implemented here."
---

<objective>
Make the project's own traceability tell the truth about the two Phase 4 requirements that are not
finished, and correct the one requirement whose text contradicts the behaviour the tests prove.

Purpose: Human Decisions 1 and 2 in 04-VERIFICATION.md, plus the VIDEO-03 wording correction from
Human Decision 3. `phase.complete` flips every Phase 4 row reading Pending, In Progress or Gaps
Found to Complete, and leaves Out, Deferred and Blocked alone — so a Pending PROBE-03 or VIDEO-09
would be falsely marked Complete when this phase closes.
Output: two `Deferred` rows with amended requirement text, two amended Phase 4 success criteria,
two Cross-cutting placement rows, and a VIDEO-03 text that matches its own tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-video-analysis/04-VERIFICATION.md
@.planning/phases/04-video-analysis/04-CONTEXT.md
@.planning/phases/04-video-analysis/deferred-items.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend Phase 4's success criteria and record both placements in ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 258-282 (Phase 4's Goal, Requirements line, the five Success Criteria — SC4 at ~269 and SC5 at ~270 — and the plan list)
    - .planning/ROADMAP.md lines 325-345 (Phase 7's section: its Goal, Requirements line and five Success Criteria, so the new prose note lands where a Phase 7 reader will see it)
    - .planning/ROADMAP.md lines 370-395 (the Cross-cutting requirement placements table, especially the PROBE-03 row at ~380, the VIDEO-11 row at ~382 whose shape VIDEO-09's must mirror, and the PERF-01/PERF-03/PERF-05 row at ~392 that already assigns the perf targets to Phase 5)
    - .planning/ROADMAP.md lines 355-370 (the Coverage table and its "All 138 v1 requirements map to exactly one phase" invariant, which these amendments must not break)
    - .planning/phases/04-video-analysis/04-CONTEXT.md lines 105-155 (D-08's "build the full precedence seam, wire one source" decision, and D-11/D-12's "measure and record, do not gate" decision — the amendments must agree with both)
  </read_first>
  <action>
    Amend Phase 4's Success Criterion 5 so it no longer asserts the under-10% overhead target as a
    Phase 4 outcome. Keep its first clause (`video.frame_rate.measured` consumes phase 3's shared
    interval statistics rather than computing its own) exactly as it is — that clause is verified.
    Replace the second clause so it states what Phase 4 actually delivers: the fused parser pass and
    a harness that MEASURES its overhead against a plain packet scan and records the number as
    evidence, with the under-10% target itself gated in Phase 5 under PERF-03 and PERF-05, per D-11
    and D-12. Do not restate the measured figure as a criterion; it is evidence, not a target.

    Amend Phase 4's Success Criterion 4 so its extraction-source clause names the arm Phase 4
    actually ships: the stream-level `coded_side_data` source is recorded; the first-frame side-data
    source arrives with the decode pass in Phase 7. Leave SC4's other three clauses (round-trip or
    reported loss, the recorded source, the non-gating `info` on shared incoherence) untouched —
    they are verified.

    Add two rows to the Cross-cutting requirement placements table, in the same voice and shape as
    the rows already there:
    - VIDEO-09: placed in 4, with its first-frame extraction arm in 7. Reason: the first-frame
      source requires a decoded frame, and the decode pass does not exist until Phase 7 — the same
      reason VIDEO-11 sits in 7. Phase 4 builds the full precedence seam and wires the stream-level
      source (D-08); Phase 7 wires the second source. Record it as a human decision taken at Phase
      4's verification, dated, so it reads as a decision rather than a drift.
    - PROBE-03: its row already explains why the requirement sits in Phase 4. Extend that row, or
      add a sibling row, recording that the under-10% overhead clause is gated in Phase 5 alongside
      PERF-03/PERF-05, again as a dated human decision.

    Add one sentence to Phase 7's section stating that Phase 7 also completes VIDEO-09's first-frame
    HDR side-data extraction arm, deferred from Phase 4 with the decode pass, and pointing at the
    Cross-cutting placements table. Do NOT add VIDEO-09 to Phase 7's `**Requirements**:` line and do
    not add a sixth success criterion — the Coverage table's one-phase-per-requirement invariant and
    the existing SC numbering both have to survive this edit.

    Use scoped edits throughout. Never rewrite `.planning/ROADMAP.md` wholesale; a whole-file write
    destroys every phase entry outside the diff window.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; python3 -c "
s=open('.planning/ROADMAP.md').read()
assert 'PERF-03' in s and 'PERF-05' in s
i=s.index('### Phase 4: Video Analysis'); j=s.index('### Phase 5:')
p4=s[i:j]
bad=[l for l in p4.splitlines() if '10%' in l.replace(' %','%') and not ('Phase 5' in l or 'PERF-0' in l)]
assert not bad, 'Phase 4 mentions a 10%% overhead figure without attributing it to Phase 5: %r' % bad
assert 'PERF-03' in p4, 'Phase 4 SC5 does not name the Phase 5 owner of the target'
assert 'first-frame' in p4.lower(), 'Phase 4 SC4 does not scope the extraction source'
k=s.index('### Phase 7: Content'); l=s.index('## Progress')
p7=s[k:l]
assert 'VIDEO-09' in p7, 'Phase 7 section does not mention the deferred VIDEO-09 arm'
assert 'VIDEO-09' not in p7[p7.index('**Requirements**'):p7.index('**Success Criteria**')], 'VIDEO-09 was added to Phase 7 Requirements line, breaking the one-phase invariant'
print('ROADMAP_AMENDED')"</automated>
    <fails_when>non-zero exit (the AssertionError names the condition and, for the overhead check, prints the offending Phase 4 line verbatim), or ROADMAP_AMENDED absent from stdout. The overhead check is region-scoped to the Phase 4 section and passes a line that names Phase 5 or PERF-0x, so correctly attributing the target cannot trip it.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; grep -cE '^\| (VIDEO-09|PROBE-03) \|' .planning/ROADMAP.md | grep -qvx 0 &amp;&amp; grep -qE '^\| VIDEO-09 \|' .planning/ROADMAP.md &amp;&amp; echo PLACEMENT_ROWS_PRESENT</automated>
    <fails_when>non-zero exit, or PLACEMENT_ROWS_PRESENT absent from stdout — the Cross-cutting placements table has no VIDEO-09 row.</fails_when>
  </verify>
  <acceptance_criteria>
    - Phase 4's Success Criterion 5 contains no under-10% assertion and names PERF-03 and PERF-05 as the owners of the target.
    - Phase 4's Success Criterion 4 names the stream-level `coded_side_data` source as what Phase 4 records and places the first-frame source in Phase 7.
    - The Cross-cutting requirement placements table contains a VIDEO-09 row explaining the split, dated and attributed to a human decision.
    - The PROBE-03 placement records that the overhead target is gated in Phase 5.
    - Phase 7's section mentions VIDEO-09's deferred first-frame arm in prose, and Phase 7's `**Requirements**:` line is unchanged.
    - The Coverage table still states 138 total and no per-phase count changed (`git diff` shows no edit inside the Coverage table's rows).
    - Phase 4 still has exactly five numbered success criteria and Phase 7 still has exactly five.
  </acceptance_criteria>
  <done>The roadmap claims for Phase 4 only what Phase 4 delivers, and says where the rest went.</done>
</task>

<task type="auto">
  <name>Task 2: Set PROBE-03 and VIDEO-09 to Deferred, with requirement text that matches</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md line 88 (PROBE-03's requirement text, whose trailing clause states the under-10% target) and line 119 (VIDEO-09's text, whose precedence clause names both extraction sources)
    - .planning/REQUIREMENTS.md lines 238-250 (the Traceability section's preamble and table header: `| Requirement | Phase | Status |`)
    - .planning/REQUIREMENTS.md lines 300-335 (the rows: PROBE-03 at ~305 reading Pending, VIDEO-09 at ~330 reading Pending, VIDEO-11 at ~332 for the shape of a legitimately-elsewhere requirement)
    - .planning/phases/04-video-analysis/04-VERIFICATION.md, the Human Decisions table and the paragraph beneath it explaining why Deferred rather than Pending
  </read_first>
  <action>
    Set PROBE-03's Status cell to the literal text `Deferred` and VIDEO-09's Status cell to the
    literal text `Deferred`, leaving both Phase cells at `Phase 4`. The exact spelling matters:
    `phase.complete` leaves Out, Deferred and Blocked untouched and flips everything else in the
    phase to Complete, so a near-miss spelling would be silently converted.

    Amend both requirement texts so the deferral is legible where the requirement itself is stated,
    not only in the traceability table:
    - PROBE-03: keep the `ParserScan` clause exactly as written — it is delivered and verified. Mark
      the under-10%-overhead clause as deferred to Phase 5's PERF-03/PERF-05, with the dated human
      decision, and record that Phase 4's scope is the fused parser pass plus its measurement
      harness.
    - VIDEO-09: keep the `hdr.mdcv` / `hdr.cll` / `hdr.dovi` clause and the stream-level
      `coded_side_data` extraction with its recorded source — all delivered and verified. Mark the
      first-frame side-data source as deferred to Phase 7 with the decode pass, with the dated human
      decision, mirroring how VIDEO-11's placement is recorded.

    Keep both checkbox markers unchecked. A deferred requirement is not a met one.

    Use scoped edits. Do not rewrite `.planning/REQUIREMENTS.md` wholesale.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; grep -qE '^\| PROBE-03 \| Phase 4 \| Deferred \|$' .planning/REQUIREMENTS.md &amp;&amp; grep -qE '^\| VIDEO-09 \| Phase 4 \| Deferred \|$' .planning/REQUIREMENTS.md &amp;&amp; echo BOTH_ROWS_DEFERRED</automated>
    <fails_when>non-zero exit, or BOTH_ROWS_DEFERRED absent from stdout — one of the two rows does not carry the literal `Deferred` cell text in the exact table format, which is the spelling `phase.complete` keys on.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; python3 -c "
s=open('.planning/REQUIREMENTS.md').read()
import re
for rid,phase in (('PROBE-03','Phase 5'),('VIDEO-09','Phase 7')):
    line=[l for l in s.splitlines() if l.startswith('- [ ] **%s**' % rid) or l.startswith('- [x] **%s**' % rid)]
    assert len(line)==1, 'expected exactly one requirement line for %s, found %d' % (rid,len(line))
    assert line[0].startswith('- [ ]'), '%s checkbox is checked; a deferred requirement is not a met one' % rid
    assert phase in line[0], '%s text does not name %s as the owner of the deferred clause' % (rid,phase)
print('REQUIREMENT_TEXTS_AMENDED')"</automated>
    <fails_when>non-zero exit (the AssertionError names the requirement and the condition), or REQUIREMENT_TEXTS_AMENDED absent from stdout.</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains the exact line `| PROBE-03 | Phase 4 | Deferred |`.
    - `.planning/REQUIREMENTS.md` contains the exact line `| VIDEO-09 | Phase 4 | Deferred |`.
    - Both requirement bullet lines still begin `- [ ]` and each names the phase that owns its deferred clause (Phase 5 for PROBE-03, Phase 7 for VIDEO-09).
    - Both requirement texts carry the decision date from 04-VERIFICATION.md's Human Decisions table.
    - No other Status cell in the traceability table changed (`git diff` shows exactly two changed table rows).
  </acceptance_criteria>
  <done>Closing Phase 4 cannot mark an unfinished overhead target or an unbuilt extraction arm as Complete.</done>
</task>

<task type="auto">
  <name>Task 3: Correct VIDEO-03's text to the behaviour its own tests prove</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md line 113 (VIDEO-03's current text, which says a `yuvj420p` to `yuv420p` + full-range change produces exactly ONE finding) and line 324 (its traceability row)
    - .planning/phases/04-video-analysis/deferred-items.md, the 04-08 section (the analysis: after the fold both sides are the same state, so a correct implementation reports ZERO; emitting a `fail` on `video.color.range` there would be a P0 false positive)
    - tests/integration/test_video_yuvj.cpp AFTER 04-16 (the landed evidence: Test 1 counts exactly one non-pass finding for the range flip, Test 2 counts zero for the two-spellings pair, and both were observed failing with the fold disabled)
    - .planning/phases/04-video-analysis/04-VERIFICATION.md (the fifth gap's `missing` entry, which is the instruction for this task)
  </read_first>
  <action>
    Rewrite VIDEO-03's requirement text so it states the property the fold actually delivers and the
    tests actually prove, in two halves rather than one:

    - The same colorimetric intent spelled two ways — a `yuvj*` pixel format on one side, the plain
      pixel format plus an explicit full-range flag on the other — produces ZERO findings, because
      pix_fmt range-folding runs before comparison and both sides resolve to the same state.
    - A genuine colour-range flip produces EXACTLY ONE finding, on `video.color.range`, and does not
      also fire `video.pix_fmt`.

    Keep the requirement id and its `- [ ]`/`- [x]` marker state as they are; this is a text
    correction, not a status change. Add a short parenthetical recording that the original wording
    asserted one finding for the zero-finding case and was corrected at Phase 4's verification, with
    the date, so the amendment is legible as a decision rather than as drift. Name the two tests
    that now carry the evidence.

    Use a scoped edit. Do not rewrite the file wholesale.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; python3 -c "
s=[l for l in open('.planning/REQUIREMENTS.md') if '**VIDEO-03**' in l]
assert len(s)==1, 'expected exactly one VIDEO-03 requirement line, found %d' % len(s)
t=s[0].lower()
assert 'zero' in t, 'VIDEO-03 text does not state the zero-finding half'
assert 'exactly **one**' in t or 'exactly one' in t, 'VIDEO-03 text does not state the exactly-one half'
assert 'video.color.range' in t
print('VIDEO03_TEXT_CORRECTED')"</automated>
    <fails_when>non-zero exit (the AssertionError names the missing half), or VIDEO03_TEXT_CORRECTED absent from stdout.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; ctest --test-dir build/x64-linux -R "integration\.video_yuvj" --output-on-failure 2>&amp;1 | tee /dev/stderr | grep -qE '[0-9]+ tests passed, 0 tests failed' &amp;&amp; echo EVIDENCE_STILL_GREEN</automated>
    <fails_when>non-zero exit, or EVIDENCE_STILL_GREEN absent from stdout, or the ctest summary echoed to stderr shows any failed test or a total of 0 tests — the requirement text is being amended to match tests that must still be passing when it is amended.</fails_when>
  </verify>
  <acceptance_criteria>
    - VIDEO-03's requirement text states both halves: zero findings for the two-spellings case, exactly one finding on `video.color.range` for a real range flip.
    - The text names the tests that carry the evidence and carries the correction date.
    - VIDEO-03's traceability row is unchanged by this task.
    - `ctest -R "integration\.video_yuvj"` still passes at the moment the text is amended.
  </acceptance_criteria>
  <done>VIDEO-03 says what a correct implementation does, and what the suite proves it does.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan adds no code symbol, file, CLI flag or check id. It amends prose and table cells in two
existing planning documents: `.planning/ROADMAP.md` (Phase 4 SC4 and SC5, two Cross-cutting
placement rows, one Phase 7 prose note) and `.planning/REQUIREMENTS.md` (the PROBE-03, VIDEO-09 and
VIDEO-03 requirement texts, and the PROBE-03 and VIDEO-09 traceability Status cells).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a traceability Status cell -> `phase.complete`'s automated transition | An automated rule reads this cell and rewrites it; the wrong spelling turns an unmet requirement into a met one with no human in the loop. |
| a requirement's own text -> every future verifier | A requirement that contradicts correct behaviour invites a later implementer to "fix" the code toward a false positive. |
| a wholesale document write -> every phase entry outside the diff window | ROADMAP.md carries seven phases; a full-file rewrite from a partial view silently deletes the rest. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-83 | Spoofing | an unmet requirement marked Complete by `phase.complete` | high | mitigate | Both rows are set to the literal `Deferred` spelling the transition rule leaves untouched, and an acceptance criterion greps for the exact table line rather than for the word alone. |
| T-4-84 | Repudiation | a requirement text that contradicts the tested, correct behaviour | high | mitigate | VIDEO-03's text is rewritten to the two-half property that `integration.video_yuvj` Tests 1 and 2 prove, both of which were observed failing with the fold disabled in 04-16. |
| T-4-85 | Tampering | a whole-file write truncating ROADMAP.md | high | mitigate | The plan mandates scoped edits and forbids a wholesale rewrite; the verify step re-parses all seven phase sections and asserts Phase 4 and Phase 7 still carry five success criteria each. |
| T-4-86 | Repudiation | a silent scope reduction indistinguishable from drift | medium | mitigate | Each amendment carries the decision date from 04-VERIFICATION.md's Human Decisions table and a Cross-cutting placements row, so a later reader finds a decision rather than an unexplained change. |
| T-4-SC | Tampering | npm/pip/cargo installs | low | accept | No package manager is invoked and no dependency manifest changes. |
</threat_model>

<verification>
- `.planning/REQUIREMENTS.md` contains both exact `| ... | Phase 4 | Deferred |` lines.
- Phase 4's section of `.planning/ROADMAP.md` contains no under-10% assertion and names PERF-03/PERF-05.
- Phase 7's `**Requirements**:` line is byte-unchanged.
- The Coverage table still totals 138 and no per-phase count changed.
- `ctest --preset x64-linux --output-on-failure` passes in full — this plan changes no code, so any test movement here is a signal that something else was touched.
- `git diff --stat` for this plan lists exactly `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md`.
</verification>

<success_criteria>
- PROBE-03 and VIDEO-09 read `Deferred` and cannot be flipped to Complete at phase close.
- Phase 4's success criteria claim only what Phase 4 delivers, and the two deferrals are recorded where every other placement decision is recorded.
- VIDEO-03's text matches the behaviour two mutation-proven tests demonstrate.
</success_criteria>

<output>
Create `.planning/phases/04-video-analysis/04-19-SUMMARY.md` when done.
</output>
