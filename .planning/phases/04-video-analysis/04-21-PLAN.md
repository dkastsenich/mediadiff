---
phase: 04-video-analysis
plan: 21
type: execute
wave: 6
depends_on: ["04-20"]
gap_closure: true
files_modified:
  - tests/golden/CORPUS_DIGEST.txt
  - tests/golden/CORPUS_DIGEST_PROVISIONAL.txt
autonomous: false
requirements: [BUILD-05, BUILD-08]

estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "`tests/golden/CORPUS_DIGEST.txt` is the designated x64-linux leg's own listing, verbatim, for every fixture — not a mixture of CI hashes and workstation hashes."
    - "Every pre-existing fixture line in the transcribed listing matches `git show 8caf1f1:tests/golden/CORPUS_DIGEST.txt`, verified by `scripts/lint_corpus_digest_provenance.sh`'s no-rewrite clause rather than by inspection."
    - "`tests/golden/CORPUS_DIGEST_PROVISIONAL.txt` carries no remaining provisional fixture entries — every hash in the digest now has designated-leg provenance."
    - "The corrected digest is pushed only after an explicit human confirmation obtained immediately beforehand."
    - "The designated leg is observed GREEN after that push, including the five designated-leg-only goldens: `inspect_container`, `ts_scan_ts_single`, `ts_scan_ts_multiprogram`, `ts_scan_ts_204` and `size_checks_size_crf20`."
  artifacts:
    - tests/golden/CORPUS_DIGEST.txt
    - tests/golden/CORPUS_DIGEST_PROVISIONAL.txt
  key_links:
    - "04-20's SUMMARY -> this plan's input: the captured listing is the ONLY source for the transcription, and re-running `scripts/gen_corpus.sh` here would substitute workstation bytes for it, which is the original defect."
    - "`scripts/lint_corpus_digest_provenance.sh` clause 4 -> the cross-check: if a transcribed pre-existing line differs from main's, the lint fails and names it, so the cross-check cannot be skipped by accident."
    - "the five designated-leg-only goldens -> why the SECOND run matters: the first run failed at the digest assertion before vcpkg bootstrap, so nothing downstream of it ran at all. Those goldens are skipped locally and have never been exercised against this branch."
  prohibitions:
    - "Never run `scripts/gen_corpus.sh` or `scripts/corpus_digest.sh` and write the result into `tests/golden/CORPUS_DIGEST.txt`. The transcription source is 04-20's captured CI listing and nothing else."
    - "Never resolve a mismatch between a transcribed pre-existing line and main's by overwriting it. A divergence is a finding about a changed recipe; stop and report it."
    - "Never push without an explicit human confirmation obtained immediately beforehand; `workflow.auto_advance` does not apply to this checkpoint."
    - "Never mark the phase complete on a red designated leg, and never treat a skipped designated-leg golden as a pass."
    - "Never set `UPDATE_GOLDENS=1` anywhere in this plan."
---

<objective>
Replace the corpus digest with the designated leg's own listing, push it behind a confirmation, and
watch the leg go green — including the five goldens that have never run against this branch.

Purpose: Human Decision 4's steps 4 and 5 in 04-VERIFICATION.md. The first CI run (04-20) failed at
the digest assertion before vcpkg bootstrap, so nothing downstream of it executed; transcribing the
captured listing is what lets the second run build, test, and exercise the designated-leg goldens.
Output: a fully designated-leg digest, an emptied provisional ledger, and observed green CI.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-video-analysis/04-VERIFICATION.md
@.planning/phases/04-video-analysis/04-20-SUMMARY.md
@tests/golden/README.md
@scripts/assert_corpus_digest.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transcribe the designated leg's listing, cross-checking every pre-existing line</name>
  <files>tests/golden/CORPUS_DIGEST.txt, tests/golden/CORPUS_DIGEST_PROVISIONAL.txt</files>
  <read_first>
    - .planning/phases/04-video-analysis/04-20-SUMMARY.md (the verbatim x64-linux listing, the run id, the commit sha, and the recorded cross-check verdict — this task's only input)
    - tests/golden/CORPUS_DIGEST.txt (the current file, to be replaced by the captured listing)
    - tests/golden/CORPUS_DIGEST_PROVISIONAL.txt (the ledger to be emptied, keeping its explanatory header)
    - scripts/corpus_digest.sh (the emission format the captured listing follows, and how its `CORPUS_DIGEST_SUMMARY=` is derived)
    - scripts/assert_corpus_digest.sh (its three excluded lines — `mkv_opus_a.webm`, `mkv_opus_b.webm`, `CORPUS_DIGEST_SUMMARY=` — and the `EXPECTED_EXCLUDED_LINE_COUNT` guard that stops the exclusion widening)
    - scripts/lint_corpus_digest_provenance.sh (its clause 4 no-rewrite guard, which must pass against the transcribed file)
  </read_first>
  <action>
    Write 04-20's captured listing into `tests/golden/CORPUS_DIGEST.txt` verbatim, including its own
    `CORPUS_DIGEST_SUMMARY=` line as the designated leg computed it. The whole file is replaced by
    the captured listing — this is the one moment in this phase where a wholesale replacement is
    correct, because the source is the designated leg rather than this workstation.

    Before writing, re-perform the cross-check independently of 04-20's recorded verdict. For every
    fixture name in `git show 8caf1f1:tests/golden/CORPUS_DIGEST.txt`, compare main's hash with the
    captured one. The two Opus fixtures are known-unstable even within the designated leg and are
    excluded by `scripts/assert_corpus_digest.sh`; the summary line is derived and also excluded. A
    divergence on any other pre-existing fixture means that recipe's output changed during Phase 4:
    STOP, record the fixture and both hashes as a finding, and do not write. Confirming this twice —
    once at capture, once at transcription — is deliberate: a single confirmation of an irreversible
    overwrite is not enough.

    Also confirm the captured listing's fixture NAME set matches this branch's corpus exactly: it
    must contain all 80 names from main plus the 57 Phase 4 added plus `video_yuv420p_pc_tagged.mp4`
    from 04-16 — 138 names, 139 lines with the summary. A name present locally but absent from the
    captured listing means the CI corpus and this branch's generator disagree, which is a finding,
    not a rounding error.

    Then empty `tests/golden/CORPUS_DIGEST_PROVISIONAL.txt` of fixture entries, keeping its header
    and adding a line recording that the digest was transcribed from the designated leg, with the
    run id and commit sha from 04-20's SUMMARY. Leaving an entry behind would claim a hash is
    workstation-derived when it is not.

    Finally, note in the SUMMARY that `bash scripts/assert_corpus_digest.sh` will now FAIL on this
    workstation for essentially every non-trivial fixture — the file is CI's answer and this machine
    produces different bytes. That is the correct, intended end state and is exactly what
    `tests/golden/README.md`'s designated-leg policy describes.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; bash scripts/lint_corpus_digest_provenance.sh &amp;&amp; test "$(sed '$d' tests/golden/CORPUS_DIGEST.txt | wc -l | tr -d ' ')" = "138" &amp;&amp; echo TRANSCRIBED_AND_GUARDED</automated>
    <fails_when>non-zero exit, or TRANSCRIBED_AND_GUARDED absent from stdout; the provenance lint names any pre-existing line that no longer matches main's, and a listing count other than 138 means the captured set and this branch's corpus disagree.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; test "$(grep -c -v -e '^[[:space:]]*#' -e '^[[:space:]]*$' tests/golden/CORPUS_DIGEST_PROVISIONAL.txt)" = "0" &amp;&amp; echo LEDGER_EMPTY</automated>
    <fails_when>non-zero exit, or LEDGER_EMPTY absent from stdout — a fixture entry remains in the provisional ledger after every hash gained designated-leg provenance.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; ctest --preset x64-linux --output-on-failure 2>&amp;1 | tail -20 | tee /dev/stderr | grep -qE '100% tests passed|tests passed, 0 tests failed' &amp;&amp; echo LOCAL_SUITE_STILL_GREEN</automated>
    <fails_when>non-zero exit, or LOCAL_SUITE_STILL_GREEN absent from stdout, or the ctest tail echoed to stderr reports any failed test — no ctest test reads the committed digest, so a failure here means something other than the digest changed.</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/golden/CORPUS_DIGEST.txt` is byte-identical to the listing recorded in `.planning/phases/04-video-analysis/04-20-SUMMARY.md`, including its `CORPUS_DIGEST_SUMMARY=` line.
    - The file holds 138 listing lines plus one summary line.
    - `bash scripts/lint_corpus_digest_provenance.sh` exits 0 with clause 4 reported as run against `8caf1f1`.
    - `tests/golden/CORPUS_DIGEST_PROVISIONAL.txt` has zero non-comment, non-blank entries and records the run id and commit sha of the run it was cleared by.
    - The SUMMARY records the second, independent cross-check verdict against main's lines, naming the two excluded Opus fixtures explicitly as expected divergences.
    - The SUMMARY states that `scripts/assert_corpus_digest.sh` now fails locally by design.
  </acceptance_criteria>
  <done>The committed digest is what the designated leg computes, and the ledger no longer claims otherwise.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Confirm, then push the corrected digest</name>
  <files>(none — an outward-facing action, not a file change)</files>
  <reversibility rating="costly">The branch is already published and the pull request already open (04-20), so this is not a new one-way door; it is an update whose content can be superseded by a further commit. What it costs is a full CI matrix run and the collaborators' attention, so it is still gated rather than taken freely.</reversibility>
  <precondition>Task 1's transcription is committed locally, the working tree is clean, and the draft pull request opened in 04-20 is still open against `main`.</precondition>
  <action>
Push the corrected `tests/golden/CORPUS_DIGEST.txt` and the emptied
`tests/golden/CORPUS_DIGEST_PROVISIONAL.txt` to `gsd/phase-04-video-analysis` — and only after a
human has confirmed, in this task, immediately beforehand. The agent performs the push; the human
authorises it.
  </action>
  <instructions>
Present the local state, then ask, then push.

1. `git status --porcelain` — must be empty. Show it.
2. `git diff HEAD~1 --stat` — must list exactly `tests/golden/CORPUS_DIGEST.txt` and
   `tests/golden/CORPUS_DIGEST_PROVISIONAL.txt`. Show it.
3. `bash scripts/lint_corpus_digest_provenance.sh` — must exit 0 with clause 4 reported as run
   against `8caf1f1`, not skipped. Show its output.
4. `ctest --preset x64-linux --output-on-failure` — must still pass in full. Show the summary line.
5. Restate Task 1's cross-check verdict: clean apart from `mkv_opus_a.webm` and `mkv_opus_b.webm`.
6. Say out loud that `bash scripts/assert_corpus_digest.sh` now fails on this workstation for most
   fixtures, by design — the committed file is CI's answer and this machine produces different bytes.

Then ask the human, in one sentence: push the corrected digest to `gsd/phase-04-video-analysis`,
yes or no?

State what happens on yes: CI runs again, and this time the x64-linux leg should PASS the digest
assertion, proceed through vcpkg bootstrap, build, and run the full suite — including the five
designated-leg-only goldens that have never been exercised against this branch, because the first
run never got past the assertion. Those five are the real remaining unknown.

On an explicit yes, run `git push` for this branch. On anything else, stop and report.
  </instructions>
  <verification>
`git ls-remote --heads origin gsd/phase-04-video-analysis` returns a ref whose sha equals local
`HEAD`, and `gh run list --branch gsd/phase-04-video-analysis --limit 1` shows a run queued or
in progress for that sha. Record both, and the human's verbatim confirmation, in the SUMMARY.
  </verification>
  <resume-signal>Say "push the digest" to proceed, or name what you want checked first.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Confirm the designated leg is green, including the five designated-leg-only goldens</name>
  <files>(none — an observation recorded in this plan's SUMMARY)</files>
  <precondition>Task 2's push completed and a CI run for the new head exists.</precondition>
  <what-built>
The corrected digest is on the remote and CI has run against it. This is the run Human Decision 4's
step 5 requires: the designated leg green, including the goldens that are skipped everywhere else.
  </what-built>
  <how-to-verify>
1. `gh run list --branch gsd/phase-04-video-analysis --limit 5` and take the run for the new head.
2. `gh run view <run-id>` — confirm the `x64-linux` leg concluded SUCCESS, and confirm the
   "Assert the corpus digest matches the committed pin (D-GAP-01)" step passed this time.
3. `gh run view <run-id> --log` — locate the Test step's output on the x64-linux leg and confirm
   each of these five reports PASSED, not skipped:
   - `unit.inspect_container` (the container+meta golden)
   - `unit.ts_scan_golden` for `ts_single.ts`
   - `unit.ts_scan_golden` for `ts_multiprogram.ts`
   - `unit.ts_scan_golden` for `ts_204.ts`
   - `integration.size_checks` (the `size_crf20.mp4` committed golden)
   A `skipped` line for any of these on the DESIGNATED leg means the leg is not claiming the
   guarantee it is supposed to claim — report it rather than counting it as green.
4. Confirm the three blocking legs (x64-linux, arm64-osx, x64-windows-static-md) and the lint job
   all concluded SUCCESS. The lint job now carries two new gates from this phase
   (`lint_corpus_digest_provenance.sh`, `lint_pragma_scope.sh`) and the three-OS matrix is the only
   place 04-17's diagnostic-pragma change is exercised against Clang, AppleClang and MSVC.
5. Record the run id, the per-leg conclusions and the five goldens' individual statuses in this
   plan's SUMMARY, quoted from the log.

If any leg is red, do NOT close the phase. Report what failed with its log excerpt; a red designated
leg means Phase 4 is not shippable, which is exactly what this whole sequence exists to establish
one way or the other.
  </how-to-verify>
  <resume-signal>Paste the run id and the per-leg conclusions, plus the five designated-leg goldens' individual PASSED/skipped lines — or report which leg is red with its failing step.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan creates no source file, symbol, CLI flag, check id or fixture. It replaces the contents of
`tests/golden/CORPUS_DIGEST.txt` with the designated leg's listing, empties
`tests/golden/CORPUS_DIGEST_PROVISIONAL.txt` of fixture entries, and records the CI evidence in
`.planning/phases/04-video-analysis/04-21-SUMMARY.md`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a CI log -> a committed golden | The transcription is a manual copy from a log into a file the project treats as authoritative; an error here is undetectable later by inspection. |
| this machine -> a branch with an open pull request | The push is visible immediately and triggers automation; there is no local undo. |
| a green summary line -> the claim that the phase is shippable | A leg can conclude success while the checks that carry the guarantee were skipped on it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-91 | Tampering | a changed pre-existing recipe overwritten during transcription | high | mitigate | The cross-check against `8caf1f1` is performed a second time at transcription, independently of 04-20's recorded verdict, and `scripts/lint_corpus_digest_provenance.sh` clause 4 re-checks it mechanically afterwards. |
| T-4-92 | Spoofing | workstation bytes substituted for the CI listing | high | mitigate | The prohibitions forbid running the corpus or digest scripts into the committed file; the listing count and the provenance lint both fail if the content did not come from the captured listing. |
| T-4-93 | Elevation of Privilege | an agent pushing without human consent | high | mitigate | The push sits behind `checkpoint:human-action` with `gate="blocking-human"`, which `workflow.auto_advance` does not auto-approve. |
| T-4-94 | Repudiation | a leg reported green while the guarantee-carrying checks were skipped on it | high | mitigate | Task 3 requires each of the five designated-leg-only goldens to be confirmed PASSED individually from the log, and states explicitly that a skip on the designated leg is a report-worthy finding rather than a pass. |
| T-4-95 | Repudiation | a provisional ledger still claiming workstation provenance after transcription | low | mitigate | The ledger is emptied of fixture entries and the emptying is asserted by a count of zero non-comment lines. |
| T-4-SC | Tampering | npm/pip/cargo installs | low | accept | No package manager is invoked and no dependency manifest changes. |
</threat_model>

<verification>
- `tests/golden/CORPUS_DIGEST.txt` is byte-identical to the listing captured in 04-20's SUMMARY.
- `bash scripts/lint_corpus_digest_provenance.sh` exits 0 with clause 4 run against `8caf1f1`.
- `tests/golden/CORPUS_DIGEST_PROVISIONAL.txt` has zero fixture entries.
- `ctest --preset x64-linux --output-on-failure` still passes locally in full.
- `bash scripts/assert_corpus_digest.sh` is EXPECTED to fail locally from here on; the SUMMARY says so explicitly rather than leaving a future reader to rediscover it.
- The CI run for the pushed head shows x64-linux SUCCESS, the digest-assertion step passing, the three blocking legs and the lint job green, and all five designated-leg-only goldens reported PASSED.
</verification>

<success_criteria>
- The committed corpus digest is the designated leg's own output for every fixture, cross-checked against main twice.
- The push happened only after an explicit human confirmation obtained immediately beforehand.
- The designated leg is observed green with the five designated-leg-only goldens individually confirmed PASSED.
</success_criteria>

<output>
Create `.planning/phases/04-video-analysis/04-21-SUMMARY.md` when done.
</output>
