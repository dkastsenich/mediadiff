---
phase: 04-video-analysis
plan: 08
type: execute
wave: 7
depends_on: ["04-07"]
files_modified:
  - src/analyzers/video/color.cpp
  - src/analyzers/video/analyzers.h
  - src/probe/orchestrator.cpp
  - src/core/checks.def
  - docs/checks/video.pix_fmt.md
  - docs/checks/video.color.range.md
  - docs/checks/video.color.primaries.md
  - docs/checks/video.color.transfer.md
  - docs/checks/video.color.matrix.md
  - docs/checks/video.color.chroma_loc.md
  - CMakeLists.txt
  - tests/unit/test_video_color.cpp
  - tests/unit/CMakeLists.txt
  - tests/integration/test_video_yuvj.cpp
  - tests/integration/CMakeLists.txt
  - tests/integration/test_doc03_coverage.cpp
autonomous: true
requirements: [VIDEO-01, VIDEO-03, VIDEO-07, VIDEO-08]

estimate:
  tokens: 55000
  raw_tokens: 110000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "A `yuvj420p` file compared against a `yuv420p` file whose colour range is limited produces EXACTLY ONE finding, on `video.color.range` — asserted as a count of exactly one, not merely as the presence of that finding."
    - "The same `yuvj420p` file compared against a `yuv420p` file whose colour range is full produces ZERO non-pass findings across the whole video group: two spellings of one intent are one intent."
    - "The range fold is a single `detail::` seam both `video.pix_fmt` and `video.color.range` read, so the two cannot drift into independently-written branches."
    - "All five deprecated `yuvj*` pixel formats fold, sourced from the linked FFmpeg's own enum comments, not from recall."
    - "`video.color.range` carries no profile-severity and no profile-tolerance override of any kind, so it fails in every profile including `transform` (VIDEO-07)."
    - "A change to `unspecified` in any colorimetry field is a real difference at the check's own severity — never a wildcard match (VIDEO-08), and the direction is visible in the finding."
    - "VIDEO-07-E1: `chroma_loc` unspecified versus left is a real difference at `warn`, not folded away as equivalent."
    - "VIDEO-08-E1: both directions are covered — a colorimetry field going from a real value to `unspecified` and from `unspecified` to a real value each produce a finding."
  artifacts:
    - src/analyzers/video/color.cpp
    - tests/integration/test_video_yuvj.cpp
    - docs/checks/video.color.range.md
  key_links:
    - "the range fold running BEFORE comparison -> `video.pix_fmt` sees a folded format on both sides -> `video.color.range` owns every range delta alone: without the fold, one intent produces two findings, which is the false-positive class this project treats as P0."
    - "one `detail::` fold seam -> both consumers -> a single provable code path: two independently-written branches could drift, and the drift would be invisible until a user reported a double finding."
    - "the absence of any profile override on `video.color.range` -> `resolve_severity` returning the baseline in every profile: the mechanism for `fails in all profiles, no exceptions` is that there is nothing to resolve."
  prohibitions:
    - "Never fold a colour-range difference into `video.pix_fmt`; after the fold, `video.color.range` owns every range delta alone."
    - "Never add a `[check.profile_severity]` or `[check.profile_tolerance]` entry to `video.color.range`."
    - "Never treat `unspecified` as matching any other value; it compares as its own value in both directions."
    - "Never write a second copy of the fold table; there is one table and one seam."
    - "Never write a dotted check-id string literal inside an analyzer."
---

<objective>
The phase's signature test made real: colour intent compared once, in one place, so two spellings
of the same intent never double-report and a genuine range flip never hides.

Purpose: VIDEO-03 is the clearest expression of this project's false-positives-are-P0 rule in the
whole phase. It is satisfiable only if the pixel-format range fold runs before comparison and only
if `video.color.range` then owns every range delta alone. VIDEO-07 adds the four other colorimetry
fields; VIDEO-08 fixes what `unspecified` means on all of them.
Output: `src/analyzers/video/color.cpp`, six registered checks with docs and DOC-03 pairs, and an
integration test that counts findings rather than merely looking for one.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-video-analysis/04-CONTEXT.md
@.planning/phases/04-video-analysis/04-RESEARCH.md
@.planning/phases/04-video-analysis/04-CHECK-ROSTER.md
@.planning/phases/04-video-analysis/04-02-SUMMARY.md
@claude_docs/03-video-analysis.md
</context>

<flagged_assumptions>
**A1 — Only `yuvj420p` is fixture-exercised.** The fold table has five entries; the corpus
exercises one, because `mjpeg` is the only LGPL encoder that accepts any of them and 420 is the
one the design doc's own acceptance criteria name. The other four are covered by unit tests over
the seam directly, not by fixtures. That is a deliberate, recorded gap rather than an oversight —
plan 04-02's own `truths` already flag it.

**A2 — `video.pix_fmt` is registered here, not in plan 04-06.** The roster assigns it to this
plan on purpose: the fold must run before comparison, and a `video.pix_fmt` registered in an
earlier plan without the fold would ship a check that double-reports the yuvj case for as long as
that plan is the tip. Registering it alongside the fold makes the single-finding property true from
the first commit that has the check at all.

**A3 — Spec-less edge probe.** Four of the 17 unresolved probe items are authored into this plan's
`truths`: **VIDEO-03-E1** (the fold runs on BOTH sides before comparison), **VIDEO-03-E2** (all
five formats fold), **VIDEO-07-E1** (`chroma_loc` unspecified versus left is a real difference) and
**VIDEO-08-E1** (both directions to and from `unspecified` fire).
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The range-fold seam, video.pix_fmt, and video.color.range</name>
  <files>src/analyzers/video/color.cpp, src/analyzers/video/analyzers.h, src/probe/orchestrator.cpp, src/core/checks.def, docs/checks/video.pix_fmt.md, docs/checks/video.color.range.md, CMakeLists.txt, tests/unit/test_video_color.cpp, tests/unit/CMakeLists.txt</files>
  <read_first>
    - `04-RESEARCH.md`'s "pix_fmt range-fold table" section — the complete five-entry table read directly from `libavutil/pixfmt.h:85-283`, with the enum comment quoted verbatim. That is the source; do not extend or shorten it from memory.
    - `claude_docs/03-video-analysis.md` §2's `video.pix_fmt` row — "range-fold table `yuvj420p -> (yuv420p, range=full)` etc. before comparison; range delta is then owned solely by `video.color.range`" and "the yuvj trap: two spellings of one intent must not double-report; normalization is the fix".
    - `claude_docs/03-video-analysis.md` §3's `video.color.range` row and §5's paragraph on why colorimetry stays `fail` under `transform`.
    - `04-CONTEXT.md`'s Specific Ideas section — the yuvj trap described as the phase's signature test, deserving an explicit single-finding assertion rather than just a pair.
    - `src/analyzers/container/mp4.cpp` lines 424-472 — `detail::compute_median_fragment_duration`'s exposed-seam pattern: a helper declared in the family's `analyzers.h` under `namespace detail` so a unit test can drive it directly. The fold follows this shape exactly.
    - `src/core/registry.h` lines 200-250 — `CheckDef`, `transform_affected`, and how `resolve_severity` consults `profile_severity`; the absence of an override is the mechanism for "no exceptions".
    - `04-CHECK-ROSTER.md` — both ids' declared attributes and the explicit note that `video.color.range` carries no overrides.
  </read_first>
  <behavior>
    - Test 1: the fold seam maps `AV_PIX_FMT_YUVJ420P` to `AV_PIX_FMT_YUV420P` with a full-range indication, and does the same for the 422, 444, 440 and 411 variants — five hand-written assertions, one per table entry.
    - Test 2: the fold seam leaves a non-`yuvj` format unchanged and does not assert a range for it.
    - Test 3: the fold seam applied to a format carrying an explicit colour range keeps the explicit range rather than overwriting it, so a `yuv420p` file that declares limited range stays limited.
    - Test 4: `video.pix_fmt` for `video_yuvj420p.mp4` emits the folded name, identical to the value emitted for `video_yuv420p_pc.mp4`.
    - Test 5: `video.color.range` for `video_yuvj420p.mp4` emits full, and for `video_yuv420p_tv.mp4` emits limited.
    - Test 6: comparing `video_range_pc.mp4` against `video_color_bt709.mp4` (which is limited) reports a non-pass `video.color.range` under `--profile sw-encoder`.
    - Test 7: the same comparison under `--profile transform`, `--profile remux` and `--profile strict-bitexact` also reports a non-pass `video.color.range` — the "no exceptions" property, asserted across every profile rather than assumed.
  </behavior>
  <action>
    Create `src/analyzers/video/color.cpp` and add a `video_color_analyzer()` accessor to
    `src/analyzers/video/analyzers.h`. One `AnalyzerSpec` named `video_color`, scope
    `ContainerFamily::other`, `required_passes = {Pass::demux_header}` — every field here is a
    demux-time `codecpar` value and no scan is needed, so declaring one would put a sweep in the
    union for nothing.

    Copy the three file-local shared patterns (`push_skip`, per-stream `Scope` derivation, the GCC
    pragma) as every other analyzer in this project does.

    Declare the fold seam in `src/analyzers/video/analyzers.h` under `namespace detail`, following
    `mp4.cpp`'s `detail::compute_median_fragment_duration` precedent, so
    `tests/unit/test_video_color.cpp` can drive it directly without going through a whole probe
    run. It takes a pixel format and a declared colour range and returns the folded format plus the
    effective range. Implement the five-entry table exactly as `04-RESEARCH.md` records it from
    `libavutil/pixfmt.h`, with the enum comment quoted in a code comment as provenance.

    Both `video.pix_fmt` and `video.color.range` read that ONE seam. Not two branches, not a copy —
    VIDEO-03's "exactly one finding" is provable only if the fold is a single code path, and this
    is the whole reason the seam exists.

    `video.pix_fmt` — `av_get_pix_fmt_name` of the FOLDED format, as a string. Evidence carries the
    raw declared format name when it differed from the folded one, so a user can see that a fold
    happened rather than wondering why the report says `yuv420p` for a file they know is `yuvj420p`.

    `video.color.range` — `av_color_range_name` of the effective range after the fold. Evidence
    carries the declared range and whether the fold supplied it. The finding message must name the
    practical effect in the user's terms, as doc 03 §3 requires: a range flip washes out or crushes
    everything downstream.

    Register both per the roster. `video.pix_fmt` carries `transform = "info"` like the other
    identity checks. `video.color.range` carries NO `[check.profile_severity]` and NO
    `[check.profile_tolerance]` sub-table at all, and its comment block above the record must state
    that the absence is deliberate and load-bearing, citing VIDEO-07 and doc 03 §5 — otherwise a
    future editor adds a `transform` demotion for symmetry and silently breaks the invariant that
    profile exists to protect.

    Write both docs. `video.color.range`'s `### Silence` section must say plainly that this check
    is not one to silence, and why: preserving colour intent through an intentional transformation
    is exactly what the `transform` profile exists to protect.

    Register `video_color_analyzer()` in `all_analyzers()` and add the source to `libmediadiff`.

    Create `tests/unit/test_video_color.cpp` covering Tests 1 through 5 — the first three drive the
    fold seam directly with hand-written expectations from the research table, the last two run
    real fixtures through the orchestrator. Register it in `tests/unit/CMakeLists.txt`.
  </action>
  <verify>
    <automated>cmake --build build/x64-linux 2>&amp;1 | tail -5 &amp;&amp; ctest --test-dir build/x64-linux -R "unit\.video_color" --output-on-failure</automated>
    <fails_when>Any of the five fold-table assertions returns a format or range differing from the hand-written expectation; `video.pix_fmt` emits different values for `video_yuvj420p.mp4` and `video_yuv420p_pc.mp4` (the fold did not run on both sides); or the cross-profile range assertion finds `video.color.range` demoted under any profile.</fails_when>
  </verify>
  <precondition>Plan 04-02 has landed, so `video_yuvj420p.mp4`, `video_yuv420p_pc.mp4`, `video_yuv420p_tv.mp4`, `video_range_pc.mp4` and `video_color_bt709.mp4` exist and were verified there to carry their intended pixel formats and ranges.</precondition>
  <acceptance_criteria>
    - **Locally provable** — `./build/x64-linux/mediadiff inspect tests/fixtures/video_yuvj420p.mp4 --json` shows `video.pix_fmt` as `yuv420p` and `video.color.range` as the full-range spelling.
    - **Locally provable** — the same command on `video_yuv420p_pc.mp4` shows identical values for both checks.
    - **Locally provable** — `grep -c 'video.color.range' src/core/checks.def` shows the id registered once, and no `[check.profile_severity]` or `[check.profile_tolerance]` sub-table follows its record.
    - **Locally provable** — comparing `video_range_pc.mp4` against `video_color_bt709.mp4` reports `video.color.range` at `fail` under each of `sw-encoder`, `hw-encoder`, `remux`, `strict-bitexact` and `transform`.
    - **Locally provable** — `bash scripts/lint_check_id_strings.sh` passes.
  </acceptance_criteria>
  <done>One fold seam, two consumers, and a colour-range check that cannot be demoted by any profile.</done>
  <reversibility rating="costly">Both check ids came from the roster gate; what remains costly is the fold table's semantics, which change what every future snapshot records as a file's pixel format.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: primaries, transfer, matrix and chroma_loc, with `unspecified` as its own value</name>
  <files>src/analyzers/video/color.cpp, src/core/checks.def, docs/checks/video.color.primaries.md, docs/checks/video.color.transfer.md, docs/checks/video.color.matrix.md, docs/checks/video.color.chroma_loc.md, tests/unit/test_video_color.cpp</files>
  <read_first>
    - `claude_docs/03-video-analysis.md` §3 in full — the extraction fields (`color_primaries`, `color_trc`, `color_space`, `chroma_location`), the `av_color_*_name` rendering, the sentence that `unspecified` compares as its own value and a change TO unspecified is metadata loss rather than a wildcard, the PQ and HLG call-outs by name, the amplified SDR-to-HDR message, and the 601-to-709 "global color shift" message.
    - `04-CHECK-ROSTER.md` — the four ids' attributes, including that `chroma_loc` is `warn` while the other three are `fail`.
    - `04-02-SUMMARY.md`'s colorimetry read-back table — which fixture carries which primaries, transfer, matrix and chroma location, so the tests assert against verified values rather than against recipe intent.
    - `src/analyzers/container/mp4.cpp` lines 198-216 — `emit_brands`' codecpar-field-to-Measurement shape with an evidence sub-object.
    - `src/util/sanitize.h` — the display-sanitisation choke point every rendered string passes through.
  </read_first>
  <behavior>
    - Test 1: for `video_color_bt709.mp4`, all four checks emit the bt709-family renderings the plan-04-02 read-back table recorded.
    - Test 2: for `video_color_bt601.mp4`, all four emit the smpte170m-family renderings, distinct from Test 1 on primaries, transfer and matrix.
    - Test 3: comparing the two reports non-pass findings on `video.color.primaries`, `video.color.transfer` and `video.color.matrix`.
    - Test 4: comparing `video_color_bt709.mp4` against `video_color_unspec.mp4` reports non-pass on those same three — a change TO unspecified is a difference, not a match.
    - Test 5: comparing `video_color_unspec.mp4` against `video_color_bt709.mp4` (the reverse direction) also reports non-pass on all three.
    - Test 6: comparing `video_chroma_left.mp4` against `video_chroma_center.mp4` reports a non-pass `video.color.chroma_loc` at `warn`.
    - Test 7: comparing a file with an unspecified chroma location against one declaring `left` reports a non-pass `video.color.chroma_loc` — unspecified is not equivalent to any placement.
    - Test 8: the finding message for a bt709-to-smpte170m matrix change names the practical effect, and the message for an SDR-to-PQ transfer change is distinguishable from an ordinary transfer change.
  </behavior>
  <action>
    Add the four remaining colorimetry checks to `src/analyzers/video/color.cpp`, each a direct
    `codecpar` field rendered through its `av_color_*_name` counterpart, each at `Scope{Kind::video,
    rank}`, each with the raw enum integer in evidence so two values that render to the same string
    are still distinguishable.

    `unspecified` is rendered and compared exactly like any other value. There is no wildcard, no
    absent-treated-as-match, and no special case: the `exact` comparator's ordinary string equality
    already produces the right answer, so the requirement here is to NOT add a special case. State
    that in a comment above the emission, citing VIDEO-08, because "we did not write the obvious
    convenience" is invisible to a later reader otherwise.

    Finding messages: doc 03 §3 asks for named practical effects rather than bare value deltas.
    Compose the message from the two values so a 601-to-709 matrix change names the global colour
    shift, and a transfer change into or out of `smpte2084` or `arib-std-b67` is called out as an
    SDR-to-HDR transition rather than as one more enum change. Route every composed message through
    the project's existing display-sanitisation choke point, as every other rendered string does.

    Register all four per the roster, each with a comment block citing this plan, VIDEO-07 and
    VIDEO-08. Write the four docs. `video.color.transfer`'s `### Why it matters` must name PQ and
    HLG explicitly, since a reader searching for either term should land on this doc.
    `video.color.chroma_loc`'s `### Tune` must explain why it is `warn` rather than `fail`: it is a
    scaler-chain drift tell, real but rarely a shipping blocker on its own.

    Extend `tests/unit/test_video_color.cpp` with Tests 1 through 8.
  </action>
  <verify>
    <automated>cmake --build build/x64-linux 2>&amp;1 | tail -5 &amp;&amp; ctest --test-dir build/x64-linux -R "unit\.video_color" --output-on-failure</automated>
    <fails_when>A rendered colorimetry value differs from plan 04-02's verified read-back table (the analyzer is reading a different field than the fixture set); a comparison against the unspecified fixture returns `pass` in either direction (a wildcard crept in); or the registry generator rejects a check for a missing or malformed doc.</fails_when>
  </verify>
  <precondition>Task 1 has landed; the colorimetry fixtures from plan 04-02 exist and their verified read-back table is in `04-02-SUMMARY.md`.</precondition>
  <acceptance_criteria>
    - **Locally provable** — `./build/x64-linux/mediadiff inspect tests/fixtures/video_color_bt709.mp4 --json` shows all four colorimetry values matching plan 04-02's read-back table.
    - **Locally provable** — comparing `video_color_bt709.mp4` against `video_color_unspec.mp4` reports non-pass on primaries, transfer and matrix; the reverse comparison does too.
    - **Locally provable** — comparing `video_chroma_left.mp4` against `video_chroma_center.mp4` reports `video.color.chroma_loc` at `warn`.
    - **Locally provable** — `./build/x64-linux/mediadiff explain video.color.transfer` prints all three sections and mentions PQ and HLG by name.
    - **Locally provable** — `bash scripts/lint_control_bytes.sh` passes.
  </acceptance_criteria>
  <done>All five colorimetry fields compare with `unspecified` as an ordinary value in both directions, and the messages tell a user what the change will look like rather than which enum changed.</done>
  <reversibility rating="costly">Four permanent check ids from the roster gate; the rendered value spellings enter committed snapshots.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The exactly-one-finding signature test and six DOC-03 pairs</name>
  <files>tests/integration/test_video_yuvj.cpp, tests/integration/CMakeLists.txt, tests/integration/test_doc03_coverage.cpp</files>
  <read_first>
    - `04-CONTEXT.md`'s Specific Ideas section — "deserves an explicit single-finding assertion, not just a pair". A DOC-03 pair proves the check fires; only a count proves nothing ELSE fired.
    - `claude_docs/03-video-analysis.md` §6 — "must produce **one** finding, on `video.color.range`, not two".
    - `tests/integration/test_size_checks.cpp` — how an existing integration test runs the real CLI, parses the JSON report and asserts over the findings array.
    - `tests/integration/test_doc03_coverage.cpp` lines 190-215 — `statuses_for`'s exact CLI invocation and JSON parse, and `any_non_clean`/`all_pass`'s definitions of triggered and clean.
    - `tests/integration/CMakeLists.txt` — the per-file comment convention and the `cli_harness.h` / `support/fixture_paths.h` includes an integration test needs.
  </read_first>
  <behavior>
    - Test 1 (the signature): comparing `video_yuvj420p.mp4` against `video_yuv420p_tv.mp4` under `--profile sw-encoder` produces exactly ONE non-pass finding across the entire report, and that finding's id is `video.color.range`. Asserted as a count equal to one over all findings, not as a lookup of that id.
    - Test 2 (the mirror): comparing `video_yuvj420p.mp4` against `video_yuv420p_pc.mp4` produces ZERO non-pass findings across the entire report — two spellings of one intent.
    - Test 3: in Test 1's report, `video.pix_fmt` is explicitly present and `pass`, not absent — the fold produced a real, equal value on both sides rather than suppressing the check.
    - Test 4: Test 1's result holds under every profile, since `video.color.range` has no overrides.
    - Test 5: `integration.doc03_coverage` passes with all six of this plan's ids covered and its count-equality `REQUIRE` satisfied.
  </behavior>
  <action>
    Create `tests/integration/test_video_yuvj.cpp` with a header comment stating what this file is
    for and why a counting assertion is required rather than a presence assertion: a check that
    fires correctly while a second check ALSO fires is still a false positive, and false positives
    are P0 in this project. Register it in `tests/integration/CMakeLists.txt`.

    Test 1 must count non-pass findings across the WHOLE report, not within the video group and not
    filtered to a set of ids — an unrelated check firing on this pair would be exactly the noise
    the requirement forbids, and a filtered count would hide it. If a genuinely unrelated check
    does fire (a size difference between two differently-encoded files, for instance), do not
    weaken the assertion: narrow the FIXTURES so the two files differ in nothing else, and record
    what had to change in the SUMMARY.

    Add six entries to `declared_pairs()` in `tests/integration/test_doc03_coverage.cpp`:

      `video.pix_fmt`             trigger `video_base.mp4` / `video_yuvj420p.mp4`;
                                  clean `video_yuvj420p.mp4` / `video_yuv420p_pc.mp4`
      `video.color.range`         trigger `video_range_pc.mp4` / `video_color_bt709.mp4`;
                                  clean `video_color_bt709.mp4` / `video_color_bt709_copy.mp4`
      `video.color.primaries`     trigger `video_color_bt709.mp4` / `video_color_bt601.mp4`;
                                  clean `video_color_bt709.mp4` / `video_color_bt709_copy.mp4`
      `video.color.transfer`      trigger `video_color_bt709.mp4` / `video_color_bt601.mp4`;
                                  clean `video_color_bt709.mp4` / `video_color_bt709_copy.mp4`
      `video.color.matrix`        trigger `video_color_bt709.mp4` / `video_color_bt601.mp4`;
                                  clean `video_color_bt709.mp4` / `video_color_bt709_copy.mp4`
      `video.color.chroma_loc`    trigger `video_chroma_left.mp4` / `video_chroma_center.mp4`;
                                  clean `video_color_bt709.mp4` / `video_color_bt709_copy.mp4`

    Note above these entries that `video.pix_fmt`'s CLEAN pair is deliberately the two-spellings
    pair rather than a byte-identical copy: a copy would prove only that equal files compare equal,
    while this pair proves the fold made two genuinely different declarations compare equal, which
    is the property that matters. Prove every pair empirically against the real binary before
    committing, and record the transcript in the commit message.
  </action>
  <verify>
    <automated>cmake --build build/x64-linux 2>&amp;1 | tail -5 &amp;&amp; ctest --test-dir build/x64-linux --output-on-failure</automated>
    <fails_when>The signature test counts more than one non-pass finding (the fold did not suppress the pixel-format delta, or an unrelated check fired), counts zero (the range delta was suppressed too), finds `video.pix_fmt` absent rather than `pass`, or `integration.doc03_coverage` reports any of the six ids uncovered or fails its count-equality `REQUIRE`.</fails_when>
  </verify>
  <precondition>Tasks 1 and 2 have landed, so all six checks emit real values.</precondition>
  <acceptance_criteria>
    - **Locally provable** — `ctest --test-dir build/x64-linux -R "video_yuvj" --output-on-failure` passes.
    - **Locally provable** — `./build/x64-linux/mediadiff compare tests/fixtures/video_yuvj420p.mp4 tests/fixtures/video_yuv420p_tv.mp4 --profile sw-encoder --json` yields exactly one non-pass finding, and it is `video.color.range`; paste the finding list into the SUMMARY.
    - **Locally provable** — the same command against `video_yuv420p_pc.mp4` yields no non-pass findings.
    - **Locally provable** — `ctest --test-dir build/x64-linux -R "doc03_coverage" --output-on-failure` passes and its `INFO` line reports the roster's running registry total.
    - **Locally provable** — `ctest --test-dir build/x64-linux --output-on-failure` reports zero failures.
  </acceptance_criteria>
  <done>The phase's signature property is a counting assertion in CI: one intent spelled two ways is one intent, and a real range flip is exactly one finding.</done>
  <reversibility rating="reversible">Tests and a coverage table.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| attacker-controlled `codecpar` colour enums -> rendered strings entering the report and snapshots | Every colorimetry value is a file-chosen integer rendered into user-visible text. |
| the fold table -> what every future snapshot records as a file's pixel format | A fold applied on one side but not the other silently changes a comparison's meaning. |
| the absence of a profile override -> the guarantee that a range flip always gates | A single added override would silently break VIDEO-07's "no exceptions" clause. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-33 | Tampering | a colour-range flip suppressed by a profile override | high | mitigate | `video.color.range` is registered with no `profile_severity` and no `profile_tolerance` sub-table, its comment block records that the absence is load-bearing, and an acceptance criterion asserts a non-pass under all five profiles. |
| T-4-34 | Tampering | one intent spelled two ways reported as two regressions | high | mitigate | A single `detail::` fold seam read by both consumers, plus an integration test that COUNTS non-pass findings rather than looking one up. A double-report is the P0 false-positive class this whole plan exists to prevent. |
| T-4-35 | Tampering | a range delta suppressed along with the pixel-format delta | high | mitigate | The same counting test asserts the count is exactly one, not zero, and separately asserts `video.pix_fmt` is present and `pass` rather than absent — so suppression is distinguishable from correct folding. |
| T-4-36 | Information Disclosure | a composed finding message carrying unsanitised bytes | medium | mitigate | Every composed message passes through the project's existing display-sanitisation choke point; `scripts/lint_control_bytes.sh` is the standing gate and is an acceptance criterion here. |
| T-4-37 | Spoofing | `unspecified` treated as matching any value | high | mitigate | No special case is written, and the plan states in a comment that its absence is deliberate; tests assert both directions to and from unspecified produce findings. |
| T-4-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan. |
</threat_model>

<verification>
- `ctest --test-dir build/x64-linux -R "video_yuvj" --output-on-failure` — the signature counting assertion.
- `ctest --test-dir build/x64-linux -R "unit\.video_color" --output-on-failure` — the fold table and the four colorimetry fields.
- `ctest --test-dir build/x64-linux --output-on-failure` — full suite including the DOC-03 gate.
- `bash scripts/lint_check_id_strings.sh && bash scripts/lint_control_bytes.sh`.

</verification>

<success_criteria>
- A `yuvj420p`-to-`yuv420p`-plus-limited-range change produces exactly one finding, on
  `video.color.range`, proven by a count.
- The same file against the full-range spelling produces zero non-pass findings.
- `video.color.range` fails in every profile, with no override to remove.
- A change to `unspecified` is a regression in both directions on all four remaining fields.
</success_criteria>

<output>
Create `.planning/phases/04-video-analysis/04-08-SUMMARY.md` when done.
</output>
