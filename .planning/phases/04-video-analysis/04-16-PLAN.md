---
phase: 04-video-analysis
plan: 16
type: execute
wave: 2
depends_on: ["04-13"]
gap_closure: true
files_modified:
  - scripts/gen_corpus.sh
  - tests/golden/CORPUS_DIGEST.txt
  - tests/golden/CORPUS_DIGEST_PROVISIONAL.txt
  - tests/integration/test_video_yuvj.cpp
  - tests/integration/test_doc03_coverage.cpp
autonomous: true
requirements: [VIDEO-03, BUILD-08]

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "A fixture pair exists in which the SAME colorimetric intent survives as two genuinely distinct declarations — one file reading back as `yuvj420p`, the other as `yuv420p` with an explicit full-range flag — and the two files are NOT byte-identical."
    - "`integration.video_yuvj` Test 2 compares that pair, asserts ZERO non-pass findings across the whole report, and has been observed FAILING with `detail::fold_pix_fmt_range` disabled."
    - "VIDEO-03's adjacency edge holds: two declarations that fold to exactly the same `(pix_fmt, color_range)` pair produce ZERO findings, while a pair differing in exactly one dimension produces EXACTLY ONE, on `video.color.range`."
    - "VIDEO-07's adjacency edge holds: a colorimetry field whose two sides resolve to the identical spelling compares `pass` and does not also fire a neighbouring colour check."
    - "`tests/integration/test_doc03_coverage.cpp`'s `video.pix_fmt` clean pair is no longer a byte-identical pair, so the DOC-03 clean half of that row carries evidence."
    - "No pre-existing line of `tests/golden/CORPUS_DIGEST.txt` is removed or changed; the one added fixture contributes exactly one new line and is named in the provisional ledger."
    - statement: "For a fixture whose colour range is undeclared on BOTH sides, `video.color.range` compares the two `unspecified` spellings as equal values rather than as a wildcard match, and a change TO `unspecified` from a declared value is still a finding."
      verification: backstop
    - statement: "When two findings carry equal severity, the `--json` report's finding order is the registry declaration order and is byte-identical across two runs over the same inputs."
      verification: backstop
    - statement: "A colorimetry field absent from both sides produces one comparable entry per check rather than a missing entry, so the whole-report non-pass COUNT stays a meaningful assertion."
      verification: backstop
    - statement: "A colorimetry field present on exactly one side is reported as a difference, never silently dropped from the count."
      verification: backstop
  artifacts:
    - scripts/gen_corpus.sh
    - tests/integration/test_video_yuvj.cpp
    - tests/integration/test_doc03_coverage.cpp
    - tests/golden/CORPUS_DIGEST_PROVISIONAL.txt
  key_links:
    - "`$OUT_DIR/video_yuv420p_tv.mp4` -> the new fixture: the plain `yuv420p` bitstream ALREADY survives the pinned mjpeg encoder (that is exactly what the existing `-strict unofficial` tv recipe produces); only the range TAG needs changing, and a stream-copy remux is the one way to change it without letting the encoder re-pick `yuvj420p`."
    - "`-movie_timescale`/`-video_track_timescale` on the remux -> `container.mp4.timescale` and `container.mp4.edit_list`: without pinning both, the remux changes the mvhd timescale from 1000 to 12800 and the edit-list segment duration with it, adding two unrelated `warn` findings that would destroy the zero-count assertion."
    - "`detail::fold_pix_fmt_range` (src/analyzers/video/color.cpp) -> both `video.pix_fmt` and `video.color.range`: the single seam this fixture pair exists to exercise; disabling it is the mutation that must turn the new test red."
  prohibitions:
    - "Never delete `tests/fixtures/video_yuv420p_pc.mp4`'s recipe or its digest line. It is byte-identical to `video_yuvj420p.mp4` and therefore useless as a two-spellings pair, but `tests/unit/test_video_color.cpp` reads it and removing it is outside this gap's scope."
    - "Never run `scripts/corpus_digest.sh` and redirect its output over `tests/golden/CORPUS_DIGEST.txt`; splice in the single new line and re-derive the summary."
    - "Never weaken an assertion to make it pass. If an unrelated check fires on the new pair, narrow the fixture recipe — that is the instruction 04-08 followed and it is the instruction here."
    - "Never introduce an encoder that the LGPL Windows pinned build does not carry (libx264/libx265/librav1e/libopenh264 are all off the table for a fixture recipe, whatever the Linux pinned build happens to ship)."
    - "Never register a new check id, and never change `video.color.range`'s absent profile override — it must keep failing in every profile."
  flagged_assumptions:
    - "Edge-probe row VIDEO-08 (category `unclassified`) is surfaced rather than resolved: the deterministic probe could not classify what edge it names. VIDEO-08 (a change TO `unspecified` is a regression, not a wildcard) is verified Complete in 04-VERIFICATION.md and is untouched by this plan; the `unspecified` backstop truths above are the nearest thing this plan can assert about it."
---

<objective>
Give VIDEO-03's "same intent, two spellings" claim a fixture pair that can actually disprove it, and
replace the vacuous test that compares two byte-identical files.

Purpose: 04-VERIFICATION.md's fifth gap. `integration.video_yuvj` Test 2 compares
`video_yuvj420p.mp4` against `video_yuv420p_pc.mp4`, which are byte-identical (sha256
`f9d92aff10ff030a…`) because the pinned mjpeg encoder normalises `-color_range pc` to a `yuvj*`
name before muxing. The test passes with the fold entirely disabled — it proves nothing. Human
Decision 3: replace it with a pair whose two spellings survive distinctly, proven non-vacuous by a
mutation check.
Output: one new fixture, a load-bearing Test 2, and a DOC-03 clean pair that also carries evidence.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-video-analysis/04-VERIFICATION.md
@.planning/phases/04-video-analysis/deferred-items.md
@.planning/phases/04-video-analysis/04-02-SUMMARY.md
@.planning/phases/04-video-analysis/04-08-SUMMARY.md
@tests/golden/README.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: A fixture whose plain yuv420p spelling survives alongside a full-range flag</name>
  <files>scripts/gen_corpus.sh, tests/golden/CORPUS_DIGEST.txt, tests/golden/CORPUS_DIGEST_PROVISIONAL.txt</files>
  <read_first>
    - scripts/gen_corpus.sh lines 1117-1170 (the VIDEO-03 signature trio: the `yuvj420p` recipe, the collapsing `yuv420p -color_range pc` recipe, and the `-strict unofficial` tv recipe whose plain `yuv420p` spelling DOES survive — this task's input)
    - scripts/gen_corpus.sh lines 1370-1430 (the later mpeg4 `-color_range` + `-movflags +write_colr` recipes, for the established form of writing an explicit range tag into an mp4)
    - .planning/phases/04-video-analysis/04-02-SUMMARY.md lines 180-200 (the read-back table for the existing trio: the empirical record of what each recipe actually produces)
    - .planning/phases/04-video-analysis/04-08-SUMMARY.md (Deviation 1: why the source is `color=c=gray` and what the ~2.4% full-vs-limited size residual costs under each profile)
    - scripts/check_corpus.sh (how the expected fixture list is extracted: every literal `$OUT_DIR/&lt;name&gt;` token in the generator, so the new recipe must write through that exact form)
    - tests/golden/CORPUS_DIGEST_PROVISIONAL.txt (created by 04-13; the new fixture belongs in it)
  </read_first>
  <action>
    Add one fixture, `video_yuv420p_pc_tagged.mp4`, to `scripts/gen_corpus.sh` immediately after the
    existing `video_yuv420p_tv.mp4` recipe, written through the literal `"$OUT_DIR/..."` form so
    `scripts/check_corpus.sh`'s mechanical extraction picks it up.

    The recipe is a stream-COPY remux of the already-generated `"$OUT_DIR/video_yuv420p_tv.mp4"`,
    not a second encode. Its options, all of which are load-bearing and were verified against the
    committed corpus with the pinned `9.0.1` binary before this plan was written:

    - `-c copy` — the plain `yuv420p` JPEG bitstream must survive untouched. Re-encoding with
      `-color_range pc` is exactly what makes the mjpeg encoder pick `yuvj420p` again, which is the
      trap that produced the byte-identical pair in the first place.
    - `-color_range pc` plus `-movflags +write_colr` — writes the full-range `nclx` colour box, so
      the file declares full range through the container's range flag while its pixel format name
      stays plain.
    - `-movie_timescale 1000` and `-video_track_timescale 12800` — pin both timescales to the values
      a fresh encode produces. Without them the remux writes an mvhd timescale of 12800 and an edit
      list segment duration to match, and `container.mp4.timescale` plus `container.mp4.edit_list`
      each fire `warn` against the baseline. Measured directly: the unpinned form produced exactly
      those two extra findings, the pinned form produced zero.
    - `-flags +bitexact -fflags +bitexact` — the project-wide determinism requirement (BUILD-08).

    Expected read-back, which this task must confirm rather than assume:
    `Video: mjpeg (Baseline) (mp4v / 0x7634706D), yuv420p(pc, bt470bg/unknown/unknown, progressive),
    320x240 [SAR 1:1 DAR 4:3]` — codec, resolution, SAR and DAR all identical to
    `video_yuvj420p.mp4`, pixel format spelled `yuv420p` rather than `yuvj420p`, range `pc`.

    Write a comment above the recipe recording WHY it is a remux: the pinned mjpeg encoder (and
    `ljpeg`, and `amv` — every encoder in this build that accepts a `yuvj*` format) normalises a
    full-range `yuv420p` request to the `yuvj*` name, so the two spellings cannot both be produced
    by a single encode. Record the byte-identity that made the previous pair vacuous, with the
    sha256 prefix from deferred-items.md, so a later reader cannot re-derive the same broken recipe.

    Regenerate the corpus, then confirm all four properties before touching the digest:
    (a) the read-back above; (b) `sha256sum` of the new fixture differs from `video_yuvj420p.mp4`'s;
    (c) running the recipe twice back-to-back produces the identical sha256; (d) `bash
    scripts/check_corpus.sh` passes with the new name in its expected list.

    Then add EXACTLY ONE line to `tests/golden/CORPUS_DIGEST.txt`: the new fixture's
    `&lt;sha256&gt;  &lt;name&gt;` pair, spliced into its `LC_ALL=C` sorted position (between
    `video_yuv420p_pc.mp4` and `video_yuv420p_tv.mp4`), and re-derive the trailing
    `CORPUS_DIGEST_SUMMARY=` as the SHA-256 of the listing that precedes it. Compute the hash with
    `sha256sum` on the single file, or take the single line out of a `scripts/corpus_digest.sh`
    run redirected to a TEMPORARY file — never over the committed digest. Add the fixture name to
    `tests/golden/CORPUS_DIGEST_PROVISIONAL.txt` in sorted position: this hash is workstation-derived
    like every other Phase 4 addition.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; bash scripts/gen_corpus.sh &gt;/dev/null &amp;&amp; bash scripts/check_corpus.sh &amp;&amp; bash scripts/lint_corpus_digest_provenance.sh &amp;&amp; test "$(sha256sum tests/fixtures/video_yuvj420p.mp4 | awk '{print $1}')" != "$(sha256sum tests/fixtures/video_yuv420p_pc_tagged.mp4 | awk '{print $1}')" &amp;&amp; echo PAIR_IS_DISTINCT</automated>
    <fails_when>non-zero exit from any stage, or PAIR_IS_DISTINCT absent from stdout — the corpus is incomplete, the provenance lint found a rewritten pre-existing line, or the new fixture is byte-identical to its partner (the exact defect being fixed).</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; git diff -- tests/golden/CORPUS_DIGEST.txt | grep -E '^-[0-9a-f]{64} ' | tee /dev/stderr | wc -l | grep -qx 0 &amp;&amp; echo NO_PREEXISTING_LINE_REWRITTEN</automated>
    <fails_when>non-zero exit, or NO_PREEXISTING_LINE_REWRITTEN absent from stdout; any line echoed to stderr is a removed fixture hash line, which the standing guard forbids.</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/gen_corpus.sh` contains the literal token `$OUT_DIR/video_yuv420p_pc_tagged.mp4` and the option strings `-movie_timescale`, `-video_track_timescale`, `-movflags +write_colr` and `-c copy` within that recipe.
    - The pinned binary's read-back of `tests/fixtures/video_yuv420p_pc_tagged.mp4` names the codec `mjpeg` and the pixel format `yuv420p(pc`, quoted verbatim in the SUMMARY.
    - `sha256sum tests/fixtures/video_yuv420p_pc_tagged.mp4` differs from `sha256sum tests/fixtures/video_yuvj420p.mp4`.
    - Two back-to-back runs of the recipe produce the identical sha256, both quoted in the SUMMARY.
    - `git diff -- tests/golden/CORPUS_DIGEST.txt` shows exactly one added fixture hash line plus the summary line replacement, and zero removed fixture hash lines.
    - `tests/golden/CORPUS_DIGEST_PROVISIONAL.txt` contains `video_yuv420p_pc_tagged.mp4` and remains sorted and unique.
    - `bash scripts/check_corpus.sh` and `bash scripts/lint_corpus_digest_provenance.sh` both exit 0.
  </acceptance_criteria>
  <done>The corpus carries two files whose colorimetric intent is identical and whose declarations are genuinely different.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite the signature mirror test and prove it goes red without the fold</name>
  <files>tests/integration/test_video_yuvj.cpp</files>
  <read_first>
    - tests/integration/test_video_yuvj.cpp (all four TEST_CASEs and the file header; Test 2 at ~110-119 is the one being replaced, and `count_non_pass` at ~68-80 is the counting helper every case shares)
    - src/analyzers/video/color.cpp (`detail::fold_pix_fmt_range` and `emit_color_range` — the seam the mutation check disables)
    - .planning/phases/04-video-analysis/deferred-items.md, the 04-08 entry (the mutation evidence already gathered: with the fold disabled, tests #689, #690 and #686 all failed while #688 PASSED — that asymmetry is exactly what this task removes)
  </read_first>
  <behavior>
    - Comparing `video_yuvj420p.mp4` against `video_yuv420p_pc_tagged.mp4` under `--profile sw-encoder` produces ZERO non-pass findings across the WHOLE report (status neither `pass` nor `skipped`), counted with the file's existing `count_non_pass` helper over the unfiltered `findings` array.
    - In that same report, `video.pix_fmt` is present with status `pass` and both sides' values are the folded plain name — not absent, not skipped.
    - In that same report, `video.color.range` is present with status `pass` and both sides' values are the full-range spelling.
    - With `detail::fold_pix_fmt_range` disabled, the zero-count assertion FAILS, because `video.pix_fmt` then compares two different declared names.
    - Test 1's existing assertion (the yuvj/limited-range pair produces exactly one non-pass finding, on `video.color.range`) is unchanged and still passes.
  </behavior>
  <action>
    Replace Test 2's candidate fixture with `video_yuv420p_pc_tagged.mp4`, keeping its
    whole-report zero-count assertion and its `sw-encoder` profile exactly as they are — the
    assertion was never the problem, the fixture was.

    Strengthen the same TEST_CASE with the two positive checks in the behaviour list: assert
    `video.pix_fmt` and `video.color.range` are each present, `pass`, and carry equal baseline and
    candidate values. A bare zero-count assertion can also be satisfied by a report in which both
    checks vanished; these two assertions close that.

    Rewrite the file-header paragraph that currently describes the fixture trio, so it records: that
    the previous mirror pair was byte-identical and therefore proved nothing; which encoder
    behaviour caused that; and what the new candidate is (a stream-copy remux of the tv member
    carrying a full-range colour box). Name the new fixture in the TEST_CASE title so a failure
    names the pair being compared.

    Then prove the rewritten test is load-bearing. Disable the fold at its seam the way
    deferred-items.md records it was disabled before, rebuild, run the yuvj integration suite, and
    confirm the rewritten Test 2 FAILS alongside Tests 1, 3 and 4. Restore the fold, rebuild, and
    re-run to green. Record both ctest outputs verbatim in the SUMMARY. A rewritten Test 2 that
    still passes with the fold disabled means the fixture pair is wrong and the task is not done.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; cmake --build --preset x64-linux 2>&amp;1 | tail -5 &amp;&amp; ctest --test-dir build/x64-linux -R "integration\.video_yuvj" --output-on-failure 2>&amp;1 | tee /dev/stderr | grep -qE '[0-9]+ tests passed, 0 tests failed' &amp;&amp; echo YUVJ_SUITE_GREEN</automated>
    <fails_when>non-zero exit, or YUVJ_SUITE_GREEN absent from stdout, or the ctest summary echoed to stderr reports any failed test or a total of 0 tests.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; grep -q 'video_yuv420p_pc_tagged.mp4' tests/integration/test_video_yuvj.cpp &amp;&amp; echo NEW_FIXTURE_WIRED</automated>
    <fails_when>non-zero exit, or NEW_FIXTURE_WIRED absent — the test still compares the old byte-identical pair.</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/integration/test_video_yuvj.cpp` references `video_yuv420p_pc_tagged.mp4` and its Test 2 TEST_CASE title names that fixture.
    - Test 2 asserts `count_non_pass(report) == 0` over the unfiltered `findings` array, and additionally asserts `video.pix_fmt` and `video.color.range` are each present with status `pass` and equal baseline/candidate values.
    - The file header states that the previous mirror pair was byte-identical and names the encoder normalisation that caused it.
    - The SUMMARY quotes the ctest output with the fold disabled, showing Test 2 among the FAILED cases, and the green run after restoring the fold.
    - `git status --porcelain src/analyzers/video/color.cpp` is empty (the mutation was reverted, never committed).
    - Tests 1, 3 and 4 are unchanged in the diff.
  </acceptance_criteria>
  <done>VIDEO-03's mirror claim is backed by a test that fails when the behaviour it names is removed.</done>
</task>

<task type="auto">
  <name>Task 3: Make the DOC-03 clean pair for video.pix_fmt carry evidence too</name>
  <files>tests/integration/test_doc03_coverage.cpp</files>
  <read_first>
    - tests/integration/test_doc03_coverage.cpp lines 240-275 (the `video.pix_fmt` row and the long comment above it explaining why its clean pair is deliberately the two-spellings pair rather than a byte-identical copy — that reasoning is right, the fixture it names is not)
    - tests/integration/test_doc03_coverage.cpp (the table's own shape: each row names a trigger pair then a clean pair, and the file's assertion loop over them)
    - .planning/phases/04-video-analysis/04-VERIFICATION.md (the sixth gap: tests that cannot fail are the defect class this task belongs to)
  </read_first>
  <action>
    Change the `video.pix_fmt` row's CLEAN pair from `video_yuvj420p.mp4` /
    `video_yuv420p_pc.mp4` — byte-identical, so it proves only that equal files compare equal — to
    `video_yuvj420p.mp4` / `video_yuv420p_pc_tagged.mp4`, the pair Task 1 produced. Leave the
    trigger pair (`video_base.mp4` / `video_noparser.mkv`) exactly as it is.

    Update the comment above the row. Its stated reasoning is already correct and must be kept: the
    clean pair is deliberately a two-spellings pair because that proves the fold made two genuinely
    different declarations compare equal. What must change is the record of which fixtures deliver
    it — add that the originally chosen candidate turned out byte-identical to its partner under the
    pinned mjpeg encoder, and name the replacement and how it is produced.

    Then prove the row is load-bearing in the same way as Task 2: with the fold disabled, this row's
    clean half must FAIL (the two declarations no longer compare equal). Run the DOC-03 suite under
    the disabled fold, confirm the failure names `video.pix_fmt`, restore, and re-run to green.
    Record both outputs in the SUMMARY.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; cmake --build --preset x64-linux 2>&amp;1 | tail -5 &amp;&amp; ctest --test-dir build/x64-linux -R "integration\.doc03_coverage" --output-on-failure 2>&amp;1 | tee /dev/stderr | grep -qE '[0-9]+ tests passed, 0 tests failed' &amp;&amp; echo DOC03_GREEN</automated>
    <fails_when>non-zero exit, or DOC03_GREEN absent from stdout, or the ctest summary echoed to stderr reports any failed test or a total of 0 tests.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; python3 -c "
import re
src=open('tests/integration/test_doc03_coverage.cpp').read()
code='\n'.join(l for l in src.splitlines() if not l.lstrip().startswith('//'))
i=code.index('\"video.pix_fmt\"')
row=code[i:code.index('}}', i)]
args=re.findall(r'fixture\(\"([^\"]+)\"\)', row)
assert len(args)==4, 'expected 4 fixture() arguments in the video.pix_fmt row, found %r' % args
assert args[3]=='video_yuv420p_pc_tagged.mp4', 'clean candidate is %r, not the new pair member' % args[3]
assert args[2]=='video_yuvj420p.mp4', 'clean baseline changed unexpectedly: %r' % args[2]
print('DOC03_ROW_UPDATED')"</automated>
    <fails_when>non-zero exit (an AssertionError prints the four fixture arguments it actually found), or DOC03_ROW_UPDATED absent from stdout. The check reads only non-comment lines and only the `fixture(...)` arguments, so a comment naming the old byte-identical fixture cannot satisfy or invalidate it.</fails_when>
  </verify>
  <acceptance_criteria>
    - The `video.pix_fmt` row of `tests/integration/test_doc03_coverage.cpp` names `video_yuv420p_pc_tagged.mp4` as its clean candidate and no longer names `video_yuv420p_pc.mp4`.
    - The trigger pair for that row is unchanged in the diff.
    - The comment above the row records that the original clean candidate was byte-identical to its partner and names the replacement.
    - The SUMMARY quotes the ctest output with the fold disabled showing this row's clean half failing, plus the green run after restoring.
    - `git status --porcelain src/analyzers/video/color.cpp` is empty.
  </acceptance_criteria>
  <done>The DOC-03 clean pair for `video.pix_fmt` proves the property its own comment claims it proves.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `tests/fixtures/video_yuv420p_pc_tagged.mp4` (new generated fixture — a stream-copy remux of `video_yuv420p_tv.mp4` carrying a full-range `colr` box)
- a new recipe block in `scripts/gen_corpus.sh` writing `$OUT_DIR/video_yuv420p_pc_tagged.mp4`
- one new `<sha256>  video_yuv420p_pc_tagged.mp4` line in `tests/golden/CORPUS_DIGEST.txt` and the matching entry in `tests/golden/CORPUS_DIGEST_PROVISIONAL.txt`
No new check id, CLI flag, public function or source file is introduced.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the fixture generator -> every assertion downstream of it | A fixture that does not carry the property its recipe claims turns every test built on it into a vacuous pass. |
| an encoder's own format negotiation -> the declaration the file ends up carrying | The requested pixel format is a request; what reaches the file is the encoder's choice, and this project has already been wrong about that once. |
| a new fixture -> `tests/golden/CORPUS_DIGEST.txt` | Adding a fixture is the moment an executor is most likely to regenerate the whole listing and clobber designated-leg hashes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-70 | Spoofing | a replacement fixture pair that is again indistinguishable | high | mitigate | Task 1 asserts the two sha256 values differ before the digest is touched, and Task 2's mutation check requires the rewritten test to go RED with the fold disabled — a pair that survives that mutation green is rejected by the acceptance criteria. |
| T-4-71 | Tampering | a full-listing regeneration clobbering restored designated-leg hashes | high | mitigate | The single new line is spliced in, `git diff` is asserted to show zero removed fixture hash lines, and `scripts/lint_corpus_digest_provenance.sh` (04-13) runs in this task's own verify chain. |
| T-4-72 | Repudiation | an unrelated check firing on the new pair and being silenced | high | mitigate | The zero-count assertion is over the UNFILTERED findings array; the recipe pins both mp4 timescales specifically because leaving them unpinned adds two container findings, and the prohibition forbids narrowing the assertion instead of the fixture. |
| T-4-73 | Information Disclosure | a media binary entering git | medium | mitigate | The fixture is synthesised by `scripts/gen_corpus.sh` from an already-generated input; `.gitignore`'s existing `tests/fixtures/` carve-out covers it, and only its hash is committed. |
| T-4-74 | Tampering | a fixture recipe depending on an encoder absent from the Windows LGPL pinned build | medium | mitigate | The recipe is a stream copy of an existing mjpeg fixture and invokes no encoder at all; the prohibition names the GPL/external encoders that must never enter a recipe. |
| T-4-SC | Tampering | npm/pip/cargo installs | low | accept | No package manager is invoked and no dependency manifest changes. |
</threat_model>

<verification>
- `bash scripts/gen_corpus.sh && bash scripts/check_corpus.sh` both succeed and the new fixture is present and non-empty.
- `bash scripts/lint_corpus_digest_provenance.sh` exits 0 with clause 4 reported as run.
- `ctest --test-dir build/x64-linux -R "integration\.video_yuvj" --output-on-failure` passes with a non-zero test count.
- `ctest --test-dir build/x64-linux -R "integration\.doc03_coverage" --output-on-failure` passes.
- `ctest --preset x64-linux --output-on-failure` passes in full; the total rises by zero or more but no previously-passing test fails. Note the new fixture is picked up by `integration.video inspect section`'s directory enumeration, so that test must still pass with it present.
- `bash scripts/assert_corpus_digest.sh` is still EXPECTED to fail on this workstation (04-13's restored designated-leg hashes). Do not act on it.
</verification>

<success_criteria>
- A fixture pair exists whose two colorimetric spellings survive distinctly and whose bytes differ.
- `integration.video_yuvj` Test 2 and the DOC-03 `video.pix_fmt` clean pair have both been observed failing with the fold disabled.
- Exactly one line was added to the corpus digest and none was rewritten.
</success_criteria>

<output>
Create `.planning/phases/04-video-analysis/04-16-SUMMARY.md` when done.
</output>
